# richtext-BCP AGENTS

## 角色

- 本项目是富文本基础卡片插件；正式职责是提供富文本卡片的查看态、编辑态和配置模型。

## 开发规则

- 查看态、编辑态、schema、模板和 i18n 要保持一致，不要只改其中一个层次。
- 文案必须走 `i18n/`；不要把用户可见文本直接写死在组件里。
- 主题必须消费 Host 注入的主题 CSS、变量和组件接口点，不得私自定义独立视觉体系。
- 若富文本配置、公共编辑协议或文件格式字段发生公共层变更，应同步核对共用文档。

## 验证

- 优先运行本项目正式的 `chipsdev` 脚本和对应测试。
