# 评分基础卡片数据模型设计

## 配置结构

```ts
type ScoreStyle = "stars" | "hearts" | "score" | "progress";

interface BasecardConfig {
  card_type: "ScoreCard";
  theme?: string;
  style: ScoreStyle;
  score: number;
  total_score: number;
  locale?: string;
}
```

## 字段说明

- `card_type`：固定为 `ScoreCard`。
- `theme`：可选基础卡片主题覆盖标识，留空时继承上层主题。
- `style`：评分展示样式。
- `score`：获得分数。
- `total_score`：总分。
- `locale`：用于编辑态文案与查看态辅助文本格式化。

## 归一化规则

- 非法 `style` 归一为 `stars`。
- `stars` 与 `hearts` 固定归一为 `total_score = 5`。
- `stars` 与 `hearts` 的 `score` 会被夹在 `0-5` 范围内，并四舍五入为整数。
- `score` 与 `progress` 的 `total_score` 会被夹在 `1-100000` 范围内。
- `score` 与 `progress` 的 `score` 会被夹在 `0-total_score` 范围内。
- 缺失 `theme` 归一为空字符串。
- 缺失 `locale` 归一为 `zh-CN`。

## 校验规则

- `card_type` 必须为 `ScoreCard`。
- `style` 必须属于 `stars`、`hearts`、`score`、`progress`。
- `total_score` 必须大于或等于 `1`，且不得超过 `100000`。
- `score` 必须大于或等于 `0`，且不得超过 `total_score`。
- `stars` 与 `hearts` 的 `total_score` 必须为 `5`，`score` 必须为整数。

## 示例

```yaml
card_type: "ScoreCard"
theme: ""
style: "progress"
score: 82.5
total_score: 100
locale: "zh-CN"
```

## 公共文档对齐

评分基础卡片的公共配置口径已同步到：

- [生态共用技术文档/插件开发/03-卡片插件开发.md](/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f/生态共用技术文档/插件开发/03-卡片插件开发.md)
- [生态共用技术文档/文件格式规范/01-卡片文件格式规范.md](/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f/生态共用技术文档/文件格式规范/01-卡片文件格式规范.md)
