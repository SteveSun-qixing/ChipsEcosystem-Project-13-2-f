# 阶段 17 — 测试补全

## 目标

为所有新增和修改的模块编写完整的单元测试，确保重构后的质量不低于旧实现。

## 背景

现有测试共 13 个文件、31 个测试用例，全部通过。重构后需要：
1. 更新受影响的现有测试。
2. 为新增模块编写新测试。
3. 确保测试覆盖所有关键逻辑路径。

## 测试文件清单

### 现有测试（需要更新）

| 文件 | 当前状态 | 更新内容 |
|------|----------|----------|
| `readium-layout.test.ts` | 通过 | 更新 import 路径（从 `domain/readium/layout` 到 `engine/layout-calculator`），新增窗口断点测试 |
| `reader-shell.test.tsx` | 通过 | 更新组件 props，新增进度条、书签按钮测试 |
| `reader-navigation.test.ts` | 通过 | 保留现有测试（滚轮阈值），新增交互意图映射测试 |
| `reader-preferences.test.ts` | 通过 | 保留不变 |
| `app.test.tsx` | 通过 | 保留不变 |

### 新增测试

| 文件 | 测试目标 |
|------|----------|
| `tests/unit/pagination-engine.test.ts` | 分页引擎核心逻辑 |
| `tests/unit/scroll-engine.test.ts` | 滚动引擎核心逻辑 |
| `tests/unit/position-tracker.test.ts` | 位置追踪与锚点恢复 |
| `tests/unit/layout-calculator.test.ts` | 布局计算增强 |
| `tests/unit/animation-controller.test.ts` | 翻页动画 |
| `tests/unit/interaction-manager.test.ts` | 交互意图映射 |
| `tests/unit/keyboard-handler.test.ts` | 键盘快捷键 |
| `tests/unit/wheel-handler.test.ts` | 滚轮处理 |
| `tests/unit/search-engine.test.ts` | 搜索引擎 |
| `tests/unit/bookmarks.test.ts` | 书签管理 |

## 各模块测试用例设计

### pagination-engine.test.ts

```typescript
describe("PaginationEngine", () => {
  describe("resolveSpreadIndex", () => {
    it("从滚动偏移准确反推 spread 索引");
    it("处理微小的子像素偏移");
    it("scrollOffset 为 0 返回 spread 0");
    it("scrollOffset 等于 maxOffset 返回最后一个 spread");
  });
  
  describe("computeTargetOffset", () => {
    it("翻到下一页：targetIndex = currentIndex + 1");
    it("翻到上一页：targetIndex = currentIndex - 1");
    it("在最后一个 spread 翻下一页不超出");
    it("在第一个 spread 翻上一页不低于 0");
    it("targetOffset = targetIndex × spreadWidth");
    it("targetOffset 不超过 maxScrollOffset");
  });
  
  describe("连续翻页无累积漂移", () => {
    it("翻页 100 次后位置与理论值误差 ≤ 1px");
    it("往返翻页后回到起始位置");
    it("scrollWidth 不是 offsetWidth 整数倍时正常工作");
  });
  
  describe("isAtBoundary", () => {
    it("在章节开头检测到 start 边界");
    it("在章节末尾检测到 end 边界");
    it("在中间位置不检测到边界");
  });
  
  describe("snapToSpread", () => {
    it("对齐到最近的 spread 边界");
    it("微小偏移量被正确对齐");
  });
});
```

### scroll-engine.test.ts

```typescript
describe("ScrollEngine", () => {
  describe("computeScrollFraction", () => {
    it("顶部返回 0");
    it("底部返回 1");
    it("中间位置返回正确比例");
    it("maxOffset 为 0 返回 0");
  });
  
  describe("isNearBoundary", () => {
    it("接近顶部检测到 previous 边界");
    it("接近底部检测到 next 边界");
    it("阈值可自定义");
  });
  
  describe("computeScrollTarget", () => {
    it("正常滚动在范围内");
    it("超出底部触发 end 边界");
    it("超出顶部触发 start 边界");
  });
});
```

### position-tracker.test.ts

```typescript
describe("PositionTracker", () => {
  describe("captureAnchor / restoreAnchor", () => {
    it("分页模式：保存 spread 索引并恢复到相同位置");
    it("分页模式：窗口缩放后使用 spreadFraction 映射到新索引");
    it("分页模式：双页→单页后位置正确恢复");
    it("滚动模式：保存/恢复滚动比例");
  });
  
  describe("computeProgress", () => {
    it("全书进度在 0–1 之间线性增长");
    it("第一章开头进度接近 0");
    it("最后一章末尾进度接近 1");
    it("带权重的进度计算正确");
  });
});
```

### interaction-manager.test.ts

```typescript
describe("InteractionManager", () => {
  describe("键盘意图映射", () => {
    it("ArrowRight 映射为 navigate next");
    it("ArrowLeft 映射为 navigate previous");
    it("Space 映射为 navigate next");
    it("Shift+Space 映射为 navigate previous");
    it("Home 映射为 navigate-boundary start");
    it("Escape 映射为 close-panel");
    it("输入框内按键不触发导航");
    it("带修饰键的方向键不触发导航");
  });
  
  describe("导航锁定", () => {
    it("锁定期间 canNavigate 返回 false");
    it("锁定过期后 canNavigate 返回 true");
  });
});
```

## 验收标准

1. 所有现有测试继续通过。
2. 所有新增测试通过。
3. 关键路径覆盖率 ≥ 80%。
4. 分页引擎的累积漂移测试证明偏移 ≤ 1px。
5. `npm run test` 全部通过。

## 预计改动量

- 新增 10 个测试文件。
- 修改 2–3 个现有测试文件。
- 总计约 800–1200 行测试代码。
