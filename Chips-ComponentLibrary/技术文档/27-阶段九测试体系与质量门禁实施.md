# 阶段九测试体系与质量门禁实施

## 1. 目标

在阶段九建立可持续执行的统一质量门禁，保证组件库在“开发-验证-交付”链路上具备稳定阻断能力。

## 2. 门禁链路

统一门禁命令：`npm run quality:gate`

执行顺序与阻断规则：

1. `lint`（`scripts/run-lint.mjs`）
2. `typecheck`（`scripts/run-typecheck.mjs`）
3. `test`（`npm run test`）
4. `test:contracts`（契约专项）
5. `test:a11y`（`scripts/run-a11y-tests.mjs`）
6. `test:perf`（`scripts/run-perf-tests.mjs`）
7. `quality:coverage`（`scripts/run-quality-coverage.mjs`）
8. `build`（`npm run build`）

任一步失败后，后续步骤标记为 `skipped`，门禁直接返回失败并阻断合入。

## 3. 测试框架增强

`@chips/testing` 已新增 Stage9 通用能力：

- `createComponentFixture`：统一组件契约 fixture 生成
- `assertAriaRole`：ARIA 语义断言
- `assertStatePriority`：状态优先级断言
- `createThemeFallbackFixture`：主题回退场景构造
- `resolveFallbackScopeValue`：作用域链回退断言
- `injectFault`：故障注入描述模型
- `CHIPS_COMPONENT_QUALITY_MATRIX`：组件矩阵覆盖数据源
- `assertComponentContractCoverage`：组件 contract 与矩阵等值断言
- `assertContractAttrMatrixCoverage`：`data-scope/data-part/data-state` fixture 覆盖断言
- `assertA11yFixtureCoverage`：ARIA fixture 覆盖断言
- `assertComponentStatePriorityCoverage`：状态优先级覆盖断言
- `createComponentMatrixReport`：机器可读矩阵报告生成

对应验证文件：`packages/testing/tests/index.test.mjs`。

## 4. Stage9 故障注入覆盖

`packages/components/tests/robustness-stage9.test.mjs` 已覆盖：

- token 缺失：`THEME_TOKEN_MISSING`
- 主题作用域异常：按高权重优先失败后向上回退
- config 源异常：`SYSTEM_UX_CONFIG_SOURCE_ERROR` + 默认值回退
- config 解析异常：`SYSTEM_UX_CONFIG_PARSER_ERROR` + 默认值回退
- i18n 适配器异常：`SYSTEM_UX_I18N_ADAPTER_ERROR` + fallback 文案
- 事件源异常：订阅失败场景安全处理

## 5. 结构化报告规范

质量门禁报告目录：`reports/quality-gate/`

- 模板：`reports/quality-gate/report-template.json`
- 最新结果：`reports/quality-gate/quality-gate-latest.json`
- 历史结果：`quality-gate-<timestamp>.json`

字段规范：

- 顶层：`schemaVersion`、`generatedAt`、`totalDurationMs`、`status`、`summary`、`steps[]`
- 汇总：`stepCount`、`passedCount`、`failedCount`、`skippedCount`、`blockingStep`
- 步骤：`name`、`command`、`status`、`exitCode`、`durationMs`

任务019补充报告：

- `reports/quality-gate/component-quality-coverage-latest.json`
- 历史结果：`component-quality-coverage-<timestamp>.json`
- 字段：`schemaVersion`、`generatedAt`、`status`、`gates[]`、`componentMatrix`
- 阻断项：组件矩阵漂移、contract attr fixture 缺失、ARIA fixture 缺失、状态优先级缺失、主题 fallback fixture 失效、perf smoke 场景缺失。

## 6. 当前基线结果（2026-03-05）

- `npm run verify`：`219 token keys / 32 contracts / 151 tests`
- `npm run quality:gate`：7 个步骤全部 `passed`
- `quality-gate-latest.json.status`：`passed`

## 7. DoD 映射

阶段九退出条件映射如下：

- 测试体系完善：已完成（测试工具与 fixture 库已落地）
- 质量门禁脚本：已完成（聚合门禁 + 结构化报告）
- 故障注入恢复：已完成（异常链路与回退均有测试）
- 可持续执行：已完成（`verify` + `quality:gate` 双入口）
