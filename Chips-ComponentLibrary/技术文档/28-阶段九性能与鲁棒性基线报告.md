# 阶段九性能与鲁棒性基线报告

## 1. 目标与范围

本报告用于交付阶段九的“性能与鲁棒性基线报告”，覆盖质量门禁中的性能阈值和故障注入恢复能力。

## 2. 执行环境

- 日期：2026-03-05
- 机器：Apple Silicon（arm64）
- 系统：Darwin 24.6.0
- Node.js：v22.18.0
- npm：10.9.3
- 命令：`npm run quality:gate`
- 报告：
  - `reports/perf/perf-stage9-latest.json`
  - `reports/quality-gate/quality-gate-latest.json`

## 3. 性能阈值与结果

阈值：

- `renderSubmitP95MaxMs <= 32`
- `themeSwitchVisibleP95Ms <= 150`
- `longTaskRatio <= 0.05`

结果（来自 `perf-stage9-latest.json`）：

- `renderSubmitP95MaxMs = 0.6833ms`（通过）
- `themeSwitchVisibleP95Ms = 10.362ms`（通过）
- `longTaskRatio = 0`（通过）

核心样本：

| 指标 | p95 (ms) | 结论 |
|---|---:|---|
| render-submit:data-grid-sort | 0.1181 | 通过 |
| render-submit:command-filter | 0.6833 | 通过 |
| render-submit:virtual-window | 0.0012 | 通过 |
| theme-switch-visible | 10.362 | 通过 |

## 4. 鲁棒性注入基线

执行文件：`packages/components/tests/robustness-stage9.test.mjs`

已验证异常类型：

- token 缺失
- 主题作用域异常
- config 源异常
- i18n 适配器异常
- 事件源异常

结论：异常路径均可进入标准诊断与回退逻辑，无全局崩溃。

## 5. 质量门禁汇总

`quality-gate-latest.json` 结果：

- 状态：`passed`
- 总耗时：`2605ms`
- 步骤：`lint/typecheck/test/test:contracts/test:a11y/test:perf/build` 全通过

## 6. 结论

阶段九性能与鲁棒性基线满足生态质量门禁要求，可作为后续阶段持续发布的默认阻断基线。
