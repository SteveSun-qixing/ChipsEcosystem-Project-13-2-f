# 交付前检查与人工验收清单

## 1. 当前状态

- 当前分支由用户管理，设置面板文档只记录验收口径，不要求代理自行合并分支。
- 当前基线：任务028迁移闭环，9 个运行时治理 Scene 已落地。
- 当前范围：主题管理、主题诊断、多语言、应用插件、卡片插件、箱子布局插件、模块插件、组件展示、预览与质量。
- 当前正式质量门禁：`npm run verify`，串行执行 `lint -> typecheck -> test -> build -> validate`。

## 2. 已完成的自动化检查

任务028.07 最终闭环前必须执行并记录：

```bash
cd Chips-EcoSettingsPanel
npm run verify
```

任务028各阶段已累计覆盖：

- 菜单注册、桌面导航、窄屏切换器与 Host command metadata。
- `AppRoot / AppProviders / AppRuntimeProvider / AppShell` 根组件结构。
- 主题、多语言、应用插件、卡片插件、箱子布局插件、模块插件运行时映射。
- 应用插件快捷方式状态读取、创建、移除和系统位置显示。
- 当前设置面板插件自保护规则，按钮层与 hook 动作层均拒绝自停用 / 自卸载。
- 主题 contract/resolve 诊断 view model。
- 组件展示页正式组件分组与 showcase 组件矩阵渲染。
- 预览与质量页正式 `chipsdev` 命令入口和私有目录读取边界。
- 权限不足、command 注册/调用失败、manifest 权限漂移和 mock Host smoke。
- 共享页面布局结构：`NavigationSplitView`、`DataGrid`、`PageLayout`、`SummaryPanel`、`CardGrid` 等正式结构断言。

## 3. 任务028收口结论

- 设置面板已迁移到应用插件 vNext 入口：`AppRoot / AppProviders / AppRuntimeProvider / scene-registry / commands`。
- 治理能力通过 SDK Domain API / Bridge 正式链路访问 Host，不在页面层直连 Host 内部模块。
- `manifest.yaml` 权限与 `src/app/settings-permissions.ts`、SDK route manifest 和公共运行时设置治理标准保持一致。
- 主题、语言、插件治理状态以 Host 返回为准，动作完成后重新读取，不前端手改治理真值。
- 页面结构已向组件库布局原语和共享 `PageLayout / SettingsRecordList / SettingsDetailPage` 收口；仍保留的项目级 CSS 只承担响应式、内容密度和设置面板场景组合样式，视觉皮肤通过主题 token 与组件 contract 派生，不再维护旧蓝色菜单、旧 Bento 展示页、传统多列表格治理页或详情弹窗模式。
- 工单104、工单105 已登记并闭环；当前 028.07 未发现新的公共接口缺口。

## 4. 待人工审核的 Host 联调清单

以下项目需要在真实 Host 开发态人工确认：

1. 打开应用窗口后，左侧菜单和顶部窄屏切换器都能切换当前 9 个 Scene，页面顶部不再出现额外的命令菜单、工具栏或命令面板；左侧菜单应呈现系统设置式中性侧边栏，不出现每项大面积蓝色按钮。
2. 主题安装、切换、卸载链路完整，且 `theme.changed` 后界面立即刷新；列表只保留切换和详情入口，卸载在主题详情页完成。
3. 主题诊断页展示当前主题 contract/resolve 结果，且不读取 ThemePack 或组件库私有目录。
4. 语言切换后，界面文案与当前语言标识立即刷新。
5. 应用插件安装、启用、停用、卸载链路完整；列表只保留启动、启停和详情入口，卸载与快捷方式管理进入应用插件详情页。
6. 当前设置面板插件在自身界面中无法停用或卸载。
7. Card Viewer、生态设置面板等应用插件可创建系统快捷方式，并显示正确位置文案。
8. 已创建快捷方式可执行移除与“显示位置”，且状态刷新正确。
9. 卡片插件、箱子布局插件、模块插件可按类型列表展示、安装后校验类型、启用、停用和卸载；列表保留启停和详情入口，卸载进入详情页。
10. 各治理详情均为当前 Scene 内二级页面，不是弹窗；左上角返回按钮可回到对应列表，详情页展示完整字段、状态和低频操作。
11. 组件展示页随主题切换呈现真实组件库主题效果，并与默认主题样式标准示范保持同一套轻内容片层、微体积和中性系统白方向。
12. 预览与质量页展示正式 `chipsdev` 命令入口，不把开发期报告伪装为运行时状态。
13. 文件选择、拖拽安装、空状态、错误态、权限不足态、确认弹窗均符合正式交互要求。

## 5. 已知后续边界

- 跨主题实时 contract 矩阵需要 Host/SDK 提供指定主题查询或正式报告消费链路；当前页面只诊断当前主题。
- 完整 CLI JSON 报告的运行时读取需要正式报告服务；当前预览与质量页只展示命令入口。
- Vite 构建存在单 chunk 超过 500 kB 的非阻塞提示，任务028记录为后续性能治理背景，不作为当前功能阻断。

## 6. 合并前约束

- 未经用户或人工审核，不执行分支合并。
- 若人工联调发现生态缺口，必须先登记工单，再决定是否继续推进。
- 若人工联调仅发现本项目实现问题，可直接在当前分支继续修正并复跑全量验证。
