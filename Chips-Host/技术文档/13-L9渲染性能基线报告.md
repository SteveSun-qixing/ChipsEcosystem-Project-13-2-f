# L9 渲染性能基线报告

> 报告日期：2026-05-23<br>
> 适用范围：`packages/unified-rendering/*`<br>
> 基线目标：渲染提交 `p95 <= 32ms`，布局计算 `p95 <= 40ms`，总渲染 `p95 <= 120ms`

---

## 1. 执行命令

```bash
npm run test:perf:l9
```

基线脚本：`scripts/benchmark-unified-rendering.ts`  
输出文件：`reports/perf/unified-rendering-baseline.json`

---

## 2. 本次结果

- 样本次数：30
- 输入规模参数：600
- 实际渲染节点：924（混合布局：Section / Navigation / Form / Grid / List / ScrollView / Table）
- 总耗时：
  - p50：24.945ms
  - p95：75.606ms
  - avg：38.159ms
- 渲染提交（render-commit）：
  - p50：3ms
  - p95：11ms
  - avg：4.300ms
- 布局计算（layout-compute）：
  - p50：6ms
  - p95：13ms
  - avg：6.900ms
- 节点计数稳定性：
  - `nodeCount`：924 / 924
  - `layoutNodeCount`：924 / 924
  - `commitNodeCount`：924 / 924

判定：通过（`render-commit p95=11ms <= 32ms`，`layout-compute p95=13ms <= 40ms`，`total p95=75.606ms <= 120ms`）。

---

## 3. 质量结论

- L9 当前实现满足架构文档中渲染提交性能阈值。
- L9 当前实现满足布局计算与总渲染耗时阈值。
- 增量更新策略在混合布局声明树下具备稳定收益。
- 节点计数在同次基准内保持稳定，可作为布局语义与提交规模漂移监测基线。
- 可作为 L9 布局计算与性能门禁补强后的性能基线。

---

## 4. 后续门禁建议

- 合并前执行：`npm run test:perf:l9`（严格阈值模式）。
- 提升节点规模（例如 1000 / 2000）补充趋势数据。
- 将基线结果纳入发布复盘，观察回归漂移。
