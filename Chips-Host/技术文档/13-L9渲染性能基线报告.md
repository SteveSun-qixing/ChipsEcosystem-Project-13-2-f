# L9 渲染性能基线报告

> 报告日期：2026-05-23<br>
> 适用范围：`packages/unified-rendering/*`<br>
> 基线目标：渲染提交 `p95 <= 32ms`

---

## 1. 执行命令

```bash
npm run test:perf:l9
```

基线脚本：`scripts/benchmark-unified-rendering.ts`  
输出文件：`reports/perf/unified-rendering-baseline.json`

---

## 2. 本次结果

- 样本次数：30
- 声明树规模：600 节点（List 增量模式）
- 总耗时：
  - p50：21.203ms
  - p95：54.243ms
  - avg：25.870ms
- 渲染提交（render-commit）：
  - p50：5ms
  - p95：9ms
  - avg：5.5ms
- 布局计算（layout-compute）：
  - p50：6ms
  - p95：27ms
  - avg：9.267ms

判定：通过（`render-commit p95=9ms <= 32ms`）。

---

## 3. 质量结论

- L9 当前实现满足架构文档中渲染提交性能阈值。
- 增量更新策略在中等规模声明树下具备稳定收益。
- 可作为 L9 诊断与质量门禁补强后的性能基线。

---

## 4. 后续门禁建议

- 合并前执行：`npm run test:perf:l9`（严格阈值模式）。
- 提升节点规模（例如 1000 / 2000）补充趋势数据。
- 将基线结果纳入发布复盘，观察回归漂移。
