# L9 渲染性能基线报告

> 报告日期：2026-03-06  
> 适用范围：`packages/unified-rendering/*`  
> 基线目标：渲染提交 `p95 <= 32ms`

---

## 1. 执行命令

```bash
npm run perf:l9 -- --runs=20 --nodes=500
```

基线脚本：`scripts/benchmark-unified-rendering.ts`  
输出文件：`reports/perf/unified-rendering-baseline.json`

---

## 2. 本次结果

- 样本次数：20
- 声明树规模：500 节点（List 增量模式）
- 总耗时：
  - p50：28.528ms
  - p95：65.444ms
  - avg：38.329ms
- 渲染提交（render-commit）：
  - p50：8ms
  - p95：21ms
  - avg：10.35ms
- 布局计算（layout-compute）：
  - p50：9ms
  - p95：16ms
  - avg：10.15ms

判定：通过（`render-commit p95=21ms <= 32ms`）。

---

## 3. 质量结论

- L9 当前实现满足架构文档中渲染提交性能阈值。
- 增量更新策略在中等规模声明树下具备稳定收益。
- 可作为 Phase 6 收口前的初始性能基线。

---

## 4. 后续门禁建议

- 合并前执行：`npm run test:perf:l9`（严格阈值模式）。
- 提升节点规模（例如 1000 / 2000）补充趋势数据。
- 将基线结果纳入发布复盘，观察回归漂移。
