{
  "name": "chips.module.chips.iconmaker.plugin",
  "version": "0.1.0",
  "private": false,
  "type": "commonjs",
  "scripts": {
    "dev": "chipsdev server",
    "build": "node ./scripts/build.mjs",
    "test": "vitest run",
    "lint": "chipsdev lint",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "validate": "chipsdev validate",
    "package": "chipsdev package",
    "verify": "npm run typecheck && npm test && npm run build && npm run validate"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8",
    "eslint": "^8.57.1",
    "@typescript-eslint/parser": "^7.18.0",
    "chips-sdk": "^0.1.0",
    "esbuild": "^0.27.3"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
