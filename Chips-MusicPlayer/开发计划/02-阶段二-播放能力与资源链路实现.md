# 阶段二：播放能力与资源链路实现

## 目标

1. 打通 `launchParams.resourceOpen` / `targetPath` 恢复；
2. 打通手动打开与拖放打开；
3. 落地左封面右歌词的正式播放器界面；
4. 完成伴生资源发现、嵌入元数据解析与保存副本链路。

## 实施项

1. `src/App.tsx`
   - 恢复启动上下文
   - 调用 `resource.resolve`
   - 发现伴生歌词 / 封面
   - 解析嵌入元数据
   - 管理保存与反馈
2. `src/hooks/useMusicPlayerController.ts`
   - 收口播放、时间轴、音量、静音与循环
3. `src/components/MusicPlayerStage.tsx`
   - 构建封面区、歌词区与控制 dock
4. `src/utils/*.ts`
   - 支持格式、歌词解析、启动参数解析、MP3 / FLAC 元数据解析

## 当前结果

- 已完成上述实现，形成可运行的首版音乐播放器
- 已把参考仓库的视觉布局重写为 React + 薯片主题链路版本
