# 基础卡片配置参数说明

本文件说明 `default-card-config.yaml` 中各字段的含义与填写规范。

- `card_type`：基础卡片正式类型标识，默认固定为 `{{ CARD_TYPE }}`。
- `theme`：可选的主题变体标识；为空时跟随当前主题系统。
- `title`：基础卡片标题，必填，建议不超过 100 个字符。
- `body`：基础卡片正文内容，必填，可包含任意文本。
- `locale`：语言代码，可选。建议使用标准语言标签，例如 `zh-CN`、`en-US`。
