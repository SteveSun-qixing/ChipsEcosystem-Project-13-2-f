# 命令行集成与工作流设计

## 1. 集成目标

`chipsdev create` 必须能够正式创建布局插件工程。

## 2. 最小工作流

建议工作流：

1. 开发者执行创建命令；
2. CLI 选择或指定布局插件模板；
3. 脚手架渲染模板到目标目录；
4. CLI 输出下一步命令；
5. 开发者在生成工程中执行 `build/test/validate`。

## 3. CLI 对接约束

- 脚手架优先暴露纯函数接口；
- 不把 CLI 的交互实现写死在模板仓；
- 错误必须可诊断；
- 生成工程结果必须可被后续 `chipsdev` 命令直接消费。
