# chips-scaffold-theme AGENTS

## 角色

- 生成主题插件正式模板；必须覆盖 token、CSS、契约、测试和一包一外观的主题工程基线。

## 开发规则

- 主题模板必须遵守“一包一外观”，不要在单模板里混入多模式切换的私有方案。
- 生成工程应消费既有 Theme Runtime 和组件契约，不应在模板里定义新的跨生态协议。
- 模板需要完整回填占位符，并生成正式可构建、可校验、可测试的主题工程。
- 主题公共规则和 token 对接标准属于共享文档，不要只在模板仓定义。

## 验证

- 优先运行 `build` 与 `test`。
