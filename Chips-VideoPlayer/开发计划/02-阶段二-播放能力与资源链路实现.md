# 阶段二：播放能力与资源链路实现

## 目标

1. 打通 `launchParams.resourceOpen` / `targetPath` 恢复；
2. 打通手动打开与拖放打开；
3. 落地基础播放器界面与控制器；
4. 完成保存副本链路。

## 实施项

1. `src/App.tsx`
   - 恢复启动上下文
   - 调用 `resource.resolve`
   - 管理保存与反馈
2. `src/hooks/useVideoPlayerController.ts`
   - 收口播放、时间轴、音量、全屏、画中画与热键
3. `src/components/VideoPlayerStage.tsx`
   - 构建视频舞台与底部 dock
4. `src/utils/*.ts`
   - 支持格式、时间格式化、启动参数解析

## 当前结果

- 已完成上述实现，形成可运行的首版播放器骨架
- 已把 DPlayer 的热键和控制条分组思路收口为项目内部模块
