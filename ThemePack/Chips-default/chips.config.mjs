/**
 * Chips Dev 配置文件（主题插件工程）。
 * 当前配置采用最小字段集，后续如需扩展可在此基础上演进。
 */

/** @type {{ type: string; srcDir: string; distDir: string; manifest: string }} */
const config = {
  type: "theme",
  srcDir: "src",
  distDir: "dist",
  manifest: "manifest.yaml"
};

export default config;

