{
  "name": "theme.theme.chips-official-default-dark-theme",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build:tokens": "tsx src/build-tokens.ts",
    "build:css": "tsx src/build-css.ts",
    "build": "npm run build:tokens && npm run build:css",
    "validate:theme": "tsx src/validate-theme.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
