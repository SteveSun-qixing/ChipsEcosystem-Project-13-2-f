# Phase 1：项目初始化与基础架构 - 任务清单

**状态**: 待开始  
**目标**: 搭建项目骨架，完成基础配置

---

## 任务列表

### P1-01：初始化项目结构

**描述**：创建完整的目录结构

**具体步骤**：
1. 创建src目录结构
   ```
   src/
   ├── index.ts
   ├── plugin.ts
   ├── types/
   ├── renderer/
   ├── editor/
   └── utils/
   ```
2. 创建tests目录结构
   ```
   tests/
   ├── setup.ts
   ├── unit/
   └── integration/
   ```
3. 创建assets目录
   ```
   assets/
   └── i18n/
   ```

**验收标准**：
- [ ] 所有目录已创建
- [ ] 每个目录有index.ts占位文件

---

### P1-02：配置构建工具

**描述**：配置Vite和构建选项

**具体步骤**：
1. 确认vite.config.ts配置正确
2. 配置构建输出格式（ES模块）
3. 配置外部依赖（vue, @chips/*）
4. 配置类型声明生成

**验收标准**：
- [ ] `npm run build`成功执行
- [ ] 生成dist/index.js
- [ ] 生成dist/index.d.ts

---

### P1-03：配置测试框架

**描述**：配置Vitest测试环境

**具体步骤**：
1. 确认vitest.config.ts配置正确
2. 创建tests/setup.ts测试设置文件
3. 配置测试覆盖率报告
4. 创建示例测试文件验证配置

**tests/setup.ts内容**：
```typescript
import { vi } from 'vitest';

// Mock window.getSelection
Object.defineProperty(window, 'getSelection', {
  value: () => ({
    rangeCount: 0,
    getRangeAt: () => null,
    removeAllRanges: () => {},
    addRange: () => {}
  })
});

// Mock document.execCommand
document.execCommand = vi.fn(() => true);

// Mock ResizeObserver
global.ResizeObserver = vi.fn().mockImplementation(() => ({
  observe: vi.fn(),
  unobserve: vi.fn(),
  disconnect: vi.fn()
}));
```

**验收标准**：
- [ ] `npm test`成功执行
- [ ] 测试环境正确设置
- [ ] 覆盖率报告生成

---

### P1-04：创建插件主类骨架

**描述**：创建RichTextCardPlugin基础结构

**文件**：src/plugin.ts

**代码骨架**：
```typescript
import type { ChipsCore } from '@chips/core';

export interface PluginMetadata {
  id: string;
  name: string;
  version: string;
  cardType: string;
  icon: string;
  description: string;
}

export class RichTextCardPlugin {
  readonly metadata: PluginMetadata = {
    id: 'chipshub:rich-text-card',
    name: '富文本卡片',
    version: '1.0.0',
    cardType: 'RichTextCard',
    icon: 'assets/icon.svg',
    description: '用于显示和编辑RTF格式的富文本内容'
  };
  
  readonly configSchema = {
    // TODO: 在Phase 2完善
  };
  
  private core: ChipsCore | null = null;
  
  async initialize(core: ChipsCore): Promise<void> {
    this.core = core;
    // TODO: 在Phase 2完善
  }
  
  async start(): Promise<void> {
    // TODO: 在Phase 2完善
  }
  
  async stop(): Promise<void> {
    // TODO: 在Phase 2完善
  }
  
  async destroy(): Promise<void> {
    this.core = null;
    // TODO: 在Phase 2完善
  }
  
  createRenderer() {
    // TODO: 在Phase 3实现
    throw new Error('Not implemented');
  }
  
  createEditor() {
    // TODO: 在Phase 4实现
    throw new Error('Not implemented');
  }
}

export default RichTextCardPlugin;
```

**验收标准**：
- [ ] 类结构正确
- [ ] TypeScript编译无错误
- [ ] 导出正确

---

## 完成标志

- [ ] 所有P1任务完成
- [ ] `npm run build`成功
- [ ] `npm test`成功
- [ ] Git提交完成
