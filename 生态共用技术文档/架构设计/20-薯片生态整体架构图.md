# 薯片生态整体架构图

> 文档状态：生态共用公开架构图  
> 修订日期：2026-06-29  
> 适用范围：Host、SDK、ComponentLibrary、ThemePack、Scaffold、五类插件、社区平台与生态工具链

---

## 1. 文档定位

本文档用 Mermaid 图集中表达薯片生态当前正式架构口径，便于开发、评审、插件接入和公共文档引用。

本文档只沉淀稳定公共架构，不记录任务过程、短期实现策略或单仓内部细节。更细的接口字段、schema、命令和测试门禁仍以各主发布文档为准。

主要依据：

- `生态共用技术文档/架构设计/薯片生态-架构设计手册.md`
- `生态共用技术文档/架构设计/03-插件架构设计.md`
- `生态共用技术文档/架构设计/13-Bridge三层设计.md`
- `生态共用技术文档/架构设计/14-Host服务域设计.md`
- `生态共用技术文档/架构设计/19-前端框架vNext公共契约与术语冻结.md`
- `生态共用技术文档/协议与接口标准/02-Bridge-API规范.md`
- `生态共用技术文档/插件开发/06-Manifest配置规范.md`
- `生态共用技术文档/插件开发/08-SDK使用指南.md`
- `生态共用技术文档/文件格式规范/01-卡片文件格式规范.md`
- `生态共用技术文档/文件格式规范/02-箱子文件格式规范.md`
- `生态共用技术文档/文件格式规范/03-CPK打包格式规范.md`

---

## 2. 总体拓扑

```mermaid
flowchart TB
  user["用户 / 开发者 / 第三方插件作者"]
  os["操作系统<br/>文件关联 / 快捷方式 / URL Scheme / Share Target"]
  community["社区平台服务器<br/>/api/v1 /admin/api/v1"]
  storage["本地与网络存储<br/>.card / .box / .cpk / 资源文件"]

  subgraph Host["Chips Host：用户设备唯一运行时承载"]
    pal["L1 PAL<br/>平台能力抽象"]
    kernel["L2 Host Kernel<br/>router / registry / event / lifecycle"]
    services["L3 Host Services<br/>20 个服务域"]
    runtime["L4 Plugin Runtime<br/>加载 / 隔离 / 会话 / 权限"]
    bridge["L5 Bridge Transport<br/>window.chips.* / Direct transport"]
    client["L6 Runtime Client<br/>协议封装 / 错误归一 / 重试"]
    hooks["L7 UI Hooks / API<br/>useChips* / Domain API"]
    dui["L8 Declarative UI<br/>View / Stack / Grid / Form / List"]
    render["L9 Unified Rendering<br/>Normalize / Validate / Resolve / Commit"]
  end

  subgraph UIInfra["公共前端基础设施"]
    headless["L10 Component Headless<br/>结构 / 状态机 / a11y"]
    themeRuntime["L11 Theme Runtime<br/>token 解析 / 作用域覆盖 / CSS 注入"]
    themePack["Theme Package<br/>theme 插件 / tokens / theme.css / 图标字体"]
  end

  subgraph Plugins["五类插件"]
    appPlugin["app 插件<br/>跨平台 surface 应用"]
    cardPlugin["card 插件<br/>基础卡片渲染 / 编辑"]
    layoutPlugin["layout 插件<br/>箱子布局页面"]
    modulePlugin["module 插件<br/>无界面能力 provider"]
    themePlugin["theme 插件<br/>视觉主题包"]
  end

  subgraph DevTools["开发与质量工具链"]
    sdk["Chips SDK<br/>类型封装 / Domain API / 测试辅助"]
    scaffold["Chips Scaffold<br/>app / card / layout / module / theme 模板"]
    chipsdev["chipsdev<br/>create / build / validate / package / run"]
    componentLib["Chips ComponentLibrary<br/>无头组件 / hooks / theme contracts"]
  end

  user --> os
  os --> pal
  services <--> storage
  services <--> community

  pal --> kernel --> services --> runtime --> bridge --> client --> hooks --> dui --> render
  render --> headless
  render --> themeRuntime
  headless --> themeRuntime
  themeRuntime --> themePack

  runtime --> appPlugin
  runtime --> cardPlugin
  runtime --> layoutPlugin
  runtime --> modulePlugin
  runtime --> themePlugin

  appPlugin --> bridge
  cardPlugin --> bridge
  layoutPlugin --> bridge
  modulePlugin --> services
  themePlugin --> themeRuntime

  sdk --> client
  sdk --> chipsdev
  scaffold --> appPlugin
  scaffold --> cardPlugin
  scaffold --> layoutPlugin
  scaffold --> modulePlugin
  scaffold --> themePlugin
  chipsdev --> scaffold
  chipsdev --> runtime
  componentLib --> headless
  componentLib --> hooks
```

核心边界：

- Host 是唯一运行时承载，生产运行时主链路由 Host 承担。
- SDK 是开发者工具包与类型化封装，不承载 Host runtime 主实现。
- 组件库主责 L10 Component Headless，不写视觉皮肤。
- 主题视觉通过 Theme Runtime 与 theme 插件注入，不在业务组件中硬编码。
- 插件访问系统能力只能通过 `window.chips.*`、Bridge 或 `chips-sdk` 正式入口。

---

## 3. 十二层运行时架构

```mermaid
flowchart BT
  l1["L1 PAL<br/>平台能力抽象<br/>OS / Electron 原生能力适配"]
  l2["L2 Host Kernel<br/>中心路由 / 服务注册 / 事件总线 / 生命周期"]
  l3["L3 Host Services<br/>file / resource / config / theme / i18n / ..."]
  l4["L4 Plugin Runtime<br/>插件加载 / 沙箱 / 会话 / 权限 / 审计"]
  l5["L5 Bridge Transport<br/>window.chips.* / IPC / Direct transport"]
  l6["L6 Runtime Client<br/>action 规范化 / envelope 解包 / 标准错误 / 重试"]
  l7["L7 UI Hooks / API<br/>useChips* / SDK Domain API"]
  l8["L8 Declarative UI<br/>语义节点 / View / Stack / Grid / Form / List"]
  l9["L9 Unified Rendering<br/>Normalize / Validate / Theme Resolve / Layout / Commit"]
  l10["L10 Component Headless<br/>状态机 / 结构 / 键盘交互 / a11y"]
  l11["L11 Theme Runtime<br/>ref / sys / motion / layout / comp token"]
  l12["L12 App / Page Domain<br/>页面状态 / 业务流程 / 编排"]
  contract["公共契约层<br/>route manifest / manifest.yaml / token contract / schema / file format"]

  l12 --> l11
  l12 --> l10
  l12 --> l9
  l12 --> l8
  l12 --> l7
  l11 --> l10
  l10 --> l9
  l9 --> l8
  l8 --> l7
  l7 --> l6
  l6 --> l5
  l5 --> l4
  l4 --> l3
  l3 --> l2
  l2 --> l1

  l1 -.-> contract
  l2 -.-> contract
  l3 -.-> contract
  l4 -.-> contract
  l5 -.-> contract
  l6 -.-> contract
  l7 -.-> contract
  l8 -.-> contract
  l9 -.-> contract
  l10 -.-> contract
  l11 -.-> contract
  l12 -.-> contract
```

依赖白名单：

- 常规依赖只允许 `Ln -> Ln-1`。
- 任何层级可以依赖公共契约层。
- 禁止 L12 页面域直连 L1-L5 细节。
- 禁止服务间跨服务源码直连，服务协作必须走 Kernel 路由。

---

## 4. Host 服务域矩阵

```mermaid
flowchart TB
  kernel["Host Kernel<br/>namespace.action 路由 / 权限 / schema / timeout / 标准错误"]

  subgraph DataServices["内容与文件服务"]
    file["file<br/>读写 / 枚举 / 拷贝 / 移动"]
    resource["resource<br/>解析 / 读取 / 统一打开 / 受控转换"]
    card["card<br/>解析 / 渲染 / 封面 / 编辑器 / 会话释放"]
    box["box<br/>箱子会话 / 条目 / 资源 / 打开"]
    zip["zip<br/>STORE ZIP 压缩 / 解压 / 列表"]
    serializer["serializer<br/>编码 / 解码 / schema 校验"]
  end

  subgraph RuntimeServices["运行治理服务"]
    plugin["plugin<br/>安装 / 启停 / 查询 / app 启动"]
    module["module<br/>provider 发现 / capability 调用 / job"]
    command["command<br/>菜单 / 工具栏 / 快捷键 / 命令面板"]
    surface["surface<br/>跨平台容器主语义"]
    window["window<br/>Desktop 兼容别名"]
    control["control-plane<br/>健康检查 / 指标 / 诊断"]
  end

  subgraph SystemServices["系统与环境服务"]
    platform["platform<br/>平台信息 / 能力快照 / 屏幕 / 电源 / 离屏导出"]
    transfer["transfer<br/>打开 / 导出 / 分享 / 定位文件"]
    association["association<br/>文件关联 / URL 打开入口治理"]
    config["config<br/>配置治理"]
    credential["credential<br/>凭证治理"]
    log["log<br/>记录 / 查询 / 导出"]
  end

  subgraph UXServices["体验基础服务"]
    theme["theme<br/>主题列表 / 应用 / resolve / CSS / contract"]
    i18n["i18n<br/>语言 / 翻译 / locale 切换"]
  end

  kernel --> file
  kernel --> resource
  kernel --> card
  kernel --> box
  kernel --> zip
  kernel --> serializer
  kernel --> plugin
  kernel --> module
  kernel --> command
  kernel --> surface
  kernel --> window
  kernel --> control
  kernel --> platform
  kernel --> transfer
  kernel --> association
  kernel --> config
  kernel --> credential
  kernel --> log
  kernel --> theme
  kernel --> i18n

  surface -.->|"Desktop 当前映射"| window
  resource --> file
  card --> resource
  card --> zip
  box --> resource
  box --> card
  module --> plugin
  command --> surface
  transfer --> platform
  association --> transfer
  theme --> plugin
  i18n --> config
```

服务域规则：

- 每个服务动作必须有输入 schema、输出 schema、权限、超时、错误码和幂等性定义。
- 新的跨平台界面能力优先进入 `surface`。
- 新的打开、导出、分享、定位文件能力优先进入 `transfer`。
- 新的系统入口治理优先进入 `association`。
- `window` 仅保留为 Desktop 兼容别名，不承载未来主语义。
- `platform.getCapabilities()` 返回结构化能力快照，不回退为字符串数组。

---

## 5. Bridge 与插件调用链

```mermaid
sequenceDiagram
  autonumber
  participant Page as app/card/layout 页面
  participant Hooks as L7 Hooks / SDK Domain API
  participant Client as L6 Runtime Client
  participant Bridge as L5 window.chips.*
  participant Runtime as L4 Plugin Runtime
  participant Kernel as L2 Kernel Router
  participant Service as L3 Host Service
  participant PAL as L1 PAL / OS

  Page->>Hooks: 调用 useChips* 或 client.domain.*
  Hooks->>Client: 发送 action + payload
  Client->>Bridge: invoke(namespace.action, payload)
  Bridge->>Runtime: 携带 session / pluginId / permissions
  Runtime->>Kernel: 构造 RouteInvocationContext
  Kernel->>Kernel: schema 校验 / 权限判定 / timeout / 熔断
  Kernel->>Service: 路由到目标服务动作
  Service->>PAL: 必要时访问平台能力
  PAL-->>Service: 平台结果
  Service-->>Kernel: route envelope 或标准错误
  Kernel-->>Runtime: 记录 trace / request / audit
  Runtime-->>Bridge: 返回结果
  Bridge-->>Client: transport 无关响应
  Client-->>Hooks: 解包 / 错误归一 / retry
  Hooks-->>Page: 稳定业务 API 结果
```

调用边界：

- 页面代码优先使用组件库环境 Provider、`useChips*` hooks 或 SDK Domain API。
- 必须直连时才使用 `window.chips.invoke/on/once/emit`。
- `file/resource/card/box/zip/module` 等服务域通过 `invoke("namespace.action", payload)` 或 SDK Domain API 访问，不发布 `window.chips.file.*` 这类子对象。
- `window.chips.*` 的形状独立于 Desktop IPC、Headless Direct transport 和未来 Web/Mobile transport。

---

## 6. 五类插件架构

```mermaid
flowchart TB
  manifest["manifest.yaml<br/>id / name / version / type / entry / permissions / runtime.targets"]
  cpkPackage[".cpk<br/>ZIP Store 插件包"]
  runtime["Host Plugin Runtime<br/>校验 / 安装 / 启用 / 会话 / 权限 / 审计"]

  subgraph App["app 插件"]
    appSurface["ui.surface<br/>window / route / tab / modal / sheet / fullscreen"]
    appEntry["entry: dist/index.html"]
    appBridge["通过 Bridge / SDK 访问 Host"]
  end

  subgraph Card["card 插件"]
    cardTypes["capabilities.cardTypes<br/>基础卡片类型"]
    cardEntry["entry: dist/index.mjs"]
    cardMount["渲染 / 编辑挂载函数"]
  end

  subgraph Layout["layout 插件"]
    layoutType["layout.layoutType<br/>箱子 active_layout_type 映射键"]
    layoutEntry["entry: dist/index.mjs"]
    layoutRuntime["消费 box.* 会话与摘要条目"]
  end

  subgraph Module["module 插件"]
    moduleProvides["module.provides<br/>capability + method + schema"]
    moduleRuntime["runtime: worker<br/>activation: onDemand / eager"]
    moduleInvoke["通过 module.invoke / job 调度"]
  end

  subgraph Theme["theme 插件"]
    themeEntry["entry.tokens / entry.themeCss"]
    themeId["themeId / displayName / parentTheme"]
    themeContracts["theme-interface / min-functional-set contracts"]
  end

  manifest --> cpkPackage --> runtime
  runtime --> appSurface
  runtime --> cardTypes
  runtime --> layoutType
  runtime --> moduleProvides
  runtime --> themeEntry

  appSurface --> appEntry --> appBridge
  cardTypes --> cardEntry --> cardMount
  layoutType --> layoutEntry --> layoutRuntime
  moduleProvides --> moduleRuntime --> moduleInvoke
  themeEntry --> themeId --> themeContracts
```

安装与运行安全流水线：

```mermaid
flowchart LR
  a["1 Manifest 校验<br/>类型 / 权限 / 入口 / 契约版本"]
  b["2 来源治理边界判定<br/>local 或发布来源"]
  c["3 plugin-init 握手<br/>sessionNonce"]
  d["4 权限快照加载<br/>最小权限"]
  e["5 沙箱注入<br/>只暴露 window.chips.*"]
  f["6 运行时审计<br/>越权 / 超频 / 异常"]
  g["7 会话结束清理<br/>订阅 / 缓存 / 临时资源"]

  a --> b --> c --> d --> e --> f --> g
```

插件边界：

- `app` 的公共容器语义是 `surface`，Desktop 当前映射为窗口。
- `card` 在 Host 通用渲染链路中 iframe 优先，并可被官方编辑引擎以正式特例在编辑态本地装配。
- `layout` 是箱子页面前端，只消费 Host 箱子会话和摘要数据，不自行扫描磁盘。
- `module` 是 Host 托管无界面能力 provider，通过 capability + method 暴露能力。
- `theme` 负责 token、CSS、组件接口点和视觉资源。

---

## 7. 内容与文件格式架构

```mermaid
flowchart TB
  subgraph ContentLevels["六级内容层次"]
    base["基础卡片<br/>最小原子内容形态"]
    composite["复合卡片<br/>可保存最小单位"]
    boxLevel["箱子<br/>卡片引用与布局容器"]
    room["房间<br/>空间内组织单位"]
    space["空间<br/>设备 / 存储位置"]
    communityLevel["社区<br/>网络公共平台"]
  end

  subgraph FileFormats["正式文件格式"]
    cardFile[".card<br/>ZIP Store<br/>保存复合卡片实体"]
    boxFile[".box<br/>ZIP Store<br/>保存 URL 引用 / 摘要 / 布局配置"]
    cpkFile[".cpk<br/>ZIP Store<br/>保存插件代码 / manifest / 资源"]
  end

  subgraph CardStruct[".card 结构"]
    cardMeta[".card/metadata.yaml"]
    cardStructure[".card/structure.yaml"]
    cardCover[".card/cover.html"]
    cardContent["content/*.yaml<br/>基础卡片配置"]
    cardAssets["根目录资源文件"]
  end

  subgraph BoxStruct[".box 结构"]
    boxMeta[".box/metadata.yaml"]
    boxStructure[".box/structure.yaml<br/>entries: url / snapshot / layout_hints"]
    boxContent[".box/content.yaml<br/>active_layout_type / layout_configs"]
    boxCover[".box/cover.html"]
    boxAssets["assets/<br/>箱子自身资源"]
  end

  base --> composite --> boxLevel --> room --> space --> communityLevel
  composite --> cardFile
  boxLevel --> boxFile
  cpkFile --> base
  cpkFile --> boxLevel

  cardFile --> cardMeta
  cardFile --> cardStructure
  cardFile --> cardCover
  cardFile --> cardContent
  cardFile --> cardAssets

  boxFile --> boxMeta
  boxFile --> boxStructure
  boxFile --> boxContent
  boxFile --> boxCover
  boxFile --> boxAssets

  boxStructure -.->|"file:// / http:// / https:// / webdav://"| cardFile
  boxStructure -.->|"可引用另一个箱子"| boxFile
```

文件格式边界：

- `.card` 保存复合卡片实体，基础卡片配置在 `content/` 中，资源位于卡片根目录。
- `.box` 是卡片与箱子的引用容器，只保存 URL、摘要快照、布局配置和箱子自身资源，不保存被引用卡片实体。
- `.cpk` 是五类插件统一包格式，以包根 `manifest.yaml` 为唯一正式清单源。
- 三类格式均采用 ZIP Store 零压缩模型。

---

## 8. 渲染、组件与主题链路

```mermaid
flowchart TB
  page["L12 App / Page Domain<br/>页面状态 / 业务流程"]
  hooks["L7 UI Hooks / SDK client<br/>系统能力注入"]
  declaration["L8 Declarative UI<br/>UINode 声明树"]

  subgraph Pipeline["L9 Unified Rendering Pipeline"]
    normalize["Node Normalize<br/>节点归一"]
    validate["Contract Validate<br/>能力与契约校验"]
    themeResolve["Theme Resolve<br/>主题链解析"]
    layoutCompute["Layout Compute<br/>cpx / 断点 / 约束"]
    commit["Render Commit<br/>DOM / CSS / adapter"]
    effects["Effect Dispatch<br/>ui / runtime / telemetry effect"]
  end

  subgraph Targets["渲染目标适配器"]
    appRoot["App Root"]
    cardIframe["Card Iframe"]
    moduleSlot["Module Slot"]
    offscreen["Offscreen Render<br/>预览 / 导出"]
  end

  subgraph ComponentTheme["组件与主题"]
    components["L10 Component Headless<br/>data-scope / data-part / data-state"]
    tokens["L11 Theme Runtime<br/>ref -> sys -> motion/layout -> comp"]
    scope["主题作用域链<br/>global -> app -> box -> composite-card -> base-card -> component"]
    css["CSS 变量与 theme.css 注入"]
    themePkg["Theme Package<br/>tokens.json / theme.css / contracts / icons"]
  end

  page --> hooks --> declaration --> normalize --> validate --> themeResolve --> layoutCompute --> commit --> effects
  commit --> appRoot
  commit --> cardIframe
  commit --> moduleSlot
  commit --> offscreen

  validate --> components
  themeResolve --> tokens --> scope --> css
  themePkg --> tokens
  components --> css
```

渲染约束：

- L8 只描述语义结构，不承载颜色、阴影、圆角、字体等视觉值。
- L9 对声明树执行归一、契约校验、主题解析、布局计算、提交与副作用分发。
- `runtime-effect` 禁止在渲染阶段直接触发。
- 非兼容能力必须在 Validate 阶段失败，不允许 silent fallback。
- 组件库只输出无头结构和接口点，视觉由主题运行时和主题包注入。

---

## 9. 生态开发到运行链路

```mermaid
flowchart LR
  dev["开发者"]
  scaffold["chipsdev create<br/>脚手架生成工程"]
  code["React / TS / manifest.yaml<br/>主题 / 多语言 / 权限"]
  build["chipsdev build<br/>构建 dist"]
  validate["chipsdev validate<br/>Manifest / 契约 / 资源门禁"]
  package["chipsdev package<br/>生成 .cpk"]
  install["chips plugin install<br/>Host 安装插件"]
  enable["chips plugin enable<br/>启用插件"]
  run["Host Runtime<br/>加载 / 会话 / Bridge 注入"]
  quality["质量门禁<br/>typecheck / test / contract / perf / a11y"]

  dev --> scaffold --> code --> build --> validate --> package --> install --> enable --> run
  code --> quality
  validate --> quality
  run --> quality
```

开发链路边界：

- 工程根 `manifest.yaml` 是插件工程唯一正式清单源。
- `runtime.targets` 是五类插件的基础要求。
- `app` 插件必须声明 `ui.surface`。
- `card` 插件正式解析依据是 `capabilities.cardTypes`。
- `layout` 插件必须声明 `layout.layoutType`。
- `module` 插件必须声明 `module.provides`。
- `theme` 插件必须使用对象形式 `entry.tokens / entry.themeCss`。

---

## 10. 社区平台与本地 Host 协作

```mermaid
flowchart TB
  host["Chips Host / 应用插件"]
  uploader["社区上传模块 / 应用插件<br/>通过 SDK / Bridge 调用 Host 能力"]
  api["社区平台 HTTP API<br/>/api/v1"]
  admin["后台管理 API<br/>/admin/api/v1"]
  auth["认证<br/>Access Token + Refresh Cookie"]
  upload["上传会话<br/>upload-sessions / resources / card / box"]
  cards["卡片接口<br/>detail / open-view / cover / render-cache"]
  boxes["箱子接口<br/>detail / list / update"]
  rooms["用户 / 空间 / 房间<br/>space / rooms / contents"]
  discover["发现与搜索<br/>discover / search"]
  serverStorage["服务端存储<br/>卡片 / 箱子 / 渲染缓存 / 封面缓存"]

  host --> uploader --> api
  api --> auth
  api --> upload
  api --> cards
  api --> boxes
  api --> rooms
  api --> discover
  admin --> serverStorage
  upload --> serverStorage
  cards --> serverStorage
  boxes --> serverStorage
  rooms --> serverStorage
  discover --> serverStorage
```

社区边界：

- 社区平台使用 JSON 与 multipart/form-data，统一响应结构为 `data` 或 `error`。
- 卡片打开入口优先使用 `open-view`，不把历史 `htmlUrl` 作为新客户端正式打开入口。
- 房间公开不代表房间内所有卡片或箱子公开，内容仍以各自 `visibility` 为准。
- 本地插件访问社区能力仍应通过 Host、SDK、Bridge 与正式网络模块/应用链路治理。

---

## 11. 图例

| 图形元素 | 含义 |
|---|---|
| 实线箭头 | 正式依赖、调用或数据流 |
| 虚线箭头 | 映射、引用、兼容别名或非直接承载关系 |
| `surface` | 跨平台界面容器公共语义 |
| `window` | Desktop 当前窗口映射或兼容别名 |
| `namespace.action` | Host Kernel 路由动作名格式 |
| ZIP Store | ZIP 零压缩存储模式 |

---

## 12. 已核对实现锚点

| 方向 | 当前核对锚点 |
|---|---|
| Host 运行时与服务域 | `Chips-Host/src/main/services/register-host-services.ts`、`Chips-Host/src/runtime/plugin-runtime.ts` |
| Bridge 与 preload | `Chips-Host/src/preload/create-bridge.ts`、`Chips-Host/packages/bridge-api/src/*` |
| Runtime Client 与 Hooks | `Chips-Host/src/renderer/runtime-client.ts`、`Chips-Host/src/renderer/hooks.ts` |
| L8 / L9 | `Chips-Host/src/renderer/declarative-ui/*`、`Chips-Host/packages/unified-rendering/src/*` |
| 主题运行时 | `Chips-Host/src/main/theme-runtime/*` |
| 插件 manifest 示例 | `Chips-EditingEngine/manifest.yaml`、`Chips-BaseCardPlugin/richtext-BCP/manifest.yaml`、`Chips-BoxLayoutPlugin/grid-BLP/manifest.yaml`、`Chips-ModulePlugin/Chips-ColorPicker/manifest.yaml`、`ThemePack/Chips-default/manifest.yaml` |
| SDK 与工具链 | `Chips-SDK/package.json`、`Chips-SDK/src/*`、`Chips-SDK/tests/*` |
| 组件库 | `Chips-ComponentLibrary/package.json`、`Chips-ComponentLibrary/packages/*` |
| Host 测试入口 | `Chips-Host/tests/contract/route-manifest.contract.test.ts`、`Chips-Host/tests/integration/host-services.test.ts`、`Chips-Host/tests/unit/*` |
