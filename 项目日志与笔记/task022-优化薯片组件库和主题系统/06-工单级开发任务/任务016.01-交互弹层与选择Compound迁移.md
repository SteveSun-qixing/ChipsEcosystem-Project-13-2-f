# 任务016.01：交互弹层与选择 Compound 迁移

## 1. 任务目标

把 `Dialog / Popover / Tabs / Menu / Select` 迁移为正式 Compound API，先收口交互弹层、导航切换和基础选择组件的 Root/Trigger/Content/Item 等 slot 规则，减少单体 props 膨胀。

## 2. 父任务

- `任务016-组件库复杂组件Compound迁移.md`
- 对应阶段任务：`05-开发任务方案/任务09-复杂组件Compound-API与样式契约.md`

## 3. 涉及项目

- `Chips-ComponentLibrary/packages/components`
- `Chips-ComponentLibrary/packages/theme-contracts`
- `Chips-Host/src/renderer/declarative-ui`
- `Chips-Host/tests/unit`
- `生态共用技术文档/组件库`

## 4. 开发内容

1. 为 `Dialog` 建立 `Root / Trigger / Content / Header / Body / Footer / Actions / Close` Compound API。
2. 为 `Popover` 建立 `Root / Trigger / Content / Arrow` Compound API。
3. 为 `Tabs` 建立 `Root / List / Trigger / Panel` Compound API。
4. 为 `Menu` 建立 `Root / Trigger / Content / Item / Group / Separator` Compound API。
5. 为 `Select` 建立 `Root / Trigger / Content / Option / Value` Compound API。
6. 保持组件库无头职责：Root 负责状态、上下文、ARIA 关系和事件分发；子组件只消费上下文并输出公开 `data-part`。
7. 补齐 TypeScript 声明、组件测试、a11y 规则、component contract 和公共文档。
8. 补齐 Host L8 `StandardCompoundSlotSchemas` 中本批组件对应 slot schema 与诊断测试。

## 5. 验收标准

- 五个组件均具备清晰 Compound API，且默认推荐 API 不再依赖 `show* / with* / has* / is*` 模式布尔值。
- `data-scope/data-part/data-state` 与 contract parts 保持一致，contract 覆盖 Root/Trigger/Content/Item 等公开部件。
- L8 可校验本批组件的必填 slot、单例 slot、可重复 slot 和 slot 类型。
- a11y 与键盘主流程测试覆盖 Dialog Esc/Close、Popover Esc、Tabs roving focus、Menu roving focus、Select listbox 选择。
- 不新增第三方依赖，不引入主题包纯视觉提前任务；只有 contract/token/CSS 确实变化时才同步主题包。

## 6. 验证命令

```bash
cd Chips-ComponentLibrary
npm run verify
npm run quality:gate

cd ../Chips-Host
npm test
```

