# 任务024.07：基础卡片脚手架 vNext 最终验收

## 1. 任务目标

对任务024全部改动进行最终验收，确认基础卡片脚手架生成工程可作为后续八个基础卡片插件迁移的正式基线。

## 2. 范围

- `Chips-Scaffold/chips-scaffold-basecard`
- `生态共用技术文档/插件开发/03-卡片插件开发.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志`
- 必要时只读核对现有 `Chips-BaseCardPlugin/*`

## 3. 开发内容

1. 复跑完整脚手架验证。
2. 生成工程后检查 manifest、三件套导出、组件库依赖、i18n、主题 token、资源桥和测试结果。
3. 核对现有基础卡片插件迁移任务是否能沿用新模板口径。
4. 写任务024最终工作日志。

## 4. 验收标准

- `chips-scaffold-basecard` 的 `build/test/test:templates/test:e2e` 全部通过。
- 生成工程符合任务024 验收标准。
- 无新增未登记的公共契约漂移。

## 5. 验证命令

```bash
cd Chips-Scaffold/chips-scaffold-basecard
npm run build
npm test
npm run test:templates
npm run test:e2e
```
