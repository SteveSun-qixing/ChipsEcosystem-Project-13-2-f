# 任务028.07：迁移文档验收与任务028闭环

## 1. 任务目标

对任务028的生态设置面板迁移成果做最终验收，补齐项目文档、README、工作日志、验证记录和后续问题工单关系，确保设置面板可以作为新框架治理能力的第一验收应用。

## 2. 背景

任务028横跨设置面板、SDK、Host、组件库、公共文档和开发工具链。前序子任务完成后，需要集中确认用户可见功能、架构红线、权限基线、文档边界和验证命令都已经闭环。

## 3. 开发内容

1. 复核任务028.01 至 028.06 的提交、工作日志、验证结果和未闭环边界。
2. 更新 `Chips-EcoSettingsPanel/README.md`、`技术文档/00-文档索引.md`、架构/数据流/测试/验收文档。
3. 如公共接口或开发工具口径发生变化，同步 `生态共用技术文档/` 中对应正式规范；仅项目内部信息保留在设置面板技术文档。
4. 复核 `manifest.yaml`、`package.json`、`chips.config.mjs`、测试和正式运行链路，确保文档不是历史口径。
5. 整理任务028工作日志，列出最终验证命令、结果、已登记工单和后续不属于任务028的明确边界。
6. 执行最终验证并提交闭环记录。

## 4. 验收标准

- 任务028所有子任务均有任务文档、工作日志和对应提交记录。
- README 与技术文档描述当前真实代码、manifest、测试和运行链路。
- 公共生态信息已同步到 `生态共用技术文档/`，项目内部实现细节留在 `Chips-EcoSettingsPanel/技术文档/`。
- 未闭环的架构缺口都有问题工单或后续任务说明，不散落在临时笔记中。
- 最终 `Chips-EcoSettingsPanel npm run verify` 通过；如涉及 SDK/Host 公共契约，相关仓库验证同步通过。

## 5. 验证命令

```bash
cd Chips-EcoSettingsPanel
npm run verify
```

视最终改动范围补跑：

```bash
cd Chips-SDK
npm test

cd ../Chips-Host
npm run build
npm test
```

## 6. 后续衔接

任务028闭环后，按顺序进入任务029。任务029启动时再纳入已经写入 `08-草稿笔记` 的卡片查看器迁移启动前复核报告。
