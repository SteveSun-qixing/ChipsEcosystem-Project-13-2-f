# 任务029.07：文档验收与任务029闭环

## 1. 任务目标

对任务029卡片查看器应用迁移做最终验收，补齐项目 README、必要内部技术文档、公共契约同步、工作日志、验证记录和后续问题边界，确保查看器成为 `.card/.box` 正式查看入口样板。

## 2. 背景

任务029会触及应用框架、SDK 文档窗口、Host 渲染、箱子布局 runtime、组件库命令消费、主题、多语言和真实素材回归。最终阶段必须重新核对当前代码、manifest、测试和正式运行链路，再决定哪些信息应沉淀到项目内部文档或生态共用技术文档。

## 3. 开发内容

1. 复核任务029.01 至 029.06 的提交、工作日志、验证结果和未闭环边界。
2. 更新 `Chips-CardViewer/README.md`，确保主显示链路写为当前真实的 `client.document.window.render(...)`。
3. 如存在项目内部技术文档，补齐 AppRuntime、Scene、命令、主题、i18n、文档窗口和测试矩阵。
4. 如公共接口、SDK 显示链路、sandbox、命令权限或 box runtime 契约发生变化，同步 `生态共用技术文档/` 中对应正式规范。
5. 清理或归档旧私有 hook、旧 i18n 工具或失效说明，禁止直接删除历史文件。
6. 汇总最终验证命令、结果、已登记工单和不属于任务029的后续边界。
7. 执行最终验证并提交闭环记录。

## 4. 验收标准

- 任务029所有子任务均有任务文档、工作日志和阶段提交记录。
- README 与技术文档描述当前真实代码、manifest、测试和运行链路。
- 公共生态信息已同步到 `生态共用技术文档/`，项目内部实现细节留在 `Chips-CardViewer` 项目内。
- 未闭环的架构缺口都有问题工单或后续任务说明。
- `Chips-CardViewer` 最终验证通过，必要时 SDK/Host 相关验证同步通过。

## 5. 验证命令

```bash
cd Chips-CardViewer
npm run lint
npm test
npm run build
npm run validate
```

视最终改动范围补跑：

```bash
cd Chips-SDK
npm test

cd ../Chips-Host
npm test
```

## 6. 后续衔接

任务029闭环后，按顺序进入任务030编辑引擎应用迁移。任务030启动时应复用 `08-草稿笔记` 中的前置勘察成果，并在开始前按实际工程量一次性创建合适颗粒度的子任务文档。
