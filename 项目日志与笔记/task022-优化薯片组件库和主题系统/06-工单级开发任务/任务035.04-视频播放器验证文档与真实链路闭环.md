# 任务035.04：视频播放器验证文档与真实链路闭环

## 1. 任务目标

完成任务035的工程验证、项目文档更新和工作日志闭环，确认迁移后的正式链路与当前实现一致。

## 2. 开发内容

1. 更新 `Chips-VideoPlayer` 的 README、技术文档与需求/验收说明中已变化的架构口径。
2. 补充单元测试覆盖：
   - AppRuntime 导出与运行时结构；
   - 命令定义 schema；
   - 启动资源恢复；
   - 播放器纯函数；
   - 字幕/音轨轨道读取；
   - 媒体错误 key；
   - 资源切换复位；
   - 全屏状态恢复；
   - Host 注入主题快照读取。
3. 执行任务035要求的正式脚本：
   - `npm run lint`
   - `npm test`
   - `npm run build`
   - `npm run validate`
4. 将阶段结论写入工作日志。

## 3. 验收标准

- 正式验证命令通过，或对未通过原因给出明确证据和后续工单。
- 文档与 `manifest.yaml`、当前代码、测试脚本保持一致。
- 任务035的未闭环风险被显式记录。
