# 任务055：安装打包发布链路验收

## 1. 任务目标

打通新框架重构后的 Host、五类插件、主题包、工作区、`.cpk`、`.card`、`.box` 和安装器发布链路，确保开发态、用户态、发布态一致运行。

## 2. 对应阶段任务

- `05-开发任务方案/任务28-安装打包发布与工作区迁移链路.md`

## 3. 涉及项目

- `Chips-Host`
- `Chips-SDK`
- `Chips-Scaffold/*`
- `Chips-BaseCardPlugin/*`
- `Chips-BoxLayoutPlugin/*`
- `Chips-ModulePlugin/*`
- `ThemePack/*`
- `release-artifacts/`

## 4. 开发内容

1. 重新核对 CPK 格式、Manifest 规范、打包与安装路径手册、Host CLI 和 chipsdev CLI。
2. 验证五类插件均可 build、validate、package，并安装到开发工作区。
3. 验证主题包可安装、启用、应用、切换和 contract 校验。
4. 验证应用插件系统快捷方式、文件关联、launchContext 和卸载清理。
5. 验证模块插件按需加载、能力发现、调用、job、取消和卸载。
6. 验证 `.card` 和 `.box` 在安装态 Host 中可打开、渲染、编辑、保存、导出。
7. 生成打包报告、安装报告、工作区迁移报告和发布阻断清单。

## 5. 验收标准

- 不只验证源码开发态，必须验证已安装副本运行态。
- `.chips-host-dev` 不被当作终端用户运行目录。
- 五类插件均有正式打包和安装验收记录。
- 发布阻断问题全部登记为工单。

## 6. 验证命令

```bash
npm run verify:workspace

cd Chips-Host
npm run build
npm test

cd ../Chips-SDK
npm test
```

## 7. 注意事项

- 不自行提交、打 tag 或发布。
- 本任务开发前必须重新核对最新打包手册、Host CLI、SDK CLI 和 release-artifacts。
- 本任务已经在继续执行前一次性拆分为 `055.01` 到 `055.08` 八个子任务；后续推进任务055时，必须按子任务编号顺序执行、验证、记录和提交阶段成果。
- 任务055不再继续新增 `055.x` 子任务文档；如果执行中发现补充事项，优先写入对应既有子任务文档，或按问题性质登记工单/后续任务。
- 后续其他大任务如需拆分，也必须在开始执行该大任务前一次性列出全部子任务，并先创建完整任务文档；不得完成一个小任务后再临时拆下一个。

## 8. 一次性子任务清单

任务055按安装打包发布链路从“开发态预检”到“最终发布闭环”拆成以下八个子任务。八个任务文档已全部创建，执行过程中不得跳号、并行抢跑或临时新增更细子任务。

| 编号 | 任务文档 | 任务边界 | 执行顺序 |
| --- | --- | --- | --- |
| 055.01 | [任务055.01-预检与开发工作区安装态抽样.md](./任务055.01-预检与开发工作区安装态抽样.md) | 复核公共契约、CPK 格式、五类插件脚本和开发工作区安装启用抽样。 | 第一阶段，必须先完成。 |
| 055.02 | [任务055.02-用户态工作区安装与文件打开抽样.md](./任务055.02-用户态工作区安装与文件打开抽样.md) | 使用正式 `chips` CLI 与临时用户工作区验证插件安装、主题应用和 `.card/.box` 文件打开分发。 | 依赖 055.01。 |
| 055.03 | [任务055.03-模块真实调用与安装器预检.md](./任务055.03-模块真实调用与安装器预检.md) | 验证模块真实能力调用、Host 安装器专项测试、app-entry 测试和 SDK 全量预检。 | 依赖 055.02。 |
| 055.04 | [任务055.04-Host-macOS正式安装器产物构建与payload洁净度验收.md](./任务055.04-Host-macOS正式安装器产物构建与payload洁净度验收.md) | 生成真实 `Chips.app` 与 `.pkg`，验证文件关联、内置插件和 payload 洁净度。 | 依赖 055.03。 |
| 055.05 | [任务055.05-模块Job取消与Provider清理验收.md](./任务055.05-模块Job取消与Provider清理验收.md) | 验证模块 job、取消、provider 禁用启用恢复和卸载清理。 | 依赖 055.04 的安装器基线，可在不破坏顺序的情况下复用前序构建产物。 |
| 055.06 | [任务055.06-安装副本系统入口与LaunchServices验收.md](./任务055.06-安装副本系统入口与LaunchServices验收.md) | 验证安装副本、系统入口、设置面板快捷方式、LaunchServices 边界和卸载清理要求。 | 依赖 055.04 与 055.05 的发布态和生命周期验收。 |
| 055.07 | [任务055.07-cardbox安装态渲染编辑保存导出验收.md](./任务055.07-cardbox安装态渲染编辑保存导出验收.md) | 验证 `.card/.box` 在安装态或安装等价语境下打开、渲染、编辑、保存和导出。 | 依赖 055.06 的安装副本入口基线。 |
| 055.08 | [任务055.08-发布报告阻断清单与任务055闭环.md](./任务055.08-发布报告阻断清单与任务055闭环.md) | 汇总发布报告、阻断清单、人工验收项、公共文档同步和任务055最终状态。 | 最终阶段，必须在 055.01-055.07 均完成后执行。 |

## 9. 阶段状态（2026-05-26）

### 阶段1：预检与开发工作区安装态抽样

状态：已完成。

已收口内容：

- 官方 `app/card/layout/module/theme` 五类工程均具备 `build / validate / package` 验收入口，其中本阶段补齐了非基础卡片工程缺失的 `npm run package` 脚本。
- 当前 20 个 `.cpk` 产物均包含包根 `manifest.yaml` 与 `dist/` 内容，且未发现嵌套历史 `.cpk`。
- 使用临时 `CHIPS_HOME` 开发工作区完成基础卡片、箱子布局、应用插件、模块插件、默认主题包的安装与启用抽样；默认主题额外完成 `theme apply` 与 `theme validate`。
- `npm run verify:workspace` 通过，开发工作区与用户工作区未混用。

尚未完成内容：

- Host 正式安装器 `.pkg` 构建、用户态 `chips` 工作区安装验收、`.card/.box` 安装态打开/编辑/导出、应用快捷方式与文件关联、模块真实能力调用与取消/卸载清理仍需后续阶段继续执行。

### 阶段2：用户态工作区安装与文件打开抽样

状态：已完成。

已收口内容：

- 使用 `chips` 正式 CLI 与临时 `CHIPS_HOME=/tmp/chips-task055-user-*` 验证用户态工作区，`chips status` 与 `chips doctor` 均显示 `kind: user`，未写入真实 `~/.chips-host`。
- 默认主题包完成用户态 `plugin install`、`plugin enable`、`theme apply`、`theme current`、`theme validate`。
- 卡片查看器应用插件完成用户态 `plugin install`、`plugin enable`、`plugin disable`、`plugin uninstall` 与 `plugin list`。
- 基础卡片、箱子布局、模块插件完成用户态安装启用矩阵抽样。
- 安装默认主题与卡片查看器后，`chips open` 可把 `.card` 与 `.box` 样例分发给 `com.chips.card-viewer`，并返回 card/box 模式与 `windowId`。

尚未完成内容：

- Host macOS 正式 `.pkg` 构建、系统级快捷方式/文件关联注册、模块真实能力调用与取消、完整卸载清理、`.card/.box` 编辑保存导出仍需后续阶段继续执行。

### 阶段3：模块真实调用与安装器预检

状态：已完成。

已收口内容：

- 使用 `chipsdev module invoke` 拉起真实 Electron Host，按 `--manifest` 安装启用 `chips.module.chips.colorpicker`，调用 `image.color.pick/pick` 并成功返回颜色分析结果。
- 运行 `Chips-Host` 安装器与 app-entry 专项测试，覆盖 macOS 安装器 staging、内置插件资源、document types、`pkgbuild/productbuild` 调用参数和启动文件分发入口。
- 运行 `Chips-SDK` 全量测试脚本，覆盖 CLI smoke、host-managed、create workspace、create card/layout/module/theme、module invoke、package compatibility 与 tooling 命令。

尚未完成内容：

- 尚未执行真实 `npm run build:installer:macos` 生成 `.pkg`，也未做系统安装后的图形界面、快捷方式和文件关联人工验收；这些保留到正式发布验收阶段。

### 阶段4：Host macOS 正式安装器产物构建与 payload 洁净度验收

状态：已完成。

已收口内容：

- 执行真实 `npm run build:installer:macos -- --output=../release-artifacts/task055-stage4-macos-installer-20260526-clean`，成功生成 `Chips.app` 与 `Chips-Host-0.1.0-macos.pkg`。
- `Chips.app/Contents/Info.plist` 包含 `.card` 文件关联，以及图片查看器提供的 `.png/.jpg/.jpeg/.webp/.gif/.bmp/.svg/.avif` 文件关联。
- 内置插件目录包含默认主题包、生态设置面板和图片查看器，默认主题包含 contract、tokens、theme.css 与 `.cpk` 产物。
- 发现 `.pkg` payload 初始混入 `._*` AppleDouble 元数据，登记并解决工单110；安装器现会净化 component pkg 的 payload 与 BOM。
- clean `.pkg` 复核命令 `pkgutil --payload-files ... | rg '/\\._|^\\._|/\\.DS_Store|CodeResources'` 无输出。
- 安装器专项测试 7 条通过，Host TypeScript 构建通过。

尚未完成内容：

- 尚未执行系统级安装后的启动台快捷方式、LaunchServices 文件关联人工验收、卸载清理、`.card/.box` 图形界面编辑保存导出；这些继续保留到任务055后续阶段。

### 阶段5：模块 job、取消与 provider 清理验收

状态：已完成。

已收口内容：

- Host 集成测试已覆盖 `module.invoke` 同步与 job 方法、`module.job.get`、`module.job.cancel`、超时失败、取消事件和 consumes 权限校验。
- 本阶段补充 provider 清理断言：模块插件 `plugin.disable` 后 provider 全量治理列表保留 `disabled` 状态，但 `status=enabled/running` 查询为空，`module.resolve` 不会选择该 provider。
- 本阶段补充 `plugin.enable` 恢复 provider、`plugin.uninstall` 后 provider 列表清空、`module.invoke` 返回 `MODULE_PROVIDER_NOT_FOUND` 的断言。
- 执行 `Chips-ModulePlugin/Chips-HtmltoPDF-Plugin` packaged e2e，验证 `.cpk` 安装启用后通过 `converter.html.to-pdf/convert` 返回 job，并生成 PDF 输出。

尚未完成内容：

- 系统级安装后的快捷方式、LaunchServices 文件关联人工验收、卸载清理、`.card/.box` 图形界面编辑保存导出仍需后续阶段继续执行。

### 阶段6：安装副本系统入口与 LaunchServices 非破坏性验收

状态：已完成非破坏性验收。

已收口内容：

- 重新生成 `release-artifacts/task055-stage6-macos-installer-20260526-clean`，得到 `Chips.app` 与 `Chips-Host-0.1.0-macos.pkg`。
- `.pkg` payload 包含 `/Applications/Chips.app`、`Contents/MacOS/Chips`、Host app-entry 和默认主题、生态设置面板、图片查看器三个内置插件 manifest。
- `Info.plist` 中 `CFBundleIdentifier = local.chips.host`，并包含 `.card` 与图片查看器扩展名 `.png/.jpg/.jpeg/.webp/.gif/.bmp/.svg/.avif` 文件关联。
- `pkgutil --payload-files` 与 `pkgutil --expand` 后元数据检查均无输出；工单110补充修复已覆盖 `Scripts/._postinstall` 等非 payload 元数据残留。
- 使用发布包内 CLI、打包后的内置插件目录、隔离 `HOME` 与临时 `CHIPS_HOME` 验证内置插件引导，默认主题、生态设置面板、图片查看器均可自动安装启用。
- 隔离 HOME 下设置面板快捷方式 `Applications/Chips Apps/生态设置面板.app` 可生成，包含中文名称、图标、临时 workspace 参数和 `--chips-launch-plugin=com.chips.eco-settings-panel`。
- `Chips-Host` 安装器专项测试 7 条通过，Host TypeScript 构建通过。

尚未完成内容：

- 未执行真实系统安装到 `/Applications/Chips.app`。
- 未执行 LaunchServices 真实注册、启动台显示、系统双击 `.card` 和卸载清理。
- 当前 `.pkg` 未签名，签名与 notarization 需进入任务055.08最终发布阻断/人工发布清单。
- `.card/.box` 图形界面编辑保存导出继续留给任务055.07。

### 阶段7：`.card/.box` 安装态渲染编辑保存导出验收

状态：自动验收已完成，真实系统 GUI 人工验收项保留到阶段8。

已收口内容：

- Host 集成测试新增真实成品素材服务路由验收，使用 `ProductFinishedProductTestingSpace/富文本基础卡片.card` 覆盖 `card.validate`、`card.render`、`card.renderEditor`、`card.unpack`、`card.pack` 与重新校验。
- Host 集成测试使用 `ProductFinishedProductTestingSpace/美食网格箱子.box` 派生临时 fixture，覆盖 `box.validate`、`box.inspect`、`box.normalizeLayoutConfig`、`box.getLayoutInitialQuery`、`box.openView`、`box.renderLayoutFrame`、`box.renderLayoutEditor`、`box.renderEntryCover`、`box.unpack`、`box.pack` 与重新校验。
- 使用阶段7安装副本 `release-artifacts/task055-stage7-macos-installer-20260526-clean/Chips.app` 内置 CLI 与隔离用户工作区，完成默认主题、卡片查看器、编辑引擎、基础卡片、网格布局和导出模块插件安装启用抽样。
- 安装副本等价 CLI 下，`chips open` 可将 `富文本基础卡片.card` 与 `美食网格箱子.box` 分发到 `com.chips.card-viewer`，分别返回 `mode: "card"` 与 `mode: "box"`。
- 发现并解决工单112：Host 主题注入边界统一把进入浏览器普通 CSS 的 `cpx` 转换为 `vw`，避免应用窗口、卡片 iframe 和箱子布局 iframe 因 raw `cpx` 排版失效。
- 公共主题文档已同步 `cpx` 浏览器落地口径，明确主题源码和 `theme.getAllCss` 可以保留 `cpx`，Host 注入普通浏览器 CSS 前统一转换。

尚未完成内容：

- 未执行真实系统安装到 `/Applications/Chips.app` 后的 GUI 打开、编辑、保存和导出人工验收。
- 未执行 LaunchServices 真实双击 `.card/.box` 的系统分发验收。
- 未在真实图形编辑引擎窗口中人工操作 `.card/.box` 保存与导出 HTML/PDF/image。
- 当前 `.pkg` 未签名，签名与 notarization 继续进入任务055.08最终发布阻断/人工发布清单。

验证记录：

- `cd Chips-Host && npm test -- --run tests/unit/theme-css-units.test.ts tests/unit/card-service-rendering.test.ts tests/unit/box-service.test.ts tests/integration/host-services.test.ts` 通过，4 个测试文件、62 条测试通过。
- `cd Chips-Host && npm run build` 通过。

下一步：

- 进入 `055.08`，汇总任务055全部自动验收、人工验收项、发布阻断清单、工单处理状态和公共文档同步情况。
