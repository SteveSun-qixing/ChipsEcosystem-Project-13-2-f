# 任务057：全生态最终质量门禁

## 1. 任务目标

在所有底座、SDK、组件库、主题、脚手架、应用插件、卡片插件、布局插件、模块插件、社区平台和真实素材回归完成后，执行最终质量门禁，判断是否具备第一版发布条件。

## 2. 对应阶段任务

- `05-开发任务方案/任务30-最终全生态质量门禁与发布验收.md`

## 3. 涉及项目

- 全生态工作区。

## 4. 开发内容

1. 汇总所有任务的验证结果、失败项、阻断项、性能结果和文档状态。
2. 执行根工作区、Host、SDK、组件库、主题包、脚手架、应用插件、卡片插件、布局插件、模块插件的正式脚本。
3. 执行社区平台 build/lint/test。
4. 执行安装打包验收和真实素材回归。
5. 执行禁用口径扫描：临时实现、TODO、硬编码主题、直连 Host、跨服务 import、未走 i18n。
6. 生成最终质量门禁报告：可发布、条件发布或不可发布。
7. 对所有 P0/P1 问题创建或补充工单。

## 5. 验收标准

- P0/P1 问题不得进入可发布结论。
- 所有正式脚本通过或有明确环境限制说明。
- 架构红线扫描无未解释违规。
- 质量门禁报告能支撑第一版发布决策。

## 6. 验证命令

```bash
npm run verify:workspace

cd Chips-Host && npm run build && npm test
cd ../Chips-SDK && npm test
cd ../Chips-ComponentLibrary && npm run quality:gate
```

## 7. 注意事项

- 不为了发布通过而降低质量门禁。
- 本任务执行前必须重新核对所有 package 脚本和 manifest，不能使用过期命令清单。

## 8. 当前状态

已完成。执行日期：2026-05-26。

本轮按用户最新要求提高执行效率，采用“先复核正式脚本与真实代码，再按失败范围定向补跑”的方式推进；没有重复执行已在任务055、任务056中完成且无新增改动的大量安装态与真实素材矩阵。

## 9. 本轮修复

1. 修复主题包脚手架 `theme-standard` 与当前组件契约漂移的问题。
   - 新增 `tokens/comp/rating.json`，内容与默认主题包当前 `rating` token 保持一致。
   - 更新 `styles/components/form-controls.css`，补齐 `rating` 组件的 root、label、item、icon、status 和 focus 样式。
   - 修复后生成主题工程通过 71 个组件 contract、722 个必需 token 校验。

2. 修复 `Chips-EditingEngine` 单测仍按旧富文本类型口径断言的问题。
   - 当前正式富文本基础卡片类型为 `base.richtext`。
   - `RichTextCard` 仅作为历史别名由 registry 解析。
   - 新建/归一化后的富文本配置必须持久化 `card_type: base.richtext`，并显式保存 `markdown_capabilities`。

## 10. 最终验证记录

| 范围 | 命令 | 结果 |
| --- | --- | --- |
| 工作区 | `npm run verify:workspace` | 通过 |
| Host | `npm run build` / `npm test` / `npm run test:contract` / `npm run test:perf:l9` | 通过 |
| SDK | `npm test` | 通过 |
| ComponentLibrary | `npm run verify` / `npm run quality:gate` | 通过 |
| 主题脚手架 | `npm run build` / `npm test` / `npm run test:e2e` | 通过 |
| EditingEngine | `npm test` / `npm run build` / `npm run validate` | 通过 |
| 应用插件抽样 | BookReader、CardViewer、EcoSettingsPanel、MusicPlayer、PhotoViewer、RichTextEditor、VideoPlayer 正式脚本 | 通过 |
| 基础卡片插件 | 8 个基础卡片插件 `lint/test/build/validate`，并补充 `typecheck` | 通过 |
| 箱子布局插件 | 2 个布局插件 `lint/test/build/validate` | 通过 |
| 模块插件 | 5 个模块插件 `lint/test/build/validate` | 通过 |
| 真实素材回归 | 任务056报告引用 | 自动化回归通过 |
| 安装打包链路 | 任务055报告引用 | 自动化闭环通过 |

## 11. 红线扫描结论

本轮执行了窄口径质量扫描：

```bash
rg -n "TODO|FIXME|HACK|临时实现|简化版|占位实现|temporary implementation|placeholder implementation" ...
rg -n "from ['\"]electron['\"]|import\\(['\"]electron['\"]\\)|BrowserWindow|ipcRenderer|ipcMain" ...
rg -n "\\{\\{\\s*[A-Z0-9_]+\\s*\\}\\}|app-standard|theme-standard" Chips-Scaffold/chips-scaffold-theme/templates/theme-standard
```

结论：

- 源码未发现新增 TODO/FIXME/HACK 或未收口占位实现。
- 应用插件、基础卡片插件、箱子布局插件、模块插件侧未发现直接 `import("electron")` 或直接依赖 `ipcRenderer/ipcMain`。
- 主题脚手架模板未发现未渲染模板占位符。
- `BrowserWindow` 在主题脚手架中仅出现于 preview matrix 描述文本，不是运行时依赖。

保留边界：

- `Chips-EditingEngine` 历史 CSS 中仍存在若干 token fallback 和封面模板内嵌颜色；这属于后续编辑引擎视觉产品化治理范围，本轮未作为任务057测试修复提交混入。
- `Chips-CommunityPlatformServer` 仍保留旧“应用市场”方向工程状态，用户已明确当前薯片生态不需要应用市场，任务051已取消并应改为“卡片和箱子分享平台”另行设计；因此不作为第一版应用市场阻断处理。
- `OpenSourceProjectHandlingFolder/DPlayer-master` 与 `thorium-reader-develop` 是开放项目同化样本，不作为当前生态正式依赖；其构建环境问题不阻断核心生态发布门禁。
- 任务059按用户最新要求不继续执行，不作为任务057前置条件。

## 12. 发布判断

自动化质量门禁结论：核心生态当前达到“条件发布”状态。

条件发布含义：

- 核心 Host、SDK、组件库、主题、脚手架、应用插件、卡片插件、布局插件、模块插件、安装打包链路和真实素材自动化回归未发现新的 P0/P1 阻断。
- 对外正式发布前仍必须完成任务055已经记录的人工系统验收项，包括真实 `/Applications` 安装、LaunchServices、GUI 保存导出、签名、公证和卸载清理。
- 任务058需要把本轮已经稳定的公共契约、开发规范、质量门禁和发布边界写入 `生态共用技术文档/`，并在每次编写前重新核对对应代码、manifest、测试和正式运行链路。
