# 任务059.05：箱子布局插件开发技能 vNext 创建与更新

## 0. 当前状态

- 状态：已取消，不再执行。
- 取消时间：2026-05-26。
- 取消原因：用户明确要求任务059取消；后续不再推进箱子布局插件开发技能创建或更新。
- 历史说明：本文档仅保留原任务设想作为历史记录，不作为后续执行计划或正式开发依据。

## 1. 任务目标

创建或更新 `chips-boxlayout-plugin-dev` 技能，使箱子布局插件开发、迁移、检查与验证基于任务025最终脚手架和公共文档口径。

## 2. 范围

- 箱子布局插件开发技能目录
- `生态共用技术文档/插件开发/04-布局插件开发.md`
- `生态共用技术文档/协议与契约/08-箱子布局运行时契约.md`
- `Chips-Scaffold/chips-scaffold-boxlayout`
- `Chips-BoxLayoutPlugin/*`

## 3. 开发内容

1. 审查旧箱子布局技能，清除旧 Box Runtime 字段和旧模板口径。
2. 写清 `layoutDefinition`、`renderView`、`renderEditor`、Box Runtime、资产桥和 cleanup。
3. 写清 `.box` 持久化 snake_case 与插件运行时 camelCase 的边界。
4. 写清 `chipsdev create layout`、`npm run verify`、打包、安装启用和 `.box` 联调。

## 4. 验收标准

- 技能与任务025最终模板、公共文档和真实 CLI e2e 一致。
- 技能不会引导布局插件直接读写 `.box` 文件或绕开 Host。

## 5. 注意事项

- 依赖任务025全部完成。
- `grid-BLP/list-BLP` 业务迁移仍按任务044/045执行。
