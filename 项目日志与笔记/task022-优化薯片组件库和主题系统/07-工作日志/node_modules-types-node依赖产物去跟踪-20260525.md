# node_modules/@types/node 依赖产物去跟踪

## 时间

2026-05-25

## 背景

用户要求解决 `node_modules/@types/node` 脏改动。检查后确认根目录 `node_modules/@types/node` 已被 `.gitignore` 忽略，没有进入 Git 状态；真正被 Git 跟踪的是 `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node` 下的 73 个依赖产物文件。

## 处理

- 核对根目录 `.gitignore` 已包含 `**/node_modules/`。
- 使用 `git rm -r --cached -- Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node` 将误跟踪的依赖产物从 Git 索引移除。
- 保留本地 `node_modules` 实体文件不动，避免影响当前本地依赖安装状态。

## 验证

- `git ls-files Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node` 返回 0 条。
- `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/package.json` 本地文件仍存在。
- 本次不修改脚手架源码、模板、正式契约或运行链路。

## 后续观察

当前仓库仍有少量历史误跟踪的 `node_modules/.package-lock.json`，不属于本次 `@types/node` 脏改动范围，后续如需要可单独做依赖产物去跟踪清理。
