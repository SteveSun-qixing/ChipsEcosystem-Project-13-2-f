# 任务001：Host 服务域与 Bridge 契约审计工作日志

## 时间

2026-05-23 17:58

## 本次目标

按 `任务001-Host服务域与Bridge契约审计.md` 审计 Host 服务域、Bridge action、SDK route manifest 与共享文档一致性，为后续 L8/L9、surface、commands、theme、plugin 迁移建立可靠契约基线。

## 阅读与核对范围

- `生态设计原稿/02-极致模块化架构.md`
- `生态共用技术文档/架构设计/14-Host服务域设计.md`
- `生态共用技术文档/协议与契约/08-箱子布局运行时契约.md`
- `生态共用技术文档/插件开发/08-SDK使用指南.md`
- `Chips-Host/src/main/services/register-host-services.ts`
- `Chips-Host/src/main/services/register-schemas.ts`
- `Chips-Host/src/preload/create-bridge.ts`
- `Chips-SDK/src/contracts/route-manifest.json`
- `Chips-SDK/tests/tooling/contract-drift.test.ts`
- Host/SDK 相关契约与集成测试

## 完成内容

1. 完成 Host runtime route manifest 与 SDK route manifest 对比，确认 Host 当前实际动作数为 153。
2. 修复 SDK route manifest 漂移，补齐 8 个 `box.*` 正式动作：
   - `box.listLayoutDescriptors`
   - `box.readLayoutDescriptor`
   - `box.normalizeLayoutConfig`
   - `box.validateLayoutConfig`
   - `box.getLayoutInitialQuery`
   - `box.renderLayoutFrame`
   - `box.renderLayoutEditor`
   - `box.releaseRenderSession`
3. 修复 SDK 契约漂移测试对多行泛型 `client.invoke<...>(...)` 的扫描缺口。
4. 新增 Host 契约测试，断言 SDK 公开 route manifest 与 Host runtime route manifest 完全一致。
5. 登记并修复工单097：Theme Runtime 五层 token 解析顺序改为 `ref -> sys -> motion/layout -> comp`，组件 token 可正式引用 layout/motion token。
6. 登记并修复工单098：Host 集成测试改用自包含、具备正式 `dist/index.mjs` 入口的插件 fixture，不再依赖外部源码插件是否已人工构建。
7. 同步更新 Host 内部主题运行时文档、共享主题系统文档与组件库 Token 对接标准。

## 漂移与工单

- `工单097-ThemeRuntime组件层无法引用layout层token阻断SDK仓库测试`：已解决。
- `工单098-Host集成测试直接安装未构建源码插件导致入口校验失败`：已解决。
- `Chips-SDK npm test` 初次因本地依赖树缺少已声明的 `archiver` 失败；核对 `Chips-SDK/package.json` 与锁文件后确认不是代码契约缺失，执行 `npm install --workspace Chips-SDK` 恢复依赖后正式测试通过。

## 验证结果

已通过：

```bash
cd Chips-Host && npm run build && npm run test:contract
cd Chips-Host && npx vitest run tests/unit/theme-runtime.test.ts tests/integration/host-services.test.ts
cd Chips-SDK && npm test
cd Chips-SDK && npx vitest run tests/tooling/contract-drift.test.ts tests/route-manifest.test.ts
cd ThemePack/Chips-default && npm run build && npm run validate:theme && npm test
cd ThemePack/Chips-theme-default-dark && npm run build && npm run validate:theme && npm test
```

关键审计结果：

```text
Host runtime routes: 153
SDK route manifest routes: 153
missingInSdk: []
extraInSdk: []
```

## 交接说明

任务001已完成。后续任务002可以基于当前 Host/SDK route manifest 一致性继续推进 L8 原语与 Modifier 补齐，不需要再沿用旧的 box route 缺口口径。
