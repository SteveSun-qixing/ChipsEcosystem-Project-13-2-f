# 任务014-组件库布局原语补齐

时间：2026-05-24 03:45:38 CST

## 本次完成

- 核对任务014、任务07方案、组件契约标准、Token 与主题对接标准，以及组件库和 ThemePack 当前实现。
- 确认 10 个布局原语组件已存在，本次重点收口 token、主题样式、测试与文档闭环。
- 扩展 `chips.layout.*` 基线：
  - `density.compact/comfortable/spacious`
  - `gap.xs/sm/md/lg/xl`
  - `size.grid-min-item/split-primary-min/split-secondary-min/navigation-primary-min`
  - `divider.thickness`
  - `focus.outline-width/outline-offset`
  - `breakpoint.compact/regular/expanded/wide`
  - `z-index.base/sticky/overlay`
  - `safe-area.*`
- 将 `chips.comp.divider.root.thickness` 改为引用 `chips.layout.divider.thickness`。
- 默认主题布局原语 CSS 去除任务014范围内的固定布局尺寸，改为 `chips.layout.*` token：
  - Grid 自动列最小宽度
  - SplitView 栏宽和 divider 轨道
  - 布局原语焦点环
  - Section/Box 分割线厚度
- 暗色主题补齐 `view/box/stack/inline/grid/section/scroll-view/spacer/divider/split-view` 布局原语样式块。
- 同步 ThemePack tracked 交付快照：
  - `theme.css`
  - `.cpk-stage/theme.css`
  - `.cpk-stage/tokens/layout.json`
  - 默认主题 `.cpk-stage/tokens/comp/*` 中缺失的布局原语 token 文件
- 新增组件库布局原语测试：
  - 10 个原语矩阵完整性
  - runtime contract 与 theme contract 文件一致
  - 所有公开 `data-part` 渲染
  - unlabeled landmark a11y 失败路径
- 新增 ThemePack 默认/暗色主题布局原语测试，防止主题 CSS 漏掉布局 scope 或回退固定布局尺寸。
- 更新公共组件文档：
  - `生态共用技术文档/组件库/02-组件契约标准.md`
  - `生态共用技术文档/组件库/03-Token与主题对接标准.md`
- 更新组件库内部清单，将已落地 Layout Primitives 与后续 Text/Label/HelperText 基础控件拆开记录。
- 保存任务015前置子代理勘察报告到 `08-草稿笔记`。

## 验证结果

```bash
cd Chips-ComponentLibrary && npm run verify
```

- 通过。
- token 校验/构建：314 keys。
- theme contracts：46 contracts。
- tests：182 passed。

```bash
cd Chips-ComponentLibrary && npm run quality:gate
```

- 通过。
- lint：159 files。
- typecheck：53 files。
- test / test:contracts / test:a11y / test:perf / build 全部通过。
- perf：render p95 max=0.6007ms，theme p95=10.6691ms，longTaskRatio=0。

```bash
cd ThemePack/Chips-default && npm run build && npm run validate:theme && npm test
```

- 通过。
- theme contract validation：46 components，271 required tokens。
- Vitest：4 files，7 tests passed。

```bash
cd ThemePack/Chips-theme-default-dark && npm run build && npm run validate:theme && npm test
```

- 通过。
- theme contract validation：46 components，271 required tokens。
- Vitest：5 files，9 tests passed。

## 注意事项

- 本次只处理任务014列出的 10 个布局原语，没有提前展开任务07后续的 `Form/List/Table/NavigationSplitView/Panel/ToolbarArea` 等布局组件。
- 主题包 `dist/` 为忽略目录，构建产物用于验证；版本管理内同步的是源码、测试、根部 `theme.css` 与 `.cpk-stage` 快照。
- worktree 中仍存在与任务014无关的既有未提交变更：
  - `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/README.md`
  - `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/http2.d.ts`
  - `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/package.json`
