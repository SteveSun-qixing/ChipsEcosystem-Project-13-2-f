# 任务015-基础展示控件第一批收口

时间：2026-05-24 04:37 CST

## 本次范围

- 任务：`任务015-组件库基础控件补齐`
- 批次：第一批基础展示控件
- 控件：`ChipsText`、`ChipsLabel`、`ChipsIcon`

## 完成内容

1. 组件库运行时与类型
   - 新增 `ChipsText`，正式 scope 为 `text`，part 为 `root`，状态为 `idle / disabled / error`。
   - 新增 `ChipsLabel`，正式 scope 为 `label`，part 为 `root / required-indicator / status`，状态为 `idle / disabled / error`。
   - 将 `ChipsIcon` 的 token 链路升级为 `chips.comp.icon.* -> chips.sys.icon.*`，保持 `IconDescriptor` 输入和 Material Symbols ligature 输出。
   - 新增 `P0_DISPLAY_COMPONENTS` 元数据。

2. Contract 与 token
   - 新增 `text.contract.json`、`label.contract.json`、`icon.contract.json`。
   - 新增组件库 `tokens/comp/text.json`、`label.json`、`icon.json`。
   - `validate-tokens` 必选前缀补入 `chips.comp.text.* / label.* / icon.*`。
   - 组件库 token 构建后基线为 `334` keys，theme-contracts 基线为 `49` contracts。

3. 默认主题与暗色主题
   - 两套主题新增 `tokens/comp/text.json`、`label.json`、`icon.json`。
   - 两套主题新增 `styles/components/display.css`。
   - 两套主题 `base.css` 将 icon 基线切到 comp token，并保留 sys icon fallback。
   - 两套主题 `theme-interface.contract.json` 与 `theme-min-functional-set.json` 增加 `text / label / icon`。
   - 同步 `.cpk-stage` token、contract 与 `theme.css` 快照。

4. 测试
   - 组件测试新增 `ChipsText`、`ChipsLabel`、`ChipsIcon` token/contract/a11y 覆盖。
   - 主题 contract 测试从 `46` components 更新为 `49` components。
   - 默认主题与暗色主题 build/tokens 测试检查 display 控件 CSS 与 token。

5. 文档
   - 新增公共正式矩阵：`生态共用技术文档/组件库/09-SwiftUI对标基础控件能力矩阵.md`。
   - 更新公共文档索引、组件库总览、组件契约标准、Token 与主题对接标准、可访问性与质量基线。
   - 更新组件库内部清单：`Chips-ComponentLibrary/技术文档/11-组件清单与实施优先级.md`。
   - 子代理 Dewey 已完成后续批次勘察报告：`08-草稿笔记/任务015-后续基础控件批次勘察报告.md`。

## 局部验证

已通过：

```bash
cd Chips-ComponentLibrary
node --test packages/components/tests/index.test.mjs packages/components/tests/a11y-stage9.test.mjs packages/theme-contracts/tests/component-contract.test.mjs packages/tokens/tests/token-contract.test.mjs
npm run build:tokens && npm run validate:tokens && npm run validate:contracts
```

结果：

- 相关 Node tests：`100` 通过。
- token 构建：`334` keys。
- contract 校验：`49` contracts。

## 待继续验证

本日志写入时完整门禁尚未全部运行，后续需要继续执行：

```bash
cd Chips-ComponentLibrary && npm run verify && npm run quality:gate
cd ThemePack/Chips-default && npm run build && npm run validate:theme && npm test
cd ThemePack/Chips-theme-default-dark && npm run build && npm run validate:theme && npm test
```

## 后续建议

下一批建议按 Dewey 勘察报告执行 `IconButton / ToggleButton / Badge / Tag / Avatar / Spinner / Progress`，继续保持每批“组件 + contract + token + 默认主题 + 暗色主题 + 测试 + 文档”闭环。
