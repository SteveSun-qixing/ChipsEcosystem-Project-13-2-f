# 任务015 基础控件第四批选择控件收口日志

时间：2026-05-24 06:50 CST

## 本次范围

- 完成任务015第四批 `SegmentedControl / ComboBox`。
- 新增正式导出：
  - `ChipsSegmentedControl`
  - `ChipsComboBox`
- 新增正式 scope：
  - `segmented-control`
  - `combo-box`

## 关键实现

- `ChipsSegmentedControl` 使用 `radiogroup/radio` 语义，公开 `root / item / indicator / label / status`，支持可访问名称、单选状态、禁用项、方向键与 Home/End 导航口径。
- `ChipsComboBox` 使用 `combobox/listbox/option` 语义，公开 `root / label / control / trigger / list / option / description / status`，支持输入过滤、弹层开关、`aria-controls`、`aria-activedescendant`、空结果文案、描述与错误关联。
- 为两个控件补齐组件库 `COMPONENT_TOKEN_MAP`、`buildComponentContract`、`TASK015_BASE_CONTROL_COMPONENTS`、类型声明和 a11y 校验规则。
- 新增 `segmented-control`、`combo-box` 组件 contract 与组件 token，并同步默认主题、暗色主题 token、CSS、theme contract、theme min functional set、`.cpk-stage` 与根 `theme.css` 发布产物。
- 更新生态共用组件文档和组件库内部清单，正式记录第四批控件落地口径。
- 子代理 Rawls 已完成第五批 `NumberInput / Stepper / Slider` 前置勘察，报告保存在 `08-草稿笔记/任务015-第五批数值控件前置勘察报告-20260524.md`。

## 验证

- `cd Chips-ComponentLibrary && node --test packages/components/tests/index.test.mjs packages/components/tests/a11y-stage9.test.mjs packages/theme-contracts/tests/component-contract.test.mjs packages/tokens/tests/token-contract.test.mjs`
  - 结果：114 个测试通过。
- `cd Chips-ComponentLibrary && npm run build:tokens && npm run validate:tokens && npm run validate:contracts`
  - 结果：`504 token keys / 62 contracts` 通过。
- `cd ThemePack/Chips-default && npm run build && npm run validate:theme && npm test`
  - 结果：`62 components / 461 required tokens` 通过，7 个测试通过。
- `cd ThemePack/Chips-theme-default-dark && npm run build && npm run validate:theme && npm test`
  - 结果：`62 components / 461 required tokens` 通过，9 个测试通过。

## 后续

- 下一批按矩阵推进 `NumberInput / Stepper / Slider`，优先复用第五批前置勘察报告中的数值解析、min/max/step/clamp 和键盘/a11y 建议。
- 当前仍需避开与本任务无关的既有脏文件：`Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/*`。
