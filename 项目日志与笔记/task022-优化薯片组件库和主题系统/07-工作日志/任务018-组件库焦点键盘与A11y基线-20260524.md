# 任务018：组件库焦点键盘与 A11y 基线工作日志

## 基本信息

- 日期：2026-05-24
- 任务文档：`06-工单级开发任务/任务018-组件库焦点键盘与A11y基线.md`
- 阶段方案：`05-开发任务方案/任务12-手势焦点可访问性与键盘交互.md`
- 涉及项目：`Chips-ComponentLibrary/packages/a11y`、`packages/components`、`packages/testing`

## 本次完成

1. 扩展 `@chips/a11y` 公共能力：
   - `normalizeKeyboardKey`
   - `createKeyboardMap`
   - `getKeyboardAction`
   - `createFocusRestorePoint`
   - `restoreFocus`
   - `getFocusTrapTarget`
   - `trapFocus`
   - `createFocusScope`
   - `createRovingTabIndex`
   - `getRovingIndexByKey`
   - `getRovingTabIndexProps`
   - 扩展 ARIA 校验规则：role 列表、requiredProps、active descendant、selected/checked 状态。

2. 扩展 `@chips/testing` 测试辅助：
   - 键盘事件 fixture 与键盘序列执行。
   - roving tabindex、active descendant、focus restored 断言。
   - focus trap fixture。

3. 组件重点路径接入与修复：
   - `ChipsDialog` 接入 `FocusScope`，支持 modal Tab focus trap，关闭后恢复到触发器。
   - `ChipsTabs.Trigger`、`ChipsMenu.Item`、`ChipsSelect.Option` 使用统一 roving tabindex 模型。
   - 修复 `ChipsSelect.Content` 渲染未定义 `label` 的问题，正确渲染 `children`。
   - `npm run test:a11y` 增加真实 DOM 键盘回归：Dialog/Menu/Select/Tabs/DataGrid。

4. 类型与文档：
   - 为 `@chips/a11y` 和 `@chips/testing` 增加 `types` 与 `exports.types`。
   - 新增 `生态共用技术文档/组件库/11-焦点键盘与A11y交互模型.md`。
   - 同步更新组件库总览、契约标准、接入指南、可访问性质量基线、包 README 和类型烟测。

## 架构结论

- L8 `focusScope` 只声明语义；L10 组件库通过 `@chips/a11y` 执行 DOM 焦点行为。
- Host 仍是唯一运行时承载；本任务没有把 Host runtime 或 Bridge 能力放入组件库。
- `@chips/a11y` 与 `@chips/testing` 是生态公共能力包，应供后续脚手架、应用和第三方插件测试复用。

## 已执行验证

```bash
cd Chips-ComponentLibrary
npm run test:a11y
npm run test:types
npm run lint
npm run typecheck
npm run verify
npm run quality:gate
git diff --check
```

验证结果：

- `test:a11y`：19 项通过。
- `test:types`：通过。
- `lint`：213 个文件通过。
- `typecheck`：55 个 JS/MJS 文件语法校验通过。
- `verify`：token 校验、token 构建、contract 校验、类型烟测、全量 243 项测试通过。
- `quality:gate`：最终状态 passed，生成 `reports/perf/perf-stage9-2026-05-24T083220783Z.json` 与 `reports/quality-gate/quality-gate-2026-05-24T083221152Z.json`。
- `git diff --check`：通过；命令输出中存在与本任务无关的 scaffold `node_modules/@types/node` CRLF 提示，本任务未触碰该路径。

## 注意事项

- `test:a11y` 与 `quality:gate` 运行期间 React 会输出少量 act 包裹提醒，但当前测试全量通过且门禁未阻断；后续如果质量门禁将 warning 升级为阻断，应把交互事件 flush 工具进一步封装到 `@chips/testing`。
- 工作区已有与本任务无关的 scaffold `node_modules/@types/node` 脏文件和旧质量报告，本任务不处理、不提交。
