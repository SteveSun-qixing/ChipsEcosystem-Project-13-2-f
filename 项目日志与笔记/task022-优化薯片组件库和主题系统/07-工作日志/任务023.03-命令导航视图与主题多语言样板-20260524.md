# 任务023.03：命令导航视图与主题多语言样板

> 日期：2026-05-24
> 范围：`Chips-Scaffold/chips-scaffold-app` 应用模板、模板测试、生成工程 e2e 检查、子代理草稿资料收口。

## 文档与预研复核

- 复核 `AGENTS.md`、`Chips-Scaffold/AGENTS.md`、`Chips-Scaffold/chips-scaffold-app/AGENTS.md`、`项目日志与笔记/AGENTS.md`。
- 复核 `06-工单级开发任务/任务023.03-命令导航视图与主题多语言样板.md`。
- 吸收子代理草稿：
  - `08-草稿笔记/任务023.03-命令导航视图与主题多语言样板-预研报告-20260524.md`
  - `08-草稿笔记/任务023.04-生成工程测试预览与公共文档收口-预研报告-20260524.md`
- 本阶段未发现需要新增公共契约或问题工单的架构缺口；应用脚手架公共说明已在 023.02 阶段同步过 `AppRuntimeProvider / useAppRuntime()` 基线。

## 完成内容

1. 命令样板升级：
   - `appCommandDefinitions` 增加 `refreshTheme.disabledReasonKey` 与 `theme.read` 权限示例。
   - 新增 `resolveAppToolbarCommands`、`resolveAppMenuGroups`、`resolveAppPaletteItems`，让工具栏、菜单、命令面板从同一 command view 派生。
   - 单元测试覆盖命令 title/description/aria/disabled reason 的双语 key，以及 SDK mock host 的 menu/toolbar/palette list、`command.setState`、invoke 链路。

2. 视图与组件样板升级：
   - 新增 `RuntimeDiagnosticsView.tsx.tpl`，集中展示运行时状态，并覆盖 `ChipsDialog`、`ChipsErrorState`、`ChipsIcon`。
   - `AppShell`、`MainScene`、`WorkspaceOverviewView`、`EnvironmentStatusView` 接入 `ChipsView` / `ChipsGrid`，减少手写布局心智。
   - `SceneListView` 在 virtual list item 中展示 scene 描述，并用 `ChipsStack` 组织 scene action 与说明。

3. 主题与多语言门禁升级：
   - 双语模板补齐 runtime、dialog、command disabled reason 相关 key。
   - `check-templates.mjs`、`run-generated-e2e.mjs`、`template-engine.test.ts` 增加新视图、新组件、命令解析 helper、i18n key 覆盖与硬编码视觉值检查。
   - README 模板补充 `RuntimeDiagnosticsView.tsx` 视图入口。

## 子代理收口

- 用户提示 4 个子代理均已完成。
- 当前尝试关闭可追溯句柄时，工具均返回 `not found`：
  - `019e59ed-f3c0-77c2-ab50-caff4cf30e32`
  - `019e59f8-020b-7320-92bc-efabd8f0bb7b`
  - `019e59b7-6bc5-7571-b730-8d82ee13669f`
  - `019e5986-de47-7b53-8b85-263aa1f55cce`
  - `019e58c6-525f-7201-9bb7-15c953765d22`
- 结论：当前工具注册表已无可关闭活动句柄，视为子代理已由运行时清理或不再可管理；本阶段不保留等待状态。

## 验证

在 `Chips-Scaffold/chips-scaffold-app` 执行：

```bash
npm run test:templates
npm run test:e2e
npm run build && npm test && npm run test:templates && npm run test:e2e
```

结果：

- `test:templates` 通过。
- `test:e2e` 通过，并生成真实应用工程后执行 `npm run verify`。
- 生成工程验证中 `tests/e2e/basic-flow.test.ts`、`tests/unit/commands.test.ts`、`tests/unit/app.test.tsx` 共 10 个测试通过。
- `preview:smoke` 只有 `host.realWorkspace` warning，`blockingCheckCount = 0`。
- `quality:gate` 通过，`failedCheckCount = 0`、`warningCheckCount = 0`。
- 完整组合门禁通过。

## 工作树备注

- 继续忽略既有无关脏文件：`Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/*`。
- 本阶段不修改这些 node_modules 文件，不纳入提交。
