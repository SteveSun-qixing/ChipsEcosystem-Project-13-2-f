# 任务023.04：生成工程测试预览与公共文档收口

> 日期：2026-05-24
> 范围：`Chips-Scaffold/chips-scaffold-app` 生成工程 e2e、模板质量检查、应用模板 README、生态共用应用插件开发文档。

## 文档与真实链路复核

- 复核 `AGENTS.md`、`Chips-Scaffold/AGENTS.md`、`Chips-Scaffold/chips-scaffold-app/AGENTS.md`、`生态共用技术文档/AGENTS.md`、`项目日志与笔记/AGENTS.md`。
- 复核任务文档 `06-工单级开发任务/任务023.04-生成工程测试预览与公共文档收口.md`。
- 复核设计与方案：
  - `生态设计原稿/01-生态概述.md`
  - `生态设计原稿/06-前端多样化和主题系统.md`
  - `生态设计原稿/19-SDK与协议.md`
  - `05-开发任务方案/任务06-应用脚手架vNext默认模板.md`
- 复核公共文档与代码真实链路：
  - `生态共用技术文档/插件开发/02-应用插件开发.md`
  - `生态共用技术文档/插件开发/06-Manifest配置规范.md`
  - `生态共用技术文档/命令行工具/02-Chips-Dev开发者命令行手册.md`
  - `Chips-SDK/src/tooling/developer-tools.cjs`
  - `Chips-Scaffold/chips-scaffold-app/scripts/run-generated-e2e.mjs`
  - `Chips-Scaffold/chips-scaffold-app/templates/app-standard/package.json.tpl`
  - `Chips-Scaffold/chips-scaffold-app/templates/app-standard/src/preview/preview-smoke.js.tpl`

## 完成内容

1. 生成工程报告断言收口：
   - `run-generated-e2e.mjs` 在真实生成工程执行 `npm run verify` 后，读取 `reports/preview/app-preview-smoke.json` 与 `reports/quality/quality-gate.json`。
   - 对 preview 报告断言 `kind = chipsdev.preview`、`previewPlan.mode = mock`、`target = app`、`reportOnly = true`、`blockingCheckCount = 0`。
   - 对 preview 必备检查项断言 `project.config / manifest / manifest.runtimeTargets / manifest.surface / manifest.entryAsset / host.mock` 均通过，并允许 sandbox 缺少真实 `Chips-Host` 时 `host.realWorkspace` 为 warning。
   - 对 quality gate 报告断言 `kind = chipsdev.quality.gate`、生成工程识别为 `app`、`failedCheckCount = 0`，并确认项目、SDK、组件库报告与主题检查项存在且未失败。

2. 模板与测试门禁同步：
   - `preview-smoke.js.tpl` 增加 `reportOnly` 边界检查。
   - `basic-flow.test.ts.tpl` 与 `template-engine.test.ts` 锁定 `reports/preview/app-preview-smoke.json`、`reports/quality/quality-gate.json` 归档路径。
   - `check-templates.mjs` 增加 preview/quality 报告路径与 preview smoke 关键断言字符串检查。
   - README 模板说明 `reports/` 为本地验证产物目录，`preview:smoke` 是报告级 smoke，真实 Host UI 联调继续使用 `chipsdev run`。

3. 公共文档收口：
   - `生态共用技术文档/插件开发/02-应用插件开发.md` 新增官方应用脚手架 vNext 工程基线，包含默认目录、AppRuntimeProvider 边界、命令目录边界、preview 开发脚本边界、默认质量脚本与 `reports/` 归档语义。
   - 核对 `06-Manifest配置规范.md` 已覆盖 `runtime.targets`、`ui.surface` 与 `chipsdev validate` 规则，本阶段无需改动。
   - 核对 `02-Chips-Dev开发者命令行手册.md` 已覆盖 `chipsdev preview` 与 `chipsdev quality gate` 的报告语义，本阶段无需改动。

## 子代理协作

- 开始 023.04 前，已按用户要求提前派发 `024-基础卡片脚手架vNext升级` 启动前复核子代理，ID：`019e5a09-cf22-7a01-a0db-8560d6a814b4`。
- 当前 024 子代理仍在运行，本阶段不等待其结果，不纳入 023.04 提交。

## 验证

在 `Chips-Scaffold/chips-scaffold-app` 执行：

```bash
npm run test:templates
npm run test:e2e
npm run build && npm test && npm run test:templates && npm run test:e2e
```

结果：

- `test:templates` 通过。
- `test:e2e` 通过，真实生成工程后执行 `npm run verify`。
- 生成工程验证中 `tests/e2e/basic-flow.test.ts`、`tests/unit/commands.test.ts`、`tests/unit/app.test.tsx` 共 10 个测试通过。
- `preview:smoke` 报告成功写入并被校验；sandbox 中 `host.realWorkspace` 为 warning，`blockingCheckCount = 0`。
- `quality:gate` 报告成功写入并被校验；`failedCheckCount = 0`。
- 完整组合门禁通过。

## 工作树备注

- 继续忽略既有无关脏文件：`Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/*`。
- 本阶段未修改这些 node_modules 文件，不纳入提交。
