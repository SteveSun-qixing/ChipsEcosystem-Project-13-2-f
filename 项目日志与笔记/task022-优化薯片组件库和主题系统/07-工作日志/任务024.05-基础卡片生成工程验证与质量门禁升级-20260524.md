# 任务024.05 基础卡片生成工程验证与质量门禁升级日志

## 时间

2026-05-24

## 本次进度

- 将 `chips-scaffold-basecard` 生成工程 e2e 从直接调用模板渲染函数升级为正式 `chipsdev create card` 链路。
- e2e 在临时生态工作区中创建工程，执行 `npm install`，再运行生成工程的 `npm run verify`。
- 生成工程新增 `typecheck`、`package`、`verify` 脚本，`verify` 串联 `lint/typecheck/test/build/validate/package`。
- 模板检查升级为递归门禁，阻断旧 `chips dev` 文本、未收口标记、脚手架身份泄露、临时实现口径、运行时视觉硬编码、浏览器持久化存储、文件系统直连和 Bridge 直连。
- 模板单测补齐 Manifest 四目标矩阵、`capabilities.cardTypes`、脚本矩阵、三件套导出、资源桥、cleanup、React 去重配置和生成文本清洁度断言。
- `vitest.config.mts` 模板增加 React / React DOM alias 与 dedupe，避免临时 workspace 链接组件库时出现双 React hooks 运行时错误。
- README 与 `chips.config.mjs` 模板收口到 `chipsdev` 正式命令口径，并补充生成工程验证链路说明。
- `templates/parameters.md` 补充 `resource_path` 说明，与 schema 和默认配置保持一致。

## 子代理复核

- 已接收并关闭 024.05 启动前复核子代理。
- 报告已保存于 `项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务024.05-基础卡片生成工程验证与质量门禁升级-启动前复核报告-20260524.md`。

## 验证结果

在 `Chips-Scaffold/chips-scaffold-basecard` 执行：

```bash
npm run build
npm test
npm run test:templates
npm run test:e2e
```

结果均通过。`test:e2e` 生成的真实工程通过 `lint/typecheck/test/build/validate/package`，并成功生成 `.cpk` 插件包。

## 后续衔接

- 下一步进入任务024.06，将基础卡片脚手架稳定口径同步到生态共用技术文档与生成工程 README。
- 024.06 需要重点复核公共文档中三件套导出、资源桥、cleanup、组件库/主题/i18n、生成工程验证脚本等描述是否与当前模板一致。
