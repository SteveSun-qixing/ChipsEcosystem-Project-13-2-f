# 任务024.07 基础卡片脚手架 vNext 最终验收日志

日期：2026-05-24

## 范围

- `Chips-Scaffold/chips-scaffold-basecard`
- `生态共用技术文档/插件开发/03-卡片插件开发.md`
- `生态共用技术文档/插件开发/06-Manifest配置规范.md`
- `生态共用技术文档/命令行工具/02-Chips-Dev开发者命令行手册.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务036-任务043`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务024.07-基础卡片脚手架vNext最终验收-启动前复核报告-20260524.md`

## 文档与代码复核

- 复核了根目录、`项目日志与笔记`、`Chips-Scaffold`、`chips-scaffold-basecard` 与 `生态共用技术文档` 的 `AGENTS.md`。
- 复核了基础卡片插件结构、卡片渲染机制、卡片文件格式、编辑引擎设计、SDK 与协议设计中与基础卡片相关的正式口径。
- 复核了卡片插件开发、Manifest 配置规范、Chips Dev 开发者命令行手册与当前 `chips-scaffold-basecard` 模板、测试和 e2e 脚本。
- 复核了任务024.01-024.06 工作日志，确认 024.06 公共文档收口日志已存在。
- 只读核对了任务036-043，确认后续八个基础卡片插件迁移任务已经包含 `basecardDefinition`、IconDescriptor、资源相对路径、组件库、主题、多语言、资源桥、`openResource`、cleanup 与正式验证命令等新模板口径。

## 本阶段处理

1. 执行 `chips-scaffold-basecard` 全量最终验收。
2. 接收并关闭 024.07 启动前复核子代理，报告已保存到 `08-草稿笔记`。
3. 定位并收口生成工程身份边界：`template.json` 是脚手架内部模板元数据，不应复制到正式基础卡片插件工程。
4. 修改模板渲染引擎，让 `renderTemplateToTarget(...)` 在生成业务工程时跳过模板根 `template.json`。
5. 更新单元测试与 e2e 门禁，断言生成工程不包含 `template.json`。

## 验证结果

在 `Chips-Scaffold/chips-scaffold-basecard` 中完成：

```bash
npm run build
npm test
npm run test:templates
npm run test:e2e
```

结果：

- `npm run build` 通过。
- `npm test` 通过，`tests/core/template-engine.test.ts` 共 2 个测试通过。
- `npm run test:templates` 通过。
- `npm run test:e2e` 通过；真实执行 `chipsdev create card` 生成基础卡片工程，生成工程的 `verify` 串联完成：
  - `npm run lint`
  - `npm run typecheck`
  - `npm test`
  - `npm run build`
  - `npm run validate`
  - `npm run package`
- 生成工程测试共 4 个测试文件、11 个测试通过，并成功输出唯一 `.cpk`。
- e2e 中出现的 npm deprecated / npm notice 为临时安装依赖提示，不影响验收结果。

补充扫描：

- `git diff --check` 针对本阶段相关代码、报告文件无空白错误。
- `card` manifest 模板未声明 `ui.surface`。
- 旧 `chips dev` 写法只出现在公共手册的废弃说明和脚手架门禁断言中，未出现在生成模板正式口径里。
- 模板、README、公共文档仍覆盖三件套导出、组件库依赖、i18n、主题 token、资源桥、cleanup、`verify` 与 `chipsdev create card` e2e。

## 验收结论

任务024 基础卡片脚手架 vNext 可以作为后续基础卡片插件迁移的正式基线。

当前基线具备：

- `manifest.yaml` 的 `type: card`、`capabilities.cardTypes`、`runtime.targets` 与 `dist/index.mjs` 入口。
- `renderBasecardView`、`renderBasecardEditor`、`basecardDefinition` 三件套导出。
- `basecardDefinition.pluginId/cardType` 与 Manifest 对齐。
- React、`@chips/component-library`、主题 token、多语言文案与组件库 i18n adapter。
- `resource_path` 卡片根目录相对路径约束。
- `resolveResourceUrl/releaseResourceUrl/openResource/importResource/importArchiveBundle/deleteResource/convertTiffToPng` 资源桥样板。
- 查看态与编辑态真实 cleanup。
- 生成工程默认 `lint/typecheck/test/build/validate/package/verify` 门禁。

## 未纳入本阶段

- 不迁移 `Chips-BaseCardPlugin/*` 八个业务基础卡片，后续按任务036-043 顺序执行。
- 不更新基础卡片开发技能，下一步按任务024.08 处理。
- 不处理当前工作树中既有的 `node_modules/@types/node/*` 脏文件。
- 不处理任务058.01/059.01 的外部编号调整变动。

## 下一步

- 进入 `任务024.08-基础卡片开发技能vNext更新`，基于本次最终验收结果更新基础卡片开发技能。
- 用户新增的生态全类型技能要求已纳入后续技能矩阵：应用插件、主题包、箱子、功能模块、卡片都需要对应技能，后续在全类型技能任务中继续收口。
