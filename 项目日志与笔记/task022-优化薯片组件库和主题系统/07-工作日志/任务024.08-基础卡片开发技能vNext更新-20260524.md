# 任务024.08 基础卡片开发技能 vNext 更新日志

日期：2026-05-24

## 范围

- 本机 Codex 技能：`/Users/sevenstars/.codex/skills/chips-basecard-plugin-dev`
- 任务文档：`项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务024.08-基础卡片开发技能vNext更新.md`
- 子代理报告：`项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务024.08-基础卡片开发技能vNext更新-启动前复核报告-20260524.md`
- 任务024最终验收产物与公共文档：
  - `Chips-Scaffold/chips-scaffold-basecard`
  - `生态共用技术文档/插件开发/03-卡片插件开发.md`
  - `生态共用技术文档/插件开发/06-Manifest配置规范.md`
  - `生态共用技术文档/命令行工具/02-Chips-Dev开发者命令行手册.md`

## 处理内容

1. 接收并关闭 024.08 启动前复核子代理；报告已保存到 `08-草稿笔记`。
2. 按子代理建议直接更新现有 `chips-basecard-plugin-dev` 技能，未新建并行 vNext 技能，避免新旧触发器并存导致后续代理误用旧口径。
3. 更新 `SKILL.md`：
   - 触发描述补齐 `chipsdev create card`、三件套导出、Manifest、schema、组件库、主题、多语言、资源桥和 `verify` 门禁。
   - 新工程流程改为 `cd <生态根工作区> && npm install && chipsdev create card <targetDir> && npm run verify`。
   - 明确 `manifest.yaml` 不得声明 `ui.surface`。
   - 明确基础卡片 UI 不直接调用 `CardService`、Bridge 文件路由、`window.chips.invoke`、文件系统或 app 插件内部实现。
4. 更新 references：
   - `official-contracts.md`：补齐任务024后的 `importArchiveBundle` 请求/结果结构、`discardedEntries`、`stripSingleRootDir`、`excludeSystemArtifacts` 等字段；强化 `pluginId/cardType` 与 Manifest 对齐。
   - `scaffold-workflow.md`：删除旧工作区硬编码路径与旧 `npx` 口径；补齐 `supports.componentLibrary: true`、生成工程不包含 `template.json`、`verify` 门禁。
   - `resource-and-app-integration.md`：将 `openResource` 示例改为使用 card-root 资源路径/正式资源身份，不再把临时预览 URL 当作稳定资源身份；补齐 archive import、TIFF 转换和资源路径清洗。
   - `editor-ui-patterns.md`：补充禁止 editor UI 绕过宿主资源/持久化链路。
   - `image-and-music-patterns.md`：标注图片/音乐卡片内容只是业务插件历史模式参考，不替代公共契约和 vNext 模板。
5. 更新 `agents/openai.yaml` 的短描述和默认提示，使 UI 元数据与 vNext 技能口径一致。

## 验证

已执行：

```bash
rg -n "npx chipsdev|\\bchips dev\\b|Project-13-2-f|componentLibrary\\\"?: false|TODO|FIXME|占位|临时|最小可用|resolvedUrl" /Users/sevenstars/.codex/skills/chips-basecard-plugin-dev
rg -n "chipsdev create card <targetDir>|npm run verify|importArchiveBundle|discardedEntries|resourceId: resourcePath|template\\.json.*must not be generated|supports\\.componentLibrary: true|renderBasecardView|renderBasecardEditor|basecardDefinition|@chips/component-library" /Users/sevenstars/.codex/skills/chips-basecard-plugin-dev
ruby -EUTF-8 -e 'require "yaml"; ...'  # 校验 SKILL.md frontmatter
ruby -EUTF-8 -e 'require "yaml"; ...'  # 校验 agents/openai.yaml
```

结果：

- 未发现旧工作区路径、旧 `npx` 创建命令、`componentLibrary: false`、误导性 `resolvedUrl` 示例、临时/占位标记。
- 关键 vNext 口径均可检索到：`chipsdev create card <targetDir>`、`verify`、`importArchiveBundle` 完整结构、`openResource` 资源身份、三件套导出、组件库、`template.json` 不生成到业务工程。
- `SKILL.md` 与 `agents/openai.yaml` 均可被 Ruby YAML 正常解析。

限制：

- `/Users/sevenstars/.codex/skills/.system/skill-creator/scripts/quick_validate.py` 依赖 PyYAML；当前系统 Python 与 Codex bundled Python 均缺少 `yaml` 模块，因此该脚本未能执行成功。已用 Ruby YAML 与标准库 frontmatter 检查替代结构验证。

## 结论

`chips-basecard-plugin-dev` 已更新为任务024后的 vNext 口径。后续基础卡片插件开发、迁移或审查应优先使用该技能，并以生态设计原稿、生态共用技术文档、当前真实模板和正式验证脚本为最终依据。

## 后续

- 下一步进入 `任务025-箱子布局脚手架vNext升级`。
- 已提前启动任务025只读复核子代理，报告完成后保存到 `08-草稿笔记`。
- 用户新增的“应用插件、主题包、箱子、功能模块、卡片都需要对应技能”要求继续保留到全类型技能任务中；本阶段只收口基础卡片技能。
