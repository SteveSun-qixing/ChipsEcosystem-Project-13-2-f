# 任务025.02 箱子布局脚手架契约与质量门禁升级日志

## 时间

2026-05-24

## 本次进度

- 升级 `chips-scaffold-boxlayout` 标准模板的 Box Runtime 类型契约，移除旧 `cardId/cardInfo/cardFile/card-window` 字段，改为正式 `documentId/documentInfo/documentFile/document-window` 字段，并补齐 `documentType`。
- 将默认布局配置从固定列数、封面比例、信息密度，调整为更通用的 `sortMode/background/topRegion/assetRefs` 配置模型；背景与顶部区域的查看态细化留给任务025.03继续实现。
- 生成工程 `package.json` 补齐 `typecheck/package/verify` 脚本，组件库依赖改为正式 semver，补齐 `@types/node` 与 `tsconfig` Node 类型入口，确保生成工程严格类型检查可通过。
- `template.json.supports.componentLibrary` 与真实依赖对齐为 `true`。
- `renderEditor` 链路补齐 `readBoxAsset/importBoxAsset/deleteBoxAsset` 资产桥透传，为任务025.04继续升级编辑器资产管理打基础。
- 强化 `scripts/check-templates.cjs` 与 `tests/core/template-engine.test.ts`，阻断旧 Runtime 字段、固定列数默认配置、旧 CLI 口径、未渲染占位符、`ui.surface`、模板身份泄露和生成工程脚本缺失。
- 接收并保留两个子代理报告：
  - `08-草稿笔记/任务025.03-箱子布局查看态Runtime样板升级-启动前复核报告-20260524.md`
  - `08-草稿笔记/任务026-模块插件脚手架vNext升级-启动前复核报告-20260524.md`

## 验证结果

在 `Chips-Scaffold/chips-scaffold-boxlayout` 下完成：

```bash
npm run build
npm test
npm run test:templates
npm run test:e2e
```

结果均通过。其中 `test:e2e` 重新生成布局插件工程，执行依赖安装、`tsc --noEmit` 与生成工程 Vitest，确认生成工程自检通过。

## 后续处理

- 任务025.03 继续升级查看态 Runtime 样板，重点补齐 `runtime.listEntries` 分页、错误态、重试、封面失败与 cleanup 后异步取消测试。
- 任务025.04 再完整实现编辑器资产桥 UI 与箱子资源生命周期，不在本阶段提前重写。
