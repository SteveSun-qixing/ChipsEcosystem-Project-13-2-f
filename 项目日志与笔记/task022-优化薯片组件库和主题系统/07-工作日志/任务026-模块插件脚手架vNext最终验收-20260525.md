# 任务026：模块插件脚手架 vNext 最终验收

## 时间

2026-05-25

## 任务范围

任务026围绕模块插件脚手架 vNext 完成从契约边界、模板矩阵、生成工程质量门禁、Host / SDK / CLI 调用链路到公共文档和 README 的完整收口。

## 子任务完成情况

1. `任务026.01-模块插件脚手架契约边界与任务拆分`
   - 完成模块插件定位复核，明确模块插件是 Host 托管的无界面能力模块；
   - 明确不生成 React 页面运行时、窗口入口、主题注入或 i18n UI 词典；
   - 将后续工作拆分为模板矩阵、质量门禁、Host / SDK / CLI 契约和公共文档收口。
2. `任务026.02-模块插件模板形态矩阵升级`
   - 完成 7 个模块模板矩阵升级；
   - 覆盖标准模块、纯函数、文件转换、HTML 渲染、图像处理、颜色提取和模块编排；
   - 生成工程统一输出 `manifest.module.provides`、contracts、源码、测试、构建和验证脚本。
3. `任务026.03-模块插件生成工程质量门禁与真实 e2e`
   - 补强生成工程 lint、typecheck、test、build、validate、package、verify 质量链路；
   - 增加真实工作区 E2E，覆盖 `chipsdev create module`、生成工程 `verify`、`.cpk` 打包和 Host 模块服务链路。
4. `任务026.04-模块插件 Host / SDK / CLI 契约复核补强`
   - 补强 Host provider 解析、语义化版本排序、enabled/running 状态过滤；
   - 补强 `timeoutMs`、job 超时、job 取消和 `module.job.cancelled` 事件语义；
   - 补强模块间调用 `module.consumes` 运行期治理；
   - 补强 SDK 与 `chipsdev module invoke` 参数解析、超时传递和错误处理。
5. `任务026.05-模块插件公共文档与 README 收口`
   - 统一 7 个模板 README 与脚手架根 README；
   - 同步脚手架内部需求、技术文档和生态公共文档；
   - 补强 README 生成测试与正式脚手架验证。

## 验收结论

模块插件脚手架 vNext 已形成完整闭环：

- 新工程通过 `chipsdev create module` 生成；
- 模块能力通过 `manifest.module.provides` 暴露；
- 模块依赖通过 `manifest.module.consumes` 声明，并由 Host 运行期校验；
- 模块调用通过 Host `module.listProviders / module.resolve / module.invoke / module.job.*` 完成；
- 生成工程 `npm run verify` 覆盖 lint、typecheck、test、build、validate 和 package；
- `.cpk` 插件包可被 Host 安装启用并通过模块服务真实调用；
- 公共文档和 README 不再扩散旧 `defineModule`、DOM 挂载、跨目录 import、旧 CLI 或临时调用口径。

## 最终验证记录

- `cd Chips-Scaffold/chips-scaffold-module && npm run build`：通过。
- `cd Chips-Scaffold/chips-scaffold-module && npm test`：通过。
- `cd Chips-Scaffold/chips-scaffold-module && npm run test:templates`：通过。
- `cd Chips-Scaffold/chips-scaffold-module && npm run test:e2e`：通过。
- 任务026.04 阶段已额外完成 Host 与 SDK 全量相关验证，包括 Host `npm test`、SDK `npm test`、CLI module invoke 与真实脚手架 E2E。

## 伴随工单

任务026.04 验证期间发现并已修复：

- 工单102：Theme Runtime 系统层同层 token 引用解析缺口；
- 工单103：chipsdev 组件矩阵误读归档 contract 阻断 SDK 全量测试。

## 状态

任务026 全部完成。后续按顺序进入任务027主题脚手架 vNext 升级；主题包处理应按后续对应任务执行，不提前混入模块脚手架任务。
