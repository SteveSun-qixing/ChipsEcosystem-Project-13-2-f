# 任务026.01 模块插件脚手架契约边界与任务拆分日志

## 时间

2026-05-25

## 本次进度

- 复习模块插件相关设计原稿与公共契约，确认模块插件的正式定位是 Host 托管的无界面 capability/method 提供者，不创建窗口、不承载 DOM 挂载、不直接 `import` 其他模块源码。
- 核对 `生态共用技术文档/插件开发/05-模块插件开发.md`、`协议与契约/06-模块能力契约.md`、Manifest 规范和 Chips Dev 手册，确认任务026应围绕 `module.provides / module.consumes`、权限、schema、错误模型、同步调用、异步 job、取消和 Host/SDK/CLI 联调展开。
- 核对 `Chips-Scaffold/chips-scaffold-module` 当前模板、脚本和测试，确认现状仍是单一 `module-standard` 模板，生成工程缺少 `typecheck/package/verify` 统一质量脚本，脚手架本地 e2e 只做文件存在性自检。
- 核对 SDK 侧 `run-cli-create-module-workspace-tests.cjs`，确认 SDK 已有更完整的 `chipsdev create module`、生成工程构建、打包、Host 安装启用、`module.invoke` 同步调用和异步 job 调用测试链路；后续任务026.03应把同类心智收口到脚手架自身门禁中。
- 核对 Host 模块运行时实现，确认 `ctx.host.invoke(...)` 禁止调用 `module.*`，模块间调用必须走 `ctx.module.invoke(...) / ctx.module.job.*`，输入输出 schema 会由 Host 在调用前后校验。
- 抽查五个现有模块样本 manifest 与入口，确认后续模板形态至少要覆盖原子转换、编排转换、HTML 渲染导出、图像处理和颜色提取等长期形态。
- 已提前启动 026.02 子代理，负责模块插件模板形态矩阵启动前复核，报告目标为 `08-草稿笔记/任务026.02-模块插件模板形态矩阵升级-启动前复核报告-20260525.md`。

## 验证结果

在 `Chips-Scaffold/chips-scaffold-module` 下完成当前 baseline 验证：

```bash
npm run build
npm test
npm run test:templates
npm run test:e2e
```

结果均通过。当前 `test:e2e` 只输出 `E2E: 模块脚手架生成工程自检通过。`，未执行真实 `chipsdev create module`、生成工程 `npm run verify`、`.cpk` 打包、Host 安装启用或 `module.invoke` 调用，因此后续 026.03 需要继续加强。

## 结论

- 任务026拆分方向成立，无需新增阻断工单。
- 已创建并提交 `026.01` 到 `026.05` 小任务文档；后续按顺序进入 `026.02-模块插件模板形态矩阵升级`。
- 026.02 不应修改现有五个业务模块仓；业务迁移仍按任务046到任务050执行。

## 后续处理

- 等待或接收 026.02 子代理报告后，开始实现模板形态矩阵。
- 在正式开始 026.02 前，继续提前派发 026.03 真实质量门禁与 e2e 复核任务。
