# 任务028.01：组件展示矩阵正式组件清单收口

## 时间

2026-05-25

## 本次目标

启动任务028后，优先修复 `Chips-EcoSettingsPanel` 当前无法通过类型检查的红灯，并让组件展示矩阵与当前组件库正式清单对齐，为后续 App/Scene/commands 迁移建立绿色基线。

## 已完成

- 复习任务028工单、生态设置面板设计、运行时设置治理接口、应用插件开发规范、组件库正式 Form Compound 口径与两个任务028启动前复核报告。
- 将组件展示页从旧 `ChipsFormField`、`ChipsFormGroup` 迁移为 `ChipsForm.Root / Section / Field / Label / Control / Hint` Compound 预览。
- 补齐 `ChipsToolbar`、`ChipsMenuBar`、`ChipsContextMenu`、`ChipsShortcut`、`ChipsNavigationSplitView` 的展示注册、预览、summary 和 i18n 文案。
- 更新 `Chips-EcoSettingsPanel/需求文档/02-生态设置面板功能需求规格.md` 的组件矩阵清单，避免继续扩散旧表单组件口径。
- 将移动端分类下拉从旧 `ariaLabel` 透传改为标准 `aria-label`，并将 `ChipsCommandPalette` 预览改为当前正式 `ariaLabel/inputPlaceholder` props，清除 React 属性告警。
- 新增任务文档 `任务028.01-组件展示矩阵正式组件清单收口.md`。

## 验证

```bash
cd Chips-EcoSettingsPanel
npm run typecheck
npm test -- component-gallery-registry.test.ts
npm run verify
```

结果：

- `npm run typecheck` 通过。
- `component-gallery-registry.test.ts` 通过。
- `npm run verify` 通过：13 个测试文件、31 个测试全部通过，build 与 `chipsdev validate` 均成功。

## 后续

任务028 后续继续推进：

- AppRoot/AppProviders/AppRuntimeProvider/scene-registry/commands 结构迁移。
- manifest command 权限补齐与 Host command registry 接入。
- `SettingsRuntimeService` 中裸 `client.invoke("platform.*")` 迁移到 SDK platform Domain API。
- 主题矩阵、组件 contract、主题 contract、缺失 token 诊断、预览与性能工具入口。
- 治理接口 mock、Host smoke、主题/语言切换和权限不足测试。
