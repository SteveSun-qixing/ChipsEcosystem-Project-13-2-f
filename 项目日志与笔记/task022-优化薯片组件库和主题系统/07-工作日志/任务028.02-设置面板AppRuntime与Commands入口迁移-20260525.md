# 任务028.02：设置面板 AppRuntime 与 Commands 入口迁移

## 时间

2026-05-25

## 本次目标

在任务028.01 绿色基线之后，将 `Chips-EcoSettingsPanel` 根入口迁移到应用插件 vNext 的 AppRuntime/Scene/Commands 结构，并确保现有设置面板功能、页面顺序、交互路径和治理语义不变。

## 已完成

- 复习任务028工单、任务028启动前复核报告、应用插件开发规范、命令系统契约、设置面板项目规则和内部技术文档。
- 新增 `src/app/AppRoot.tsx`、`AppProviders.tsx`、`AppRuntimeProvider.tsx`、`AppShell.tsx`、`scene-registry.ts`，并将 `src/App.tsx` 与 `src/app/App.tsx` 收口为轻量标准入口。
- 保留当前 `RuntimeProvider` / `I18nProvider` 作为设置面板真实运行时来源，同时通过 `ChipsEnvironmentProvider`、`ChipsThemeProvider` 与 `AppRuntimeProvider` 注入 SDK client、主题、语言、surface、session、launchParams、错误态和刷新动作。
- 新增 `src/commands/settings-commands.ts` 与 `useSettingsCommands.ts`，为现有七个设置 Scene 生成稳定 command id、`handlerId`、i18n key、图标、菜单 placement、工具栏 placement 和命令面板关键词，并通过 `client.command.*` 注册、调用、监听和卸载清理。
- 在 `AppShell` 中接入 `ChipsCommandProvider`、`ChipsMenuBar`、`ChipsToolbar`、`ChipsCommandPalette`，侧栏导航和窄屏切换器也统一走 Scene command 调用。
- 补齐 `manifest.yaml` 的 `command.read / command.write / command.invoke` 权限，并更新 `tests/e2e/basic-flow.test.ts`。
- 补充中英文命令菜单、工具栏和命令面板文案。
- 升级 `tests/unit/app.test.tsx` 的 RuntimeProvider mock，覆盖命令注册、查询、调用、状态和 `onInvoked` 事件链路。
- 更新 `README.md`、`技术文档/00-文档索引.md`、`01-应用架构与模块边界设计.md`、`03-运行时数据流与接口映射设计.md`、`04-测试策略与质量门禁设计.md`，同步 AppRuntime/Scene/Commands 入口与 command 权限基线。
- 启动任务029提前勘察子代理，要求其只读并将报告保存到 `08-草稿笔记`。

## 验证

```bash
cd Chips-EcoSettingsPanel
npm run typecheck
npm test -- app.test.tsx menu-registry.test.ts basic-flow.test.ts
npm run verify
```

结果：

- `npm run typecheck` 通过。
- `app.test.tsx / menu-registry.test.ts / basic-flow.test.ts` 通过。
- `npm run verify` 通过：lint、typecheck、13 个测试文件 31 个测试、build、`chipsdev validate` 均成功。
- build 阶段仍有 Vite 单 chunk 大于 500 kB 的提示，当前不阻断任务028.02；后续如引入预览与诊断入口，应结合应用插件 vNext 质量脚本评估 code-splitting。

## 后续

任务028后续继续推进：

- 迁移 `SettingsRuntimeService` 中裸 `platform.*` 调用到 SDK platform Domain API。
- 追加主题矩阵、组件 contract、主题 contract、缺失 token 诊断、预览与性能入口。
- 补强 Host mock/smoke、权限不足、主题切换、语言切换和 command metadata schema 测试。
- 继续把私有页面壳层样式向组件库布局原语和正式系统 UX 组件收敛。
