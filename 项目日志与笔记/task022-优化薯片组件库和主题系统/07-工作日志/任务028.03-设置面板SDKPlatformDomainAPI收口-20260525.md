# 任务028.03：设置面板 SDK Platform Domain API 收口

## 时间

2026-05-25

## 本次目标

把设置面板运行时服务中残留的裸 `platform.*` Host action 调用收口到 SDK `client.platform.*` Domain API，同时补齐文件选择器过滤器在 SDK / Host PAL / 公共文档中的正式表达。

## 已完成

- 复习任务028工单、任务028复核报告、运行时设置治理接口、SDK 使用指南、系统接口标准、SDK 平台封装文档、Host PAL 类型与实现。
- 登记 `工单104-platformOpenFile文件过滤器契约缺口阻断设置面板DomainAPI收口`，并创建独立任务说明。
- 在 `Chips-SDK/src/api/platform.ts` 中为 `PlatformDialogFileOptions` 增加 `filters`，并导出 `PlatformDialogFileFilter`。
- 在 `Chips-Host/packages/pal/src/types.ts` 中为 `DialogFileOptions` 增加 `filters`。
- 在 `Chips-Host/packages/pal/src/node-adapter.ts` 中补齐文件过滤器归一化，并映射到 Electron、macOS、Linux、Windows 打开文件对话框链路。
- 将 `SettingsRuntimeService` 的 `revealPath/openPluginFileDialog/showConfirm/showMessage` 迁移到 `client.platform.shellShowItemInFolder/openFile/showConfirm/showMessage`。
- 更新 `runtime-service.test.ts`、SDK `client.test.ts`、Host `pal-dialog-electron.test.ts` 与 `host-services-pal-routing.test.ts`。
- 同步 `生态共用技术文档/插件开发/08-SDK使用指南.md`、`协议与接口标准/05-系统接口标准.md`、`协议与接口标准/07-运行时设置治理接口标准.md`、`Chips-SDK/技术文档/06-主题与插件能力封装设计.md`、`Chips-EcoSettingsPanel/技术文档/03-运行时数据流与接口映射设计.md`。

## 验证

```bash
cd Chips-EcoSettingsPanel
npm test -- runtime-service.test.ts
npm run typecheck
npm run verify

cd ../Chips-SDK
npx vitest run tests/client.test.ts
npm test

cd ../Chips-Host
npx vitest run tests/unit/pal-dialog-electron.test.ts tests/unit/host-services-pal-routing.test.ts
npm run build
npm test
```

结果：

- 设置面板 `runtime-service.test.ts` 通过：9 个测试全部通过。
- 设置面板 `npm run typecheck` 通过。
- 设置面板 `npm run verify` 通过：13 个测试文件、32 个测试全部通过，build 与 `chipsdev validate` 成功；仍存在 Vite 单 chunk 大小提示，不阻断本任务。
- SDK `tests/client.test.ts` 通过：34 个测试全部通过。
- Host `pal-dialog-electron.test.ts` 与 `host-services-pal-routing.test.ts` 通过：14 个测试全部通过。
- SDK `npm test` 通过：Vitest、CLI smoke、Host 管理命令、各类型脚手架 E2E、module invoke、package compatibility、tooling 全部通过。
- Host `npm run build && npm test` 通过：35 个测试文件、214 个测试全部通过。

## 后续

- 任务028.03 已完成验证闭环，后续进入主题矩阵、contract 诊断、预览与质量入口扩展。
- 子代理已完成任务029启动前复核，报告保存在 `08-草稿笔记`，进入任务029时再纳入提交。
