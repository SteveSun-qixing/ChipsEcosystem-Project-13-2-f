# 任务028.04：主题 Contract 诊断与预览质量入口

## 时间

2026-05-25

## 本次目标

为设置面板新增当前主题 contract/resolve 诊断入口，以及 `chipsdev` 预览与质量报告命令入口；同时在任务开始前一次性补齐任务028剩余小任务文档。

## 已完成

- 复习任务028主文档、任务028.01/028.02/028.03、生态设置面板设计、主题系统设计、性能与质量门禁设计、SwiftUI 预览与性能工具调研、`chipsdev` 命令手册和 028.04 子代理复核报告。
- 按用户要求在启动任务028.04代码开发前，一次性补齐任务028剩余小任务文档：028.04、028.05、028.06、028.07。
- 更新任务028主文档，增加子任务拆分表和后续新增任务文档规则。
- 明确本任务首批边界：只消费 `client.theme.contract.get()`、`client.theme.resolve([])` 和正式 `chipsdev` 报告命令口径；不读取 ThemePack、组件库或 Host 工作区私有路径，不伪造跨主题实时 contract 矩阵。
- 新增 `theme-diagnostics` Scene：通过 `SettingsRuntimeService.getThemeContract()` 与 `resolveThemeDiagnostics([])` 消费 SDK Theme Domain API，在 feature 层映射当前主题 summary、coverage、解析链、组件 Contract 行和诊断列表。
- 新增 `preview-quality` Scene：展示 `chipsdev preview`、`component gallery`、`theme inspect`、`quality gate`、`diagnostics` 正式报告命令入口和运行时消费边界。
- 将新增 Scene 纳入菜单注册、Scene 注册、命令元数据、侧栏、窄屏切换器、菜单栏、工具栏和命令面板。
- 补齐中英文 i18n、页面样式、runtime service 单测、view model 单测、页面渲染测试、菜单顺序与 App 根组件断言。
- 更新 `Chips-EcoSettingsPanel/README.md`、`技术文档/01-应用架构与模块边界设计.md`、`03-运行时数据流与接口映射设计.md`、`04-测试策略与质量门禁设计.md`。

## 验证

```bash
cd Chips-EcoSettingsPanel
npm test -- runtime-service.test.ts theme-diagnostics-page.test.tsx preview-quality-page.test.tsx menu-registry.test.ts app.test.tsx
npm run verify
```

结果：

- 定点测试通过：6 个测试文件、17 个测试全部通过。
- `npm run typecheck` 通过。
- `npm run verify` 通过：16 个测试文件、36 个测试全部通过，build 与 `chipsdev validate` 成功。
- Vite 仍提示单 chunk 超过 500 kB，这是现有构建提示，不阻断本任务。

## 后续

- 任务028.05 继续处理权限不足、Host smoke、主题切换和语言切换真实链路验证。
- 028.05 子代理复核报告已写入 `08-草稿笔记`，正式进入 028.05 时再纳入提交与处理。
