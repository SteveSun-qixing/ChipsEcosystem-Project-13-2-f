# 任务028.05：权限不足与 Host Smoke 验证补强

## 1. 本轮目标

补齐生态设置面板在正式 Host/SDK 权限治理下的错误态、权限不足、主题切换、语言切换和 Host mock smoke 验证，确保任务028迁移后的治理入口不只在页面 mock 中可渲染，也能通过正式 SDK/Bridge action 序列被测试覆盖。

## 2. 已完成内容

- 复核任务028.05 文档、子代理启动前复核报告、生态设置面板设计原稿、运行时设置治理接口标准、Bridge API 规范、Host 服务域设计和 SDK 测试辅助说明。
- 登记并闭环 `工单105-Host对外接口基线漏列command子域与设置面板权限基线漂移`。
- 同步公共文档：
  - `06-Chips-Host对外接口基线.md` 补齐 `command` 子域、公开服务命名空间、动作、权限和事件；
  - `07-运行时设置治理接口标准.md` 补充 Scene 命令与导航基线，更新设置面板 vNext 最小权限。
- 设置面板新增 `src/app/settings-permissions.ts`，集中维护 manifest 与组件库环境注入共用的权限清单。
- `normalizeSettingsError()` 已保留 SDK 标准 `permission` 诊断字段、`messageKey`、`requestId` 和 `traceId`。
- 新增权限不足测试：
  - 主题读取/写入；
  - 语言写入；
  - 插件安装、启停、卸载、启动、快捷方式创建/删除；
  - 平台文件选择、确认弹窗、消息弹窗、显示位置；
  - command 注册与调用。
- 新增 `host-smoke.test.ts`，使用 `chips-sdk/testing` mock Host 验证设置面板正式 SDK/Bridge action 序列，不创建平行 Host 启动器。
- 更新设置面板 README、运行时数据流文档和测试策略文档。

## 3. 边界说明

- 本轮没有修改 Host runtime 主实现，也没有新增 Host/SDK API。
- Host smoke 使用 `chips-sdk/testing` 正式 mock Host，未读取 Host 私有模块、ThemePack 源码、组件库 contract 私有目录或本地报告 JSON。
- 028.06 后续不再拆小数子任务；如果需要更细任务说明，直接完善 `任务028.06-设置面板声明式布局与页面壳层收口.md`。

## 4. 验证记录

已执行：

```bash
cd Chips-EcoSettingsPanel
npm test -- runtime-permissions.test.ts settings-commands.test.tsx runtime-hooks.test.tsx basic-flow.test.ts host-smoke.test.ts errors.test.ts
npm run verify
```

结果：

- 聚焦测试通过：6 个测试文件，20 条测试。
- `npm run verify` 通过：lint、typecheck、19 个测试文件 / 48 条测试、build、validate 均通过。
- 构建阶段仍有 Vite chunk size warning，属于既有非阻塞提示，不影响本轮质量门禁。
