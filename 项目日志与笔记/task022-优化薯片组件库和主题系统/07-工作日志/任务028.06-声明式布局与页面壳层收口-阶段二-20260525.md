# 任务028.06：声明式布局与页面壳层收口 - 阶段二

## 时间

- 2026-05-25 14:14 CST

## 本阶段目标

- 继续收口设置面板组件展示、主题诊断和预览质量页面的重复页面结构。
- 保持既有功能、交互入口、SDK/Host 消费边界和 i18n 文案语义不变。
- 清理旧页面壳层 class，把重复卡片、摘要、指标和说明文案统一到设置面板共享 UI。

## 完成内容

- 新增 `src/shared/ui/PageLayout.tsx`，沉淀 `PageStack / PageSection / SummaryPanel / CardGrid / BentoCardGrid / CardGridItem / CardDescription / MetricCard`。
- `ComponentGalleryPage` 改为通过 `PageSection + BentoCardGrid + CardGridItem + CardDescription` 渲染正式组件分组和 Bento 卡片。
- `ThemeDiagnosticsPage` 清理中间态重复 JSX，改为通过 `SummaryPanel / CardGrid / MetricCard` 展示当前主题、覆盖率指标、主题链和诊断卡片。
- `PreviewQualityPage` 改为通过 `SummaryPanel / CardGrid / CardDescription` 展示 chipsdev 预览、contract 和质量报告入口。
- `styles.css` 移除旧 `gallery-page / gallery-bento-grid / diagnostics-page / diagnostics-hero / metric-grid / chain-list / diagnostic-list / preview-quality-grid` 等页面私有壳层样式，改为共享 `settings-*` 布局类；保留组件预览所需的局部内容样式。
- 更新组件展示、主题诊断、预览质量页面测试，断言共享页面布局结构并排除旧壳层 class。
- 更新设置面板内部技术文档，记录共享页面布局边界、Bento 响应式策略和质量门禁断言口径。

## 验证记录

```bash
cd Chips-EcoSettingsPanel
npm test -- component-gallery-page.test.tsx preview-quality-page.test.tsx theme-diagnostics-page.test.tsx
npm run typecheck
npm run verify
```

- 聚焦测试通过：3 个测试文件，3 条测试通过。
- `npm run typecheck` 通过。
- `npm run verify` 通过：lint、typecheck、全量 test、build、validate 全部通过。
- Vite 仍提示产物 chunk 超过 500 kB，这是既有体积提示，本阶段无阻断错误。

## 边界说明

- 本阶段未修改主题包、SDK、Host 或组件库公共契约。
- 本阶段未暂存或处理 `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/*` 的既有脏文件。
- `08-草稿笔记` 中的 028.07 与 029 子代理复核报告属于后续任务材料，本阶段不纳入提交。
