# 任务029.07：文档验收与任务029闭环

## 时间

2026-05-25

## 背景

任务029.07要求对卡片查看器应用迁移做最终文档验收，重新核对当前代码、`manifest.yaml`、测试和公共契约，确保 README、项目内部技术文档和工作日志都反映任务029最终真实实现，而不是旧版卡片专用显示链路或未实现的产品承诺。

## 本次处理

- 复核任务029主文档与 `029.01` 至 `029.07` 子任务文档，确认任务029已经按开始前拆分的子任务顺序推进。
- 复核 `07-工作日志`，确认 `029.01` 至 `029.06` 均已有阶段日志；本次补齐 `029.07` 闭环日志。
- 重写 `Chips-CardViewer/README.md`：
  - 将旧的 `.card` 单文件口径更新为 `.card/.box` 正式查看入口；
  - 将旧的 `client.card.compositeWindow.render(...)` 口径更新为当前真实主入口 `client.document.window.render({ filePath, locale, mode: "view" })`；
  - 补充 App / Scene / Surface 结构、命令系统、主题/i18n/a11y、`webDocumentUrl` 托管文档路径和测试矩阵；
  - 移除旧工作区绝对路径示例，改用 `<生态工作区>/Chips-CardViewer`。
- 新增 `Chips-CardViewer/技术文档/卡片查看器应用架构与验收矩阵.md`：
  - 明确 CardViewer 是 Host/SDK 显示链路消费方，不解析 `.card/.box`，不 import 基础卡片或箱子布局插件源码；
  - 记录 `empty`、`document-file`、`hosted-document` 三类内部 scene；
  - 记录 `.card/.box` 文档窗口、托管文档、命令、主题、i18n 和验收矩阵；
  - 明确未实现的最近打开、导出、诊断面板等能力不在任务029中承诺。
- 修正空态用户可见范围标记：
  - `DropZone` 的文件标记由硬编码 `.card` 改为 i18n 文案；
  - 中英文语言包新增 `card-viewer.dropzone.fileMark: ".card / .box"`；
  - `dropzone.test.tsx` 覆盖 `.card / .box` 标记，避免 UI 与 manifest 支持范围再次漂移。

## 公共文档判断

- `生态共用技术文档/插件开发/08-SDK使用指南.md` 已在任务029.06补充 `client.document.window.onNodeError(...)` 的正式语义；
- `生态共用技术文档/组件库/07-复合卡片窗口与SDK显示链路标准.md` 已覆盖 `chips.composite:node-error`、fatal error、资源打开、sandbox 与 SDK 事件消费边界；
- `生态共用技术文档/协议与契约/08-箱子布局运行时契约.md`、`09-卡片信息与打开契约.md`、`10-资源打开契约.md` 和 `13-命令系统契约.md` 已覆盖本轮 CardViewer 消费的公共契约；
- 本次新增内容主要是 CardViewer 项目内部实现和验收矩阵，因此沉淀到 `Chips-CardViewer/技术文档/`，没有重复改写公共规范。

## 行为保持

- 空态仍展示“拖入卡片或箱子文件”和“打开文件”，不新增产品标题或入口。
- 打开按钮仍通过 command registry 触发，再调用 `client.platform.openFile(...)`。
- `.card/.box` 文件仍只通过 `client.document.window.render(...)` 正式链路显示。
- 资源打开仍统一转发到 `client.resource.open(...)`。
- 主题变化仍触发内容 iframe render session 重建，语言变化仍通过组件库 Environment 和本地 i18n adapter 同步。

## 验证

- `cd Chips-CardViewer && npm run lint`：通过。
- `cd Chips-CardViewer && npm test`：通过，6 个测试文件、26 项。
- `cd Chips-CardViewer && npm run build`：通过。
- `cd Chips-CardViewer && npm run validate`：通过，manifest 校验 `ok: true`。

## 后续衔接

- 任务029闭环后按顺序进入任务030编辑引擎应用迁移。
- 任务030开始前需要一次性拆出合适颗粒度的 `030.x` 子任务文档，采用“串行主线 + 可并行模块”的执行方式，不机械细拆。
- 后续创建子代理时默认使用 fork 上下文，不再指定模型；子代理任务不限于只读勘察，可按依赖关系承担独立代码块、验证块或文档块。
