# 任务031.04：组件库壳层、主题 i18n、焦点键盘与 A11y 收口

## 本次进度

- 复核 `任务031.04-组件库壳层主题i18n焦点键盘与A11y收口.md`、`生态共用技术文档/组件库/11-焦点键盘与A11y交互模型.md`、`生态设计原稿/15-查看器设计.md`、任务031.02/031.03日志和当前 `Chips-BookReader` 代码。
- 确认 `@chips/a11y` 已是生态内部 workspace 包，本次只为 `Chips-BookReader` 声明正式依赖，并在 `package-lock.json` 补齐对应 workspace 依赖关系，不引入第三方库。
- `PanelShell` 接入 `createFocusScope(...)`：
  - 抽屉/对话框统一 `role="dialog"`、`aria-modal`、标题关联。
  - `Tab / Shift+Tab` 在面板内循环。
  - `Escape` 关闭当前面板。
  - 关闭后恢复到打开前触发元素。
  - 搜索面板保留搜索框 `autoFocus`，其他面板聚焦首个可操作元素。
- `NavigationTree` 改为使用 `createRovingTabIndex(...)`：
  - 目录树输出 `role="tree"` / `role="treeitem"`。
  - 当前项使用单一 Tab 入口。
  - `ArrowUp / ArrowDown / Home / End` 移动焦点，`Enter / Space` 激活章节跳转。
- `SearchPanel` 搜索结果接入纵向 roving tabindex，结果项补充 `aria-label` 与当前结果语义。
- `BookmarkPanel` 书签打开入口接入纵向 roving tabindex，删除按钮保留独立真实按钮并补充独立 `aria-label`。
- 补充中英文 i18n key：搜索结果可访问标签、书签打开/删除标签、未命名章节 fallback。
- 更新 `Chips-BookReader/技术文档/02-书籍阅读器实现说明.md`，记录面板 FocusScope、roving tabindex、键盘路径和验证结果。

## 修改范围

- `Chips-BookReader/package.json`
- `package-lock.json`
- `Chips-BookReader/i18n/zh-CN.json`
- `Chips-BookReader/i18n/en-US.json`
- `Chips-BookReader/src/components/PanelShell.tsx`
- `Chips-BookReader/src/components/ContentsPanel.tsx`
- `Chips-BookReader/src/components/SourcePanel.tsx`
- `Chips-BookReader/src/components/PreferencesPanel.tsx`
- `Chips-BookReader/src/components/NavigationTree.tsx`
- `Chips-BookReader/src/components/SearchPanel.tsx`
- `Chips-BookReader/src/components/BookmarkPanel.tsx`
- `Chips-BookReader/src/components/ReaderShell.tsx`
- `Chips-BookReader/src/styles/reader-panels.css`
- `Chips-BookReader/tests/unit/reader-a11y.test.tsx`
- `Chips-BookReader/技术文档/02-书籍阅读器实现说明.md`

## 验证结果

```bash
cd Chips-BookReader
npm test -- --run tests/unit/reader-a11y.test.tsx tests/unit/reader-shell.test.tsx tests/unit/reader-preferences.test.ts
npm run lint
npm test
npm run build
npm run validate
```

- 指定 031.04 测试：3 个文件，8 个测试通过。
- `npm run lint`：通过。
- `npm test`：29 个文件，128 个测试通过。
- `npm run build`：通过；Vite 仍提示入口 chunk 超过 500 kB，这是阅读器现有大型阅读引擎/Readium 资源导致的体积警告，不阻断本任务。
- `npm run validate`：通过，manifest 校验 ok。

## 后续衔接

- 任务031.05 继续做真实素材回归、文档验收和任务031闭环。
- 031.05 需要用真实电子书/Host 启动链路复核本次新增的面板焦点恢复、目录/搜索/书签键盘路径、主题切换和语言切换后 UI 状态是否一致。
- 本阶段未处理 9 个后续任务子代理结果；按用户要求，等任务31完整完成后再统一汇总子代理状态。
