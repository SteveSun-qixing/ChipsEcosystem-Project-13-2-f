# 任务033：图片查看器应用迁移任务结束日志

## 1. 任务身份与边界

本轮以子代理身份独立负责 `任务033-图片查看器应用迁移`。执行边界如下：

1. 主要写入范围限定为 `Chips-PhotoViewer/` 与任务033相关文档、日志、草稿。
2. 未修改 `Chips-BookReader`、`Chips-MusicPlayer` 或其它并行任务项目。
3. 子代理开发阶段未执行任何 Git 操作，包括 `git add`、`git commit`、`git checkout`、`git branch`、`git merge`、`git reset` 等。
4. 未引入新第三方依赖。
5. 未直接删除文件；不再被正式源码引用的旧入口移动到 `Chips-PhotoViewer/归档/20260526-旧运行时入口/`。

## 2. 启动前阅读与复核

启动前已复核：

1. 规则文档：
   - `AGENTS.md`
   - `生态设计原稿/AGENTS.md`
   - `生态共用技术文档/AGENTS.md`
   - `项目日志与笔记/AGENTS.md`
2. 任务文档：
   - `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务033-图片查看器应用迁移.md`
3. 设计原稿：
   - `生态设计原稿/01-生态概述.md`
   - `生态设计原稿/07-插件系统架构.md`
   - `生态设计原稿/15-查看器设计.md`
   - `生态设计原稿/19-SDK与协议.md`
4. 公共技术文档：
   - `生态共用技术文档/协议与契约/01-插件契约规范.md`
   - `生态共用技术文档/协议与契约/10-资源打开契约.md`
   - `生态共用技术文档/协议与契约/11-资源图像转换契约.md`
   - `生态共用技术文档/协议与契约/12-图像颜色提取模块能力契约.md`
   - `生态共用技术文档/协议与契约/13-命令系统契约.md`
   - `生态共用技术文档/插件开发/02-应用插件开发.md`
   - `生态共用技术文档/架构设计/17-L8声明式UI实现与接口细则.md`
   - `生态共用技术文档/组件库/01-组件库对外使用总览.md`
   - `生态共用技术文档/组件库/05-可访问性与质量基线.md`

## 3. 子任务与过程记录

任务033开始前一次性拆分并创建以下子任务文档：

1. `任务033.01-图片查看器现状冻结与资源契约复核.md`
2. `任务033.02-AppRuntime-Scene-Environment与启动链路迁移.md`
3. `任务033.03-命令系统组件库壳层与图片查看动作注册.md`
4. `任务033.04-主题i18n焦点A11y真实素材回归与文档闭环.md`

过程日志已写入：

1. `任务033.01-图片查看器现状冻结与资源契约复核-20260526.md`
2. `任务033.02-AppRuntime-Scene-Environment与启动链路迁移-20260526.md`
3. `任务033.03-命令系统组件库壳层与图片查看动作注册-20260526.md`
4. `任务033.04-主题i18n焦点A11y真实素材回归与文档闭环-20260526.md`

启动前复核草稿已写入：

1. `08-草稿笔记/任务033-图片查看器应用迁移-启动前复核报告-20260526.md`

## 4. 实现收口内容

### 4.1 AppRuntime / Scene / Environment

已新增并接入：

1. `Chips-PhotoViewer/src/app/AppRoot.tsx`
2. `Chips-PhotoViewer/src/app/AppProviders.tsx`
3. `Chips-PhotoViewer/src/app/AppRuntimeProvider.tsx`
4. `Chips-PhotoViewer/src/app/AppShell.tsx`
5. `Chips-PhotoViewer/src/app/scene-registry.ts`
6. `Chips-PhotoViewer/src/runtime/chips-client.ts`
7. `Chips-PhotoViewer/src/runtime/environment-client.ts`
8. `Chips-PhotoViewer/src/runtime/launch-context.ts`
9. `Chips-PhotoViewer/src/runtime/theme-runtime.ts`

`src/App.tsx` 已收敛为轻量 re-export，真实运行时由 `src/app/` 承载。

### 4.2 启动与资源打开

保留并继续支持：

1. 空启动。
2. `launchParams.targetPath`。
3. `launchParams.resourceOpen.filePath`。
4. `launchParams.resourceOpen.resourceId`。
5. 电子书基础卡片 `chips.book-card` 的 `image-sequence` payload。
6. 应用内文件选择打开。
7. 拖拽打开。

图片资源解析仍通过 `client.resource.resolve(...)`；保存真实本地图片仍通过 `client.file.copy(...)`；Host 托管资源保存通过 `client.resource.readBinary(...)` 和 `client.file.write(...)`。

### 4.3 命令系统

已新增：

1. `Chips-PhotoViewer/src/commands/photo-viewer-commands.ts`
2. `Chips-PhotoViewer/src/commands/usePhotoViewerCommands.ts`

以下动作已注册为 Host command：

1. 打开图片。
2. 保存图片。
3. 上一张。
4. 下一张。
5. 缩小。
6. 放大。
7. 适应窗口。
8. 实际大小。

命令 metadata 使用 `titleKey / descriptionKey / ariaLabelKey`，图标使用 `IconDescriptor`，并根据当前图片加载状态、保存状态和图片序列边界更新 command state。

### 4.4 组件库壳层、主题、i18n 与 A11y

已完成：

1. `PhotoViewerStage` 底部浮动 dock 改为 `ChipsMenuBar + ChipsToolbar`。
2. 命令文案、菜单文案、工具栏文案、disabled reason 和视口可访问名称补齐到 `i18n/zh-CN.json` 与 `i18n/en-US.json`。
3. `src/global.css` 改为使用主题 token fallback，移除旧私有 `photo-viewer-button` 工具栏视觉体系。
4. 图片视口补充 `role="group"` 与可访问名称。
5. 反馈提示补充 `role="status"` / `role="alert"` 与 `aria-live`。
6. 空态保留真实按钮和焦点可见入口。

### 4.5 归档

以下旧脚手架/旧运行时入口不再被正式源码引用，已按规则移动到归档目录：

1. `Chips-PhotoViewer/归档/20260526-旧运行时入口/ProjectScopePanel.tsx`
2. `Chips-PhotoViewer/归档/20260526-旧运行时入口/useChipsBridge.ts`
3. `Chips-PhotoViewer/归档/20260526-旧运行时入口/useChipsClient.ts`

## 5. 文档更新

已更新项目内部文档：

1. `Chips-PhotoViewer/技术文档/01-图片查看器技术文档.md`
2. `Chips-PhotoViewer/开发计划/01-图片查看器开发计划.md`

本轮未新增或修改生态共用技术文档。原因是任务033没有新增公共契约，只是按已有公共契约迁移图片查看器应用。

## 6. 测试与验证

任务033阶段日志记录的最终正式验证命令：

```bash
cd Chips-PhotoViewer
npm run lint
npm test
npm run build
npm run validate
```

结果：

1. `npm run lint`：通过。
2. `npm test`：通过。
   - 5 个测试文件通过；
   - 17 个测试用例通过。
3. `npm run build`：通过。
   - 成功输出 `dist/`。
4. `npm run validate`：通过。
   - `ok: true`
   - manifest 为 `com.chips.photo-viewer`
   - 类型为 `app`
   - 输出目录为 `dist`

新增测试：

1. `Chips-PhotoViewer/tests/unit/commands.test.ts`
   - 覆盖 command metadata；
   - 覆盖 raw 文案字段禁用；
   - 覆盖命令状态派生；
   - 覆盖组件库 command view 状态投影。

## 7. 未闭环风险

1. 本轮未执行真实 Host 窗口级 `resource.open` 联调，只完成 `Chips-PhotoViewer` 应用自身正式脚本验证。
2. TIFF 转 PNG、缩略图、颜色提取不在应用层复制；后续若要增加 UI 入口，应通过 Host `resource.convertTiffToPng` 或模块 `image.color.pick` 正式能力接入。
3. 多图片查看器并存优先级治理、纯内存图片转交和卸载后文件关联回收仍属于后续生态扩展，不是任务033迁移阻塞。

## 8. 工单与 Git 状态

本轮没有发现必须新增登记的问题工单。

子代理开发阶段没有执行任何 Git 操作。主代理补写本结束日志时仅做 `git status`、`git diff`、`git ls-files --others --exclude-standard`、`git check-ignore` 等只读复核，没有执行 `git add`、`git commit`、`git checkout`、`git branch`、`git merge`、`git reset` 等会改变仓库状态的操作。最终提交、整合、验证和提交记录由主代理负责。

主代理在 2026-05-26 继续复核任务033时，按任务033范围重新执行：

1. `npx tsc --noEmit`：通过。
2. `npm run lint`：通过。
3. `npm test`：通过，5 个测试文件、17 个测试用例通过。
4. `npm run build`：通过，输出 `dist/`。
5. `npm run validate`：通过，`ok: true`，manifest 为 `com.chips.photo-viewer`。

归档规则已由 `66b1b46d 提交归档目录跟踪规则调整` 更新为不忽略 `归档/`，因此 `Chips-PhotoViewer/归档/20260526-旧运行时入口/` 会随任务033提交。

## 9. 详细交接清单

本节为主代理在 2026-05-26 基于当前工作区状态、任务033阶段日志和文件清单补写的详细结束交接记录，便于后续审核、整合和提交。

### 9.1 工作区与范围

1. 工作区：`/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f-worktree-20260523-161048`。
2. 任务目录：`项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务`。
3. 日志目录：`项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志`。
4. 草稿目录：`项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记`。
5. 主要代码范围：`Chips-PhotoViewer/`。
6. 与并行任务隔离：任务033自身实现没有触碰 `Chips-BookReader/`、`Chips-MusicPlayer/`、`Chips-RichTextEditor/`、`Chips-VideoPlayer/` 或基础卡片插件目录。

### 9.2 任务拆分与执行顺序

任务033在开工前一次性拆为四个子任务，颗粒度保持为可独立验收的阶段，而不是逐点过细拆分：

1. `033.01` 负责现状冻结、契约复核、资源打开链路和迁移差距确认。
2. `033.02` 负责 AppRuntime、Scene、Environment、启动链路和运行时上下文迁移。
3. `033.03` 负责 Host Commands、组件库命令消费壳层、菜单工具栏与图片查看动作注册。
4. `033.04` 负责主题 token、多语言、焦点/A11y、真实素材回归口径、项目内部文档与最终验证。

该拆分符合“迁移应用时保持现有功能、交互、使用逻辑不变，只切换到新框架架构”的任务原则。

### 9.3 代码交付文件清单

本轮 `Chips-PhotoViewer` 代码层交付可按以下类别复核：

1. 应用入口与配置：
   - `Chips-PhotoViewer/config/app-config.ts`
   - `Chips-PhotoViewer/manifest.yaml`
   - `Chips-PhotoViewer/src/App.tsx`
2. AppRuntime / Scene / Environment：
   - `Chips-PhotoViewer/src/app/AppRoot.tsx`
   - `Chips-PhotoViewer/src/app/AppProviders.tsx`
   - `Chips-PhotoViewer/src/app/AppRuntimeProvider.tsx`
   - `Chips-PhotoViewer/src/app/AppShell.tsx`
   - `Chips-PhotoViewer/src/app/scene-registry.ts`
   - `Chips-PhotoViewer/src/runtime/chips-client.ts`
   - `Chips-PhotoViewer/src/runtime/environment-client.ts`
   - `Chips-PhotoViewer/src/runtime/launch-context.ts`
   - `Chips-PhotoViewer/src/runtime/theme-runtime.ts`
3. Commands：
   - `Chips-PhotoViewer/src/commands/photo-viewer-commands.ts`
   - `Chips-PhotoViewer/src/commands/usePhotoViewerCommands.ts`
4. 视图与样式：
   - `Chips-PhotoViewer/src/components/PhotoViewerStage.tsx`
   - `Chips-PhotoViewer/src/global.css`
5. 多语言：
   - `Chips-PhotoViewer/src/i18n/messages.ts`
   - `Chips-PhotoViewer/i18n/zh-CN.json`
   - `Chips-PhotoViewer/i18n/en-US.json`
6. 测试：
   - `Chips-PhotoViewer/tests/unit/app.test.tsx`
   - `Chips-PhotoViewer/tests/unit/commands.test.ts`
7. 项目内部文档：
   - `Chips-PhotoViewer/技术文档/01-图片查看器技术文档.md`
   - `Chips-PhotoViewer/开发计划/01-图片查看器开发计划.md`

### 9.4 旧入口归档说明

旧脚手架/旧运行时入口已从正式源码路径移出，并放入：

1. `Chips-PhotoViewer/归档/20260526-旧运行时入口/ProjectScopePanel.tsx`
2. `Chips-PhotoViewer/归档/20260526-旧运行时入口/useChipsBridge.ts`
3. `Chips-PhotoViewer/归档/20260526-旧运行时入口/useChipsClient.ts`

主代理补写日志时复核到：这些归档文件物理存在，但当前根目录 `.gitignore` 第 34 行会忽略 `归档/`。因此后续正式提交时需要显式决定归档文件的版本控制策略；否则 Git 视角可能只呈现原路径删除，而不会自动纳入归档副本。该点不影响任务033功能验证，但影响“禁止直接删除文件”的提交审计口径。

### 9.5 行为保持清单

任务033迁移后应保持以下用户行为不变：

1. 空启动仍展示图片查看器空态，并保留手动打开入口。
2. 应用内文件选择打开保持可用。
3. 拖拽图片打开保持可用。
4. `launchParams.targetPath` 启动保持可用。
5. `launchParams.resourceOpen.filePath` 启动保持可用。
6. `launchParams.resourceOpen.resourceId` 启动保持可用。
7. `chips.book-card` 图片序列 payload 保持可用，上一张/下一张依据序列边界更新状态。
8. 缩放、适应窗口、实际大小、拖拽平移继续走已有图片查看交互模型，不重写第二套相机逻辑。
9. 保存真实本地图片继续通过正式 file/client 链路，Host 托管资源继续通过 resource binary 读取与 file write 链路。
10. TIFF 转 PNG、缩略图、颜色提取等生态能力没有复制到应用层，后续需要 UI 入口时继续走 Host/模块正式能力。

### 9.6 架构迁移收口

本轮迁移的核心架构状态如下：

1. `src/App.tsx` 已收敛为轻量入口，具体应用树由 `src/app/AppRoot.tsx` 接管。
2. `AppRuntimeProvider` 负责 Host client、launch context、surface environment、语言、平台形态、图片目标、资源解析、保存动作和反馈状态。
3. `AppProviders` 接入 `ChipsEnvironmentProvider` 与 `ChipsThemeProvider`，图片查看器视觉与运行时上下文从旧私有实现迁入生态统一环境。
4. `scene-registry` 固化图片查看器 scene 身份，后续 Host 或多 scene 能力可从这里扩展。
5. 运行时客户端封装位于 `src/runtime/`，没有把 Host 主实现复制进应用，符合 Host 唯一运行时承载原则。

### 9.7 Commands 与组件库收口

命令系统迁移状态如下：

1. `photo-viewer-commands.ts` 定义八个图片查看命令：打开图片、保存图片、上一张、下一张、缩小、放大、适应窗口、实际大小。
2. 命令 metadata 使用 `titleKey`、`descriptionKey`、`ariaLabelKey`、`disabledReasonKey`，未使用 raw 文案。
3. 命令图标使用 `IconDescriptor`，供组件库菜单/工具栏统一消费。
4. `usePhotoViewerCommands.ts` 通过 command adapter 注册、注销、监听 `command.invoked` 并同步状态。
5. `PhotoViewerStage` 底部操作入口改为 `ChipsMenuBar` 与 `ChipsToolbar`，动作仍转发到 runtime handler 或已有 camera API。
6. 命令 enabled/disabled 状态依据是否有图片、是否保存中、图片序列边界和图片加载状态派生。

### 9.8 主题、多语言与 A11y 收口

主题、多语言与可访问性迁移状态如下：

1. 用户可见文案补入 `zh-CN` 和 `en-US`，覆盖 scene、菜单、工具栏、command、disabled reason、反馈和视口可访问名称。
2. `src/global.css` 使用主题 token fallback 表达 surface、canvas、text、border、success、error、focus ring、shadow、radius 等视觉语义。
3. 旧私有按钮样式体系移除，工具条视觉交给组件库命令组件。
4. 图片视口补充 `role="group"` 和可访问名称。
5. 状态反馈使用 `role="status"`、`role="alert"` 和 `aria-live`。
6. 空态入口保留真实按钮与焦点路径，避免只提供不可访问的视觉提示。

### 9.9 验证记录

任务033阶段日志记录的最终正式验证命令为：

```bash
cd Chips-PhotoViewer
npm run lint
npm test
npm run build
npm run validate
```

阶段日志记录结果：

1. `npm run lint`：通过。
2. `npm test`：通过，5 个测试文件、17 个测试用例通过。
3. `npm run build`：通过，成功输出 `dist/`。
4. `npm run validate`：通过，`ok: true`，manifest 为 `com.chips.photo-viewer`，类型为 `app`，输出目录为 `dist`。

主代理补写本结束日志时没有重新执行耗时验证命令，只做文件清单、Git 只读状态与归档忽略规则复核。后续正式提交前建议主代理在整合其它并行任务后再次运行任务033正式验证命令，确保当前多任务脏工作区下仍然完全通过。

### 9.10 后续整合审核点

主代理后续收口任务033时建议重点审核：

1. `manifest.yaml` 的 `command.*` 权限、图片 handler、runtime targets 与正式应用插件契约是否完全一致。
2. command metadata 是否持续保持 key-only，不回退到 raw 文案。
3. `PhotoViewerStage` 是否只消费 runtime/camera action，没有绕过 command/runtime 重新引入私有桥接。
4. `src/runtime/chips-client.ts` 是否只做 SDK/Bridge 类型化封装，没有承载 Host 主实现。
5. `src/global.css` 是否继续使用主题 token fallback，没有恢复旧私有按钮视觉体系。
6. `Chips-PhotoViewer/归档/` 是否需要通过 `git add -f` 或调整忽略规则进入提交，以满足归档审计要求。
7. 当前工作区还有多个并行任务的未提交改动，整合任务033时需要按路径筛选，避免把 032、034、035、037、040 等任务的变更混入同一提交。

## 10. 结束结论

`任务033-图片查看器应用迁移` 已完成。图片查看器已迁移到新 AppRuntime / Scene / Environment / Commands / 组件库 / 主题 / i18n / A11y 基线，现有图片打开、拖拽、图片序列、缩放、平移和保存逻辑保持不变，并通过项目正式验证脚本。
