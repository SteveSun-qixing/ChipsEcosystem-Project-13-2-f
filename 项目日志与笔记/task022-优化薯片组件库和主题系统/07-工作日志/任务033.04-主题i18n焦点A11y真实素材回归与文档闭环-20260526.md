# 任务033.04：主题、i18n、焦点 A11y、真实素材回归与文档闭环

## 1. 本阶段目标

完成图片查看器迁移后的主题、多语言、A11y、测试和项目内部文档收口。

## 2. 已完成

1. `i18n/zh-CN.json` 与 `i18n/en-US.json` 补齐：
   - scene 文案；
   - 菜单和工具栏文案；
   - command 标题、描述、aria label；
   - command disabled reason；
   - 图片视口可访问名称。
2. `src/global.css` 改为使用主题 token fallback：
   - surface/canvas/on-surface；
   - border；
   - success/error；
   - focus ring；
   - floating shadow；
   - pill radius。
3. 移除旧私有 `photo-viewer-button` 工具栏视觉体系，底部 dock 交给组件库命令消费组件。
4. `PhotoViewerStage` 补齐：
   - 图片视口 `role="group"` 与可访问名称；
   - 空态按钮焦点；
   - 反馈 `role="status"` / `role="alert"` 与 `aria-live`。
5. 更新 `Chips-PhotoViewer/技术文档/01-图片查看器技术文档.md`。
6. 更新 `Chips-PhotoViewer/开发计划/01-图片查看器开发计划.md`。
7. 按“不直接删除文件”规则，将不再被引用的旧脚手架/旧运行时入口移动到 `Chips-PhotoViewer/归档/20260526-旧运行时入口/`：
   - `ProjectScopePanel.tsx`
   - `useChipsBridge.ts`
   - `useChipsClient.ts`

## 3. 正式验证结果

已通过：

```bash
cd Chips-PhotoViewer
npm run lint
npm test
npm run build
npm run validate
```

验证结果：

1. `npm run lint`：通过。
2. `npm test`：5 个测试文件通过，17 个测试用例通过。
3. `npm run build`：通过，成功输出 `dist/`。
4. `npm run validate`：通过，`ok: true`。

## 4. 未闭环风险

1. 本轮未执行真实 Host 窗口级 `resource.open` 联调，只完成应用自身正式脚本验证。
2. TIFF 转 PNG、缩略图、颜色提取不在应用层复制，后续若产品需要入口，应通过 Host/模块正式能力新增命令或面板。
3. 多图片查看器并存优先级治理、纯内存图片转交和卸载后文件关联回收仍属于后续生态扩展，不是任务033迁移阻塞。

## 5. 结论

任务033迁移实现已完成并通过 `Chips-PhotoViewer` 正式验证脚本。
