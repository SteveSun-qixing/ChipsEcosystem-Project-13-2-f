# 任务035：视频播放器应用迁移完成

## 时间

2026-05-26

## 完成内容

1. 将 `Chips-VideoPlayer` 入口迁移到应用脚手架 vNext 结构：
   - `src/App.tsx` 轻量导出 `AppRoot`
   - 新增 `src/app/AppRoot.tsx`
   - 新增 `src/app/AppProviders.tsx`
   - 新增 `src/app/AppRuntimeProvider.tsx`
   - 新增 `src/app/AppShell.tsx`
   - 新增 `src/app/scene-registry.ts`
2. 新增运行时基础文件：
   - `src/runtime/chips-client.ts`
   - `src/runtime/launch-context.ts`
   - `src/runtime/theme-runtime.ts`
3. 接入 Host command registry：
   - 新增 `src/commands/video-player-commands.ts`
   - 新增 `src/commands/useVideoPlayerCommands.ts`
   - Manifest 补齐 `command.read / command.write / command.invoke`
   - 注册打开、保存、播放/暂停、前后跳转、静音、全屏、画中画、更多面板命令
4. 将旧的局部 Bridge / SDK hook 入口移入 `Chips-VideoPlayer/src/归档/`，主线改为统一 `src/runtime/chips-client.ts` 与 `ChipsEnvironmentProvider`。
5. 保留现有播放器功能与交互逻辑：
   - 资源打开、文件打开、拖放、本地副本保存
   - 播放、暂停、时间轴、音量、倍速、字幕、音轨、全屏、画中画
6. 补齐主题、多语言和 A11y：
   - 语言包新增命令文案、播放器描述、A11y label
   - `ChipsEnvironmentProvider` 与 `ChipsThemeProvider` 串联 Host 当前主题和语言
   - 主舞台、视频标题、状态反馈、更多面板 tab/tabpanel 增加可访问语义
   - 控制层、更多面板、反馈和焦点样式通过 `--chips-*` 主题 token 派生项目 CSS 变量
   - 运行时工具按钮改为 `ChipsIcon + IconDescriptor`，移除业务组件内联 SVG 图标体系
7. 更新项目文档：
   - `README.md`
   - `技术文档/00-文档索引.md`
   - `技术文档/01-视频播放器总体架构设计.md`
   - `技术文档/03-播放会话与交互控制设计.md`
   - `技术文档/04-界面结构与主题多语言接入设计.md`
   - `需求文档/03-视频播放器非功能与验收需求.md`
8. 补充测试：
   - `tests/unit/commands.test.ts`
   - `tests/unit/app.test.tsx`
   - `tests/unit/video-player-controller.test.ts`
   - `tests/unit/theme-runtime.test.ts`
   - 覆盖 MP4/WEBM/MOV 资源识别、Host `resourceOpen` 启动恢复、字幕/音轨轨道读取、媒体错误 key、资源切换复位、全屏状态恢复、主题快照读取和命令状态派生。

## 验证命令

在 `Chips-VideoPlayer` 中执行：

```bash
npx tsc --noEmit
npm test -- --run
npm run lint
npm run build
npm run validate
```

最终结果：

- `npx tsc --noEmit`：通过
- `npm test -- --run`：7 个测试文件、22 个测试全部通过
- `npm run lint`：通过
- `npm run build`：通过，构建输出 `dist`
- `npm run validate`：通过，`ok: true`

主代理在 2026-05-26 继续按任务035范围复核，重新执行上述全部正式命令，结果保持一致：TypeScript、lint、7 个测试文件/22 个测试、build、validate 均通过。

历史整体验证记录如下，后续已被当前 7 个测试文件、22 个测试的最终验证覆盖：

```bash
npm run lint && npm test -- --run && npm run build && npm run validate
```

历史结果：

- `npm run lint`：通过
- `npm test -- --run`：5 个测试文件、15 个测试全部通过
- `npm run build`：通过，构建输出 `dist`
- `npm run validate`：通过，`ok: true`

补充说明：收尾审计中有一次用于搜索 token 的 `rg` 命令参数顺序写错，命令本身报错；随后使用正确参数重新检索，确认 `chips.sys.color.success` 为生态公开状态色语义基线。该失误不涉及代码验证链路，也不影响上述正式验证结果。

## 风险与边界

- 视频编解码能力仍由当前 Host / Chromium / OS 支持范围决定，本应用不复制转码或解码模块能力。
- 远端资源另存仍是下载语义，本地文件源另存使用 `platform.saveFile + file.copy`。
- 未发现需要新增问题工单的生态阻断缺口。
