# 任务036：package-lock依赖锁补齐

## 时间

- 2026-05-26 16:48 CST

## 背景

串行收口任务045-050前清理工作区时发现根 `package-lock.json` 仍有一处未提交改动：`Chips-BaseCardPlugin/book-BCP` 对 `@chips/component-library` 的依赖已写入 `package.json`，但锁文件对应 workspace package 依赖清单尚未提交。

## 复核结果

- `Chips-BaseCardPlugin/book-BCP/package.json` 当前正式依赖包含：
  - `@chips/component-library`
  - `react`
  - `react-dom`
- 根 `package-lock.json` 的 `packages["Chips-BaseCardPlugin/book-BCP"].dependencies` 需要同步 `@chips/component-library`。
- `node_modules/@chips/component-library` 在根锁文件中为 workspace link，未引入外部新第三方库。

## 验证记录

在 `Chips-BaseCardPlugin/book-BCP` 执行：

```bash
npm run verify
```

结果：

- `lint`：通过；
- `typecheck`：通过；
- `test`：通过，6 个测试文件、18 条用例全部通过；
- `build`：通过；
- `validate`：通过，`ok: true`；
- `package`：通过，生成 `dist/chips.basecard.book-0.1.0.cpk`。

## 状态

本阶段仅补齐根锁文件记录，不改变图书基础卡片插件运行时代码。
