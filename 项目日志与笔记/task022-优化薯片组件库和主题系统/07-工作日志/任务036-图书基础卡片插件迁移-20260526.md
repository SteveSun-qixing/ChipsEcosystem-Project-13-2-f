# 任务036-图书基础卡片插件迁移

日期：2026-05-26

## 本阶段完成

- 复核根目录 `AGENTS.md`、`Chips-BaseCardPlugin/AGENTS.md`、任务036文档、基础卡片插件结构、卡片渲染机制、卡片插件开发、Manifest、卡片文件格式、资源打开、主题 token、多语言、组件库接入和 SDK 使用规范。
- 确认 `Chips-BaseCardPlugin/book-BCP` 不存在更深层 `AGENTS.md`，本阶段未拆分 `任务036.xx` 子任务文档。
- 收口正式基础卡片契约：
  - `manifest.yaml` 声明 `type: card`、`capabilities.cardTypes: [base.book]`、完整 `runtime.targets`。
  - 入口导出 `renderBasecardView`、`renderBasecardEditor`、`basecardDefinition`，并保持 `pluginId/cardType` 与 manifest 对齐。
  - 新配置持久化 `card_type: "base.book"`，`BookCard` 仅作为运行时别名保留。
- 收口资源链路：
  - 普通电子书通过 `importResource(...)` 写入卡片根目录相对路径。
  - EPUB/EPUB3 尝试解析 OPF 标题、作者和封面，封面仍通过正式资源导入链路保存。
  - ZIP/CBZ 图片包通过 `importArchiveBundle(...)` 的图片过滤、单根目录剥离和系统噪声排除导入，不保存原压缩包。
  - 替换资源后通过 `deleteResource(...)` 登记旧引用删除，查看态通过 `resolveResourceUrl/releaseResourceUrl/openResource` 处理资源 URL 与打开意图。
- 修复严格类型与测试锚点：
  - 修正 `Uint8Array<ArrayBufferLike>` 写入 `Blob/File` 的 DOM 类型问题，显式复制到 `ArrayBuffer`。
  - 将测试与模板文档中的旧 `BookCard` 持久化口径改为 `base.book`。
  - 对齐组件库 `ChipsButton`、`ChipsTextField`、`ChipsEmptyState` 的真实 DOM 与 i18n adapter 用法。
- 更新插件 README、需求文档、架构文档和参数说明，记录正式 `base.book`、完整验证链路与运行时别名边界。

## 修改范围

- `Chips-BaseCardPlugin/book-BCP/src/`
- `Chips-BaseCardPlugin/book-BCP/tests/`
- `Chips-BaseCardPlugin/book-BCP/i18n/`
- `Chips-BaseCardPlugin/book-BCP/templates/`
- `Chips-BaseCardPlugin/book-BCP/docs/`
- `Chips-BaseCardPlugin/book-BCP/README.md`

## 验证记录

已通过：

```bash
cd Chips-BaseCardPlugin/book-BCP
npm run lint
npm run typecheck
npm test
npm run build
npm run validate
npm run package
npm run verify
```

`npm run verify` 已串联 `lint/typecheck/test/build/validate/package`，测试结果为 `6 passed / 18 passed`，并成功输出 `dist/chips.basecard.book-0.1.0.cpk`。

主代理在 2026-05-26 继续复核任务036时重新执行 `npm run verify`，结果保持一致：lint、typecheck、6 个测试文件/18 个测试、build、validate、package 全部通过。

## 风险与备注

- 本阶段未发现必须登记的新架构缺口或公共契约问题工单。
- 本阶段未修改 `Chips-BookReader`、`Chips-PhotoViewer`、其他基础卡片插件或 Host/SDK 主实现。
- 子代理未执行任何 Git 操作。
