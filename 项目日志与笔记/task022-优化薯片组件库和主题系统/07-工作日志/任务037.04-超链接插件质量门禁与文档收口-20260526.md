# 任务037.04：超链接插件质量门禁与文档收口

## 时间

2026-05-26

## 本阶段完成内容

- 补齐 `typecheck`、`package`、`verify` 正式脚本。
- 更新默认配置模板和参数说明。
- 更新 README 与插件说明，明确 `openResource(...) -> resource.open` 正式边界。
- 构建产物已生成：
  - `Chips-BaseCardPlugin/hyperlink-BCP/dist/index.mjs`
  - `Chips-BaseCardPlugin/hyperlink-BCP/dist/chips.basecard.hyperlink-0.1.0.cpk`
- 搜索确认源码和测试中没有残留旧的 `openHyperlinkInSystemBrowser`、`transfer.openExternal`、`platform.openExternal` 或 `window.open` 调用。

## 验证命令

```bash
cd Chips-BaseCardPlugin/hyperlink-BCP
npm run lint
npm run typecheck
npm test -- --run
npm run build
npm run validate
npm run package
npm run verify
```

## 验证结果

- `npm run lint`：通过。
- `npm run typecheck`：通过。
- `npm test -- --run`：通过，4 个测试文件、15 个测试用例全部通过。
- `npm run build`：通过，生成 `dist/index.mjs`。
- `npm run validate`：通过，`ok: true`。
- `npm run package`：通过，生成 `dist/chips.basecard.hyperlink-0.1.0.cpk`。
- `npm run verify`：通过，串联 lint/typecheck/test/build/validate/package。

## 未闭环风险

- 未发现需要登记的新架构工单。
- 这次未执行真实 Host UI 打开外链端到端测试；当前验证覆盖插件导出、schema、查看态资源意图、编辑态配置、build、validate 和 package。

## Git

本阶段未执行任何 Git 操作。
