# 任务039-音乐基础卡片插件迁移

日期：2026-05-26

## 本阶段完成

- 复核根目录 `AGENTS.md`、`Chips-BaseCardPlugin/AGENTS.md`、任务039文档、基础卡片插件结构、卡片渲染机制、SDK 与协议、编辑引擎、卡片插件开发、Manifest、资源打开、资源图像转换、主题 token、组件库接入和多语言规范。
- 核对 `Chips-BaseCardPlugin/music-BCP` 当前 manifest、schema、入口导出、查看态、编辑态、元数据解析、测试与 `Chips-MusicPlayer` 对 `MusicCardOpenPayload` 的消费口径。
- 保持正式内容配置 `card_type: "MusicCard"` 与 `basecardDefinition.cardType: "base.music"` / manifest `capabilities.cardTypes` 对齐，不改动音乐播放器已消费的公共 payload 结构。
- 接入 `@chips/component-library` 现有 workspace 组件：
  - 查看态：`ChipsImage`、`ChipsEmptyState`
  - 编辑态：`ChipsForm`、`ChipsMedia`、`ChipsProgress`、`ChipsToolbar`、`ChipsButton`、`ChipsErrorState`
- 补齐查看态与编辑态预览资源的 `releaseResourceUrl(...)` cleanup 覆盖。
- 新增 `typecheck`、`package`、`verify` 脚本，并同步根 `package-lock.json` 中 `music-BCP` 对 `@chips/component-library` 的 workspace 依赖记录。
- 修复 `typecheck` 暴露的音频元数据解析严格类型问题，包括字节数组所有权、无检查索引、制作团队更新和测试私有 dispose 类型。
- 更新插件 README 与架构文档，记录组件库接入、资源释放和完整验证链路。

## 验证记录

已通过：

```bash
cd Chips-BaseCardPlugin/music-BCP
npm run lint
npm run typecheck
npm test
npm run build
npm run validate
npm run package
npm run verify
```

`npm run verify` 已串联 `lint/typecheck/test/build/validate/package` 并成功输出 `dist/chips.basecard.music-0.1.0.cpk`。

## 风险与备注

- 本阶段未发现需要新增公共契约的问题工单；音乐 payload 已在 SDK、音乐播放器和公共文档中存在正式口径。
- 子代理未执行任何 Git 操作。
