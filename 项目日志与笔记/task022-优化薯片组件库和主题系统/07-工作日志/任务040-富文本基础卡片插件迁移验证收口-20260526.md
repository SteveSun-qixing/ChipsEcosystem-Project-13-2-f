# 任务040：富文本基础卡片插件迁移验证收口

## 1. 时间

- 2026-05-26 13:40 CST

## 2. 阶段范围

- 收口 `Chips-BaseCardPlugin/richtext-BCP` 的 TypeScript 类型链路。
- 验证富文本基础卡片插件的正式 `lint/typecheck/test/build/validate/package/verify` 脚本。
- 本阶段未执行任何 Git 操作。

## 3. 本阶段处理内容

1. 修复编辑器粘贴代理签名：
   - `editorViewOptionsCtx.handlePaste` 继续向上游 `prev.handlePaste` 传递 ProseMirror 提供的真实 `Slice`。
   - 保持现有 Markdown 粘贴覆盖逻辑不变。
2. 收口 Markdown 扩展类型：
   - 为 `micromark-extension-mark/index.js` 与 `mdast-util-mark/index.js` 增加插件本地类型声明。
   - 通过 `tsconfig paths` 仅影响本插件类型检查，不改变运行时打包解析。
   - 为 `mdast-util-to-markdown` 扩展 `insert/mark/subscript/superscript` 构造名，匹配富文本 Markdown 序列化语义。
3. 修正测试 Bridge mock：
   - 测试中写入 `window.chips` 时补齐正式 Bridge 形状所需的 `on/emit`。
   - 保持 `file.read` 测试仍走正式 Bridge action，不绕开 Host/SDK 契约。
4. 补齐公开入口类型与内部文档口径：
   - `renderBasecardView/renderBasecardEditor` 的公开 context 接受正式 `Record<string, unknown>` 配置输入，运行时内部统一执行 `normalizeBasecardConfig(...)`。
   - `BasecardConfig` 明确扩展 `Record<string, unknown>`，保证 schema 输出可直接作为基础卡片正式配置记录流转。
   - 修正插件内部需求/技术文档中关于“占位图标”的旧表述，当前正式实现为 `ChipsIcon + IconDescriptor`。

## 4. 验证结果

在 `Chips-BaseCardPlugin/richtext-BCP` 内执行：

```bash
npm run typecheck
npm run lint
npm test
npm run build
npm run validate
npm run package
npm run verify
```

结果：

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，5 个测试文件、27 条用例全部通过。
- `npm run build`：通过，生成 `dist/index.mjs` 与 `dist/index.css`。
- `npm run validate`：通过，`errors: []`。
- `npm run package`：通过，输出 `dist/chips.basecard.richtext-0.1.0.cpk`。
- `npm run verify`：通过，完整串行执行 lint、typecheck、test、build、validate、package；最终复验时间为 2026-05-26 13:42 CST。

说明：曾并行执行 `npm run build` 与 `npm run validate`，`validate` 抢在 `dist` 生成前报告 `构建输出目录不存在`。随后按正式顺序重跑 `validate` 与完整 `verify` 均通过，确认不是代码或产物问题。

## 5. 文档与工单

- 已有任务拆分文档覆盖 `任务040.01`、`任务040.02`、`任务040.03`，本阶段未继续拆分。
- 本阶段未发现需要新增公共契约的变化；新增内容均为富文本插件本地类型检查、测试适配、公开入口类型对齐和插件内部文档口径修正。
- 本阶段未登记新问题工单。

## 6. 后续交接

- 主代理审核时可重点复核本地类型声明是否符合插件边界。
- 由于完整 `npm run verify` 已通过，任务040在当前 fork 工作区具备合并审核条件。
