# 任务041：评分基础卡片插件迁移

## 1. 时间

- 2026-05-26 15:00 CST

## 2. 阶段范围

- 按 `任务041-评分基础卡片插件迁移.md` 与 `任务041.01`、`任务041.02`、`任务041.03` 收口评分基础卡片插件。
- 将 `Chips-BaseCardPlugin/score-BCP` 迁移到组件库、主题 token、i18n 与基础卡片正式 SDK/Bridge context 链路。
- 为评分卡片所需的公共 `ChipsRating` 只读语义补强组件库测试和公共组件文档。
- 本阶段未执行任何 Git 操作。

## 3. 开工复核

- 已复核根 `AGENTS.md`、`项目日志与笔记/AGENTS.md`、`Chips-BaseCardPlugin/AGENTS.md`、`Chips-ComponentLibrary/AGENTS.md`、`ThemePack` 相关 `AGENTS.md`。
- 已复核生态设计原稿中的基础卡片插件、插件系统、主题多语言、卡片渲染和 SDK 协议资料。
- 已复核生态共用技术文档中的基础卡片插件契约、manifest 规范、组件库 token/主题对接、A11y 与焦点键盘模型。
- 已复核 `score-BCP` 当前 `manifest.yaml`、`package.json`、schema、查看态、编辑态、测试、README 与技术/需求文档。

## 4. 本阶段处理内容

1. 任务拆分：
   - 已在正式开发前创建 `任务041.01-评分基础卡片契约与Schema基线.md`。
   - 已创建 `任务041.02-评分查看态组件库与主题i18n迁移.md`。
   - 已创建 `任务041.03-评分编辑态A11y与质量门禁收口.md`。
2. 组件库与主题基线：
   - 补齐公共 `ChipsRating` 组件、rating component contract、rating token 源、默认/暗色主题 token 与主题 CSS。
   - 修正 `ChipsRating readOnly` 语义：只读阻止提交并设置 `aria-readonly`，但不把已选评分项降级为 disabled 视觉。
   - 新增组件库测试覆盖只读评分保持 active 视觉状态。
3. 评分插件迁移：
   - `package.json` 增加 `@chips/component-library` 与 `typecheck/package/verify` 正式脚本。
   - `src/index.ts` 补齐正式资源桥 context 类型：`importArchiveBundle`、`convertTiffToPng`、`openResource` 等可选字段；评分业务仍不消费资源桥。
   - 查看态改为 `ChipsRating / ChipsProgress / ChipsText`，保留四种展示样式与无外层壳视觉。
   - 编辑态改为 `ChipsSegmentedControl / ChipsRating / ChipsNumberInput / ChipsProgress`，保留样式切换、五分制点击、数字输入与归一化语义。
   - i18n 补齐评分项读屏、数字输入递增递减、进度百分比和进度预览文案。
   - 废弃的本地 `score-symbol-icon.tsx` 按规则移动到插件 `归档/`，未直接删除。
4. 测试与文档：
   - 更新查看态、编辑态、集成测试，断言正式组件库 `data-scope/data-part`、`role="progressbar"`、`role="radiogroup"`、键盘 End 调整评分和 cleanup 链路。
   - 进一步补强爱心查看态、主题 CSS 注入、查看态 cleanup、编辑器容器样式恢复和 `ChipsSegmentedControl` 键盘切换测试。
   - 更新 README、需求规格、架构设计、数据模型与模板参数说明。
   - 更新公共组件文档中 `ChipsRating readOnly` 的正式语义。

## 5. 验证结果

在 `Chips-BaseCardPlugin/score-BCP` 内执行：

```bash
npm run typecheck
npm test -- --run
npm run verify
```

结果：

- `npm run typecheck`：通过。
- `npm test -- --run`：通过，4 个测试文件、15 条用例全部通过。
- `npm run verify`：通过，串行完成 `lint/typecheck/test/build/validate/package`；生成 `dist/index.mjs` 与 `dist/chips.basecard.score-0.1.0.cpk`。

在 `Chips-ComponentLibrary` 内执行：

```bash
npm run verify
npm run test:contracts
```

结果：

- `npm run verify`：通过，token 校验 802 keys、contract 校验 71 个组件、254 条测试全部通过。
- `npm run test:contracts`：通过，52 条 contract 测试全部通过，官方 ThemePack contracts 与 71 个组件契约一致。

在 `ThemePack/Chips-default` 内执行：

```bash
npm run validate:theme && npm test
```

结果：

- `validate:theme`：通过，71 components、722 required tokens。
- `npm test`：通过，5 个测试文件、12 条用例全部通过。

在 `ThemePack/Chips-theme-default-dark` 内执行：

```bash
npm run validate:theme && npm test
```

结果：

- `validate:theme`：通过，71 components、722 required tokens。
- `npm test`：通过，6 个测试文件、16 条用例全部通过。

## 6. 文档与工单

- 已同步 `生态共用技术文档/组件库` 中 Rating 契约、token 对接与 A11y 口径。
- 已同步 `Chips-BaseCardPlugin/score-BCP` 内 README、需求规格、架构设计、数据模型和模板参数说明。
- 本阶段未发现需要登记的新问题工单。

## 7. 后续交接

- 任务041在当前 fork 工作区已完成实现、文档与质量门禁验证。
- 主代理后续提交时需包含插件 `归档/` 下的旧评分图标文件。
- 本阶段没有执行 `git add`、`git commit`、`git checkout`、`git branch`、`git merge` 或 `git reset`。

## 8. 主代理复核记录

- 2026-05-26 15:05 CST：主代理按当前工作区重新复核 `score-BCP` 的 manifest、入口导出、schema、查看态、编辑态、测试与归档文件。
- 复核中将 `src/index.ts` 的 `importArchiveBundle` context 类型补齐为基础卡片公共契约完整版，补充 `include / stripSingleRootDir / excludeSystemArtifacts / entries / discardedEntries` 等字段；评分业务仍不消费资源桥，但导出 context 不再缩水。
- 已重新执行 `cd Chips-BaseCardPlugin/score-BCP && npm run verify`：通过，串行完成 `lint / typecheck / test / build / validate / package`；测试为 4 个文件、15 条用例全部通过；`chipsdev validate` 输出 `ok: true`；`chipsdev package` 成功生成 `dist/chips.basecard.score-0.1.0.cpk`。
