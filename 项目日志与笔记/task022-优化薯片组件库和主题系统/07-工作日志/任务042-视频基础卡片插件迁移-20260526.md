# 任务042：视频基础卡片插件迁移工作日志

## 时间

2026-05-26

## 本轮目标

推进 `Chips-BaseCardPlugin/video-BCP` 迁移到新基础卡片插件架构、组件库、主题 token、多语言和 SDK/Host 正式资源链路。

## 完成内容

1. 复核任务042及子任务042.01、042.02、042.03，确认本轮不再继续拆分任务。
2. 复核基础卡片插件、插件系统、资源打开、主题 token、多语言、组件库接入、A11y质量基线、编辑引擎基础卡片运行时等设计与公共技术文档。
3. 收口 `video-BCP` 资源路径卫生：
   - 拒绝绝对路径、`~`、协议 URL、`blob:`、`data:`、查询串、片段和 `..` 逃逸路径；
   - `collectResourcePaths(...)` 覆盖视频、封面和全部字幕。
4. 收口查看态资源打开 payload：
   - `openResource(...)` 主 `resourceId` 使用卡片根目录相对视频路径；
   - `chips.video-card` payload 中视频、封面和字幕资源同样保存稳定相对路径，不写入临时解析 URL。
5. 收口编辑态组件库与 i18n：
   - 元信息、URL 输入和字幕字段迁移到 `ChipsTextField`；
   - URL 导入动作迁移到 `ChipsButton`；
   - 校验结果返回 i18n key，面板展示时按当前语言翻译。
6. 补充测试：
   - unsafe 资源路径归一；
   - 查看态 resolve/release 与 openResource payload；
   - 组件库文本输入后的失焦提交；
   - URL 导入控件结构。
7. 更新 `README.md`、需求文档、技术文档、模板参数说明和 `template.json`，使文档与实际字幕、播放参数、组件库和资源打开链路一致。
8. 登记 `工单108-视频资源首帧封面与缩略图正式模块能力缺口`，并创建独立任务说明。
9. 编写任务042复核草稿报告到 `08-草稿笔记`。

## 验证结果

在 `Chips-BaseCardPlugin/video-BCP` 执行：

- `npm run lint`：通过
- `npm run typecheck`：通过
- `npm test`：通过，4 个测试文件，12 个测试用例
- `npm run build`：通过，生成 `dist/index.mjs`
- `npm run validate`：通过
- `npm run package`：通过，生成 `dist/chips.basecard.video-0.1.0.cpk`
- `npm run verify`：通过，覆盖 lint/typecheck/test/build/validate/package

说明：首次 `npm run validate` 与 `npm run build` 并行执行时，因 `validate` 抢在 `dist/` 生成前读取产物而失败；随后按顺序重跑 `validate` 已通过，`verify` 也完整通过。

## 风险与后续

- 本轮复核已将浏览器 `video + canvas` 抽帧实现移入 `Chips-BaseCardPlugin/video-BCP/归档/video-cover-任务042-工单108-20260526.ts`，正式运行时代码不再生成持久化默认封面。
- 当前视频导入只写入视频资源并清空旧封面；查看态在未设置封面时继续通过视频元素回退显示首帧。工单108完成后，应通过 Host/模块正式能力生成缩略图，并同步 SDK/编辑引擎资源桥契约。

## 最终复核补充

- 已确认 `Chips-BaseCardPlugin/video-BCP/src/` 与测试中不存在 `extractVideoCoverFile`、`video-cover` 或浏览器 `canvas/toBlob/createObjectURL` 抽帧调用。
- 已确认 `README.md`、需求文档、技术文档、模板参数说明、工单108与草稿复核报告的媒体预处理口径一致：正式运行时代码不生成持久化默认封面，旧实现仅保留在 `归档/`。
- 已把查看态与编辑态 CSS 中负 `letter-spacing` 调整为 `0`，符合前端显示规范。
- 已将 URL 下载错误与字幕类型徽标改为插件语言包文案，运行时代码不再硬编码这两处可见文本。
- 最终重跑 `npm run verify`：通过，覆盖 `lint/typecheck/test/build/validate/package`；测试结果为 4 个测试文件、12 个测试用例全部通过，构建产物 `dist/index.mjs` 与 `dist/chips.basecard.video-0.1.0.cpk` 已重新生成。
- 已补充执行 `Chips-SDK` 的 `npm test` 相关公共契约验证：SDK Vitest 7 个测试文件、78 个测试用例通过；CLI smoke、host-managed、create app、create card 集成链路通过。命令在 180 秒预算内推进到 create layout 集成测试安装依赖阶段后超时，未得到 SDK 全量最终完成信号；本任务直接相关的视频基础卡片正式门禁仍以 `video-BCP npm run verify` 为准，已完整通过。

## Git 操作声明

任务042收口完成后，由主代理按任务范围精确暂存并提交；不包含并行任务的其他未收束改动。
