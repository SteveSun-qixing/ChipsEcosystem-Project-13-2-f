# 任务046 CardToHTML 模块插件迁移日志

## 时间

- 2026-05-26

## 本轮范围

- 任务：`任务046-CardToHTML模块插件迁移`
- 模块：`Chips-ModulePlugin/Chips-CardtoHTML-Plugin`
- 公共契约：`生态共用技术文档/协议与契约/07-文件转换能力契约.md`

## 开工复核

- 已复核 `AGENTS.md`、任务046工单、模块插件能力契约、Manifest 配置规范、文件转换能力契约、卡片渲染链路、卡片文件格式、资源打开契约、SDK 模块调用封装、CardToHTML 当前源码、manifest、schema、README、技术文档与测试。
- 任务046本轮不需要拆分 `046.xx` 子工单：改动集中在 CardToHTML 原子模块契约收口、稳定性修复、测试与文档同步。

## 完成内容

- 保持 `converter.card.to-html/convert` job 方法与 Host `card.render(... target: "offscreen-render")` 正式链路不变，不复制基础卡片渲染逻辑。
- 为复合卡片 iframe 外置 sibling HTML 增加整次转换内全局唯一文件名分配器：
  - 优先使用 `data-node-id`；
  - 重复 `data-node-id` 或混合 `iframe[src]` / `iframe[srcdoc]` 时追加递增序号；
  - 避免多基础卡片导出时后写覆盖前写。
- `conversion-manifest.json` 增加 Host 渲染诊断保留：
  - `renderDiagnostics`
  - `renderConsistency`
  - `contentFiles`
- 输出目录准备后立即检查 job 取消，取消或结构化转换错误发生后也清理本次已准备的部分输出。
- 将 render session 子文档读取到非文本内容时的错误归入 `CONVERTER_HTML_ASSET_REWRITE_FAILED`，更贴近资源/子文档离线化阶段语义。
- 补充单元测试覆盖：
  - 混合 iframe 来源与重复 `data-node-id` 文件名去重；
  - conversion manifest 保留 Host 渲染诊断；
  - 输出目录准备后取消任务会清理部分输出。
- 同步模块 README、需求文档、技术文档与生态公共文件转换契约。

## 修改文件

- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/src/exporter.ts`
- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/src/types.ts`
- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/tests/unit/module-definition.test.ts`
- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/README.md`
- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/需求文档/01-卡片转HTML模块需求规格说明.md`
- `Chips-ModulePlugin/Chips-CardtoHTML-Plugin/技术文档/01-卡片转HTML模块技术方案.md`
- `生态共用技术文档/协议与契约/07-文件转换能力契约.md`

## 验证结果

在 `Chips-ModulePlugin/Chips-CardtoHTML-Plugin` 执行：

- `npx tsc --noEmit`：初次复核发现 3 个严格类型错误，已修复后通过。
- `npm test`：通过，11 个单元测试全绿。
- `npm run lint`：通过。
- `npm run build`：通过，生成 `dist/index.mjs`。
- `npm run validate`：通过。

说明：曾并行执行 `build` 与 `validate`，`validate` 在构建产物写入前读取 `dist` 导致一次时序失败；随后在 `build` 完成后顺序重跑 `validate` 已通过。

## 追加收口

- 对 `extractAssetRoot` 的单元素候选根路径做显式空值保护，满足 `noUncheckedIndexedAccess` 严格检查。
- 收紧 `isCardToHtmlError` 的错误码读取类型守卫，避免把普通 `Error` 直接断言为结构化转换错误。
- 调整单元测试中的 Host `invoke` mock 类型，使其符合 `CardToHtmlContext["host"]["invoke"]` 的泛型输出契约。
- 追加复核 `npx tsc --noEmit`、`npm test -- --run`、`npm run lint`、`npm run build`、`npm run validate` 均通过。

## 风险与后续

- 本轮未发现必须登记的新生态缺口工单。
- CardToHTML 已具备供 `HtmlToPDF` / `HtmlToImage` 消费的目录态 HTML 中间产物诊断基础；后续任务047及之后的模块迁移应继续对齐相同的 job、错误码、取消和 manifest 诊断语义。

## Git 声明

- 本日志随任务046收口提交归档。
