# 任务055：安装打包发布链路验收阶段1日志

日期：2026-05-26

## 本次目标

推进任务055的第一阶段：核对安装打包发布链路文档、脚本、现有 `.cpk` 产物与开发工作区安装态，形成后续正式发布验收前的预检矩阵。

## 已阅读

- `任务055-安装打包发布链路验收.md`
- `任务28-安装打包发布与工作区迁移链路.md`
- `生态设计原稿/01-生态概述.md`
- `生态设计原稿/03-底层内核和打包模式.md`
- `生态设计原稿/07-插件系统架构.md`
- `生态共用技术文档/开发与运维/04-打包与安装路径手册.md`
- `生态共用技术文档/文件格式规范/03-CPK打包格式规范.md`
- `生态共用技术文档/插件开发/06-Manifest配置规范.md`
- `Chips-SDK/cli/index.js`
- `Chips-Host/src/main/cli/index.ts`
- 官方插件与主题工程的 `package.json`、`chips.config.mjs`、`manifest.yaml`

## 完成内容

1. 补齐官方非基础卡片工程的 `npm run package` 入口，统一委托 `chipsdev package`。
2. 更新共用打包手册，明确官方 `app/card/layout/module/theme` 工程统一使用 `npm run package` 作为正式打包验收入口。
3. 扫描当前 20 个 `.cpk` 产物，确认包根 `manifest.yaml`、`dist/` 内容存在，未发现嵌套 `.cpk`。
4. 使用临时 `CHIPS_HOME` 完成五类插件代表样本的开发工作区安装态抽样：
   - 基础卡片插件：`chips.basecard.book`
   - 箱子布局插件：`chips.layout.grid`
   - 应用插件：`com.chips.card-viewer`
   - 模块插件：`chips.module.chips.colorpicker`
   - 主题包：`theme.theme.chips-official-default-theme`
5. 默认主题样本完成 `theme apply`、`theme current`、`theme validate`。
6. 形成阶段预检报告：`08-草稿笔记/任务055-安装打包发布链路预检矩阵-20260526.md`。

## 验证结果

通过：

```bash
npm run verify:workspace
```

通过：

```bash
cd Chips-BoxLayoutPlugin/list-BLP && npm run package
cd Chips-EcoSettingsPanel && npm run package
cd Chips-ModulePlugin/Chips-HtmltoImage-Plugin && npm run package
cd ThemePack/Chips-default && npm run package
```

通过：

```bash
CHIPS_HOME=/tmp/chips-task055-layout-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin install .../chips.layout.grid-0.1.0.cpk
CHIPS_HOME=/tmp/chips-task055-layout-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin enable chips.layout.grid
CHIPS_HOME=/tmp/chips-task055-app-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin install .../com.chips.card-viewer-0.1.0.cpk
CHIPS_HOME=/tmp/chips-task055-app-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin enable com.chips.card-viewer
CHIPS_HOME=/tmp/chips-task055-module-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin install .../chips.module.chips.colorpicker-0.1.0.cpk
CHIPS_HOME=/tmp/chips-task055-module-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin enable chips.module.chips.colorpicker
CHIPS_HOME=/tmp/chips-task055-card-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin install .../chips.basecard.book-0.1.0.cpk
CHIPS_HOME=/tmp/chips-task055-card-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev plugin enable chips.basecard.book
CHIPS_HOME=/tmp/chips-task055-theme-workspace.* CHIPS_WORKSPACE_KIND=dev npx chipsdev theme validate
```

## 阶段结论

任务055阶段1完成。当前官方工程已经具备统一打包脚本入口，现有 `.cpk` 产物结构符合阶段预检要求，开发工作区安装态抽样确认 Host 消费安装副本。

## 后续范围

下一阶段继续验证 Host 正式安装器、用户态 `chips` 工作区、应用快捷方式和文件关联、模块真实调用与取消/卸载、`.card/.box` 安装态打开与编辑导出链路。
