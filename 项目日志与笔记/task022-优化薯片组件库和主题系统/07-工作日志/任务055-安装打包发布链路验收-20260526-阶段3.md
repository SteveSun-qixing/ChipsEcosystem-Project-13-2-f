# 任务055：安装打包发布链路验收阶段3日志

日期：2026-05-26

## 本次目标

补齐模块插件真实调用和 Host 安装器发布链路预检，避免任务055只停留在 `.cpk` 结构、安装和文件打开分发层面。

## 完成内容

1. 阅读 `chipsdev module invoke` 实现，确认其会拉起真实 Electron Host，并在当前目录或 `--manifest` 指向模块工程时先构建、安装和启用模块插件。
2. 选择 `Chips-ModulePlugin/Chips-ColorPicker` 作为低风险同步模块样本，调用 `image.color.pick/pick`。
3. 使用 `ProductFinishedProductTestingSpace/测试音频-专辑封面.png` 做输入，成功获得 `backgroundColor`、`accentColor`、`palette` 和 `metadata`。
4. 阅读 Host 安装器 CLI、macOS installer 与相关单测。
5. 运行 Host 安装器和 app-entry 专项测试，确认发布脚本与启动文件分发入口单测通过。
6. 运行 SDK 全量测试脚本，覆盖 CLI、脚手架、module invoke 和 package compatibility。
7. 生成阶段报告：`08-草稿笔记/任务055-模块调用与安装器预检-20260526.md`。

## 验证结果

通过：

```bash
npx chipsdev module invoke \
  --manifest "$PWD/Chips-ModulePlugin/Chips-ColorPicker/manifest.yaml" \
  --capability image.color.pick \
  --method pick \
  --input "{\"imagePath\":\"$PWD/ProductFinishedProductTestingSpace/测试音频-专辑封面.png\",\"options\":{\"sampleSize\":64}}" \
  --timeout-ms 30000
```

返回结果包含：

- `mode: "sync"`
- `installedPluginId: "chips.module.chips.colorpicker"`
- `backgroundColor`
- `accentColor`
- `palette`
- `metadata.algorithm: "oklab-kmeans-v1"`

通过：

```bash
cd Chips-Host
npm test -- --run tests/unit/macos-installer.test.ts tests/unit/app-entry.test.ts
```

结果：2 个测试文件、12 个测试通过。

通过：

```bash
cd Chips-SDK
npm test -- --run tests/run-cli-module-invoke-tests.cjs
```

实际结果：SDK 固定全量测试脚本执行完成，Vitest 7 个测试文件 81 个测试通过，CLI smoke、host-managed、create workspace/card/layout/module/theme、module invoke、package compatibility 和 tooling 命令均通过。

## 阶段结论

任务055阶段3完成。模块插件真实 Host 调用链路通过，Host 安装器发布脚本单测预检通过，SDK 全量工具链回归通过。

## 后续范围

后续仍需执行真实 macOS `.pkg` 产物构建、系统级快捷方式和文件关联验收、模块异步 job/取消/卸载清理，以及 `.card/.box` 真实 UI 渲染编辑保存导出。
