# 任务055：安装打包发布链路验收阶段4日志

日期：2026-05-26

## 本次目标

执行 Host macOS 正式安装器真实构建，检查 `Chips.app`、`.pkg`、内置插件、文件关联和 payload 洁净度，补齐任务055此前只停留在安装器单测预检的问题。

## 完成内容

1. 执行真实安装器构建命令，生成 `Chips.app` 与 `Chips-Host-0.1.0-macos.pkg`。
2. 检查 `Info.plist`，确认 `.card` 与图片查看器文件关联写入。
3. 检查内置插件目录，确认默认主题、生态设置面板和图片查看器进入 `Chips.app/Contents/Resources/builtin-plugins`。
4. 检查 `.pkg` payload，发现 AppleDouble `._*` 元数据污染，登记并解决工单110。
5. 重新构建 clean 安装器产物，复核 `.pkg` payload 不再包含 `._*`、`.DS_Store` 或 `CodeResources`。

## 验证结果

通过：

```bash
cd Chips-Host
npm test -- --run tests/unit/macos-installer.test.ts
npm run build
npm run build:installer:macos -- --output=../release-artifacts/task055-stage4-macos-installer-20260526-clean
```

通过：

```bash
pkgutil --payload-files \
  release-artifacts/task055-stage4-macos-installer-20260526-clean/Chips-Host-0.1.0-macos.pkg \
  | rg '/\._|^\._|/\.DS_Store|CodeResources'
```

结果：无输出。

产物信息：

- `release-artifacts/task055-stage4-macos-installer-20260526-clean/Chips.app`
- `release-artifacts/task055-stage4-macos-installer-20260526-clean/Chips-Host-0.1.0-macos.pkg`
- `Chips.app` 约 919M。
- `.pkg` 约 356M。
- 当前 `.pkg` 未签名，`pkgutil --check-signature` 显示 `Status: no signature`。

## 阶段结论

任务055阶段4完成。Host macOS 正式安装器可生成真实 `Chips.app` 与 `.pkg`，文件关联、内置插件和 payload 洁净度已通过检查。系统级安装、快捷方式、LaunchServices 人工验收与 `.card/.box` 图形界面编辑保存导出继续留到后续阶段。
