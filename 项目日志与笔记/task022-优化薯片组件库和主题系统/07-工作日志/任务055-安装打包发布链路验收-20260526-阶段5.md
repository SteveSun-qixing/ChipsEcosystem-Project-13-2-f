# 任务055：安装打包发布链路验收阶段5日志

日期：2026-05-26

## 本次目标

补齐模块插件 job、取消和 provider 清理验收，避免任务055只验证同步模块调用和 `.cpk` 安装。

## 完成内容

1. 复核模块能力契约、`module.job.*` 服务实现、`plugin.disable/uninstall` 对模块 provider 的清理逻辑。
2. 在 Host 集成测试中补充 provider 清理断言：
   - `plugin.disable` 后 provider 全量列表保留 `disabled` 状态；
   - `status=enabled/running` 查询为空；
   - `module.resolve` 不选择 disabled provider；
   - `plugin.enable` 后 provider 恢复；
   - `plugin.uninstall` 后 provider 列表清空；
   - 卸载后 `module.invoke` 返回 `MODULE_PROVIDER_NOT_FOUND`。
3. 执行 Host 集成测试，确认 31 条通过。
4. 执行 HtmltoPDF packaged e2e，验证真实 `.cpk` 安装启用后 `converter.html.to-pdf/convert` 返回 job 并生成 PDF。

## 验证结果

通过：

```bash
cd Chips-Host
npm test -- --run tests/integration/host-services.test.ts
```

结果：31 条测试通过。

通过：

```bash
cd Chips-ModulePlugin/Chips-HtmltoPDF-Plugin
npm run e2e
```

结果：

- `chips.module.chips.htmltopdf.plugin-0.1.0.cpk` 打包成功。
- 插件安装启用成功。
- `providerCount = 1`。
- `converter.html.to-pdf/convert` 返回 job。
- job completed 后生成 PDF，`pageCount = 1`，输出大小 193 bytes。

## 阶段结论

任务055阶段5完成。模块插件 job、取消、超时、provider 状态过滤、disable/enable/uninstall 清理和 packaged job 真实产物调用均已覆盖。
