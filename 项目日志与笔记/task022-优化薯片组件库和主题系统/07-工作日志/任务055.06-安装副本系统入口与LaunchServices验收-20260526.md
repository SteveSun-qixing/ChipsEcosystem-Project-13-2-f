# 任务055.06：安装副本系统入口与 LaunchServices 验收日志

日期：2026-05-26

## 背景

任务055进入安装副本与系统入口验收阶段。本阶段目标是在不污染真实 `/Applications`、真实 `~/.chips-host` 和用户 LaunchServices 数据库的前提下，对 `Chips.app`、`.pkg`、内置插件引导、设置面板快捷方式和文件关联声明做非破坏性验收。

## 完成内容

1. 复核任务055.06文档、Host 安装器实现、安装路径手册和 Host 安装器内部文档。
2. 使用 `pkgutil --payload-files` 检查 `.pkg` payload，确认关键路径包含：
   - `./Applications/Chips.app`
   - `./Applications/Chips.app/Contents/MacOS/Chips`
   - `./Applications/Chips.app/Contents/Resources/app/dist/src/main/electron/app-entry.js`
   - 三个内置插件的 `manifest.yaml`
3. 使用 `pkgutil --check-signature` 检查签名状态，当前 `.pkg` 仍为 `Status: no signature`。
4. 使用 `PlistBuddy` 检查 `Chips.app/Contents/Info.plist`：
   - `CFBundleIdentifier = local.chips.host`
   - `.card` 文件关联存在；
   - 图片查看器扩展名 `.png/.jpg/.jpeg/.webp/.gif/.bmp/.svg/.avif` 文件关联存在。
5. 使用发布包内 `Resources/app/dist/src/main/cli/index.js` 与 `Resources/builtin-plugins`，在隔离 `HOME` 和临时 `CHIPS_HOME` 中触发 Host 内置插件引导。
6. 验证隔离用户态工作区自动安装启用：
   - `theme.theme.chips-official-default-theme`
   - `com.chips.eco-settings-panel`
   - `com.chips.photo-viewer`
7. 验证隔离 HOME 下设置面板快捷方式生成：
   - 路径：`/tmp/chips-task055-stage6-home-*/Applications/Chips Apps/生态设置面板.app`
   - `Info.plist` 含中文应用名称；
   - `AppIcon.icns` 存在；
   - 启动脚本包含 `--workspace=<临时CHIPS_HOME>`；
   - 启动脚本包含 `--chips-launch-plugin=com.chips.eco-settings-panel`。
8. 发现并修复工单110补充缺口：最终 product archive 展开目录中可能残留 `Scripts/._postinstall` 这类非 payload 元数据。已在 component pkg 与 product archive flatten 前统一递归清理。
9. 重新生成任务055.06专用 clean 产物：
   - `release-artifacts/task055-stage6-macos-installer-20260526-clean/Chips.app`
   - `release-artifacts/task055-stage6-macos-installer-20260526-clean/Chips-Host-0.1.0-macos.pkg`

## 验证命令

```bash
cd Chips-Host
npm test -- --run tests/unit/macos-installer.test.ts
npm run build
npm run build:installer:macos -- --output=../release-artifacts/task055-stage6-macos-installer-20260526-clean
```

```bash
pkgutil --payload-files release-artifacts/task055-stage6-macos-installer-20260526-clean/Chips-Host-0.1.0-macos.pkg \
  | rg '/\._|^\._|/\.DS_Store|CodeResources'

pkgutil --expand release-artifacts/task055-stage6-macos-installer-20260526-clean/Chips-Host-0.1.0-macos.pkg /tmp/chips-task055-stage6-expand
find /tmp/chips-task055-stage6-expand -name '._*' -o -name '.DS_Store' -o -name 'CodeResources'
```

两类元数据检查均无输出。

隔离 HOME 验收输出摘要：

```json
{
  "pluginNames": ["生态设置面板", "图片查看器"],
  "runtimeNames": ["薯片官方 · 默认主题", "生态设置面板", "图片查看器"],
  "launcherExists": true,
  "plistHasChineseName": true,
  "executableHasPluginArg": true,
  "executableHasWorkspace": true,
  "iconExists": true
}
```

## 未执行项

以下事项需要真实系统安装或人工环境确认，本阶段未执行，必须进入任务055.08最终人工验收清单：

1. 使用 `.pkg` 安装到真实 `/Applications/Chips.app`。
2. LaunchServices 真实注册验证。
3. 启动台中显示 `生态设置面板.app`。
4. 系统双击 `.card` 后由 Host 接收并分发。
5. 系统卸载清理和 LaunchServices 回收。
6. 发布签名与 notarization。

## 结论

任务055.06的非破坏性安装副本验收已完成。发布包 payload 与展开目录均已净化，内置插件引导和设置面板快捷方式在隔离 HOME 下可运行；真实系统级安装、LaunchServices、启动台和卸载清理保留为最终人工验收项。
