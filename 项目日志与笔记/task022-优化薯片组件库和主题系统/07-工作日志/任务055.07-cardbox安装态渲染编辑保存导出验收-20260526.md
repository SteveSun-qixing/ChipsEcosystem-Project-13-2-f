# 任务055.07：cardbox 安装态渲染编辑保存导出验收工作日志

## 时间

2026-05-26

## 本次目标

收口 `.card/.box` 在安装副本或安装副本等价语境下的打开、渲染、编辑、保存和导出验收，确认真实成品素材可通过 Host 正式文档服务路由运行，并记录 GUI 人工验收边界。

## 开工复核

已复核以下任务与设计规范：

- `06-工单级开发任务/任务055.07-cardbox安装态渲染编辑保存导出验收.md`
- `生态设计原稿/05-卡片文件格式.md`
- `生态设计原稿/06-箱子文件格式.md`
- `生态设计原稿/09-卡片渲染机制.md`
- `生态共用技术文档/文件格式规范/01-卡片文件格式规范.md`
- `生态共用技术文档/文件格式规范/02-箱子文件格式规范.md`
- `生态共用技术文档/协议与契约/09-卡片信息与打开契约.md`
- `生态共用技术文档/开发与运维/04-打包与安装路径手册.md`

## 完成内容

- 在 Host 集成测试中补充真实成品素材验收，覆盖 `ProductFinishedProductTestingSpace/富文本基础卡片.card` 的校验、渲染、编辑器渲染、解包、修改、重打包与重新校验。
- 在 Host 集成测试中补充 `ProductFinishedProductTestingSpace/美食网格箱子.box` 的安装态等价验收。测试会在临时 workspace 中派生 fixture，修正旧工作区绝对 `file://` 前缀后覆盖箱子校验、检查、布局配置归一、打开视图、渲染布局、渲染布局编辑器、渲染条目封面、解包、修改、重打包与重新校验。
- 使用 `release-artifacts/task055-stage7-macos-installer-20260526-clean/Chips.app` 内置 CLI，在隔离 HOME 和临时用户工作区下完成安装副本等价抽样。
- 安装副本等价抽样中安装并启用了卡片查看器、编辑引擎、富文本/图片/音乐基础卡片、网格布局、CardToHTML、HtmlToPDF、HtmlToImage 模块插件。
- 安装副本等价抽样中，`chips open` 可将 `富文本基础卡片.card` 与 `美食网格箱子.box` 分发到 `com.chips.card-viewer`。
- 收口工单112：Host 应用窗口、卡片 iframe、箱子布局 iframe 的主题注入边界统一把浏览器普通 CSS 不识别的 `cpx` 转换为响应式 `vw`。
- 同步公共主题文档，明确 `cpx` 源 token 与主题 CSS 可保留，进入浏览器普通 CSS 前由 Host 统一转换。
- 更新任务055.07子任务文档与任务055父任务阶段状态。

## 验证记录

```bash
cd Chips-Host
npm test -- --run tests/unit/theme-css-units.test.ts tests/unit/card-service-rendering.test.ts tests/unit/box-service.test.ts tests/integration/host-services.test.ts
```

结果：通过。4 个测试文件、62 条测试通过。

```bash
cd Chips-Host
npm run build
```

结果：通过。

安装副本等价 CLI 抽样：

- `HOME=/tmp/chips-task055-stage7-home-current-mYxOML`
- `CHIPS_HOME=/tmp/chips-task055-stage7-home-current-mYxOML/.chips-host`
- `CHIPS_BUILTIN_PLUGINS_DIR=release-artifacts/task055-stage7-macos-installer-20260526-clean/Chips.app/Contents/Resources/builtin-plugins`

关键结果：

- `chips status` 显示 `kind: user`；
- `theme current` 返回 `chips-official.default-theme`；
- `theme validate` 返回默认主题 `ok: true`；
- `chips open ProductFinishedProductTestingSpace/富文本基础卡片.card` 返回 `mode: "card"`、`pluginId: "com.chips.card-viewer"`；
- `chips open ProductFinishedProductTestingSpace/美食网格箱子.box` 返回 `mode: "box"`、`pluginId: "com.chips.card-viewer"`。

## 未覆盖与后续

- 未执行真实系统安装到 `/Applications/Chips.app`。
- 未执行 LaunchServices 真实双击 `.card/.box` 分发。
- 未在真实 GUI 中人工操作编辑引擎保存和导出 HTML/PDF/image。
- `.pkg` 签名与 notarization 未执行。

以上内容进入 `任务055.08` 最终发布阻断与人工验收清单。
