# 任务056.01：真实素材清单矩阵与回归基线

时间：2026-05-26

## 本次进度

- 新增 `scripts/collect-finished-product-materials.mjs`，用于扫描 `ProductFinishedProductTestingSpace` 并生成真实素材回归矩阵。
- 根 `package.json` 增加 `npm run materials:inventory`，后续任务可复用同一素材清单入口。
- 生成以下过程性基线文件：
  - `项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务056.01-真实素材清单矩阵-20260526.json`
  - `项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务056.01-真实素材清单矩阵-20260526.md`
- 更新任务056父文档和 `任务056.01` 子文档，写入当前基线、交付结果和验证记录。

## 素材基线

- 文件总数：76。
- 纳入任务056正式回归：68。
- 归档历史素材：6，仅作历史参考，不作为首轮正式通过项。
- 正式回归覆盖：
  - 21 个 `.card` 成品素材。
  - 1 个 `.box` 成品素材。
  - 图片、音频、视频、EPUB、图片 ZIP、网页 ZIP、网页目录和美食源图素材。

## 关键发现

- `美食网格箱子.box` 的条目仍保存源工作区 `file://` URL。后续 `056.02` 自动化回归继续沿用任务055阶段的做法：解包到临时目录后把 URL 改写到当前 worktree，再打包临时派生 `.box` 进行验证，不直接修改真实素材。
- 本阶段没有发现需要立即登记的新工单；用户明确暂缓的发布前人工验收/签名门禁相关工单未处理。

## 验证记录

```bash
npm run materials:inventory
```

结果：通过，成功生成真实素材清单 JSON 与 Markdown。

```bash
npm run verify:workspace
```

结果：通过，生态工作区校验通过。

## 下一步

- 进入 `056.02`，基于清单中 `category=card/box` 的素材扩展 Host、CardViewer、EditingEngine 和箱子布局真实服务链路回归。
