# 任务056.02：卡片箱子真实服务链路回归

时间：2026-05-26

## 本次进度

- 复核卡片文件格式、箱子文件格式、卡片信息与打开契约、箱子布局运行时契约和 Host 对外服务基线。
- 在 Host 集成测试中新增全量真实 `.card/.box` 服务链路回归，覆盖 `ProductFinishedProductTestingSpace` 中 21 个真实卡片和 1 个真实箱子。
- 在 CardViewer 测试 fixture 中补充真实素材动态收集工具，排除 `归档/` 历史素材并断言当前任务056基线数量。
- 在 CardViewer 单元测试中新增真实启动参数回归，逐个消费 22 个真实 `.card/.box` 路径并交给 Host `document.window.render` 正式入口。
- 更新任务056父文档和 `任务056.02` 子任务文档，写入交付结果、验证命令和后续输入。

## 覆盖范围

- 真实卡片：21 个 `.card` 全部通过校验、信息读取、封面渲染、查看态渲染、质量门禁和渲染会话释放。
- 真实箱子：`美食网格箱子.box` 使用临时派生副本修正源工作区 `file://` URL 后，完成校验、封面、检查、布局配置、视图会话、条目列表、条目详情、条目封面、布局查看态、布局编辑态、条目打开、解包、重打包和重新校验。
- 查看器入口：CardViewer 不重写卡片/箱子底层渲染，只把真实 `targetPath` 交给 Host 文档窗口正式链路。

## 关键边界

- 没有直接修改 `ProductFinishedProductTestingSpace` 中的真实素材。
- `美食网格箱子.box` 的旧工作区 URL 只在测试临时 workspace 中派生修正。
- 任务055.07 已覆盖一个真实复合卡片的编辑器渲染、解包、修改、重打包和重新校验；本阶段聚焦全量真实 `.card/.box` 消费面，编辑保存导出大样本继续交给 `056.04`。
- 用户明确暂缓的发布前人工验收、签名或正式发布门禁类工单未处理。

## 验证记录

```bash
cd Chips-Host
npm test -- --run tests/integration/host-services.test.ts -t "regresses all task056 real"
```

结果：通过。

```bash
cd Chips-Host
npm test -- --run tests/integration/host-services.test.ts
```

结果：通过，33 条集成测试通过。

```bash
cd Chips-CardViewer
npm test -- --run tests/unit/app.test.tsx -t "逐个消费任务056"
```

结果：通过。

```bash
cd Chips-CardViewer
npm test -- --run tests/unit/app.test.tsx tests/e2e/basic-flow.test.tsx
```

结果：通过，15 条测试通过。

## 下一步

- 等待或整合 `056.03` 媒体应用真实资源打开回归结果。
- 等待或整合 `056.04` 模块转换、编辑保存、导出真实回归结果。
- 最后执行 `056.05`，汇总主题、语言、失败项、阻断项和真实素材回归报告。
