# 任务056.03 媒体应用真实资源打开回归工作日志

时间：2026-05-26

## 本次目标

使用 `ProductFinishedProductTestingSpace` 中的真实图片、音频、视频、EPUB、图片 ZIP 和网页包素材，补强媒体应用通过正式资源打开契约接收真实文件的回归测试，并修复真实素材暴露出的启动解析缺口。

## 阅读与复核

已复核：

1. 根 `AGENTS.md`
2. `ProductFinishedProductTestingSpace/AGENTS.md`
3. `生态共用技术文档/协议与契约/10-资源打开契约.md`
4. `生态设计原稿/15-查看器设计.md`
5. `任务056.03-媒体应用真实资源打开回归.md`
6. `任务056.01-真实素材清单矩阵-20260526.json`
7. `Chips-PhotoViewer / Chips-MusicPlayer / Chips-VideoPlayer / Chips-BookReader` 的 manifest、启动解析和现有测试
8. `Chips-BaseCardPlugin/webpage-BCP` 的 AGENTS、网页包查看态和测试入口

## 完成内容

1. PhotoViewer
   - 新增真实 `图片.jpg` 的 `resource-open-service` 单图启动回归；
   - 新增真实 `电子书图片.zip` 与 `电子书图片/` 解包目录的 `image-sequence` payload 回归；
   - 修正普通单图入口优先级为 `resourceOpen.filePath -> targetPath -> resourceOpen.resourceId`。
2. MusicPlayer
   - 新增真实 `测试音频.mp3` 与 `测试音频-专辑封面.png` 的音乐卡片 payload 回归；
   - 覆盖音频、封面、标题、歌手上下文保留。
3. VideoPlayer
   - 新增真实 `测试视频.mp4` 的资源打开回归；
   - 新增 `chips.video-card` payload 识别；
   - 增加播放入口参数归一与应用：`autoplay / loop / muted / playbackRate / startTime`。
4. BookReader
   - 新增真实 `电子书.epub` 的启动解析回归；
   - 新增真实 EPUB 加载、目录、章节和渲染链路回归；
   - 新增 `chips.book-card` / `mode: "ebook"` 的真实素材上下文回归。
5. webpage-BCP
   - 新增 `昙花网页.zip` 与 `昙花/` 目录的真实网页包查看态回归；
   - 覆盖正式 `file.read` 桥读取入口 HTML、`srcdoc` 注入 `<base href>`、`openResource` 暴露 `text/html` 意图。
6. 文档同步
   - 更新图片查看器启动恢复文档；
   - 更新视频播放器启动恢复与播放入口文档；
   - 更新任务056.03状态和验收记录。

## 验证记录

已通过：

```bash
cd Chips-PhotoViewer && npm test
cd Chips-MusicPlayer && npm test
cd Chips-VideoPlayer && npm test
cd Chips-BookReader && npm test
cd Chips-BaseCardPlugin/webpage-BCP && npm test
cd Chips-PhotoViewer && npm run lint
cd Chips-MusicPlayer && npm run lint
cd Chips-VideoPlayer && npm run lint
cd Chips-BookReader && npm run lint
cd Chips-BaseCardPlugin/webpage-BCP && npm run lint
cd Chips-PhotoViewer && npm run build
cd Chips-VideoPlayer && npm run build
cd Chips-BaseCardPlugin/webpage-BCP && npm run typecheck
```

测试结果：

1. PhotoViewer：5 个测试文件，19 个测试通过。
2. MusicPlayer：14 个测试文件，58 个测试通过。
3. VideoPlayer：7 个测试文件，24 个测试通过。
4. BookReader：29 个测试文件，135 个测试通过。
5. webpage-BCP：6 个测试文件，23 个测试通过。

## 风险记录

1. 网页包不属于独立媒体应用插件，本次覆盖的是网页基础卡片的正式资源打开边界。
2. BookReader 真实 EPUB e2e 在 jsdom 环境里使用 Node `Blob` 补齐 `stream()`，只作用于测试，不改运行时代码。
3. 主题默认/暗色视觉抽样留给 `056.05` 汇总，本次提供真实资源启动输入。

## Git

未执行任何 git 操作。
