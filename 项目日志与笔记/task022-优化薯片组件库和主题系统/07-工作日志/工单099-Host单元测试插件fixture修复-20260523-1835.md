# 工单099：Host 单元测试插件 fixture 修复日志

## 时间

- 2026-05-23 18:35 CST

## 背景

执行 `task022/任务002-Host-L8原语与Modifier补齐` 的 Host 全量测试时发现，部分单元测试仍直接安装未构建源码插件，导致正式入口校验报错：

- `Plugin entry not found: dist/index.mjs`
- `Plugin entry not found: dist`

## 修复内容

- `Chips-Host/tests/unit/card-service-rendering.test.ts`
  - 新增自包含富文本基础卡片插件 fixture；
  - 新增自包含图片基础卡片插件 fixture；
  - 将测试中的源码插件安装替换为正式 fixture manifest 安装；
  - 保留 `manifest.yaml`、`entry: dist/index.mjs` 和实际 `dist/index.mjs` 入口文件。
- `Chips-Host/tests/unit/host-application-render-protocol.test.ts`
  - 新增自包含富文本基础卡片插件 fixture；
  - 新增自包含网格布局插件 fixture；
  - 将 managed render protocol 测试中的源码插件安装替换为正式 fixture manifest 安装。

## 验证结果

- `cd Chips-Host && npx vitest run tests/unit/card-service-rendering.test.ts`：通过，19 个用例全部通过。
- `cd Chips-Host && npx vitest run tests/unit/host-application-render-protocol.test.ts`：通过，3 个用例全部通过。
- `cd Chips-Host && npm run build`：通过。
- `cd Chips-Host && npm test`：通过，35 个测试文件、193 个用例全部通过。

## 结论

工单099已解决。Host 单元测试不再依赖外部源码插件是否已人工构建，同时继续保留正式插件入口完整性校验。
