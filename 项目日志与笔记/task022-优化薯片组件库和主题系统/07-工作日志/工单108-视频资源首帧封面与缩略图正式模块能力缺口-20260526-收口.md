# 工单108：视频资源首帧封面与缩略图正式模块能力缺口收口日志

时间：2026-05-26 17:55 CST

## 本次收口

- 复核并保留已有 Host/SDK/EditingEngine/video-BCP 工单108相关改动，没有重写已有工作。
- 修正 Host 视频抽帧非本地 URI 错误归一，`resource.extractVideoFrame` 对远程 URL 返回 `RESOURCE_VIDEO_FRAME_UNSUPPORTED_URI`，不再泄漏 TIFF 错误码。
- 补强 SDK `card.editorPanel.resources.extractVideoThumbnail` 桥接入口参数校验，并归一结构化错误对象的响应消息。
- 补充 video-BCP 编辑器测试，证明上传视频后可通过宿主正式能力生成默认封面；宿主能力缺失或抽帧失败时不会恢复旧浏览器 `video + canvas` 抽帧，也不会破坏视频导入。
- 更新生态公共契约：
  - `生态共用技术文档/协议与契约/11-资源图像转换契约.md`
  - `生态共用技术文档/插件开发/03-卡片插件开发.md`
- 更新工单108任务说明和 `项目日志与笔记/问题工单.md`，状态改为已完成。

## 验证

- `cd Chips-Host && npm test -- --run tests/unit/host-services-pal-routing.test.ts tests/contract/route-manifest.contract.test.ts`
  - 通过：19 tests。
- `cd Chips-SDK && npx vitest run tests/card.test.ts tests/resource.test.ts`
  - 通过：31 tests。
- `cd Chips-EditingEngine && npm test -- --run tests/unit/plugin-host.test.tsx`
  - 通过：13 tests。
- `cd Chips-BaseCardPlugin/video-BCP && npm test -- --run tests/unit/editor-panel.test.tsx`
  - 通过：6 tests。
- `cd Chips-BaseCardPlugin/video-BCP && npx tsc -p tsconfig.json --noEmit`
  - 通过。

## 备注

- 未执行任何 Git 操作。
- 未修改任务052、任务053、任务054、任务051、任务059.02或开放项目报告。
- SDK 全量 `npm test -- --run tests/card.test.ts tests/resource.test.ts` 会被 `package.json` 固定脚本解释为全量测试后追加参数；该命令首次运行时因新增断言期望暴露了结构化错误消息为 `[object Object]` 的问题，已修复并改用 `npx vitest run tests/card.test.ts tests/resource.test.ts` 做专项验证。
