# 下一批子代理任务安排：任务051 至 054

## 时间

- 2026-05-26

## 安排原则

- 本批按 4 个子代理并行，每个子代理独立处理一个完整任务，不再把同一任务拆给多个子代理。
- 子代理不得执行 Git 写操作；Git 暂存、提交、分支、合并和推送只由主代理处理。
- 子代理需要自己阅读根 `AGENTS.md`、更深层 `AGENTS.md`、相关设计原稿、生态共用技术文档、当前代码、manifest、测试和正式运行链路。
- 正式公共契约写入 `生态共用技术文档/`；任务过程日志写入 `07-工作日志`；勘察草稿可写入 `08-草稿笔记`。
- `任务055` 至 `任务058` 是串行发布验收段，本批不派发。
- `任务059` 不受顺序影响，但本批优先保证 `051` 至 `054` 完整收口；如果主线等待且有空闲子代理，再考虑派发 `059.xx`。

## 子代理 A：任务051 社区平台与应用市场对齐

用户已经给出完整派发说明，可直接使用。核心目标：

- 完整处理 `任务051-社区平台与应用市场对齐`。
- 核对社区服务器、应用市场、插件安装链路、Host/SDK/脚手架文档。
- 对齐五类插件类型、manifest 元数据、`.cpk` 校验、权限、签名/哈希、下载元数据、API 返回结构与 Host 安装链路。
- 完成后写 `07-工作日志/任务051-...`，列出修改文件、验证命令、新工单或风险，并声明未执行 Git 写操作。

## 子代理 B：任务052 开放项目 MusicPlayer 同化样本

派发说明：

```text
你是薯片生态重构任务的子代理之一，使用 fork 工作区独立完成一个完整任务。重要规则：

1. 绝对不要执行任何 Git 写操作：不要 git add、commit、checkout、branch、merge、push、reset。最终只汇报你改了哪些文件、验证结果、仍需主代理处理什么。
2. 你不是独自在代码库里工作。主代理和其他子代理可能同时在其他任务改动。不要回退别人改动；只处理你明确负责的任务范围。
3. 修改代码或文档前，先阅读本 worktree 根 AGENTS.md，并阅读相关更深层 AGENTS.md。按要求先看生态设计原稿与生态共用技术文档。
4. 所有正式公共契约/对外接口/跨生态信息，应写到“生态共用技术文档”；项目内部细节写项目内部文档；过程日志写 task022 的 07-工作日志；草稿勘察写 08-草稿笔记。
5. 不做临时版、简化版、占位实现；不新增未选型第三方库；所有 UI 仍需走 React、主题 token、多语言。

你的任务：完整处理 `任务052-开放项目MusicPlayer同化样本`。

工作入口：
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务052-开放项目MusicPlayer同化样本.md`
- 相关设计原稿：`生态设计原稿/01-生态概述.md`、`02-极致模块化架构.md`、`07-插件系统架构.md`、`19-SDK与协议.md`
- 相关公共文档：应用插件开发规范、SDK 使用指南、命令行工具手册、主题系统、多语言、组件库与 Web 项目同化相关文档。
- 相关项目：`OpenSourceProjectHandlingFolder/MusicPlayer-main`、`Chips-MusicPlayer`、`Chips-Scaffold/chips-scaffold-app`、`Chips-SDK`、`Chips-ComponentLibrary`。请先用 rg/find 实际定位。

目标：
- 核对 MusicPlayer-main 的 README、许可证、目录结构、资源、依赖、运行方式和构建脚本。
- 生成同化报告：入口、资源、全局样式、硬编码文案、浏览器 API、媒体 API、外部依赖、可迁移交互与不可复用代码。
- 判断它应成为独立应用插件、组件参考，还是合并为 `Chips-MusicPlayer` 的交互参考。
- 不直接复制旧项目绕开 Host/SDK/组件库/主题/i18n；如需要新增依赖，先写风险和等待用户决策。
- 如发现同化 CLI、脚手架、SDK 或公共文档漂移，按工单流程登记或明确建议主代理登记。
- 完成后在 `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志` 写任务052日志；勘察报告可写到 `08-草稿笔记`。
- 运行与改动范围匹配的正式验证命令；如外部项目依赖安装受限，记录限制与证据。

最终回复要求：
- 明确任务052是否完成。
- 列出修改文件路径。
- 列出验证命令和结果。
- 列出任何新发现工单或风险。
- 再次声明未执行 Git 写操作。
```

## 子代理 C：任务053 开放项目 DPlayer 同化样本

派发说明：

```text
你是薯片生态重构任务的子代理之一，使用 fork 工作区独立完成一个完整任务。重要规则同任务052：绝对不要执行任何 Git 写操作；只处理任务范围；先读 AGENTS、设计原稿、公共技术文档；公共契约写生态共用技术文档，过程日志写 07-工作日志，勘察草稿写 08-草稿笔记；不做临时版或旧架构绕行。

你的任务：完整处理 `任务053-开放项目DPlayer同化样本`。

工作入口：
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务053-开放项目DPlayer同化样本.md`
- 相关设计原稿：`生态设计原稿/07-插件系统架构.md`、`19-SDK与协议.md`、视频播放器/基础卡片/媒体能力相关设计原稿。
- 相关公共文档：组件库、主题系统、a11y、应用插件开发、基础卡片插件开发、模块能力契约、文件转换/媒体能力相关契约。
- 相关项目：`OpenSourceProjectHandlingFolder/DPlayer-master`、`OpenSourceProjectHandlingFolder/DPlayer-master-wiki`、`Chips-VideoPlayer`、`Chips-BaseCardPlugin/video-BCP`、`Chips-ComponentLibrary`。

目标：
- 核对 DPlayer README、源码、构建脚本、播放器 API、样式、许可证和依赖。
- 输出同化报告：播放器状态机、控件、字幕、全屏、弹幕、主题样式、事件模型、安全与依赖风险。
- 判断哪些能力应沉淀到组件库媒体控件、视频播放器应用、视频基础卡片或未来 Host/模块媒体能力。
- 对照主题 token、i18n、a11y 和键盘标准列出差距；不得把 DPlayer 原样塞入生态绕开正式链路。
- 如果需要引入或保留 DPlayer 依赖，先形成依赖决策文档并等待用户确认。
- 完成后写任务053工作日志；必要勘察报告写 08-草稿笔记。
- 运行与改动范围匹配的验证命令，并记录结果或限制。

最终回复要求：
- 明确任务053是否完成。
- 列出修改文件路径。
- 列出验证命令和结果。
- 列出任何新发现工单或风险。
- 再次声明未执行 Git 写操作。
```

## 子代理 D：任务054 开放项目 Thorium 长期同化评估

派发说明：

```text
你是薯片生态重构任务的子代理之一，使用 fork 工作区独立完成一个完整任务。重要规则同任务052：绝对不要执行任何 Git 写操作；只处理任务范围；先读 AGENTS、设计原稿、公共技术文档；公共契约写生态共用技术文档，过程日志写 07-工作日志，勘察草稿写 08-草稿笔记；不做临时版或旧架构绕行。

你的任务：完整处理 `任务054-开放项目Thorium长期同化评估`。

工作入口：
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务054-开放项目Thorium长期同化评估.md`
- 相关设计原稿：生态概述、极致模块化架构、插件系统架构、SDK 与协议、阅读器/图书/文件格式相关设计。
- 相关公共文档：应用插件开发、基础卡片插件开发、模块能力契约、文件格式规范、主题系统、多语言、a11y、打包发布规范。
- 相关项目：`OpenSourceProjectHandlingFolder/thorium-reader-develop`、`OpenSourceProjectHandlingFolder/thorium-reader-repowiki`、`Chips-BookReader`、`Chips-BaseCardPlugin/book-BCP`、`Chips-ModulePlugin/*`。

目标：
- 核对 Thorium README、架构、包结构、构建脚本、许可证、阅读格式和外部资源。
- 输出大型项目同化评估报告：进程模型、状态管理、阅读核心、格式解析、无障碍、国际化、主题、依赖、安全与性能风险。
- 清楚区分短期可吸收能力、长期生态能力、仅作竞品参考的内容。
- 对 EPUB/PDF/DOCX/漫画图片等格式建立能力缺口清单，并判断落点应是 Host、模块插件、阅读器应用还是基础卡片。
- 不强行迁移完整 Thorium；不引入未决依赖；必要时登记或建议登记阻断工单。
- 完成后写任务054工作日志；详细评估报告写 08-草稿笔记。
- 运行可行的验证命令；如依赖体量或环境限制导致无法完成构建/测试，记录证据和风险。

最终回复要求：
- 明确任务054是否完成。
- 列出修改文件路径。
- 列出验证命令和结果。
- 列出任何新发现工单或风险。
- 再次声明未执行 Git 写操作。
```

## 主代理后续收口顺序

1. 先接收并复核任务051 至 054 子代理结果。
2. 主代理统一跑必要验证、整合日志与公共文档，按任务粒度提交。
3. 确认 051 至 054 均完成后，再进入 `任务055` 至 `任务058` 串行验收段。
4. 如果串行段等待期间存在空闲能力，再处理 `任务059.xx` 技能资产建设。
