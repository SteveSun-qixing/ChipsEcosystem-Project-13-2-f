# 任务025.05 箱子布局脚手架文档与生态契约同步启动前复核报告

日期：2026-05-24

角色：task022 前置勘察子代理，提前为 `任务025.05-箱子布局脚手架真实 CLI e2e 与公共文档收口` 做只读启动前复核。

复核性质：只读勘察；除本报告外未修改代码、模板、公共文档或任务文档。未提交 Git，未创建/切换分支。复核基于当前 HEAD `f2b0441a` 以及当前工作区文件；当前仅发现 `Chips-Scaffold/chips-scaffold-basecard/node_modules/@types/node/*` 存在无关未提交变更，本次未触碰。

## 1. 025.05 的准确目标边界

### 应同步和验收的内容

025.05 是任务025的最终收口任务，应在 025.04 完成后再启动。准确目标是把已经稳定的箱子布局脚手架 vNext 产物同步到真实 CLI e2e、生成工程 README 与生态公共文档：

1. `Chips-Scaffold/chips-scaffold-boxlayout/scripts/run-generated-e2e.cjs`
   - 从当前“直接调用 `renderTemplateToTarget`”升级为在临时生态根中真实执行 `chipsdev create layout <targetDir>`。
   - 生成工程后执行 `npm install` 与 `npm run verify`，让生成工程默认 `lint/typecheck/test/build/validate/package` 全链路通过。
   - 断言生成工程无 `template.json`、无旧 `cardId/cardInfo/cardFile/card-window` 字段、`manifest.entry` 指向 `dist/index.mjs`、`dist/` 中唯一 `.cpk` 输出、`layoutDefinition.layoutType` 与 `manifest.layout.layoutType` 对齐。

2. `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/README.md.tpl`
   - 说明 `layoutDefinition`、`manifest.layout.layoutType`、生成工程脚本、Host/SDK 正式运行时、查看态 Runtime、编辑器 asset bridge、`props + assetRefs` 运行时对象和 `.box/content.yaml` 存储边界。
   - 明确箱子自有资源只保存 `assets/` 包内相对路径，不持久化绝对路径、`file://`、`blob:`、`data:`、Host 临时资源 URL、凭证或会话 ID。

3. `生态共用技术文档/插件开发/04-布局插件开发.md`
   - 同步脚手架生成工程目录、默认脚本、正式 CLI、layout manifest、查看态 `initialView + listEntries + renderEntryCover + openEntry + readBoxAsset/prefetchEntries`、编辑态 `readBoxAsset/importBoxAsset/deleteBoxAsset`、配置归一校验和 cleanup 的稳定口径。
   - 明确 `.box/content.yaml` 使用 `schema_version/asset_refs`，插件运行时对象由 Host/SDK 注入为 `schemaVersion/assetRefs`，插件不直接写 YAML。

4. `生态共用技术文档/命令行工具/02-Chips-Dev开发者命令行手册.md`
   - 核对并同步 `chipsdev create layout <targetDir>`、生成工程验证脚本、布局插件没有独立 `chipsdev run`、正式联调需 `chipsdev package -> chipsdev plugin install -> plugin enable -> 由箱子查看器/编辑器打开 .box`。

5. 必要时只读核对 `Chips-SDK/tests/run-cli-create-layout-workspace-tests.cjs`
   - 它已经覆盖 SDK 侧真实 `node cli/index.js create layout`、工作区依赖适配、`lint/build/test/validate/package`。
   - 025.05 主要应补脚手架包自己的 e2e，不应把 SDK 测试当作脚手架 e2e 的替代。

### 不属于 025.05 的内容

1. 不继续重写查看态主流程；`initialView`、分页、封面、打开动作、读取箱子资源和 cleanup 属于 025.03，025.05 只复核并记录稳定口径。
2. 不继续实现编辑器 UI 和资产桥生命周期；`read/import/deleteBoxAsset` 的实际消费、路径安全、资源替换/清空和编辑器测试属于 025.04，025.05 只在 025.04 完成后验收并写入稳定文档。
3. 不迁移 `grid-BLP/list-BLP` 真实业务插件本体；后续按任务044/045处理。
4. 不改 Host、SDK、组件库或主题系统来迁就脚手架；若 025.04 后发现正式链路仍缺能力，应在后续正式工单处理，025.05 不写临时绕路。
5. 不把任务过程、排查记录、短期分支策略写进生态共用技术文档；这些只留在 `项目日志与笔记/`。
6. 不使用旧卡片技能、旧卡片字段或旧 `card-window/cardId/cardInfo/cardFile` 口径作为布局插件正式依据。

## 2. 一致性风险清单

### P0：025.05 启动前必须先确认 025.04 已收口

1. 当前勘察时模板编辑器仍未实际消费资产桥。
   - 证据：`src/editor/panel.tsx.tpl` 的 props 声明包含 `readBoxAsset/importBoxAsset/deleteBoxAsset`，但组件只解构 `entries/config/locale/onChange`；UI 只包含排序 select、硬编码 `Preview` 和 `{entries.length} entries`。
   - 影响：若 025.04 未完成，025.05 不能把“编辑器资产桥已完成”写入 README 或公共文档，也不能把当前模板作为正式 vNext 产物验收。
   - 025.05 处理：启动时先读 025.04 工作日志和 diff，确认编辑器 asset bridge 行为、i18n、组件库控件、cleanup 和测试已经落地；否则应退回 025.04 范围。

2. 当前 JSON schema 与 TypeScript schema 对资源路径的约束不一致且偏宽。
   - 证据：`contracts/layout-config.schema.json.tpl` 的 `assetPath` 仅为 `^assets/.+`，`assetRefs.items` 只是 string；当前 `layout-config.ts.tpl` 的 `isSafeBoxAssetPath` 禁止开头 scheme、反斜杠和 `.`/`..` segment，但勘察时仍未覆盖查询串、片段、后续 segment 中的 `:` 等更保守规则。
   - 影响：公共文档若写“生成工程已阻断临时 URL/绝对路径/逃逸路径”，必须先由 025.04 代码和测试证明。
   - 025.05 处理：复核 `schema.test.ts.tpl` 是否覆盖 `assets/a.png?x=1`、`assets/a.png#hash`、`assets/file://a.png`、`assets/../a.png`、`assets\\a.png`、`file:///tmp/a.png`、`blob:`、`data:` 等场景。

3. 当前脚手架 `test:e2e` 还没有走真实 `chipsdev create layout`。
   - 证据：`scripts/run-generated-e2e.cjs` 直接 `require("../dist/src/core/template-engine")` 并调用 `renderTemplateToTarget(options)`。
   - 影响：没有覆盖 SDK CLI 的 `ensurePackageBuildArtifact`、`create layout` 参数推导、`adaptWorkspaceDependencies`、工作区依赖和生成工程 `verify/package` 真实链路。
   - 025.05 处理：这是 025.05 的核心改造点。

4. 025.05 公共文档收口必须建立在 025.04 后的真实代码上。
   - 证据：025.05 任务文档明确“在箱子布局脚手架模板完成 vNext 升级后”再补真实 CLI e2e 和公共文档。
   - 影响：如果 025.04 尚未提交或测试未通过，025.05 文档会把未落地行为固化成公共契约。
   - 025.05 处理：启动时先查 `07-工作日志/任务025.04-*.md`、`git show` 或当前 diff，确认 025.04 产物。

### P1：025.05 需要主动修正文档/README 漂移

1. 生成工程 README 当前缺少 vNext 关键边界。
   - 证据：`README.md.tpl` 只说明目录、导出、`EmbeddedDocumentFrame` 和常用命令，尚未说明 `initialView`、分页、`renderEntryCover/openEntry`、编辑器 asset bridge、`props + assetRefs`、`.box` snake_case 与运行时 camelCase 转换。
   - 影响：开发者按 README 无法理解生成模板与 Host/SDK/`.box` 的完整契约。
   - 建议：025.05 在 README 中补“运行时能力”“编辑器资源桥”“配置与资源边界”“验证与打包”四个短小但完整的小节。

2. 公共文件格式规范示例仍保留较旧的网格字段。
   - 证据：`生态共用技术文档/文件格式规范/02-箱子文件格式规范.md` 的 `content.yaml` 示例含 `column_count/gap/cover_ratio/show_tags/information_density`；而 025.02 已明确模板不再把固定列数作为默认布局配置。
   - 影响：025.05 若只改布局插件开发文档，文件格式规范仍可能让新模板和公共示例出现口径冲突。
   - 建议：虽然 025.05 任务范围未显式列出该文件，但用户要求本轮复核该规范。主代理应评估是否把示例改成中性的 `sort_mode/background/top_region/asset_refs`，或至少在报告中记录需后续同步；不要把旧字段写成所有布局的默认标准。

3. `生态共用技术文档/插件开发/04-布局插件开发.md` 需要补足脚手架 vNext 的最终状态。
   - 证据：该文档已有 `chipsdev create layout <targetDir>`、`renderEntryCover`、asset bridge、`props/asset_refs` 等口径，但没有明确生成工程 `verify` 脚本、真实 CLI e2e 验收、`.cpk` 唯一输出断言，也没有把 025.03/025.04 模板行为整理成“默认生成工程会包含”的描述。
   - 影响：公共开发者文档与脚手架模板仍会靠近但不完全一致。

4. CLI 手册应避免让布局插件看起来可以独立 `chipsdev run`。
   - 证据：CLI 手册已写“布局插件没有独立的 `chipsdev run` 窗口入口”，并给出 `create/build/test/lint/validate/package/plugin install/plugin enable` 链路。
   - 影响：025.05 更新时应保留该口径，不要因为生成工程有 `dev: chipsdev server` 就误写成正式 Host 运行入口。

5. 包级 README 缺失需明确边界。
   - 证据：当前 `Chips-Scaffold/chips-scaffold-boxlayout` 未发现包根 `README.md`，只有模板 `README.md.tpl` 与内部 `需求文档/技术文档/开发计划`。
   - 影响：用户提到“README、template README”；025.05 任务文档只列模板 README。若主代理认为脚手架包也需要对外 README，应先确认是否属于 025.05 扩围；否则只更新模板 README 与公共文档。

### P2：建议随 025.05 增强的质量门禁

1. `scripts/check-templates.cjs` 目前能阻断旧字段、旧 CLI、模板身份泄露、缺脚本和查看态关键能力，但还不能断言真实 e2e 生成工程的 `.cpk` 唯一性、manifest entry 产物存在、README 是否包含 asset bridge 与 camelCase/snake_case 边界。
2. `tests/core/template-engine.test.ts` 覆盖直接模板渲染，不覆盖 SDK CLI 路径；025.05 不一定要重写它，但 e2e 应覆盖真实 CLI 路径。
3. SDK 侧 `run-cli-create-layout-workspace-tests.cjs` 已有真实 CLI 工作区集成测试，脚手架 e2e 可以参考其临时 workspace、依赖复制/软链、npm cache 隔离和 `node Chips-SDK/cli/index.js create layout ...` 模式；但脚手架包 e2e 应追加 `npm run verify` 和 `.cpk`/manifest/layoutDefinition 断言。

## 3. 025.04 完成后，025.05 应如何复核 025.04 产物

025.05 启动后的第一步应先复核 025.04，而不是直接改文档。

建议复核顺序：

1. 阅读 025.04 工作日志和实际 diff。
   - 期望存在 `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志/任务025.04-*.md`。
   - 核对是否只改了 `src/editor`、`src/schema`、`contracts`、`i18n`、`tests`、必要模板检查脚本等范围。

2. 核对编辑器资产桥是否真实消费。
   - `panel.tsx.tpl` 或新增 editor 子组件中应实际调用 `importBoxAsset(`、`readBoxAsset(`、`deleteBoxAsset(`。
   - 上传/替换/清空资源后应输出完整 normalized config，且 `assetRefs` 与当前 `props.background/topRegion` 同步。
   - 资源预览只消费 `readBoxAsset` 返回的 `resourceUrl`，不得写回配置。

3. 核对路径安全和 schema 一致性。
   - `layout-config.ts.tpl` 与 `contracts/layout-config.schema.json.tpl` 对 asset path 的禁止项应一致或 JSON schema 不宽于 TS 校验。
   - `validateLayoutConfig` 应校验 `assetRefs`，不能只校验 props 中的 image 字段。

4. 核对 i18n 与组件库。
   - 新增可见文案必须进入 `i18n/zh-CN.json.tpl` 和 `i18n/en-US.json.tpl`，key 集合一致。
   - 编辑器 UI 应使用组件库正式控件或主题 token，不回退到硬编码英文文案与私有视觉样板。

5. 核对编辑器 cleanup。
   - `renderEditor` 返回 cleanup，unmount 后容器清空；如果 025.04 增加 style snapshot/restore 或异步预览，请确认 cleanup 后未决 Promise 不写 DOM。

建议检查命令：

```bash
cd Chips-Scaffold/chips-scaffold-boxlayout
npm run build
npm test
npm run test:templates
npm run test:e2e
```

025.05 完成真实 CLI e2e 后，`npm run test:e2e` 还应包含：

```bash
node /临时生态根/Chips-SDK/cli/index.js create layout validation-projects/layout-smoke
cd /临时生态根/validation-projects/layout-smoke
npm install
npm run verify
```

可选同步验证：

```bash
cd Chips-SDK
node ./tests/run-cli-create-layout-workspace-tests.cjs
```

025.05 需要对齐的公共文档条目：

1. `插件开发/04-布局插件开发.md`
   - `chipsdev create layout <targetDir>` 是正式初始化入口。
   - 生成工程默认包含 `manifest.yaml/chips.config.mjs/src/view/src/editor/src/schema/src/shared/contracts/i18n/tests`。
   - 生成工程默认脚本为 `lint/typecheck/test/build/validate/package/verify`。
   - `layoutDefinition.layoutType`、`manifest.layout.layoutType`、`.box metadata.active_layout_type`、`.box content.layout_configs` key 必须一致。
   - 查看态默认先用 `initialView`，再通过 `runtime.listEntries` 分页；封面优先 `renderEntryCover`；打开只用 `openEntry`；箱子资源只用 `readBoxAsset`。
   - 编辑态只通过 `onChange` 交完整配置快照，只通过 asset bridge 管理箱子自有资源，不直接写文件。
   - `.box` 存储为 `schema_version/asset_refs`，插件运行时对象为 `schemaVersion/assetRefs`。

2. `命令行工具/02-Chips-Dev开发者命令行手册.md`
   - `chipsdev create layout <targetDir>` 后，在布局工程内运行 `npm run verify` 或 `chipsdev build/test/lint/validate/package`。
   - 布局插件没有独立 `chipsdev run`；正式联调必须打包安装到开发工作区，再由箱子查看/编辑正式应用消费。
   - `chipsdev package` 输出 `.cpk`，`chipsdev plugin install` 替换同 ID 旧副本，`plugin enable` 后才参与 Host 布局加载。

3. `文件格式规范/02-箱子文件格式规范.md`（建议评估纳入）
   - 示例配置宜与 vNext 中性模型对齐：`sort_mode/background/top_region/asset_refs`。
   - 如果保留历史网格示例，应明确它只是某布局自定义字段示例，不是标准模板默认字段。

## 4. 025.05 不可做事项

1. 不写临时方案、简化方案、占位实现或 TODO。
2. 不引入兼容性设计；当前第一版正式发布前，不为旧 `card*` 字段、旧箱子类型或旧导入方式保留兼容写法。
3. 不把过程日志、启动前复核、e2e 排查记录、短期任务状态写入生态共用技术文档。
4. 不把旧卡片技能或基础卡片脚手架口径作为箱子布局插件的正式依据；布局插件以箱子设计原稿、箱子运行时契约、布局插件开发文档、Host/SDK/脚手架当前代码与测试为准。
5. 不迁移真实 `grid-BLP/list-BLP` 本体，不把其业务字段、视觉偏好或历史安全口径照搬为标准模板。
6. 不把 `file://`、`blob:`、`data:`、Host 临时 URL、绝对路径、凭证、sessionId 持久化进布局配置或 README 示例。
7. 不在公共文档中承诺 025.04 未经测试证明的行为。
8. 不自行创建、切换、合并或归档分支；不提交代码。

## 5. 实际阅读过的文件路径清单

### 规则与技能

- `AGENTS.md`
- `项目日志与笔记/AGENTS.md`
- `生态设计原稿/AGENTS.md`
- `生态共用技术文档/AGENTS.md`
- `Chips-Scaffold/AGENTS.md`
- `Chips-SDK/AGENTS.md`
- `/Users/sevenstars/.codex/skills/chips-boxlayout-plugin-dev/SKILL.md`
- `/Users/sevenstars/.codex/skills/chips-boxlayout-plugin-dev/references/official-contracts.md`
- `/Users/sevenstars/.codex/skills/chips-boxlayout-plugin-dev/references/scaffold-workflow.md`
- `/Users/sevenstars/.codex/skills/chips-boxlayout-plugin-dev/references/box-file-and-assets.md`

### 任务与日志

- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025-箱子布局脚手架vNext升级.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025.01-箱子布局脚手架契约核对与任务拆分.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025.02-箱子布局脚手架契约与质量门禁升级.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025.03-箱子布局查看态Runtime样板升级.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025.04-箱子布局编辑器与资产桥样板升级.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/06-工单级开发任务/任务025.05-箱子布局脚手架真实CLIe2e与公共文档收口.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志/任务025.01-箱子布局脚手架契约核对与任务拆分-20260524.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志/任务025.02-箱子布局脚手架契约与质量门禁升级-20260524.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/07-工作日志/任务025.03-箱子布局查看态Runtime样板升级-20260524.md`
- `项目日志与笔记/task022-优化薯片组件库和主题系统/08-草稿笔记/任务025.04-箱子布局编辑器与资产桥样板升级-启动前复核报告-20260524.md`

### 设计原稿与公共文档

- `生态设计原稿/06-箱子文件格式.md`
- `生态设计原稿/10-布局插件结构.md`
- `生态共用技术文档/插件开发/04-布局插件开发.md`
- `生态共用技术文档/协议与契约/08-箱子布局运行时契约.md`
- `生态共用技术文档/文件格式规范/02-箱子文件格式规范.md`
- `生态共用技术文档/命令行工具/02-Chips-Dev开发者命令行手册.md`

### 脚手架与 SDK/Host 代码

- `Chips-Scaffold/chips-scaffold-boxlayout/package.json`
- `Chips-Scaffold/chips-scaffold-boxlayout/scripts/check-templates.cjs`
- `Chips-Scaffold/chips-scaffold-boxlayout/scripts/run-generated-e2e.cjs`
- `Chips-Scaffold/chips-scaffold-boxlayout/src/cli/boxlayout-scaffold-api.ts`
- `Chips-Scaffold/chips-scaffold-boxlayout/src/core/template-engine.ts`
- `Chips-Scaffold/chips-scaffold-boxlayout/src/core/types.ts`
- `Chips-Scaffold/chips-scaffold-boxlayout/tests/core/template-engine.test.ts`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/template.json`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/README.md.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/manifest.yaml.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/package.json.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/chips.config.mjs.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/tsconfig.json.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/vitest.config.mts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/contracts/layout-config.schema.json.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/index.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/schema/layout-config.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/editor/panel.tsx.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/editor/runtime.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/view/page.tsx.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/view/runtime.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/shared/types.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/src/shared/i18n.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/i18n/zh-CN.json.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/i18n/en-US.json.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/tests/unit/schema.test.ts.tpl`
- `Chips-Scaffold/chips-scaffold-boxlayout/templates/boxlayout-standard/tests/unit/layout-definition.test.tsx.tpl`
- `Chips-SDK/cli/index.js`
- `Chips-SDK/tests/run-cli-create-layout-workspace-tests.cjs`
- `Chips-SDK/src/api/box.ts`
- `Chips-Host/packages/box-service/src/box-service.ts`
