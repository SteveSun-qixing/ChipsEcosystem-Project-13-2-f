# 任务055.08：安装打包发布链路最终闭环报告

## 结论

任务055已经完成自动化安装打包发布链路闭环。开发态、用户态、发布态、安装副本等价态、五类插件、主题包、模块能力、`.cpk`、`.card`、`.box`、Host 安装器和 SDK CLI 都已有当前证据支撑。

当前仍不是对外正式发布结论。真实系统安装、LaunchServices、启动台、GUI 保存导出、签名、公证和卸载清理仍需在正式发布前人工闭环；按用户要求，本轮暂不登记新工单、不处理该项。

## 已完成的发布链路基线

| 范围 | 结论 |
| --- | --- |
| 工作区边界 | 开发工作区和用户工作区隔离，`.chips-host-dev` 未作为终端用户运行目录 |
| 五类插件 | app/card/layout/module/theme 均有正式构建、校验、打包、安装启用验收入口 |
| 主题包 | 默认主题可安装、启用、应用、校验，`cpx` 浏览器注入口径已收口 |
| 应用插件 | 卡片查看器、生态设置面板、图片查看器、编辑引擎完成安装启用和入口抽样 |
| 模块插件 | 能力发现、调用、job、取消、provider 生命周期和 packaged e2e 已覆盖 |
| `.card` | 真实富文本基础卡片通过 Host 服务路由完成校验、渲染、编辑器渲染、解包、重打包 |
| `.box` | 真实美食网格箱子通过 Host 服务路由完成检查、布局渲染、布局编辑器、条目封面、解包、重打包 |
| 安装器 | 真实 `Chips.app` 与 `.pkg` 可构建，payload 洁净，内置插件可引导 |
| SDK | 全量测试通过，覆盖 CLI smoke、脚手架、模块 invoke、package compatibility 与 tooling |

## 已解决工单

| 工单 | 结果 |
| --- | --- |
| 工单110 | macOS `.pkg` payload AppleDouble 元数据污染已修复 |
| 工单112 | 主题 `cpx` 进入浏览器普通 CSS 前转换为 `vw`，应用窗口/卡片/箱子链路统一 |
| 工单113 | command shortcut 从系统全局快捷键拆出，改为插件窗口 surface 作用域派发 |

## 发布前人工验收项

以下事项作为正式发布前人工验收项记录，本轮暂不处理：

1. Apple Developer 签名与 notarization。
2. 真实安装到 `/Applications/Chips.app`。
3. LaunchServices 注册与系统双击 `.card`。
4. `.box` 是否进入系统级 document type 的发布决策与验收。
5. 启动台、快捷方式和应用插件启动。
6. 真实 GUI 中 `.card/.box` 编辑、保存、重开和导出。
7. 卸载清理和 LaunchServices 回收。

## 最终验证

```bash
npm run verify:workspace
```

通过。

```bash
cd Chips-Host
npm run build
npm test -- --run tests/unit/macos-installer.test.ts tests/unit/app-entry.test.ts tests/unit/theme-css-units.test.ts tests/unit/card-service-rendering.test.ts tests/unit/box-service.test.ts tests/unit/host-services-pal-routing.test.ts tests/unit/command-shortcuts.test.ts tests/integration/host-services.test.ts
```

通过。Host 关键测试 8 个文件、93 条测试通过。

```bash
cd Chips-SDK
npm test
```

通过。SDK 全量测试通过。

## 后续衔接

任务056可以基于任务055形成的自动化发布链路基线继续推进成品测试空间真实素材回归。对外正式发布前，不得把上述人工验收项误写为已完成。
