# 任务056.02：卡片箱子真实服务链路回归结果

生成时间：2026-05-26T00:00:00+08:00

## 摘要

- 回归输入：任务056.01冻结的 `ProductFinishedProductTestingSpace` 正式 `.card/.box` 素材。
- 自动化覆盖：21 个真实 `.card`、1 个真实 `.box`。
- 归档素材：继续排除 `ProductFinishedProductTestingSpace/归档`。
- 阻断项：本阶段未发现新的 P0/P1 真实素材阻断。

## Host 服务链路

### 全量真实卡片

每个真实 `.card` 均执行：

1. `card.validate`
2. `card.readInfo(status/metadata/cover)`
3. `card.renderCover`
4. `card.render`，启用 `verifyConsistency`
5. `card.releaseRenderSession`

断言重点：

- 状态为 `ready`，文件存在且有效。
- `cardId` 是 10 位 Base62。
- `coverRatio` 符合 `宽:高` 格式。
- 封面 MIME 为 `text/html`。
- 渲染结果返回 Host 托管文档 URL。
- 质量门禁通过，无 diagnostics。
- 复合卡片文档包含 `chips-composite`。

### 真实箱子

`美食网格箱子.box` 使用临时派生副本执行，避免直接改写真实素材。

覆盖动作：

1. `box.validate`
2. `box.renderCover`
3. `box.inspect`
4. `box.normalizeLayoutConfig`
5. `box.getLayoutInitialQuery`
6. `box.openView`
7. `box.listEntries`
8. `box.readEntryDetail`
9. `box.renderEntryCover`
10. `box.renderLayoutFrame`
11. `box.renderLayoutEditor`
12. `box.openEntry`
13. `box.unpack`
14. `box.pack`
15. 重新 `box.validate`
16. 释放布局渲染会话并关闭箱子查看会话

断言重点：

- 箱子名称为 `美食网格箱子`。
- 当前布局为 `chips.layout.grid`。
- 条目数量为 15。
- 条目 URL 在临时派生副本中指向当前 worktree 的真实成品测试空间。
- 条目摘要 `contentType` 为 `chips/card`。
- 条目打开返回 `document-window`、`documentType=card`、`pluginId=com.chips.card-viewer`。

## CardViewer 入口链路

- 新增 `allRealCardDocuments`、`allRealBoxDocuments`、`allRealDocumentPaths` 和 `expectAllRealDocumentsAvailable`。
- App 单元测试逐个设置 `launchParams.targetPath`，确认 CardViewer 对 `.card/.box` 统一调用：

```ts
document.window.render({
  filePath,
  locale: "zh-CN",
  mode: "view",
});
```

## 后续输入

- `056.03` 使用同一真实素材矩阵继续验证图片、音频、视频、EPUB、图片 ZIP、网页 ZIP 和网页目录。
- `056.04` 继续验证模块转换、编辑保存、导出和重新消费。
- `056.05` 汇总默认主题、暗色主题、多语言抽样、失败项和阻断项。
