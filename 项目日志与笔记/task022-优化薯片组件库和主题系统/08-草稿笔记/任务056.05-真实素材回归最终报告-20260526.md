# 任务056.05：真实素材回归最终报告

日期：2026-05-26

## 1. 总结论

任务056已经完成真实素材自动化回归主链路收口。当前可作为任务057全生态最终质量门禁的输入证据。

自动化范围内未发现新的 P0/P1 真实素材阻断。需要继续保留的边界是：`Chips-EditingEngine` 编辑保存、导出、重新打开端到端链路未在任务056中提交完成，因为该项目存在既有 WIP，本轮未触碰相关脏改动。

用户已明确任务059不再继续执行，因此任务059不作为任务056最终报告或任务057的前置条件。

## 2. 真实素材矩阵

`056.01` 已冻结 `ProductFinishedProductTestingSpace` 当前真实素材矩阵：

| 指标 | 数量 |
| --- | ---: |
| 文件总数 | 76 |
| 纳入任务056正式回归 | 68 |
| 归档历史素材 | 6 |
| 真实 `.card` | 21 |
| 真实 `.box` | 1 |

正式回归素材覆盖：

- 卡片：富文本、图片、音乐、视频、图书、网页、评分、超链接和组合卡片相关真实 `.card`。
- 箱子：`美食网格箱子.box`，布局为 `chips.layout.grid`，条目数 15。
- 媒体资源：图片、音频、视频、EPUB、图片 ZIP、网页 ZIP、网页目录和美食源图素材。
- 模块输入：真实 `.card`、真实网页目录派生 HTML 中间产物、真实 JPEG、真实非支持矩阵输入。

## 3. 分阶段结果

| 子任务 | 范围 | 结果 |
| --- | --- | --- |
| `056.01` | 真实素材清单矩阵与回归基线 | 通过 |
| `056.02` | 卡片箱子真实服务链路回归 | 通过 |
| `056.03` | 媒体应用真实资源打开回归 | 通过 |
| `056.04` | 模块转换编辑保存导出真实回归 | 模块插件侧通过，编辑引擎端到端未纳入完成项 |
| `056.05` | 主题语言抽样与最终报告 | 完成 |

## 4. 卡片与箱子服务链路

`056.02` 已覆盖 21 个真实 `.card`：

- `card.validate`
- `card.readInfo(status/metadata/cover)`
- `card.renderCover`
- `card.render(verifyConsistency=true)`
- `card.releaseRenderSession`

`056.02` 已覆盖 1 个真实 `.box` 的当前 worktree 临时派生副本：

- `box.validate`
- `box.renderCover`
- `box.inspect`
- `box.normalizeLayoutConfig`
- `box.getLayoutInitialQuery`
- `box.openView`
- `box.listEntries`
- `box.readEntryDetail`
- `box.renderEntryCover`
- `box.renderLayoutFrame`
- `box.renderLayoutEditor`
- `box.openEntry`
- `box.unpack`
- `box.pack`
- 重打包后重新 `box.validate`
- 布局渲染会话与箱子查看会话释放

CardViewer 已逐个消费 22 个真实 `.card/.box` 路径，并统一调用 Host 文档窗口入口：

```ts
document.window.render({
  filePath,
  locale: "zh-CN",
  mode: "view",
});
```

## 5. 媒体资源打开链路

| 素材 | 项目 | 结果 |
| --- | --- | --- |
| `图片.jpg` | `Chips-PhotoViewer` | 通过 |
| `测试音频.mp3` | `Chips-MusicPlayer` | 通过 |
| `测试音频-专辑封面.png` | `Chips-MusicPlayer` | 通过 |
| `测试视频.mp4` | `Chips-VideoPlayer` | 通过 |
| `电子书.epub` | `Chips-BookReader` | 通过 |
| `电子书图片.zip` / `电子书图片/` | `Chips-PhotoViewer` | 通过 |
| `昙花网页.zip` / `昙花/` | `webpage-BCP` | 通过 |

关键修复与验证：

- PhotoViewer 按 `resourceOpen.filePath -> targetPath -> resourceOpen.resourceId` 优先级恢复真实资源。
- VideoPlayer 消费 `chips.video-card` payload 中的 `autoplay/loop/muted/playbackRate/startTime`。
- BookReader 覆盖真实 EPUB 加载、目录和章节渲染。
- 网页包通过网页基础卡片正式链路验证 `file.read` 与 `openResource`。

## 6. 模块转换链路

| 能力 | 真实素材 | 结果 |
| --- | --- | --- |
| CardToHTML | `富文本基础卡片.card` | 通过 |
| HtmlToImage | `昙花/index.html` 派生正式 HTML 中间产物 | 通过 |
| HtmlToPDF | `昙花/index.html` 派生正式 HTML 中间产物 | 通过 |
| FileConversion | `富文本基础卡片.card -> image` | 通过 |
| FileConversion | 图片、音频、视频、网页 ZIP 直接转换结构化拒绝 | 通过 |
| ColorPicker | `图片.jpg` | 通过 |

已修复问题：

- HtmlToPDF 读取 CardToHTML manifest 时优先消费 `requestedThemeId/requestedLocale`，避免真实 CardToHTML -> PDF 链路丢失主题和语言诊断。

边界：

- HtmlToImage/HtmlToPDF 的正式输入是 CardToHTML 目录态中间产物，不是裸网页目录。
- FileConversion 首版公开矩阵不包含图片、音频、视频、网页 ZIP 直接转换；当前结构化拒绝属于契约内行为。
- `Chips-EditingEngine` 端到端保存、导出、重新打开仍需在既有 WIP 收口后继续验证。

## 7. 主题抽样

默认主题抽样：

- `npm run verify:workspace` 通过。
- Host 构建与服务测试通过。
- CardViewer、BookReader、VideoPlayer、MusicPlayer 专项测试通过。

暗色主题抽样：

- CardViewer 覆盖 `theme.changed` 事件后，用同一真实文档路径和当前语言重新创建内容 iframe 渲染会话。
- VideoPlayer 覆盖从 Host 注入的 `data-chips-theme-id=chips-official.dark-theme` 与 `data-chips-theme-version=2.0.0` 恢复主题状态。
- CardViewer 主题/a11y 测试确认文档流表面使用 `--chips-sys-color-surface` token，不把窗口背景写死为白色。

主题抽样结论：

- 默认主题和暗色主题均有代码级抽样证据。
- 抽样范围未发现新的主题链路 P0/P1 阻断。

## 8. 多语言抽样

CardViewer：

- 覆盖 `language.changed` 后更新 `document.documentElement.lang`、`dir`、`data-chips-locale`。
- 覆盖托管文档 iframe 标题从中文切换到英文。
- 覆盖组件库同步 i18n adapter、本地语言包 fallback 和缺失 key 回退。
- 真实 `.card/.box` 启动渲染携带 `locale: "zh-CN"`。

BookReader：

- 覆盖 `zh-CN` 与 `en-US` 本地语言包解析。
- 覆盖 `zh-CN` 为 `ltr`、`ar` 为 `rtl` 的方向解析。
- 覆盖命令标题、描述和 aria 文案 key 在中英文语言包中存在。

MusicPlayer / VideoPlayer：

- 命令元数据测试覆盖使用 i18n key 字段。
- VideoPlayer 媒体轨道测试覆盖字幕、音轨语言元数据读取。

多语言抽样结论：

- 核心应用的命令与关键窗口文案均有 i18n key 或同步 adapter 验证。
- 抽样范围未发现新的多语言 P0/P1 阻断。

## 9. 验证命令

```bash
npm run verify:workspace
cd Chips-Host && npm run build
cd Chips-Host && npm test -- --run tests/integration/host-services.test.ts
cd Chips-CardViewer && npm test -- --run tests/unit/app.test.tsx tests/unit/theme-a11y.test.ts tests/unit/commands.test.ts tests/unit/hosted-document-window.test.tsx tests/e2e/basic-flow.test.tsx
cd Chips-BookReader && npm test -- --run tests/unit/app-runtime.test.ts tests/unit/commands.test.ts tests/unit/reader-shell.test.tsx tests/unit/readium-css-manager.test.ts
cd Chips-VideoPlayer && npm test -- --run tests/unit/theme-runtime.test.ts tests/unit/commands.test.ts tests/unit/video-player-controller.test.ts
cd Chips-MusicPlayer && npm test -- --run tests/unit/commands.test.ts tests/unit/launch-resource.test.ts
```

全部通过。

模块侧沿用 `056.04` 已通过验证：

```bash
cd Chips-ModulePlugin/Chips-CardtoHTML-Plugin && npm test
cd Chips-ModulePlugin/Chips-HtmltoImage-Plugin && npm test
cd Chips-ModulePlugin/Chips-HtmltoPDF-Plugin && npm test
cd Chips-ModulePlugin/Chips-HtmltoPDF-Plugin && npm run build
cd Chips-ModulePlugin/Chips-FileConversion-Plugin && npm test
cd Chips-ModulePlugin/Chips-ColorPicker && npm test
```

## 10. 阻断与后续边界

无新增 P0/P1 自动化阻断。

保留边界：

- `Chips-EditingEngine` 编辑保存、导出、重新打开端到端链路未在任务056中完成。
- 用户已暂缓的发布前人工验收、签名和正式发布门禁不在本任务处理。
- 任务059按用户最新要求不再继续执行。

任务057建议：

- 以任务056报告中的通过命令作为快速质量门禁输入。
- 优先执行全工作区正式验证脚本，再按失败范围定向补跑项目专项测试。
- 避免重复跑已经稳定通过且与改动无关的大量测试，提升任务057执行效率和速度。
