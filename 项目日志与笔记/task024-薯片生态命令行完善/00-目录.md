# task024-薯片生态命令行完善

## 文档列表

- `01-生态命令行架构问题与目标.md`：记录当前生态命令行体系的问题、影响范围和需要达到的目标。
- `02-CLI交互式界面工具设计草案.md`：记录 `chips` / `chipsdev` 直接回车进入键盘交互界面的产品目标与交互模型。
- `03-生态命令行扩展架构设计方案.md`：记录应用插件与模块插件 CLI 命令声明、动态发现、执行链路、TUI、输出错误和实施阶段的架构方案。
- `04-阶段一Host命令索引实现记录.md`：记录 `manifest.cli.commands` 解析、Host 命令索引路由、SDK 封装、公共文档同步和验证结果。
- `05-阶段二CLI执行与打包校验记录.md`：记录传统 CLI 插件命令执行器、Host 固定命令根保护、`chipsdev validate/package` 类型专属字段门禁和验证结果。
- `06-阶段三脚手架CLI声明与安装门禁记录.md`：记录应用/模块插件脚手架默认 CLI 声明、命令根生成规则、Host 安装字段门禁补齐和验证结果。
- `07-阶段四TUI交互式命令构建器记录.md`：记录 Host CLI 无依赖 TUI 命令构建器、空命令入口、键盘控件、非 TTY 回退和验证结果。
- `08-阶段五CLI异步任务进度与取消记录.md`：记录模块插件 CLI job 等待、stderr 进度输出、Ctrl+C 取消和文档验证结果。
- `09-阶段六CLI输出路径覆盖与批量输入记录.md`：记录 `path.role: output`、覆盖策略、`--overwrite`、批量输入格式、SDK 打包校验和验证结果。
- `10-阶段七Shell补全动态发现记录.md`：记录 `chips completion`、`chips __complete`、动态命令索引补全、chipsdev 转发和保留命令根更新。
- `11-阶段八CLI-TUI操作日志记录.md`：记录 Host 持久化日志、CLI/TUI 操作记录、shell completion 记录和文档验证结果。
- `12-阶段九TUI系统指令合并记录.md`：记录 TUI 合并 Host 系统指令与插件命令、系统指令执行回流传统 CLI 和文档验证结果。
- `13-阶段十TUI交互可读性优化记录.md`：记录 TUI 步骤化布局、参数输入提示、当前选择说明和 iconMaker 输出目录声明修正。
- `14-阶段十一Host自动承载应用CLI记录.md`：记录应用 CLI 命令自动拉起 Electron Host 承载、IconMaker 输出目录递归写入和验证结果。
- `归档/11-阶段八TUI历史搜索记录.md`：原 TUI 历史搜索阶段记录，已按“不需要历史命令搜索”的口径归档，不作为当前实现依据。

## 边界说明

本目录保存命令行完善任务的过程性分析、问题梳理和阶段目标，不作为生态公共契约最终发布位置。后续形成稳定协议后，需要同步沉淀到 `生态共用技术文档/命令行工具/`、`生态共用技术文档/插件开发/` 或 `生态共用技术文档/协议与契约/` 的对应正式文档。
