# 8. 不迁移清单

以下旧工作区内容不直接迁入当前 worktree：

- 旧 CardViewer 单体根组件和旧 Provider 结构；
- 旧 CardViewer 文件组织本身，但其中的 `CardViewerSource`、来源解析、封面 surface、舞台选择和壳层动作职责需要重映射到当前新架构；
- 社区旧自研卡片查看页 UI；
- 旧 `runCardPipeline`、富文本内联、URL 替换、CDN 上传主链路；
- 旧 `card_pipeline_jobs` 作为最终主表；
- 旧 task023 目录内容直接复制到当前 `项目日志与笔记/task023-薯片生态高级感`；
- 任何仅为旧架构兼容而存在的 `targetPath/fileOpenMode/htmlUrl/communityHtmlUrl` 新增主口径。
