# 13. 最终验收审计

## 1. 审计时间

- 审计时间：2026-05-29
- 当前 worktree：`/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f-worktree-20260523-161048`
- 当前分支：`task022-优化薯片组件库和主题系统`

## 2. 完成标准逐项核对

| 完成标准 | 当前证据 | 结论 |
|---|---|---|
| 旧主工作区新增需求均在本 worktree 中有新架构实现或明确不迁移说明 | `02-已核对的输入来源/02-旧主工作区新增提交.md` 已覆盖 `2e7d28bf`、`bbf09032`、`8708ab3b`、`6529e78e`、`6cb0eab4`、`f9b5b837`、`6e6b20fb`、`d3c6d0b0`；实施记录 01-06 分别记录迁移、替代或不迁移结果 | 通过 |
| 旧主工作区提交中的架构调整均已融合、替代、归档不迁移或登记工单 | `12-主工作区架构调整融合矩阵.md` 覆盖 `CardViewerSource`、`document.window`、Host `cardSource`、CardViewer 壳层、社区 Web 宿主、源 `.card` 缓存、上传会话、CommunityUploader、BookReader 焦点修复；实施记录均有旧证据与新落点 | 通过 |
| CardViewer 本地与社区打开入口统一到结构化来源模型 | `Chips-SDK/src/types/card-viewer.ts` 定义 `CardViewerSource`；`Chips-CardViewer/src/app/AppRuntimeProvider.tsx` 优先解析 `launchParams.cardSource`；`Chips-Host/packages/card-open-service/src/card-open-service.ts` 和 `Chips-CommunityPlatformServer/packages/web/src/pages/DocumentPluginRoutePage.tsx` 注入结构化 `cardSource` | 通过 |
| `document.window` 统一事件面覆盖 ready、resize、error、node-error、resource-open 和 cleanup | `Chips-SDK/src/api/document.ts` 暴露 `render/onReady/onResize/onError/onResourceOpen/onNodeError`，订阅函数均返回 cleanup；`Chips-SDK/tests/document-window.test.ts` 覆盖 card/box resize 与事件面 | 通过 |
| CardViewer 支持封面/内容切换，并通过正式 Host/SDK 封面链路读取本地封面 | `Chips-CardViewer/src/components/ViewerCoverSurface.tsx`、`DocumentFileScene.tsx`、`HostedDocumentScene.tsx` 实现 cover/content 模式；本地卡片走 `client.card.readInfo(..., ["cover"])`，箱子封面走 `client.box.renderCover(...)`；`Chips-CardViewer/tests/unit/app.test.tsx` 覆盖切换 | 通过 |
| BookReader iframe 焦点下键盘翻页稳定 | `Chips-BookReader/src/interaction/interaction-manager.ts` 的 `attachToFrame` 挂载当前 `KeyboardHandler`；`Chips-BookReader/tests/unit/interaction-manager.test.ts` 与全量测试通过 | 通过 |
| 社区卡片打开页轻量化，列表热路径不读取大字段 | `CardService.getOpenViewAccessible` 只取打开必要列，不取 `cardMetadata/cardStructure`；`paginateCardSummaries` 使用数据库 limit/offset 和 summary columns；`cards.test.ts` 断言 open-view 不返回完整 JSONB | 通过 |
| 社区上传以永久源 `.card`、上传会话、资源直传、渲染缓存和 worker 为主链路 | `upload-session.service.ts`、`routes/upload.ts`、`card-render-cache.service.ts`、`worker.ts`、`storage/s3.ts`、`db/schema/upload-sessions.ts` 和 `db/schema/card-render.ts` 落地；`POST /upload-sessions`、资源 presign、源 `.card` 提交、view/cover 缓存入队均有测试 | 通过 |
| CommunityUploader 模块插件通过正式模块能力发布卡片 | `Chips-ModulePlugin/Chips-CommunityUploader-Plugin/manifest.yaml` 为 `type: module`，提供 `community.card.publish/publish`；`src/index.ts` 导出 `moduleDefinition`；`src/publisher.ts` 通过 `ctx.host.invoke` 使用 Host 能力并调用上传会话接口 | 通过 |
| 旧 pipeline 和旧查看页实现已按规则归档或脱离正式构建 | server 旧 pipeline 文件已移动到 `Chips-CommunityPlatformServer/packages/server/归档/20260529-旧上传流水线归档/`；CommunityUploader 旧 card-to-html 导出器已移到 `Chips-ModulePlugin/Chips-CommunityUploader-Plugin/归档/20260529-旧cardtohtml导出实现归档/`；正式源码搜索未命中 `runCardPipeline/CARD_PIPELINE/CARD_HTML/communityHtmlUrl/card_pipeline_jobs` | 通过 |
| 相关测试、构建、validate 和文档全部同步通过 | 见下方验证命令；公共契约已更新 `生态共用技术文档/协议与契约/09-社区平台API契约.md`，项目技术文档和实施记录已同步 | 通过 |

## 3. 最终验证命令

以下命令均已通过：

```bash
npm --prefix Chips-CommunityPlatformServer/packages/server test -- src/pipeline/card-unpack.test.ts src/routes/cards.test.ts src/routes/upload.test.ts src/services/card.service.test.ts src/services/upload-session.service.test.ts src/storage/s3.test.ts
npm --prefix Chips-CommunityPlatformServer/packages/server run build
npm --prefix Chips-CommunityPlatformServer/packages/web run build
npm --prefix Chips-ModulePlugin/Chips-CommunityUploader-Plugin test
npm --prefix Chips-ModulePlugin/Chips-CommunityUploader-Plugin run build
npm --prefix Chips-ModulePlugin/Chips-CommunityUploader-Plugin run validate
npm --prefix Chips-CardViewer test
npm --prefix Chips-CardViewer run build
npm --prefix Chips-CardViewer run validate
npx --prefix Chips-SDK vitest run Chips-SDK/tests/document-window.test.ts Chips-SDK/tests/route-manifest.test.ts
npx --prefix Chips-Host vitest run Chips-Host/tests/unit/card-open-service.test.ts
npm --prefix Chips-Host run build
npm --prefix Chips-BookReader test
npm --prefix Chips-BookReader run build
npm --prefix Chips-BookReader run validate
```

BookReader build 仍有 Vite chunk size warning，但 `build` 与 `validate` 均成功；该 warning 不影响本迁移完成标准。

## 4. 残留口径说明

- `htmlUrl` 仍作为数据库和详情/summary 历史兼容字段存在，但 `open-view` 不再返回该字段，社区详情页不再以它作为查看器启动入口。
- `targetPath` 与 `webDocumentUrl` 仅保留为 CardViewer 旧启动输入识别，不作为新主协议。
- `src/pipeline/card-unpack.ts` 继续保留，因为源 `.card` 检查、封面缓存和渲染缓存构建仍需要 `unpackCard`。
