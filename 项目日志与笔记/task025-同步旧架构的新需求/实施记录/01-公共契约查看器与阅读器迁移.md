# 公共契约、CardViewer 与 BookReader 迁移实施记录

## 1. 源码一比一对照

| 旧源码或旧行为 | 旧证据 | 新架构落点 | 迁移方式 | 验证 |
|---|---|---|---|---|
| `CardViewerSource` 结构化来源模型 | `develop:Chips-CardViewer/src/types/viewer-source.ts`、`develop:Chips-SDK/src/types/card-viewer.ts` | `Chips-SDK/src/types/card-viewer.ts`、`Chips-CardViewer/src/types/viewer-source.ts` | 融合迁移。SDK 暴露公共类型，CardViewer 本地解析保留比例校验和来源归一。 | `Chips-CardViewer/tests/unit/app.test.tsx` 覆盖 `parseCardViewerSource`。 |
| `document.window.onResize` 与 iframe 文档类型标记 | `develop:Chips-SDK/src/api/document.ts` | `Chips-SDK/src/api/document.ts` | 融合迁移。保留当前 `onNodeError`，补齐 `onResize` 和 `data-chips-document-type` 标记。 | `Chips-SDK/tests/document-window.test.ts` 覆盖 card/box resize。 |
| Host 打开 `.card/.box` 时传结构化来源 | `develop:Chips-Host/packages/card-open-service/src/card-open-service.ts`、`develop:Chips-Host/src/main/core/file-association.ts` | `Chips-Host/packages/card-open-service/src/card-open-service.ts`、`Chips-Host/src/main/core/file-association.ts`、`Chips-Host/src/main/services/register-host-services.ts` | 融合迁移。`.card/.box` 主入口改传 `launchParams.cardSource`，非卡片通用文件仍保留 `targetPath`。 | `Chips-Host/tests/unit/card-open-service.test.ts` 覆盖 cardSource 传参。 |
| CardViewer 从 `cardSource` 恢复查看态 | 旧 `ViewerSourceProvider`、旧 `App.tsx` 启动解析 | 当前 `AppRuntimeProvider`、`scene-registry/AppShell` | 职责重映射。不复制旧单体根组件，来源解析进入当前 AppRuntime。 | `Chips-CardViewer/tests/unit/app.test.tsx` 覆盖本地、社区和远程不支持来源。 |
| CardViewer 封面/内容切换 | 旧 `ViewerCoverSurface.tsx/css`、旧 `ViewerStage.tsx` | 当前 `AppRuntimeProvider`、`DocumentFileScene`、`HostedDocumentScene`、`ViewerCoverSurface` | 职责重映射。封面来源由 runtime 读取，Scene 内切换 content/cover，不新增平行运行时。 | `Chips-CardViewer/tests/unit/app.test.tsx` 覆盖本地卡片封面切换。 |
| BookReader iframe 焦点键盘翻页 | `develop:Chips-BookReader/src/interaction/interaction-manager.ts` 与测试 | `Chips-BookReader/src/interaction/interaction-manager.ts` | 直接融合。`attachToFrame` 同步挂载 `KeyboardHandler`。 | `Chips-BookReader/tests/unit/interaction-manager.test.ts` 通过。 |

## 2. 当前验证

- `npm --prefix Chips-Host test -- tests/unit/card-open-service.test.ts`：通过。
- `npm --prefix Chips-CardViewer test -- tests/unit/app.test.tsx`：通过。
- `npm --prefix Chips-BookReader test -- tests/unit/interaction-manager.test.ts`：通过。
- `Chips-SDK/tests/document-window.test.ts` 的 Vitest 用例已通过；通过 `npm --prefix Chips-SDK test -- tests/document-window.test.ts` 会继续执行完整 CLI 集成脚本，本轮记录为“单测通过，完整 SDK 脚本待后续统一跑”。

## 3. 后续继续项

- CardViewer 封面 surface 还需要在社区 Web document surface 下补齐 `plugin.surface.resize` 发布验证。
- 社区 open-view、render-status 和 Web 插件路由已完成第一段迁移，详见 `实施记录/02-社区打开链路迁移.md`。
- 源 `.card` 渲染缓存、上传会话和 CommunityUploader 模块插件仍需继续迁移。
- 公共契约文档待代码链路进一步收口后统一同步。
