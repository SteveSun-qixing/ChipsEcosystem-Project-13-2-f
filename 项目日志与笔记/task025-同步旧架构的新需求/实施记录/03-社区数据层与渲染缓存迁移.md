# 社区数据层与渲染缓存迁移实施记录

## 1. 源码一比一对照

| 旧源码或旧行为 | 旧证据 | 新架构落点 | 迁移方式 | 验证 |
|---|---|---|---|---|
| `cover_ratio` 普通列与列表索引 | `develop:packages/server/src/db/schema/cards.ts`、`boxes.ts`、`rooms.ts`、`0001_community_server_performance.sql` | 当前 `cards.ts`、`boxes.ts`、`rooms.ts`、`0001_community_open_view_cache_base.sql` | 融合迁移。迁入最终 summary 热路径需要的列与索引，不迁回旧 `card_pipeline_jobs`。 | `npm --prefix Chips-CommunityPlatformServer/packages/server run build` 通过。 |
| 源 `.card` 字段、上传会话表、渲染缓存表 | `develop:db/schema/upload-sessions.ts`、`card-render.ts`、`0002_card_source_render_cache.sql` | 当前 `db/schema/upload-sessions.ts`、`card-render.ts`、`0001_community_open_view_cache_base.sql` | 融合迁移。先落数据契约，供缓存 worker 和后续 CommunityUploader 使用。 | server build 通过，迁移资产已复制到 `dist/db/migrations`。 |
| `CardRenderCacheService` | `develop:services/card-render-cache.service.ts` | 当前 `services/card-render-cache.service.ts` | 融合迁移。接入源 `.card` 下载、Host 文件转换、缓存上传、续期、过期清理、私有缓存流代理与 Redis 队列。 | `cards.test.ts` 覆盖缓存命中/缺失；`card.service.test.ts` 覆盖缓存失效和删除清理。 |
| S3 bucket 分层与私有缓存代理能力 | `develop:storage/buckets.ts`、`storage/s3.ts`、`storage/init.ts` | 当前 `storage/buckets.ts`、`storage/s3.ts`、`storage/init.ts` | 融合迁移。源 `.card`、公开/私有 view cache、公开/私有 cover cache bucket 已成为正式对象存储层；旧 `CARD_HTML` 不再作为正式 bucket 常量。 | server build 通过。 |
| `/view`、`/cover`、`/render-cache/*`、`/cover-cache/*` | `develop:routes/cards.ts` | 当前 `routes/cards.ts` | 融合迁移。`open-view` 优先看 ready cache；`/view` 命中缓存续期并 302，缺失入队；旧 `htmlUrl` 不再作为 view 成功回退。 | server route 单测通过。 |
| Worker 任务循环 | `develop:packages/server/src/worker.ts` | 当前 `packages/server/src/worker.ts` | 融合迁移。worker 使用独立 Host runtime workspace，消费 Redis 队列，处理缓存清理。 | server build 通过。 |
| Host runtime 隔离目录 | `develop:services/host-integration.ts` | 当前 `services/host-integration.ts` | 融合迁移。server 与 worker 分别使用 `.chips-server-host-server` / `.chips-server-host-worker`，避免并发进程争用。 | server build 通过。 |

## 2. 当前边界

- 旧上传流水线已移入 `Chips-CommunityPlatformServer/packages/server/归档/20260529-旧上传流水线归档/`，不再参与正式构建。
- `src/pipeline/card-unpack.ts` 保留为 `.card` 读取工具，因为 `utils/card-file.ts` 与 `card-render-cache.service.ts` 的源 `.card` 检查、封面缓存生成仍依赖 `unpackCard`。
- 上传会话迁移已写入 `sourceCardBucket/sourceCardKey/sourceCardSha256` 并入队 view/cover 缓存，详见 `实施记录/04-社区上传会话迁移.md`。
- Drizzle snapshot 仍保留当前仓原有快照，新增迁移 SQL 已进入 `_journal.json` 并被 build 复制。后续如果统一运行 `drizzle-kit generate`，需要核对生成快照是否只反映本轮 schema 变化。

## 3. 验证记录

- `npm --prefix Chips-CommunityPlatformServer/packages/server test -- src/routes/cards.test.ts src/services/card.service.test.ts`：通过。
- `npm --prefix Chips-CommunityPlatformServer/packages/server run build`：通过。
- `npm --prefix Chips-CommunityPlatformServer/packages/web run build`：通过。
