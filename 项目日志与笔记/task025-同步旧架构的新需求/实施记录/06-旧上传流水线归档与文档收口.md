# 旧上传流水线归档与文档收口实施记录

## 1. 源码一比一对照

| 对照项 | 旧源码证据 | 当前新架构落点 | 处理结果 | 验证 |
|---|---|---|---|---|
| 旧 `runCardPipeline` 主链路 | `develop:packages/server/src/pipeline/card-pipeline.ts`、`cdn-upload.ts`、`richtext-inline.ts`、`url-replace.ts` | 上传会话、`Chips-CommunityUploader-Plugin`、`CardRenderCacheService` | 已归档。资源外置和重打包由模块插件承担，服务器只保存源 `.card` 并入队缓存。 | `rg` 确认正式源码不再引用 `runCardPipeline`。 |
| 旧 pipeline 环境变量 | `CARD_PIPELINE_ASSET_UPLOAD_CONCURRENCY`、`CARD_PIPELINE_HTML_UPLOAD_CONCURRENCY` | `CARD_RENDER_CACHE_UPLOAD_CONCURRENCY`、worker 与上传会话变量 | 已移除正式 `env.ts` 和 `deploy/.env.example` 口径。 | server build 通过。 |
| 旧 `chips-card-html` bucket | 旧 pipeline HTML 目录输出 | `chips-card-render-cache`、`chips-card-cover-cache` 与私有缓存代理 | 已从正式 `Bucket` 常量和清理逻辑移除。历史对象不作为新主链路。 | `s3.test.ts` 改为 render cache bucket 断言。 |
| 旧 `htmlUrl` 查看回退 | `GET /api/v1/cards/:cardId/view` 旧 HTML 302 | `/view` 只在 ready render cache 命中时 302，缺失则入队并返回 202 | 已移除旧成功回退。前端只在 `viewState=cache_ready` 时启动 CardViewer。 | server/web build 通过，`cards.test.ts` 更新。 |
| 旧箱子条目 `communityHtmlUrl` | 箱子条目匹配社区卡片后返回旧 HTML 地址 | `communityViewUrl`、`communityRenderStatusUrl` | 已改为 view 与 render-status 语义，避免箱子条目继续扩散旧 HTML 主口径。 | server/web build 通过。 |
| 旧社区 Web 上传入口 | `POST /api/v1/upload/card` | `POST /api/v1/upload-sessions` + `POST /api/v1/upload-sessions/:uploadId/card` | Web 工作区已迁到上传会话；旧接口仅作为过渡 API 保留。 | web build 通过。 |

## 2. 归档位置

旧上传流水线文件已移动到：

```text
Chips-CommunityPlatformServer/packages/server/归档/20260529-旧上传流水线归档/
```

已归档文件：

- `pipeline/card-pipeline.ts.archived`
- `pipeline/cdn-upload.ts.archived`
- `pipeline/richtext-inline.ts.archived`
- `pipeline/richtext-inline.test.ts.archived`
- `pipeline/url-replace.ts.archived`
- `pipeline/url-replace.test.ts.archived`
- `scripts/test-pipeline.ts.archived`

保留文件：

- `Chips-CommunityPlatformServer/packages/server/src/pipeline/card-unpack.ts`
- `Chips-CommunityPlatformServer/packages/server/src/pipeline/card-unpack.test.ts`

保留原因：`utils/card-file.ts` 和 `card-render-cache.service.ts` 仍需要 `unpackCard` 读取源 `.card`，用于上传源文件检查、封面缓存生成和渲染缓存构建。

## 3. 文档收口

已同步更新：

- `Chips-CommunityPlatformServer/技术文档/04-工作区上传页技术方案.md`
- `Chips-CommunityPlatformServer/技术文档/05-数据模型与接口映射.md`
- `Chips-CommunityPlatformServer/技术文档/07-启动与运行说明.md`
- `Chips-CommunityPlatformServer/技术文档/08-社区Web插件宿主链路.md`
- `Chips-CommunityPlatformServer/技术文档/09-生产域名与对象存储部署说明.md`
- `生态共用技术文档/协议与契约/09-社区平台API契约.md`

当前正式口径：

- 社区卡片发布主链路是上传会话 + CommunityUploader 模块插件 + 源 `.card` + view/cover 渲染缓存；
- `POST /api/v1/upload/card` 仅作为过渡接口保留，不执行旧 pipeline；
- 新客户端应使用上传会话接口；
- view 打开成功依赖 ready render cache，不再依赖旧 `htmlUrl`。

## 4. 验证记录

- `npm --prefix Chips-CommunityPlatformServer/packages/server run build`：通过。
- `npm --prefix Chips-CommunityPlatformServer/packages/web run build`：通过。
