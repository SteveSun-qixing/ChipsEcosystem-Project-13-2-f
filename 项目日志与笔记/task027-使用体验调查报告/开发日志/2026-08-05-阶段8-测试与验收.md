# 阶段8开发日志：测试和验收

> 日期：2026-08-05
> 阶段：阶段 8（测试和验收）
> 任务清单：P8-01 ~ P8-26

## 目标

用测试与验收矩阵确认全链路符合要求。

## 完成内容

### 卡片恢复一致性（P8-01 ~ P8-08）

`Chips-Host/tests/unit/card-transfer-gold-loop.test.ts` 黄金回环覆盖：

- P8-01 文件路径与条目顺序一致（`restoredPaths == originalPaths`）；
- P8-02 配置文件 URL 恢复为原始相对路径（content YAML 与 cover.html）；
- P8-03 资源字节一致（逐个 `zip.readEntry` 对比）；
- P8-04 ZIP Store 条目顺序一致；
- P8-05 条目修改时间一致（逐条目对比 modifiedTime）；
- P8-06 条目 Store 模式一致（新增 compressionMethod=0 且 compressedSize==size 断言）；
- P8-07 封面 HTML 和封面资源恢复一致；
- P8-08 下载结果可被正式 Host 打开、渲染和编辑：模块下载后调用 `card.readInfo` / `card.render` / `card.open` 验证。

### 浏览器边界（P8-09 ~ P8-13）

- P8-09 社区网页无上传入口：Web 工作区为“客户端发布”信息页，无 dropzone/文件选择/队列；
- P8-10 无下载 .card 入口：网页只显示“请在薯片客户端中打开并下载”提示；
- P8-11 Web CardViewer 不声明下载能力：manifest Web target 只查看；
- P8-12 浏览器端无 .card 打包器：全仓无浏览器 ZIP 打包/保存 sink；
- P8-13 手机浏览器只查看：无下载、无打包代码路径。

### 客户端能力（P8-14 ~ P8-17）

- P8-14 / P8-15 Desktop / Mobile 客户端上传、查看、下载：模块 manifest 支持 desktop/mobile，module-definition.test 覆盖上传/下载/远程打开；
- P8-16 客户端插件统一调用卡片传输模块：SDK `communityCardTransfer` + CardViewer 下载动作；
- P8-17 取消（`COMMUNITY_TRANSFER_CANCELLED`）、重试（指数退避）、断网（`NETWORK_FAILED`）、签名刷新（`AUTH_EXPIRED`）、磁盘不足/失败清理（下载失败删除输出）都有稳定错误状态。

### 弱服务器约束（P8-18 ~ P8-22）

- P8-18 上传大卡片时 API 不接收文件字节：card-transfer 全路由 JSON-only，二进制 payload 返回 415/400；
- P8-19 下载大卡片时 API 不返回文件字节：下载计划只返回对象 URL + 预签名 GET；
- P8-20 查看时浏览器主要读取对象存储/CDN：view/cover 缓存与资源 URL；
- P8-21 Worker 有界并发：`CARD_RENDER_WORKER_CONCURRENCY` 默认 1，渲染队列 + 超时重排；
- P8-22 对象存储保存网络资源卡片、资源、查看缓存、封面缓存。

### 第三方库（P8-23 ~ P8-26）

- 本任务新增运行期依赖仅 `yaml@^2.8.2`（传输模块），选型记录见模块技术文档；
- MIT 许可证、活跃维护、包体极小、纯 JS 跨端、无已知高危；替代方案与安全风险已核对。

## 验证

- `Chips-Host`：`npm test` 266 条通过（含黄金回环、entry plan、card-service rendering）。
- `Chips-SDK`：`npm test` 全量通过（含 community-card-transfer、tooling 测试）。
- `Chips-CardViewer`：`npm test` 36 条通过；`build` 成功。
- `Chips-CommunityUploader-Plugin`：`npm test` 6 条通过；`validate` 通过。
- `Chips-CommunityPlatformServer`：`vitest run` 53 条通过；`tsc --noEmit` 通过。
- Web 包：`tsc --noEmit` 通过。

## 状态

- [x] P8-01 ~ P8-26 全部完成。
- 所有阶段（0-8）任务清单完成。
