# task028-整理 Git 仓库：提交方案 v2（待用户审核）

> 生成日期：2026-08-05
> 说明：按用户审核意见修订——投资问卷提交、.DS_Store 全部屏蔽、工单122 补任务文件夹、布局插件逐个提交、问题工单.md 按 hunk 随对应提交、一个工单一个提交消息（工单119/120/121/118 全部拆分）、提交信息风格对齐历史提交（"修复工单NNN…"标题 + "本次提交…"详细正文）。
> 所有提交均未执行，等待用户审核。

---

## 零、执行约定（本方案配套）

1. **问题工单.md 按段落拆分**：每个工单提交只携带自己的段落（`git add -p` 按 hunk 暂存）。段落归属：
   - 工单118 段落 → 提交 05；工单119 段落 → 提交 11；工单120 段落 → 提交 12；
   - 工单121 段落 → 提交 29；工单122 段落 → 提交 31；工单123 段落 → 提交 32；
   - 工单125 段落 → 提交 43；工单124 段落 → 提交 45；工单126 段落 → 提交 46；
   - 工单094 复核段落 → 提交 28。
2. **多任务同文件按 hunk 拆分**：host-runtime.ts（工单119 资源部分 / 工单120 主题部分）、App.tsx（视觉收口 / 工单126 页脚）、ProfilePage.tsx（视觉收口 / 工单126 本人判定）、card-service.ts（entryPlan / 渲染帧资源基址）、publisher.ts（上传链路 / 下载链路）、i18n 字典（阶段5 / 阶段7 / 阶段6）、web i18n 字典（C40/C41）、问题工单.md。执行时使用 `git add -p` 精确暂存。
3. **.DS_Store 屏蔽**：根 .gitignore 新增 `.DS_Store`，并 `git rm --cached` 全部已跟踪 .DS_Store（生态共用技术文档/.DS_Store 等），作为提交 51 一并收口。
4. **.codex-runtime-logs 屏蔽**：根与社区服务器 .gitignore 新增忽略规则，作为提交 49。
5. **__dbg3.test.ts**：移入 packages/server/src/routes/归档/ 后不提交，作为提交 50。
6. **工单122 补建文件夹**：新增 项目日志与笔记/工单122-暗夜主题dist构建产物陈旧导致SDK工具链质量门禁失败/任务说明.md（从问题工单.md 对应段落补全背景、复现、期望与处理结果），作为提交 31。
7. **暂留接口**：CardViewer 的 canDownloadActiveCard 恒为 false 为暂留接口，后续开发，本次不修复、不在提交信息中声明为缺陷。
8. 提交顺序按文件最后编写时间编排（C01→C51），与历史节奏一致。

---

## 一、提交总览（51 个提交，按时间顺序）
> 说明：提交标题不含时间；布局插件提交标题使用中文布局名，正文保留插件目录名。

| # | 时间 | 提交标题 |
|---|---|---|
| C01 | 06-01~03 | 新增task026薯片生态宣传片制作稿与短片渲染工程 |
| C02 | 06-06 | 添加开源项目miniPaint图像编辑器素材 |
| C03 | 06-11~12 | 新增投资问卷申请草稿与推敲笔记 |
| C04 | 06-29 | 新增薯片生态整体架构图文档 |
| C05 | 06-29 | 修复工单118社区服务器本地启动链路 |
| C06 | 06-29 | 收口工单118社区服务器纳入生态根npm workspace |
| C07 | 06-30 | 完成工单119社区Web插件宿主辅助资源读取服务端链路 |
| C08 | 06-30 | 完成工单120社区Web插件宿主主题注入服务端链路 |
| C09 | 06-30 | 外置CardtoHTML导出HTML中的Host主题文件资源 |
| C10 | 06-30 | 放宽音乐播放器CSP支持社区Web远端资源 |
| C11 | 07-06 | 完成工单119音乐播放器社区远端资源恢复 |
| C12 | 07-06 | 收口工单120社区Web插件宿主主题运行时与测试 |
| C13 | 07-06 | 新增社区服务器一键本地启动脚本 |
| C14 | 07-06 | 按资源打开契约归一社区Web相对资源路径 |
| C15 | 07-06 | 收口社区Web页面朴素白视觉与token化阴影 |
| C16 | 07-06 | 迁移卡片查看器统一来源与封面surface高度协议 |
| C17 | 07-06 | 统一社区卡片与箱子详情页承载并开放查看缓存链路 |
| C18 | 07-06 | 新增堆叠卡片箱子布局插件 |
| C19 | 07-06 | 新增封面轮播箱子布局插件 |
| C20 | 07-06 | 新增文档库箱子布局插件 |
| C21 | 07-06 | 新增无限画布箱子布局插件 |
| C22 | 07-06 | 新增地图箱子布局插件 |
| C23 | 07-06 | 新增瀑布流箱子布局插件 |
| C24 | 07-06 | 新增散落箱子布局插件 |
| C25 | 07-06 | 新增时间线箱子布局插件 |
| C26 | 08-02 | 加固网格与列表箱子布局编辑器图标与资产路径安全 |
| C27 | 08-02 | 刷新组件库性能质量门禁与发布就绪报告 |
| C28 | 08-02 | 复核工单094组件库质量门禁与存储归类缺口 |
| C29 | 08-03 | 登记工单121社区卡片完整回下载与源文件直传契约缺口 |
| C30 | 08-05 | task027阶段0修订公共规范与术语并冻结社区传输边界 |
| C31 | 08-05 | 登记并收口工单122暗夜主题dist构建产物陈旧 |
| C32 | 08-05 | 解决工单123网络资源卡片渲染缓存永不生成 |
| C33 | 08-05 | 工单121社区API改造为控制面 |
| C34 | 08-05 | 工单121统一卡片传输模块能力与契约 |
| C35 | 08-05 | 工单121无压缩归档改写恢复核心与上传主流程 |
| C36 | 08-05 | 工单121Host ZIP Store支持条目顺序与修改时间恢复 |
| C37 | 08-05 | 工单121SDK新增communityCardTransfer类型化封装 |
| C38 | 08-05 | 工单121客户端下载与远程打开链路 |
| C39 | 08-05 | 工单121网页查看缓存与Web CardViewer会话 |
| C40 | 08-05 | 工单121移除社区网页浏览器上传链路 |
| C41 | 08-05 | 工单121卡片查看器接入客户端下载链路 |
| C42 | 08-05 | 归一basecard导出单卡片文档运行时资源基址 |
| C43 | 08-05 | 解决工单125应用插件渲染进程访问社区API的CORS缺口 |
| C44 | 08-05 | 社区认证响应补发refreshToken明文 |
| C45 | 08-05 | 解决工单124应用插件脚手架复制模板元数据 |
| C46 | 08-05 | 收口工单126社区网页版页脚渲染范围与本人判定 |
| C47 | 08-05 | 修订AGENTS.md第三方库引入政策 |
| C48 | 08-05 | 新增薯片社区客户端应用插件 |
| C49 | 08-05 | 屏蔽本地运行日志与PID文件进入版本库 |
| C50 | 08-05 | 归档社区服务器调试残留测试文件 |
| C51 | 08-05 | 屏蔽全仓.DS_Store元数据文件 |

---

## 二、详细提交信息

### C01 新增task026薯片生态宣传片制作稿与短片渲染工程

本次提交新增 task026 宣传片任务的制作稿、工业制作方案和卡片生态短片渲染工程。

具体内容：

1. 新增统一制作规范，明确宣传片世界观、视觉规则、镜头规则与交付规格；新增卡片生态、软件生态、总生态三支宣传片逐镜头制作稿与旁白字幕总表，以及交付清单与验收标准。
2. 新增卡片生态逐镜头工业制作方案，覆盖 C01-C12 十二个镜头的 Three.js/HTML/SVG/Canvas 实现方案（信息散落、信息被软件分开、提出问题、第一张卡片诞生、卡片承载文字与图片、卡片承载视频与音乐、多种信息都成为卡片、卡片组合与嵌套、封面与打开、分享与传递、卡片组成箱子与空间、片尾价值宣言）。
3. 新增卡片生态短片渲染工程（08-卡片生态短片工程/）：Three.js 场景源码、关键帧/视频/音频渲染脚本、入口页与工程配置。
4. 渲染产物（dist/output/keyframes）与 node_modules 已由忽略规则排除，视频成品不入库，本次只提交工程源码、脚本与文档。

### C02 添加开源项目miniPaint图像编辑器素材

本次提交按开放项目同化评估目录的既有引入模式（DPlayer/Thorium/MusicPlayer），引入 miniPaint 图像编辑器素材。

具体内容：

1. 新增 OpenSourceProjectHandlingFolder/miniPaint-master/：miniPaint v4.14.3（MIT 许可）HTML5 在线图像编辑器完整源码，含核心模块、图层、滤镜、选区、绘制工具、多格式导入导出与预构建 bundle，共 292 个文件。
2. 新增 miniPaint-repowiki/：miniPaint DeepWiki 架构文档镜像（Overview/Architecture/Core-Systems/Tool-Framework/Layer-Management/Selection-System/State-Management/Effects 等 33 篇）。
3. 新增 miniPaint-master.zip：GitHub 下载的原始源码压缩包。
4. 该批素材尚未进行同化评估，仅入库保留，供后续图像编辑/绘图能力评估使用。

### C03 新增投资问卷申请草稿与推敲笔记

本次提交保存奇绩创坛创业营申请问卷的填写稿与逐问题推敲过程。

具体内容：

1. 新增投资问卷.md（第一版填写稿）与推敲后版本.md（第二版精炼稿），以及如何写好一份申请.md（评审视角要点摘录）。
2. 新增推敲笔记/：50 篇逐问题推敲记录，覆盖项目名称、一句话概括、项目描述、创始人阶段、竞品、反共识、融资、投资人质疑应对与叙事顺序等。
3. 该目录为纯文档申请材料，与代码生态无依赖。

### C04 新增薯片生态整体架构图文档

本次提交补充生态共用技术文档中的薯片生态整体架构图。

具体内容：

1. 新增 生态共用技术文档/架构设计/20-薯片生态整体架构图.md，作为生态整体架构的正式图示文档。
2. 该提交只新增文档，不修改运行时代码与契约。

### C05 修复工单118社区服务器本地启动链路

本次提交按工单118 处理方案修复社区服务器本地启动阻断，并同步登记工单记录（问题工单.md 工单118 段落随本提交 hunk 暂存）。

具体内容：

1. 修复 packages/server/src/services/box.service.ts 的语法回归（esbuild 报 "Expected ":" but found ";" "），恢复 communityCards 空数组分支与 cardsByFileId 映射；同时将 enrichCardRefs 从逐条 findFirst 查询重构为一次 findMany 批量查询 + Map 匹配，消除 N+1 查询，返回字段不变。
2. packages/server/package.json 收口依赖：chips-host 由 file: 路径改为版本号 0.1.0；移除 chips.basecard.image、chips.basecard.richtext、chips.module.chips.htmltoimage.plugin、theme.theme.chips-official-default-theme 四个 file: 插件依赖，改由 Host 集成层按生态根路径运行时安装，不再进入 node_modules 快照。
3. pnpm-workspace.yaml 的 packages 新增 ../Chips-Host，将 Host 纳入社区 pnpm 工作区；新增 .npmrc 开启 link-workspace-packages，保证 chips-host: 0.1.0 解析为本地 workspace 链接，不再回退远端 registry。
4. pnpm-lock.yaml 同步依赖变化（新增 ../Chips-Host importer，移除 milkdown/prosemirror/remark 等 richtext 插件依赖树）。
5. 更新 技术文档/07-启动与运行说明.md，补充"依赖安装与工作区解析"章节，说明生态根 npm workspaces 与社区 pnpm 双入口解析方式。
6. 新增 项目日志与笔记/工单118-社区服务器本地启动链路阻断/任务说明.md，登记背景、证据、处理结果与验证。

### C06 收口工单118社区服务器纳入生态根npm workspace

本次提交将社区服务器子包纳入生态根 npm workspace，统一本地依赖解析（package-lock.json 于 2026-08-05 因后续 workspace 变化整体重新生成，最终内容以本提交为准）。

具体内容：

1. 根 package.json workspaces 新增 Chips-CommunityPlatformServer/packages/*，将 @ccps/server/@ccps/web/@ccps/admin 纳入生态根工作区。
2. package-lock.json 重新生成，多个子包依赖由版本号解析改为 file: 链接解析（chips-sdk、chips-host、@chips/component-library 等），消除远端 registry 404 与 file 快照漂移。
3. 更新 生态共用技术文档/开发与运维/02-依赖管理指南.md：根 workspace 覆盖列表增加社区服务器 packages，依赖映射表补充 @ccps/server -> chips-host。

### C07 完成工单119社区Web插件宿主辅助资源读取服务端链路

本次提交按工单119 要求，为社区 Web 插件宿主补齐辅助资源读取服务端能力（host-runtime.ts 仅暂存资源相关 hunk，主题 hunk 随提交 C08）。

具体内容：

1. 新增 packages/server/src/services/web-resource-open-plan.ts：从 host-integration 抽出的资源打开计划纯函数模块，MIME 扩展表扩展为音频（.aac/.flac/.m4a/.mp3/.oga/.ogg/.opus/.wav）、视频（.m4v/.mov/.mp4/.ogv/.webm）、电子书/文档（.azw/.azw3/.djvu/.doc/.docx/.epub/.fb2/.md/.mobi/.pdf/.rtf/.txt）与图片共 40+ 扩展名；resolveWebResourceOpenPlanFromPlugins 按意图归一、扩展名/MIME 推断生成 resource-handler 能力候选并匹配插件，无匹配回退 external。
2. host-runtime.ts bootstrap 补齐资源读取：resource.resolve 支持 / 开头同源绝对路径归一；新增 resource.readBinary 与 resource.readMetadata 本地实现（本地文件句柄读取或 fetch 后字节数组载荷），供音乐/视频/书籍应用恢复封面、歌词、字幕与元数据。
3. host-integration.ts 删除内联的 WebResourceOpenRequest/WebResourceOpenPlan 与旧辅助函数，改为从新模块 import 并 re-export；defaultAppPlugins 新增 Chips-MusicPlayer、Chips-VideoPlayer、Chips-BookReader。
4. 新增 web-resource-open-plan.test.ts：图片通配 MIME 命中 photo-viewer、mp3/flac→music-player、mp4→video-player、epub→book-reader、URL 无扩展名用 fileName 推断、无匹配回退 external 共 5 用例。

### C08 完成工单120社区Web插件宿主主题注入服务端链路

本次提交按工单120 要求，为社区 Web 插件宿主补齐主题 CSS 与图标字体注入服务端能力（host-runtime.ts 仅暂存主题相关 hunk，资源 hunk 已随提交 C07）。

具体内容：

1. host-runtime.ts bootstrap 主题注入：entry 页在插件 CSS 前注入 theme.css 外链（base、theme.css、bootstrap.js 三件套）；新增 theme.css 与 theme-assets/:themeId/* 两个受控路由；bootstrap 注入 themeRuntime/themeSnapshot 并支持 theme.getCurrent/getAllCss/theme.resolve。
2. cpx 单位落地：新增 CPX_LENGTH_PATTERN、formatCssNumber、convertCpxToViewportLength（按 1024 基准转 vw）、normalizeThemeCssForBrowser（带引号/注释/url() 保护的 CSS 扫描替换）与 createThemeStylesheet（tokens 转 :root CSS 变量块）。
3. resolveContentType 扩展 .woff/.ttf/.otf 字体 MIME；theme-assets 路由按扩展名返回 MIME 并带 Cache-Control。
4. 技术文档/08-社区Web插件宿主链路.md 新增主题注入规则 6 条（theme.css 外链、cpx→vw、字体资产改写、CSP 兼容说明）。

### C09 外置CardtoHTML导出HTML中的Host主题文件资源

本次提交让卡片转 HTML 模块导出时，将 Host 主题插件产物中的根外字体/图标资源复制进产物并改写为相对 URL，杜绝 file:// 本地路径泄漏。

具体内容：

1. exporter.ts 新增 THEME_ASSET_DIR=assets/theme；isHostThemeAssetPath 识别 theme.theme.*/dist/ 主题插件产物路径；createStablePathHash（FNV-1a 32 位）为资源文件名加哈希前缀防止不同主题同名资产覆盖；sanitizeAssetFileName 清洗非法文件名字符。
2. 新增 ResolvedAssetReference 与 OfflineAssetRewriteState 类型；extractAssetRoot 改为预先提取引用并过滤 Host 主题路径，目录引用直接用、文件引用取父目录；rewriteStandaloneAssetUrls 对根外引用逐条复制并 split/join 改写 URL。
3. ABSOLUTE_URL_PATTERN 新增转义引号分支，支持 JS 字符串内 url(\"...\") 场景；仅当无 assetRoot 且无任何复制且 HTML 未变时才推 CONVERTER_HTML_NO_ASSET_ROOT 警告。
4. module-definition.test.ts 新增 3 用例：导出资源基址相对化、@font-face file:// 字体外置（assetCount=2）、srcdoc script 字符串内 URL 改写。
5. README、技术文档 01 与需求文档 01 同步（新增 FR-18，说明 assets/theme/ 目录与根外主题资源改写规则）。

### C10 放宽音乐播放器CSP支持社区Web远端资源

本次提交放宽音乐播放器入口页 CSP，适配社区 Web 查看链路中的 HTTP 音频、封面与歌词资源。

具体内容：

1. index.html CSP：img-src/media-src/font-src 增加 http: https:，connect-src 由 'self' 放宽为 'self' http: https:。
2. 新增 tests/unit/index-html-csp.test.ts，解析 CSP meta 断言 media-src/img-src 含 http: https: blob:、connect-src 含 http: https:，防止后续收紧破坏社区 Web 查看链路。

### C11 完成工单119音乐播放器社区远端资源恢复

本次提交完成工单119 应用侧修复：主音频为社区远端 http(s) 资源时，仍按音乐卡片 payload 恢复封面与歌词（问题工单.md 工单119 段落与工单119 任务说明随本提交）。

具体内容：

1. AppRuntimeProvider.tsx 结构性重构：embeddedMetadata 与 companionSelection 声明提升；歌词加载、标题/艺术家/专辑解析、封面解析（resolveArtworkUriFromResource）与嵌入封面落盘（persistEmbeddedArtworkPngToWorkspace → resolveEmbeddedArtworkUrl 回退）全部移出"仅本地文件可读嵌入元数据"分支。
2. 行为变化：主音频为 http(s) 直接可播的社区远端资源时，也能按 MusicCardOpenPayload.resources.cover/lyrics 恢复封面与歌词上下文。
3. launch-resource.test.ts 新增社区 Web 远端资源回归用例；技术文档/02 新增"4.1 音乐卡片辅助资源恢复"约定（封面走 client.resource.resolve、歌词走 client.resource.readBinary）。
4. 新增 项目日志与笔记/工单119-社区Web插件宿主辅助资源读取能力缺口/任务说明.md，登记处理结果。

### C12 收口工单120社区Web插件宿主主题运行时与测试

本次提交收口工单120 的主题运行时服务端实现与链路测试（问题工单.md 工单120 段落与工单120 任务说明随本提交）。

具体内容：

1. host-integration.ts 新增 WebThemeRuntimeView 接口；getWebThemeRuntime() 并行调用内核 theme.getCurrent/theme.getAllCss/theme.resolve，并将主题 CSS 中 file:// 字体 URL 改写为 /api/v1/host/theme-assets/:themeId/*（保留 ?/# 后缀，防路径逃逸校验）；resolveWebThemeAssetPath 按 manifest 定位主题插件并校验资产必须落在安装目录内。
2. RuntimePluginRecord.manifest 新增 theme 元信息（themeId/displayName/version/parentTheme/tokensPath/themeCssPath/contractPath）。
3. 新增 host-runtime.test.ts：覆盖 entry 注入顺序（theme.css 在 app.css 前）、theme.css 内容（Material Symbols 字体、24cpx→2.34375vw、--chips-layout-gap-md token 变量）、bootstrap 的 theme.getCurrent/getAllCss/theme.resolve、theme-assets 字体 MIME 与原始字节、resource.resolve/readBinary/readMetadata 行为。
4. 新增 项目日志与笔记/工单120-社区Web插件宿主缺少主题CSS与图标字体注入/任务说明.md，登记处理结果与本地启动验证证据。

### C13 新增社区服务器一键本地启动脚本

本次提交补充社区服务器一键本地启动链路（工单118 处理结果的脚本部分）。

具体内容：

1. 新增 scripts/start-local-dev.sh：前置检查（docker/pnpm/curl/lsof/nc）→ compose 启动 postgres/redis/minio 并等待端口健康 → 三重清理旧进程（PID 文件 + 端口进程 + 命令行模式，kill_tree 递归）→ 依次启动 server（等待 /api/v1/health）、worker（监控 ECONNREFUSED/THEME_CONTRACT_INVALID 等错误）、web（5173）、admin（5174）；支持 stop（保留 Docker）、stop:all（连 Docker 一起停）、status、logs [component]；日志与 PID 写入 .codex-logs/。
2. Chips-CommunityPlatformServer/package.json 新增 start:local/stop:local/status:local/logs:local 脚本入口。
3. .gitignore 新增 .codex-logs/ 忽略规则。

### C14 按资源打开契约归一社区Web相对资源路径

本次提交按资源打开契约口径（payload 内资源标识为卡片根相对路径、实际可读入口由外层运行时归一），同步书籍阅读器、图片查看器与书籍基础卡片的启动恢复逻辑。

具体内容：

1. BookReader src/utils/launch-resource.ts：sourceId 优先级改为顶层 resourceOpen.resourceId（最高）→ payload.resources.book.resourceId → relativePath；tests 与 e2e 新增社区渲染缓存 URL 回归用例；技术文档/02 同步启动恢复规则。
2. PhotoViewer src/utils/launch-resource.ts：新增 decodePathSegment、normalizePlainPath、isSourceForRelativePath、resolveResourceBaseFromDirectSource、resolvePayloadSourceId 辅助函数；图片序列按顶层命中资源推导同一资源基准并还原绝对 URL；initialIndex 匹配支持相对路径；tests 新增序列推导与 initialIndex 断言；技术文档/01 同步。
3. book-BCP src/render/view.tsx：createImagePayload/createEbookPayload 删除 resolvedUrls 参数，openResource 一律传卡片根相对路径（image.file_path/config.book_file）；render-view.test.tsx 断言同步改为相对路径。
4. video-BCP tests/unit/editor-panel.test.tsx：新增 waitForVideoPreviewSrc 轮询助手，修复视频预览 src 异步设置的时序竞态。
5. 契约依据：生态共用技术文档/协议与契约/10-资源打开契约.md 新增规则 4（外层运行时基于资源基准 URL 归一相对资源标识；BookCardOpenPayload 以顶层 filePath/resourceId 为实际读取入口；基础卡片发起意图时 resources.*.resourceId 保存卡片根相对路径）——该文档最终收口随提交 C30。

### C15 收口社区Web页面朴素白视觉与token化阴影

本次提交对社区 Web 页面做视觉收口：去除常驻光晕渐变背景，封面阴影与交互态 token 化（App.tsx 仅暂存视觉相关 hunk，工单126 页脚 hunk 随提交 C46）。

具体内容：

1. HomePage：移除 home-simple__glow 三色光斑与玻璃拟态渐变，改为纯白背景、细边框与柔和阴影。
2. ProfilePage：删除 PROFILE_BACKGROUND_STREAMS/ORBS 与全部 ambient/frost/orb/stream 层及 7 组 keyframes；作品封面阴影改为消费 --ccps-sys-shadow-card/-hover/-selected/-contact token（hover translateY(-5px)、贴地接触阴影、内描边高光）。
3. runtime.css：nectar-dusk 主题色板整体改浅色（accent #2f7fff、canvas #ffffff、text #1a2237），新增三枚参考阴影 token 与系统映射；body 背景去除径向渐变，按钮加边框/底色/阴影并强化 hover。
4. theme/registry.ts：detectPreferredTheme 固定返回 ccps.web.pear-mist；nectar-dusk 的 colorScheme 改为 light。
5. 技术文档 03/06 与需求文档 03/05 同步视觉收口口径。

### C16 迁移卡片查看器统一来源与封面surface高度协议

本次提交对卡片查看器做结构收口：封面 surface 与文档 surface 统一通过 plugin.surface.resize 高度协议与宿主协作，chrome 通信改走 SDK 事件面。

具体内容：

1. CardViewerShell：children prop 改名 content，渲染进主区域。
2. ViewerChrome：移除 useChipsBridge，改为 SDK client.events 的 emit/on（含 unsubscribe 清理），trigger 行为不变。
3. ViewerCoverSurface：新增 client prop 与 rootRef，新增 publishResize 协议——测量滚动高度/视口高度/安全区并发送 plugin.surface.resize 载荷 {height, contentHeight, safeBlockEnd, viewportHeight, reason, stable}；挂载即发"非稳定 + 160ms 稳定"成对事件并监听 window resize。
4. DocumentFileScene/HostedDocumentScene：封面分支透传 client。
5. 删除 ViewerSourceProvider.tsx（来源解析已收敛到 types/viewer-source.ts 与 AppRuntimeProvider；归档动作随提交 C41 的归档目录一并收口）。

### C17 统一社区卡片与箱子详情页承载并开放查看缓存链路

本次提交将社区卡片与箱子详情页统一委托给 DocumentPluginRoutePage 承载，并补齐 open-view 查看缓存的补排、受控封面与 Range 分段读取链路。

具体内容：

1. BoxDetailPage 删除整页手写 hero/sidebar/引用列表结构，改为委托 DocumentPluginRoutePage（source/loading/pending/pendingLabel/trigger），与卡片详情页统一承载。
2. CardDetailPage 只加载 open-view 轻量字段并轮询 render-status，viewState=cache_ready 时使用缓存 viewUrl。
3. cards.ts：新增 shouldEnqueueOpenViewRenderJob——open-view 未命中且卡片 ready 时立即补排 view_miss（priority 10），不再让用户无限 pending；coverUrl 在 ready/有缓存/有封面时始终返回受控封面入口 /cards/:cardId/cover；新增 normalizeRangeHeader 与 sendRenderCacheObject 统一写 206/accept-ranges/content-range/content-type/content-length/etag；render-cache 与 cover-cache 路由支持 Range 分段读取；修复文件尾部重复的 escapeHtmlText/createCoverPreparingHtml 定义。
4. cards.test.ts 新增 Range 206 用例、open-view 未命中补排用例、缓存就绪不补排用例。

### C18 新增堆叠卡片箱子布局插件

本次提交新增第 9 个箱子布局插件 cardStack-BLP（堆叠卡片布局），延续 grid/list 布局插件正式基线。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/cardStack-BLP/（24 个正式文件）：manifest.yaml（desktop/headless supported）、chips.config.mjs、package.json（含 typecheck/verify 脚本）、contracts/layout-config.schema.json、i18n 双语、图标资产与来源说明。
2. schema/layout-config.ts：配置 normalize/validate 与 isSafeBoxAssetPath 安全路径校验。
3. view/page.tsx：主题 token 化渲染、滚轮/键盘/拖拽甩出导航、分页加载、封面预取、EmbeddedDocumentFrame 封面渲染、空态/错误态/重试。
4. editor/panel.tsx 与 frame-region-editor.tsx：背景区/顶部区配置与资源导入替换删除。
5. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C19 新增封面轮播箱子布局插件

本次提交新增箱子布局插件 coverFlow-BLP（封面轮播布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/coverFlow-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（主题 token 化封面轮播渲染、键盘/滚轮导航、封面预取、空态/错误态）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C20 新增文档库箱子布局插件

本次提交新增箱子布局插件 documentLibrary-BLP（文档库布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/documentLibrary-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（文档条目网格/列表渲染、分页加载、空态/错误态/重试）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C21 新增无限画布箱子布局插件

本次提交新增箱子布局插件 infiniteCanvas-BLP（无限画布布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/infiniteCanvas-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（无限画布平移/缩放渲染、卡片浮层、空态/错误态）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C22 新增地图箱子布局插件

本次提交新增箱子布局插件 map-BLP（地图布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/map-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（地图背景渲染、坐标标记、标签展示）、editor/panel.tsx（MapSourceMode 图片/宿主地图、MapProjection 投影、坐标与 labelOverride 标记编辑）与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C23 新增瀑布流箱子布局插件

本次提交新增箱子布局插件 masonry-BLP（瀑布流布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/masonry-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（瀑布流最短列分配算法、动态高度渲染、空态/错误态）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义（含多变宽高比最短列分配用例）与 schema 校验测试。无 TODO/占位实现。

### C24 新增散落箱子布局插件

本次提交新增箱子布局插件 scattered-BLP（散落布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/scattered-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（散落排布渲染、键盘/滚轮导航、空态/错误态）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. 独有 src/shared/scatter.ts 散落算法模块。tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C25 新增时间线箱子布局插件

本次提交新增箱子布局插件 timeline-BLP（时间线布局）。

具体内容：

1. 新增 Chips-BoxLayoutPlugin/timeline-BLP/：manifest、schema（含 isSafeBoxAssetPath 校验）、view/page.tsx（时间线轴渲染、分组条目、空态/错误态）、editor/panel.tsx 与 frame-region-editor.tsx、contracts schema、i18n 双语、图标资产、设计方案.md。
2. tests：布局定义与 schema 校验测试。无 TODO/占位实现。

### C26 加固网格与列表箱子布局编辑器图标与资产路径安全

本次提交加固 grid-BLP 与 list-BLP 编辑器：去除 ligature 图标依赖、资产路径安全校验与类型检查脚本。

具体内容：

1. 两个插件 package.json 新增 typecheck（tsc --noEmit）与 verify 串行脚本。
2. 两个 panel.tsx：新增 StableSelect/StableNumberInput（固定 ▾/-/+ 文本符号，避免无 Material Symbols 字体环境把 ligature 显示为英文名）；字体声明 token 化（var(--chips-comp-text-root-font...)）；onValueChange 参数显式类型。
3. 两个 frame-region-editor.tsx：移除上传/删除命令的 ligature 图标；list 版新增 sanitizeAssetFileName、导入文件名加时间戳前缀、safeDeleteAsset 返回 Promise<boolean>、删除失败中止模式切换并回滚新导入资源、导入后 isSafeBoxAssetPath 校验。
4. list-BLP schema/layout-config.ts：即使 frame mode 不使用 assetPath（none/html），只要 background/topRegion.assetPath 存在且非安全 assets/ 相对路径即报错，堵住绕过 mode 校验的注入路径；schema.test.ts 补充对应用例。

### C27 刷新组件库性能质量门禁与发布就绪报告

本次提交重跑组件库质量门禁并刷新报告产物。

具体内容：

1. reports/perf/perf-stage9-latest.json 与带时间戳快照：stage9 性能用例重跑（renderSubmitP95 0.72ms、themeSwitchVisibleP95 9.48ms，passed）。
2. reports/quality-gate/quality-gate-latest.json 与快照：lint/typecheck/test:types/test/test:contracts/test:a11y/test:perf/quality:coverage/build 九步门禁全部 passed。
3. reports/quality-gate/component-quality-coverage-latest.json 与快照：71 个组件契约覆盖。
4. reports/release/release-readiness-latest.json 与快照：发布就绪检查（0.1.0/minor、CHANGELOG、ROLLBACK、兼容性声明）全部 passed。

### C28 复核工单094组件库质量门禁与存储归类缺口

本次提交按 2026-08-02 复核结论，补充工单094 的可复现证据与后续路线判断（问题工单.md 工单094 复核段落随本提交 hunk 暂存）。

具体内容：

1. 已解决或废弃工单/工单094-组件库冻结技术栈与质量门禁覆盖漂移/任务说明.md 新增"2026-08-02 复核补充"：7 条可复现证据（typecheck 实为 node --check 且 npx tsc 报 3886 诊断、lint 无 ESLint、quality:coverage 非代码覆盖率、perf 非真实渲染、build 只产 token、主实现集中在 16999 行单文件、存储归类与状态不一致）。
2. 记录 shadcn/ui 路线判断：不采用全量重构，仅借鉴其源码组织/registry/CLI 流程。
3. 工单状态更新为"待处理"。

### C29 登记工单121社区卡片完整回下载与源文件直传契约缺口

本次提交登记工单121 并沉淀端到端上传链路调查结论（问题工单.md 工单121 段落随本提交 hunk 暂存）。

具体内容：

1. 新增 项目日志与笔记/工单121-社区卡片完整回下载与源文件直传契约缺口/任务说明.md：登记目标体验（上传 .card 后网页查看、网页下载完整卡片）、源 .card 私有保存与公共契约范围矛盾、CommunityUploader 外置资源导致 source.card 非原始完整包、multipart 上传对弱服务器风险；期望行为（区分原始完整包与发布处理包、源文件直传、下载权限差异）与暂行处理（不公开 source.card 下载入口，实施前先冻结公共契约）。
2. 新增 项目日志与笔记/上传链路与文件信息流向调查/01-端到端上传与文件信息流向总览.md：端到端上传与文件信息流向调查结论（外置资源、URL 改写、重新打包、对象存储预签名等环节）。

### C30 task027阶段0修订公共规范与术语并冻结社区传输边界

本次提交按 task027 阶段0 修订公共规范与术语，为社区卡片传输链路冻结"浏览器只查看、客户端统一模块传输、网络资源卡片为正式卡片类型"的契约基础（同时收口 C14 的资源打开契约文档）。

具体内容：

1. 文件格式规范/01-卡片文件格式规范.md 升级 v1.1.0：明确 .card 使用 ZIP Store 模式与正式 MIME application/vnd.chips.card+zip；新增 1.4/1A 卡片类型——完整离线卡片与网络资源卡片两种正式形态、类型对比表、可逆转换与恢复清单字段、完整恢复验收标准（字段语义一致而非字节一致）、网络资源卡片校验规则；7.1/7.2 资源组织改为双模式并移除"空壳卡片/半空壳卡片"旧口径；12.2 移除人为文件大小上限；附录 B 检查清单更新。
2. 协议与契约/09-社区平台API契约.md 升级 v1.3：5.4 上传接口更新为 card-transfer 控制面并明确旧 upload-sessions/upload/card 已移除；新增 7.0 架构边界（浏览器只查看、客户端统一传输模块、服务器只做控制面不代理字节）；7.1/7.2 上传/下载会话与 plan 契约；8 节移除"社区内 .card 回下载不在范围"口径；1.5 客户端来源与 CORS；2.2/2.3 认证响应补 refreshToken 明文。
3. 协议与接口标准/01-薯片协议规范.md 新增"社区卡片传输边界"章节：能力边界（浏览器只查看、客户端统一模块 upload/openRemote/download/getStatus/cancel 五个能力名）、文件字节链路、术语（完整离线卡片/网络资源卡片/可逆恢复清单）。
4. 协议与契约/10-资源打开契约.md：新增规则 4（单卡查看态运行时基于资源基准 URL 归一相对资源标识，社区 Web 导出 HTML 必须得到可访问的绝对 resource.resourceId；BookCardOpenPayload 以顶层 filePath/resourceId 为实际读取入口；基础卡片发起意图时 resources.*.resourceId 保存卡片根相对路径）——收口 C14 的契约依据。
5. 插件开发/03-卡片插件开发.md 与 组件库/07-复合卡片窗口与SDK显示链路标准.md：openResource.resourceId 与 payload.resources.*.resourceId 一律卡片根相对路径、实际可读 URL 由外层运行时归一；单卡文档运行时允许资源基准为相对 URL 并按当前文档 URL 归一。
6. 协议与契约/07-文件转换能力契约.md：card→html 导出结构 assets/ 下新增 theme/ 目录（主题文件资源存在时），主题 CSS 引用的根外资源必须复制并改写为导出目录相对 URL。
7. CardViewer manifest.yaml：description 更新为 Web target 只负责查看、Desktop/Mobile 客户端 target 可接入统一卡片传输模块打开社区卡片并下载完整卡片。

### C31 登记并收口工单122暗夜主题dist构建产物陈旧

本次提交按审核要求补建工单122 独立任务文件夹并登记处理结果（问题工单.md 工单122 段落随本提交 hunk 暂存）。

具体内容：

1. 新增 项目日志与笔记/工单122-暗夜主题dist构建产物陈旧导致SDK工具链质量门禁失败/任务说明.md：登记暗夜主题 dist 构建产物陈旧导致 SDK 工具链质量门禁失败的背景、证据、处理结果与验证。
2. 问题工单.md 工单122 段落随本提交暂存，工单状态标记已处理。

### C32 解决工单123网络资源卡片渲染缓存永不生成

本次提交按工单123 修复网络资源卡片渲染队列依赖 SHA256 导致查看缓存永不生成的问题（问题工单.md 工单123 段落随本提交 hunk 暂存）。

具体内容：

1. card-render-cache.service.ts 新增 resolveSourceVersion：sourceCardSha256 缺失时用 `network-card:<sourceCardKey>` 作为缓存版本指纹；createCacheVersion 入参扩展 sourceCardKey；enqueueForCard/findReadyCache/renderViewJob/renderCoverJob 移除 sourceCardSha256 必填校验（只要求 bucket+key），落库统一写入 resolveSourceVersion 结果。
2. 新增 card-render-cache.service.test.ts：无 sourceCardSha256 的网络资源卡仍能创建 job（指纹落库 network-card:<key>）并推 Redis；bucket/key 缺失返回 null。
3. env.ts：CARD_RENDERER_VERSION 默认值升级到 community-card-renderer-4（renderer-2 主题字体等根外资源复制进产物、renderer-3 相对资源基准 URL 归一、renderer-4 视频/电子书基础卡片最新 resource.open payload）。
4. 技术文档 07/09 同步渲染缓存版本演进与 Range 验证说明。

### C33 工单121社区API改造为控制面

本次提交按工单121 与 task027 阶段3，把社区 API 改造为控制面：服务器不接收大文件字节，只签发对象地址、保存元数据并调度缓存生成。

具体内容：

1. 新增 schemas/card-transfer.schemas.ts：上传/下载会话、objects presign、complete、download plan 的 zod schema 与类型导出。
2. 新增 routes/card-transfer.ts：POST upload-sessions（创建会话 + 预创建卡片，clientMetadata 记录 transferKind/cardId/versionId/resourcePrefix/networkCardObjectKey，对象键布局 cards/:cardId/versions/:sessionId/{resources,network-card/card.card}）；POST objects:presign（按 role 计算对象键并签发 PUT 地址，携带 x-amz-meta 头）；POST complete（headObject 逐个校验已上传对象大小，更新卡片 sourceCard* 字段、cardMetadata/cardStructure/resourceManifest（schemaVersion 1.0.0、mode network-resource-card、restoreManifest）、publishedByClient、status=ready，并 enqueue view/cover 缓存）；POST abort；POST download-sessions 与 GET plan（下载计划只返回对象存储 URL + 预签名 GET（responseContentDisposition attachment）与恢复清单，不流字节）。
3. requireJsonBody + 415：card-transfer 全路由只接受 application/json，octet-stream 卡字节被 415 拒绝，API 进程不经过文件字节。
4. upload.ts 移除旧 /upload-sessions、/upload-sessions/:id/resources/presign、/upload-sessions/:id/card、/upload/card 与 receiveProcessedCardUpload/persistProcessedCard 辅助函数，只保留 /upload/box；upload.test.ts 重写为 box 成功、.card 拒绝、旧端点 404 三用例。
5. app.ts 注册 cardTransferRoutes，multipart fileSize 限制改为仅 MAX_BOX_SIZE_MB；error-handler.plugin.ts 新增 FST_ERR_CTP_INVALID_MEDIA_TYPE/415 → VALIDATION_ERROR 映射。
6. s3.ts 新增 createPresignedGetUrl（手写 AWS4-HMAC-SHA256 签名、UNSIGNED-PAYLOAD、可选 response-content-disposition）；s3.test.ts 补充算法/过期/文件名用例。
7. 新增 card-transfer.test.ts：创建会话、presign 键布局、complete 校验与双 enqueue、恢复清单缺 schemaVersion 400、对象键逃逸 400、下载计划（attachment 文件名）、二进制提交 415 共 6+ 用例。

### C34 工单121统一卡片传输模块能力与契约

本次提交按工单121 与 task027 阶段1，把 CommunityUploader 重构为社区卡片统一传输模块 community.card.transfer 的能力与契约部分。

具体内容：

1. manifest.yaml：provides 由 community.card.publish 改为 community.card.transfer 的 upload/download/openRemote 三方法（mode: job，各挂 input/output schema）；permissions 新增 platform.external/plugin.manage/window.control/zip.manage；runtime.targets.mobile.supported 改为 true；description 更新为"上传、网络资源卡片生成、远程打开和完整卡片下载恢复的官方社区卡片传输模块"。
2. 新增 contracts/upload|download|openRemote.input|output.schema.json 六个契约；删除 publish.input/output.schema.json（内容归档到 contracts/归档/20260805-publish能力归档/）。
3. types.ts：新增 CommunityCardTransferServer/ClientInfo、Upload/Download/OpenRemote 请求与结果类型、HostZipEntryMeta；index.ts 改为 upload/download/openRemote 三方法转发；错误码 COMMUNITY_UPLOADER_* 全部改为 COMMUNITY_TRANSFER_*。
4. publisher.ts 网络层：新增 sleep、isTransientHttpStatus、isAuthExpiredStatus、fetchWithRetry（指数退避、5xx/408/429 重试最多 3 次、401/403 抛 AUTH_EXPIRED、每次前检查 AbortSignal、网络失败抛 NETWORK_FAILED）、fetchControlPlane、writeBinaryFile；buildApiUrl 支持 apiPath 自带 /api/v1 前缀去重。
5. 新增技术文档/01-社区卡片传输模块技术方案.md（能力、运行边界、错误码、进度阶段、权限说明、第三方依赖评审：运行期唯一依赖 yaml@^2.8.2）；README 更新安装与使用说明。

### C35 工单121无压缩归档改写恢复核心与上传主流程

本次提交按工单121 与 task027 阶段2/4，实现无压缩归档的读取、URL 改写与恢复核心，以及客户端上传主流程（publisher.ts 仅暂存上传链路 hunk，下载 hunk 随提交 C38）。

具体内容：

1. publisher.ts 上传链路：uploadCommunityCard 流程 inspect（card.readInfo + zip.list 并行 + assertZipStoreEntries 拒绝非 Store 压缩）→ unpack → scan（collectResources 只收集元数据，size 优先 file.stat，不整包读入内存）→ session（createUploadSession）→ upload-resources（presignObjects 按批申请 PUT 地址，逐资源读字节直传）→ rewrite（collectTextResourceMap/collectRichTextContentFileMap 收集 file-backed 富文本，writeRewardedCard 把 content YAML 与 cover.html 的相对路径改写为对象存储/CDN URL，富文本内联为 content_source:inline + content_text）→ pack（card.pack 生成网络资源卡片）→ submit（presign network-card 对象并直传，completeUpload 提交资源清单与可逆恢复清单）；进度阶段 inspect→unpack→scan→session→upload-resources→rewrite→pack→submit→completed 经 job.reportProgress 上报。
2. card-rewrite.ts：replaceYamlResourceUrls/restoreYamlResourcePaths（含富文本 file-backed 内容内联与还原）、replaceCoverHtmlUrls/restoreCoverHtmlPaths、resolveOriginalPath（保留 ?/# 后缀）；createRestoreManifest 记录 schemaVersion 1.0.0、原始 ZIP 条目顺序/CRC/压缩方式/修改时间与资源清单；extractEntryPlan 按顺序提取 path+modifiedTime 供 card.pack 恢复。
3. 新增 card-rewrite.test.ts：候选路径收集、内联重写、恢复还原、无映射保持 inline、普通资源字段 round trip 共 6 用例。
4. module-definition.test.ts 上传用例重写：控制面 URL、objects:presign、complete 提交、restoreManifest 结构与网络卡片直传断言，并新增富文本内联上传用例。

### C36 工单121Host ZIP Store支持条目顺序与修改时间恢复

本次提交按工单121 与 task027 阶段2/8，让 Host ZIP Store 打包支持恢复原始条目顺序与修改时间，并新增黄金回环验证（card-service.ts 仅暂存 pack 相关 hunk，渲染帧 hunk 随提交 C42）。

具体内容：

1. zip-service/types.ts 新增 ZipEntryPlan {path, modifiedTime?}；zip-store.ts 新增 applyEntryPlan（按 plan 顺序输出条目、覆盖 modifiedTime、未在 plan 中的文件字典序追加），compress 支持 options.entryPlan；index.ts 导出 ZipEntryPlan。
2. card-packer.ts pack 支持 options.entryPlan 并透传 packUntilSizeStabilizes；card-service.ts pack 透传 entryPlan。
3. register-host-services.ts：card.pack 与 zip.compress 路由入参新增 entryPlan 并透传服务层。
4. zip-store.test.ts 新增 entryPlan 顺序与修改时间保留用例。
5. 新增 card-transfer-gold-loop.test.ts：完整离线卡片 → 网络资源卡片（URL 改写 + 删除包内资源）→ 完整离线卡片（恢复相对路径 + 写回资源字节 + 按 entryPlan 重新打包）黄金回环，断言文件路径与条目顺序一致、资源字节一致、配置 URL 还原、封面引用还原、条目修改时间一致、Store 模式一致（compressionMethod 0 且 compressedSize==size）。

### C37 工单121SDK新增communityCardTransfer类型化封装

本次提交按工单121 与 task027 阶段1（P1-05），为统一卡片传输模块提供 SDK 类型化封装。

具体内容：

1. 新增 src/api/community-card-transfer.ts：COMMUNITY_CARD_TRANSFER_CAPABILITY 常量与 CommunityCardTransferServer/ClientInfo/Upload/Download/OpenRemote 输入输出类型；upload/download/openRemote 经 module.invoke 调用，getStatus/cancel 经 module.job.get/cancel 承载。
2. core/client.ts 注入 communityCardTransfer；index.ts 导出类型与能力常量；types/client.ts 的 Client 接口新增字段。
3. package.json test 脚本加入新测试文件；新增 tests/community-card-transfer.test.ts：断言 upload/download 的 module.invoke 完整载荷（capability/method/input 透传）。

### C38 工单121客户端下载与远程打开链路

本次提交按工单121 与 task027 阶段6（模块侧），实现客户端下载完整卡片与远程打开社区卡片链路（publisher.ts 仅暂存下载/远程打开 hunk）。

具体内容：

1. publisher.ts 下载链路 downloadCommunityCard：createDownloadSession → getDownloadPlan（解析 networkCard/resources/restoreManifest）→ downloadBinary 逐个下载（fetchWithRetry 指数退避、AbortSignal 取消、网络失败可重试）→ card.unpack → restoreDownloadedCard（restoreYamlResourcePaths/restoreCoverHtmlPaths 恢复相对路径、富文本按恢复清单还原 file-backed、资源字节写回原始相对路径）→ extractEntryPlan → card.pack 按 ZIP Store 与原始条目顺序重新打包 → verifyRestoredCard（card.readInfo/card.render/card.open 依次验证，失败抛 DOWNLOAD_VERIFY_FAILED 并清理输出路径）。
2. publisher.ts 远程打开链路 openRemoteCommunityCard：校验 cardId + server → 创建下载会话取 plan → 下载网络卡片到 os.tmpdir() 临时工作区 → surface.open 启动 com.chips.card-viewer（kind window、target plugin、launchParams cardSource local-file + communityServer 透传、presentation 1024x720）→ 返回 opened/url/localCardPath/surfaceId。
3. module-definition.test.ts 新增下载恢复全流程（还原 source:file/file_path、封面引用、entryPlan 断言、字节写回、清理）与 openRemote（surface.open 载荷断言）用例；新增瞬时失败重试与 401 鉴权过期用例。
4. 下载失败时删除输出路径，不留下看似成功的 .card。

### C39 工单121网页查看缓存与Web CardViewer会话

本次提交按工单121 与 task027 阶段5，让社区卡片详情页只加载查看缓存并接入 Web CardViewer 会话，同时提供客户端下载引导。

具体内容：

1. DocumentPluginRoutePage.tsx/css：新增 pending prop 与独立 pending 渲染分支（spinner + pendingLabel）；error/sessionError 与无 source 分支拆开；source.kind === community-card 时渲染下载提示条（role="note"，"请在薯片客户端中打开并下载"）。
2. CardDetailPage 在 viewState 为 cache_ready 时重新拉取一次 open-view（保证 cardSource 携带最新 coverUrl/coverRatio），并透传 pending 状态。
3. i18n 双语新增 card.downloadInClientHint 等文案。
4. 技术文档 05/08 同步 open-view 补排、受控封面与 Range 代理约束说明。

### C40 工单121移除社区网页浏览器上传链路

本次提交按工单121 与 task027 阶段7，移除社区网页浏览器上传组件与打包/下载假设，工作区改为客户端发布信息页。

具体内容：

1. 删除 UploadDropzone.tsx/UploadQueue.tsx/UploadQueueItem.tsx（归档到 packages/web/归档/20260805-浏览器上传组件归档/）。
2. WorkspacePage.tsx 重写为"客户端发布"信息页：移除上传队列状态机、WORKSPACE_UPLOAD_CONCURRENCY 并发调度与 pollCardRenderUntilSettled 轮询，改为图标 + 三步说明卡片（创建/发布/查看）；WorkspacePage.css 删除 dropzone/queue 样式并新增 client-publish 三步网格（移动端折叠为单列）。
3. api/content.ts 删除 UploadSessionResponse/CardUploadResponse 与 uploadCard/uploadBox；lib/ui.ts 删除 getUploadTypeFromFile；types/community.ts 删除上传队列类型。
4. i18n 双语删除 19 条 workspace 上传文案，新增 workspace.clientOnly* 系列与 card.downloadInClientHint。
5. 全仓 grep 确认无浏览器上传/下载 .card 入口与"空壳"旧口径残留。

### C41 工单121卡片查看器接入客户端下载链路

本次提交按工单121 与 task027 阶段6（应用侧），让卡片查看器接入统一传输模块的下载能力并统一 document-flow 外壳（含旧 ViewerSourceProvider 归档）。

具体内容：

1. manifest.yaml：permissions 新增 module.invoke/network.request/platform.external；runtime.targets.mobile.supported 改为 true；description 更新（Web 只查看，Desktop/Mobile 可下载）。
2. AppRuntimeProvider.tsx：新增 resolveCommunityTransferServer（从 launchParams.communityServer 提取 baseUrl/accessToken）；新增 canDownloadActiveCard 与 downloadActiveCard——打开目标为 community-card 且携带 communityServer 时经 client.platform.saveFile 选择输出路径、client.communityCardTransfer.download 下载、client.platform.showMessage 反馈（该能力当前为暂留接口，后续继续开发）；surfaceMode 改为 openedTarget ? document : immersive 并同步 data-chips-surface-mode 属性；openFile/openFilePath 增加 kind !== file 守卫。
3. AppShell.tsx：新增下载浮动按钮（仅 canDownloadActiveCard 时渲染）与外部 chrome 动作 download-card/toggle-cover；usesExternalChrome 时包 ViewerChrome.Provider（plugin.chrome.update 载荷 + plugin.chrome.action 回传）。
4. CardWindow.tsx/css：导入 DocumentWindowResizePayload 修复缺失导入；document-flow 视口 CSS 变量与 frame-host 容器，loading/error overlay 移入视口内部并条件渲染；iframe 背景 token 化（--chips-sys-color-surface）。
5. ViewerStage.tsx 新增 client prop 并透传；i18n 双语新增 downloadCard/downloadingCard/downloadComplete/downloadFailed/downloadUnavailable 五条。
6. tsconfig 排除归档目录；将 ViewerSourceProvider.tsx 归档到 归档/20260805-旧ViewerSourceProvider/。
7. tests：app.test.tsx 新增 surface-mode 切换断言与社区托管文档 chrome 协议用例；e2e/basic-flow、hosted-document-window（高度期望同步）、viewer-cover-surface（client.events mock）更新；技术文档/卡片查看器应用架构与验收矩阵.md 同步 document/immersive surface 与外部 chrome 职责。

### C42 归一basecard导出单卡片文档运行时资源基址

本次提交让基础卡片导出文档在社区缓存等外部域运行时，按当前文档 URL 归一相对资源基址与 resource-open 资源标识（实现于 08-05 收口，测试用例 06-30 先行编写；card-service.ts 仅暂存渲染帧 hunk，pack hunk 已随 C36）。

具体内容：

1. card-service.ts createBasecardFrameDocument：新增 resolveRuntimeResourceBaseUrl（相对 base 按 window.location.href 解析，异常返回空）；resolveResourceUrl 重构为基于运行时基址解析；新增 resolveOpenResourceId（完整 URL 直通，相对路径按运行时基址归一）；openResource 事件 resourceId 字段改用归一化后的完整 URL。
2. card-service-rendering.test.ts：新增用例模拟社区缓存域（JSDOM URL https://community.example/cache/gallery.html）验证图片 src 解析为绝对 URL、resource-open 消息携带完整 resourceId 与 nodeId/intent；修复既有断言 TS 类型收窄。

### C43 解决工单125应用插件渲染进程访问社区API的CORS缺口

本次提交按工单125 放行应用插件渲染进程 Origin 访问社区 HTTP API（问题工单.md 工单125 段落随本提交 hunk 暂存）。

具体内容：

1. env.ts 新增 CORS_EXTRA_ORIGINS 环境变量（逗号分隔白名单，默认空，生产生效）。
2. app.ts 新增 isClientCorsOriginAllowed：chips-render: 前缀 Origin 放行、null Origin（file:// 页面）放行、命中 CORS_EXTRA_ORIGINS 白名单放行；CORS 插件回调合并该判定。
3. 新增 auth.test.ts 的 isClientCorsOriginAllowed 四用例（chips-render 前缀/null/白名单/拒绝未知）。
4. 09-社区平台API契约 1.5 节客户端来源与 CORS 描述随 C30 已收口（本提交只含代码实现）。

### C44 社区认证响应补发refreshToken明文

本次提交让社区认证接口向非浏览器客户端补发 refreshToken，支撑客户端会话恢复。

具体内容：

1. auth.ts：register/login 响应 data 新增 refreshToken 明文（原仅 accessToken + HttpOnly Cookie，客户端无法读取 Cookie）。
2. 新增 auth.test.ts 注册/登录用例：断言 data.accessToken/data.refreshToken 明文返回且 chips_refresh_token Cookie 仍存在。

### C45 解决工单124应用插件脚手架复制模板元数据

本次提交按工单124 修复应用插件脚手架把模板元数据 template.json 复制进生成工程的问题（问题工单.md 工单124 段落随本提交 hunk 暂存）。

具体内容：

1. template-engine.ts：新增 SCAFFOLD_METADATA_FILE_NAMES（template.json，可扩展集合）；copyTemplateDir 遍历时文件级排除模板元数据文件，不参与渲染/复制与 filesCreated 计数。
2. template-engine.test.ts：断言生成工程 targetDir 下 template.json 不存在（ENOENT）。
3. run-generated-e2e.mjs：生成工程校验新增 template.json 存在即抛错，防止回归。

### C46 收口工单126社区网页版页脚渲染范围与本人判定

本次提交按工单126 收口网页版可处理的页脚渲染范围与个人主页本人判定（顶部 SiteHeader 挂载仍待处理，客户端同构导航已由社区客户端承担；App.tsx/ProfilePage.tsx 仅暂存本工单 hunk，视觉 hunk 已随 C15；问题工单.md 工单126 段落随本提交）。

具体内容：

1. App.tsx：新增 isProfileRoute（^/@[^/]+/?$ 匹配个人社区页），AppChrome 的 SiteFooter 只在欢迎/简介/认证/工作区页渲染，个人社区页不再渲染页脚。
2. ProfilePage.tsx：新增 isProfilePending（state.loading || isAuthLoading），本人判定（设置/上传/管理入口显示）必须等待登录态恢复与公开资料请求都完成，避免刷新时误渲染成访客视图。
3. 技术文档 02/03/06 与需求文档 02/03 同步页脚渲染范围与本人判定口径。

### C47 修订AGENTS.md第三方库引入政策

本次提交修订工作区 AGENTS.md 的第三方库引入政策与禁止事项口径。

具体内容：

1. 第三方库政策由"不要引入未在现有技术选型中明确使用的第三方库；如确有必要，先记录并等待用户决策"放宽为"可以在任务范围内适当引入第三方库；引入前必须核对现有技术选型、许可证、维护状态、包体影响、安全风险和跨端兼容性；若会改变基础架构、长期运行时依赖或公共协议，先记录方案并等待用户确认"。
2. "禁止做的事"第 12 条同步该口径（未经选型评估不引入、涉及架构或长期运行时依赖先询问）。
3. 文件末尾补换行。

### C48 新增薯片社区客户端应用插件

本次提交新增薯片生态官方社区客户端应用插件 com.chips.community-client（task027 浏览器只查看、客户端统一传输链路的客户端落地）。

具体内容：

1. 新增 Chips-CommunityClient/（type: app，desktop supported）：manifest.yaml（19 项最小权限、cli.commands chips-community open）、chips.config.mjs、index.html（CSP 放行 http/https/file/blob）、config/、i18n 双语字典、assets/icons。
2. 页面与组件：欢迎页/关于页/登录注册/个人主页（ProfileHero/WorkGrid/WorkTile/ProfileSettingsDialog）/工作区（真实 .card 批量上传入口）/箱子详情/设置页（服务器地址、语言、账号退出）/admin 外部浏览器重定向；SiteHeader/SiteFooter 同构顶部导航并挂载到 AppShell（工单126 客户端侧暂行处理）；9 条 HashRouter 路由与 ProtectedRoute/GuestRoute 守卫。
3. 认证会话：refreshToken 存 Host credential（community.refreshToken）、accessToken 仅内存，401 自动刷新队列合并并发，启动 tryRestore 恢复会话。
4. 卡片传输：统一经 SDK communityCardTransfer（openRemote/upload/download），1.2s 轮询模块 job、30 分钟超时，进度与错误码归一；卡片点击由本地卡片查看器打开；服务器地址可配置并持久化。
5. 工程规范：3 条应用命令（openWorkspace/openSettings/refreshTheme）与 CommandRouterBridge；多语言与主题系统接入（--ccps-* token 移植）；4 个单元测试（app/i18n、命令契约、传输服务、服务器配置）+ e2e smoke；6 份技术文档；脚手架基线场景视图与卡片详情页已归档；构建产物 dist 由忽略规则排除。

### C49 屏蔽本地运行日志与PID文件进入版本库

本次提交补充 .codex-runtime-logs 运行日志与 PID 文件的忽略规则。

具体内容：

1. 根 .gitignore 与 Chips-CommunityPlatformServer/.gitignore 新增 .codex-runtime-logs/ 忽略规则（此前仅忽略 .log，.pid 文件未忽略导致整个目录显示为未跟踪）。
2. 被忽略内容不进入任何提交。

### C50 归档社区服务器调试残留测试文件

本次提交按"禁止删除"原则，将社区服务器调试残留测试文件移入归档目录。

具体内容：

1. 将 packages/server/src/routes/__dbg3.test.ts（console.log 探测 redis mock 的调试残留）移动到 packages/server/src/routes/归档/ 下，正式测试集不再包含该文件。

### C51 屏蔽全仓.DS_Store元数据文件

本次提交按用户要求屏蔽全部 .DS_Store 元数据文件。

具体内容：

1. 根 .gitignore 新增 .DS_Store 忽略规则。
2. git rm --cached 移除已跟踪的 .DS_Store（生态共用技术文档/.DS_Store 等），保留工作区文件本身。
3. 各提交中不包含任何 .DS_Store 变更。

---

## 三、不提交清单

- 各项目 node_modules/dist/cpk 等已被 .gitignore 覆盖的内容。
- .codex-runtime-logs/（两处，已由 C49 屏蔽）。
- .DS_Store（全部，已由 C51 屏蔽）。
- 本目录 task028-整理git仓库/（分析过程文件，如需入库由用户另行决定）。

## 四、执行方式（待审核后执行）

1. 按 C01→C51 顺序逐个提交：`git add <文件列表>`，多任务同文件使用 `git add -p` 按 hunk 精确暂存（问题工单.md、host-runtime.ts、App.tsx、ProfilePage.tsx、card-service.ts、publisher.ts、i18n 字典）。
2. 每个提交使用对应中文提交信息（标题 + "本次提交…" 正文）。
3. 全部提交完成后再由用户决定是否 push。
