# 分析报告：Chips-CommunityPlatformServer（2026-06-29 ~ 08-05）

## 总体情况
- 全部改动未提交。diff 7317 行（pnpm-lock 占约 2700 行）。
- 时间跨度：06-29（工单118）→ 07-06（Web 宿主页面重构）→ 08-05（task027 阶段3/5/7 + 工单125/126）。

## 任务簇 1：工单118 本地启动链路修复（06-29~30）
- box.service.ts：修复语法回归，enrichCardRefs 从逐条 findFirst 重构为一次 findMany + Map 匹配（消除 N+1）。
- pnpm-workspace.yaml 新增 ../Chips-Host；新增 .npmrc（link-workspace-packages=true）。
- packages/server/package.json：chips-host 改 0.1.0 版本号，移除 4 个 file: 插件依赖。
- pnpm-lock.yaml 同步。根 package.json 新增 start:local 等委托脚本。
- scripts/start-local-dev.sh（478 行，07-06 编写）：一键启动 Docker 基础设施 + server/worker/web/admin，支持 stop/status/logs。
- 技术文档/07 补充依赖安装与一键启动章节。

## 任务簇 2：工单119 Web 宿主辅助资源读取（06-30 + 07-06）
- host-runtime.ts bootstrap：resource.resolve 支持同源绝对路径；新增 resource.readBinary/readMetadata 本地实现（本地文件句柄或 fetch + 字节数组载荷）。
- host-runtime.test.ts（新增 375 行）：JSDOM eval bootstrap + mock fetch 验证三接口。

## 任务簇 3：工单120 Web 宿主主题 CSS 与图标字体注入（06-30 + 07-06）
- host-runtime.ts：entry 注入 theme.css 外链；新增 /plugin-sessions/:id/theme.css 与 /theme-assets/:themeId/* 路由；bootstrap 注入 themeRuntime 并支持 theme.getCurrent/getAllCss/theme.resolve；cpx→vw 转换（1024 基准）。
- host-integration.ts：WebThemeRuntimeView、getWebThemeRuntime()（并行调内核主题三接口）、resolveWebThemeAssetPath（逃逸校验）、rewriteWebThemeCssAssetUrls（file:// → 受控路由，保留 ?/# 后缀）。
- 技术文档/08 补充主题注入规则 6 条。

## 任务簇 4：工单125 客户端 Origin CORS（08-05）
- env.ts 新增 CORS_EXTRA_ORIGINS；app.ts 新增 isClientCorsOriginAllowed（chips-render: 前缀 / null Origin / 白名单放行）。
- auth.test.ts 新增 4 用例。

## 任务簇 5：工单121 + 阶段3 card-transfer 控制面（08-05）
- card-transfer.schemas.ts（新增）：上传/下载会话、presign、complete、plan 的 zod schema。
- card-transfer.ts（新增 497 行）：upload-sessions/objects:presign/complete/abort/download-sessions/plan 六路由；对象键布局 cards/:cardId/versions/:sessionId/{resources,network-card/card.card}；headObject 校验；下载计划只返回预签名 GET（attachment 文件名）。
- app.ts 注册路由 + multipart 上限改为仅 MAX_BOX_SIZE_MB；error-handler 415 映射。
- s3.ts 新增 createPresignedGetUrl（手写 AWS4-HMAC-SHA256）；s3.test.ts 补充用例。
- upload.ts 大删：移除 upload-sessions/resources presign/card、upload/card；只保留 /upload/box。upload.test.ts 重写。
- card-transfer.test.ts（新增 613 行，6 用例）。

## 任务簇 6：阶段5 查看缓存 + Range 代理（07-06 + 08-05）
- cards.ts：open-view 未命中补排（view_miss priority 10）、受控封面入口、Range 头归一与 sendRenderCacheObject（206/content-range/etag）、删除重复函数定义。
- card-render-cache.service.ts（08-05）：resolveSourceVersion——无 SHA256 用 network-card:<key> 指纹（工单123）；移除 sourceCardSha256 必填；streamPrivateCache 支持 range。
- s3.ts getObjectStream 透传 Range；env.ts CARD_RENDERER_VERSION 升到 community-card-renderer-4。
- CardDetailPage.tsx：cache_ready 时重新拉取 open-view；DocumentPluginRoutePage 新增 pending 分支与下载提示条。

## 任务簇 7：阶段7 移除浏览器上传（08-05）
- 删除 UploadDropzone/UploadQueue/UploadQueueItem（归档到 packages/web/归档/20260805-浏览器上传组件归档/）。
- WorkspacePage 重写为"客户端发布"信息页（45 行）；WorkspacePage.css 新增 client-publish 三步网格。
- content.ts 删除 uploadCard/uploadBox；ui.ts 删除 getUploadTypeFromFile；community.ts 删除上传队列类型。
- i18n 删除 19 条 workspace 上传文案，新增 clientOnly* 与 card.downloadInClientHint。

## 任务簇 8：认证响应补 refreshToken（08-05）
- auth.ts：register/login 响应 data 新增 refreshToken 明文。
- auth.test.ts（新增 150 行）：双 token 断言 + Cookie 仍在。

## 任务簇 9：工单126 网页端页脚范围（08-05）
- App.tsx：isProfileRoute 正则匹配 /@username/，SiteFooter 不在个人社区页渲染。
- ProfilePage.tsx：isProfilePending 等待登录态恢复再做本人判定。

## 任务簇 10：Web 页面视觉收口（07-06 + 08-05 朴素白）
- HomePage：移除 glow 光斑与玻璃拟态，纯白背景。
- ProfilePage：删除色幕/光晕/keyframes，封面阴影 token 化（--ccps-sys-shadow-card*）。
- runtime.css：nectar-dusk 改浅色（accent #2f7fff），新增阴影参考/系统 token，body 纯色。
- registry.ts：detectPreferredTheme 固定 pear-mist，nectar-dusk colorScheme 改 light。

## 任务簇 11：Web 资源打开与宿主面交互（07-06）
- lib/host-runtime.ts：HostResourceOpenRequest.resource 新增 payload 字段。
- HostedPluginSurface.tsx：透传 resource.payload；focusOpenedTab；openHostedRoute/openExternalUrl 新增 preferCurrentPage；window.open 移除 noopener,noreferrer。

## 任务簇 12：web-resource-open-plan 模块化（06-30）
- web-resource-open-plan.ts（新增 185 行）：从 host-integration 抽出纯函数模块，MIME 扩展表 40+ 扩展名（音频/视频/电子书/图片），resolveWebResourceOpenPlanFromPlugins 生成能力候选。
- host-integration.ts 对应删除内联实现并 re-export；defaultAppPlugins 新增 MusicPlayer/VideoPlayer/BookReader。
- web-resource-open-plan.test.ts（新增 5 用例）。

## 遗留项
- __dbg3.test.ts（8 行）：console.log 探测 redis mock 的调试残留，建议归档不提交。
- 8 个技术/需求文档同步更新（02/03/05/06/07/08/09 + 需求 02/03/05），归属各自任务簇。
