# 分报告 06：历史背景与工具链问题

> 调查日期：2026-08-06
> 调查对象：`项目日志与笔记/问题工单.md`（124 条工单）、`task022`、`task023`、`Chips-SDK`、`Chips-Scaffold`、组件库开发计划
> 关键结论：task022 完成的是"自动化条件发布"基线（71 contract / 802 token / 256 测试 / 9 步门禁）；task023 只执行了主题包升级与设置面板改版，壳层组件与视觉回归门禁未落地；组件库重构的核心输入是工单094（技术栈口径决策 + 门禁真实化）+ 工单114/task023（壳层与视觉回归）。

---

## 一、与组件库/前端相关的工单汇总

### 未闭环（重构范围）

| 工单 | 标题 | 状态 | 核心问题 |
|---|---|---|---|
| 工单094 | 组件库冻结技术栈与质量门禁覆盖漂移 | 待处理（2026-08-02 复核） | 冻结技术栈零落地、门禁浅层化、16999 行单文件、contract 分层；**2026-08-02 复核已明确 shadcn/ui 路线结论：不以 shadcn/ui 全量重构 Chips L10，保留 contract/token/theme/i18n/Host 边界，只借鉴源码组织/compound API/registry 流程；改选 Base UI/Radix/React Aria 需另立架构选型工单** |
| 工单114 | 前端框架视觉产品化与高级感闭环缺口 | 处理中 | 缺 L11 主题视觉产品化、L12 应用壳层、视觉回归门禁；第一版不得宣称高级视觉语言完成 |
| 工单116 | SDKCliCommand类型错误阻断应用插件typecheck | 处理中 | chips-sdk index.d.ts `export * from './src/index'` 穿透源码，5 处类型错误阻断所有应用插件 verify |
| 工单111 | chipsdevBuild未识别主题包正式构建脚本 | 处理中 | chipsdev build 对 type:theme 推到 src/main.ts 失败，未委托主题包 npm run build |
| 工单067 | 图标系统规范与运行时实现未收口 | 待处理 | 运行时 UI 图标系统未按冻结规范正式落地 |
| 工单130 | 布局插件iframe文档缺少主题图标字体注入 | 待处理 | box-service 布局 view/editor 文档缺 @font-face，ChipsIcon 显示文本 |
| 工单047 | 基础卡片插件脚手架契约漂移 | 待处理 | 与工单042 同主题的脚手架三方契约漂移 |
| 工单096 | 社区服务器pnpm工作区缺少组件库本地包映射 | 待处理 | 社区 pnpm workspace 未映射 @chips/component-library 本地包 |

### 已解决（历史教训）

工单012/013（主题插件运行时接线）、014（卡片查看器主题变量断裂）、015（渲染服务硬编码主题）、019-025（工作区主题隔离/初始化/窗口背景/刷新/浅色写死/启用过滤/开发命令）、032（编辑引擎主题与多语言运行时未接入）、042（基础卡片脚手架）、054（卡片转HTML主题/语言覆盖）、063（iframe srcdoc CSP）、075（社区前端主题词典）、088（模块颜色选取器）、097（ThemeRuntime 组件层 token 引用顺序）、102（sys 同层 token 引用）、103（归档 contract 污染矩阵）、107（Rating contract token 缺失）、112（cpx 未转换排版失效）、115（DataGrid key）、120（Web 宿主主题注入）、122（暗色主题 dist 过期缺 538 token——**反复出现类问题，本次又发现**）。

## 二、task022 规划与完成情况

- 规划：32 个阶段任务分 A-F 六阶段（契约与运行时地基 → 开发默认路径（脚手架/布局原语/SwiftUI 对标/Compound/State）→ 应用级交互（导航命令/焦点 a11y/动效/主题契约/预览矩阵）→ 规模化工具（Web 同化/设置面板/示例迁移）→ 全生态迁移（应用/卡片/布局/模块/主题包/脚手架/社区平台/安装发布/回归）→ 最终验收），落地为 59+ 工单级任务；
- 完成：任务058 为最后一个任务（058.01-058.05 完成），任务059 取消；
- **完成口径："自动化条件发布"**，不是"对外正式发布"；正式对外发布前仍需人工系统验收（签名/公证/真实安装/LaunchServices/双击 .card/.box）；
- 明确不属于 task022：正式对外发布、视觉产品化（工单114/task023）、分享平台、更多主题；
- 组件库最终基线：71 contract、802 token keys、254 组件测试、9/9 quality gate 步骤 passed。

## 三、task023 执行现状

- 规划：6 阶段（冻结视觉目标与样张 → 主题包视觉 token 升级 → 组件库产品级壳层 → 应用私有样式治理 → 视觉回归门禁 → 文档沉淀），拆 9 个任务；
- 2026-05-26 收窄口径：本轮只做①主题包 token/CSS 升级；②应用私有控件替换为组件库已有正式组件；组件库没有的只登记"缺少的组件.md"，不临时实现；
- **执行结果**：
  - ✅ 主题包侧：视觉设计语言 14 篇文档 + 样式标准示范.html；冷白微体积视觉 token 体系；两个主题包五层 token 完备；
  - ✅ 设置面板改版（系统设置式界面层级）；
  - ❌ **组件库产品级壳层未落地**（全部只登记在"缺少的组件.md"）；
  - ❌ **视觉回归门禁未建立**（无截图/阈值/评审清单）；
  - ❌ 工单114 仍"处理中"；
  - 无工作日志目录。

## 四、缺少的组件清单（task023 记录，18 项）

ChipsAppShell、ChipsSettingsShell、ChipsMediaShell、ChipsReaderShell、ChipsWorkbenchShell、ChipsStatusBar、ChipsCommandDock、ChipsActivityBar、ChipsResourcePanel/ChipsFileManager、ChipsFileDropZone、ChipsPropertyGrid、ChipsInspectorField、ChipsTaskProgressCenter、ChipsTimeline/ChipsMediaTimeline、ChipsVolumeControl、ChipsCropEditor/ChipsCropDialog、ChipsAssetGrid/ChipsTemplateGrid、ChipsPreviewPane。

（另有 16 类已正式导出组件确认不需登记：Button 族、Text/Label/Icon、Badge/Tag、输入族、数值族、选择族、日期族、Dialog/Popover/Menu/Tooltip、Tabs、Toolbar 等命令消费族、Form、DataGrid/Tree/VirtualList、布局族、SplitPane 等工作台部件、Image/Media/ErrorState、系统 UX 族。）

## 五、SDK 工具链支持现状

| 项 | 现状 |
|---|---|
| SDK scripts | 仅 test 一个正式脚本（vitest 8 文件 + 12 个 run-cli 串行 runner） |
| mock-host.ts（1204 行） | 覆盖 theme/i18n/surface/command/cli.task/platform/control-plane；**theme mock 为静态字符串（默认 `:root{--chips-sys-color-surface:#ffffff}`）+ 空 tokens + 空 contract，无 cpx 转换、无字体注入、无解析**；i18n.translate 缺失返回 key（与真实 Host 抛错不一致）；**不 mock card/document/resource/box/file/association/transfer 域**（调用抛 SERVICE_NOT_FOUND） |
| src/api/theme.ts | ThemeApi 6 动作 + onChanged，类型含 ThemeDiagnostic/ThemeContractView/ResolvedTheme 等，与 Host 契约一致 |
| src/api/i18n.ts | I18nApi 4 动作 + onChanged，与 Host 一致 |
| chipsdev CLI | component 只有 gallery 子命令（矩阵+报告摘要）；preview 支持 --target component；quality 汇总门禁报告；**无组件创建/代码生成命令** |
| chips.config.mjs | 模板 JSDoc 引用 `import('chips-dev-tools').ChipsConfig`，但**工作区不存在 chips-dev-tools 包或 ChipsConfig 类型声明——类型引用悬空** |
| chipsdev build | 优先委托工程 build 脚本，但工单111 证明对 type:theme 有识别缺口 |
| 脚手架 | app/basecard/boxlayout 模板均依赖 `@chips/component-library ^0.1.0`；module 模板不依赖；**无组件库自身脚手架/生成器** |

## 六、组件库开发计划完成度

- 阶段一~九：完成（架构契约、工程基线、token 流水线、Tamagui 适配、Ark 封装与 Primitive、Base-Interactive、Data-Form+Workbench、System-UX、测试体系与质量门禁）；
- 阶段十（发布与生态接入验收）：进行中——release-bundle/CHANGELOG/ROLLBACK/验收模板已落地；
- 阶段十一（卡片显示 iframe 高级组件专项）：进行中——CardCoverFrame/CompositeCardWindow 实现与契约已落地，但开发计划索引（2026-03-05）标注"真实 SDK 统一显示接口未交付受工单001阻断"**已过期**（工单012 已于 2026-03-10 解决，task029 已把 CardViewer 迁到 client.card.compositeWindow.render 统一链路，实际链路可用）。

## 七、对外承诺能力清单（生态共用技术文档/组件库/01-组件库对外使用总览.md 摘录）

1. 定位：生态统一无头组件标准实现；
2. 技术基线：React + Ark UI + Style Dictionary + Tamagui (Core)——**未兑现**；
3. 包结构：@chips/component-library 聚合三包；所有包提供正式 types/exports.types；禁止业务工程 declare module 补类型——**primitives/tokens 未兑现**；
4. 使用边界：禁止复制 Ark UI 内部实现、禁止硬编码组件视觉值；
5. 主题包要求：必须覆盖契约 token key、支持 data-scope/part/state 选择器、遵循六级权重链；
6. 组件交付：Display/Base/Interactive/Complex Compound/Data-Form/Command Consumers/Workbench/System-UX/Layout Primitives/iframe 组件全部宣称已交付；
7. React Environment Hooks：ChipsEnvironmentProvider/useChipsTheme/useChipsI18n/createChipsI18nText 等 + State/Binding/Form（createBinding/useBinding/useChipsFormState 等）宣称已交付；
8. 命令消费：命令组件以 Host command.* registry 为唯一动作来源，文案走 i18n key——**命令组件实际是每次渲染重新拉取 + 应用侧零消费**；
9. 统一文档显示链路：封面走 Host card.renderCover → SDK → CardCoverFrame；复合窗口仅 view|preview；应用层不得重复实现渲染引擎；
10. 测试边界：@chips/testing 主责契约断言/ARIA fixture；chips-sdk/testing 是应用插件正式 Host simulator 主发布位置。

## 八、问题清单

| # | 问题 | 证据 | 严重度 |
|---|---|---|---|
| 1 | 工单094 待处理（技术栈口径 + 门禁真实化 + 单文件拆分），是重构核心输入 | 问题工单.md | 高 |
| 2 | 工单114/task023 的壳层组件（18 项）与视觉回归门禁未落地 | 缺少的组件.md；task023 | 高 |
| 3 | SDK 类型入口穿透源码阻断应用插件 typecheck（工单116） | Chips-SDK/index.d.ts | 高 |
| 4 | SDK mock-host 与真实 Host 行为漂移（i18n translate 返回 key vs 抛错；theme 无转换/字体） | mock-host.ts vs register-host-services.ts | 中 |
| 5 | chipsdev 无组件生成命令；chips.config 类型引用悬空 | SDK CLI；模板 JSDoc | 中 |
| 6 | chipsdev build 不识别主题包构建脚本（工单111） | 工单111 | 中 |
| 7 | 暗色主题 dist 过期属反复出现类问题（工单122 处理后又复发） | 工单122；本次 T1/T2 | 中 |
| 8 | iframe 文档字体注入缺口（工单130 待处理） | 工单130 | 中 |
| 9 | 文档漂移面广：开发计划状态过期、CHANGELOG 基线过时、FormField 宣称已落地实际已归档 | 各文档 | 中 |
| 10 | 未闭环工单合计 8 条应纳入重构范围 | 问题工单.md | 中 |
