# 阶段七：查看器、设置面板与布局宿主图标接入

## 1. 阶段目标

把统一图标体系接入除编辑引擎外的正式应用与宿主场景，重点完成设置面板治理 UI 的图标消费，并对查看器、布局宿主等仓库做正式巡检与必要改造。

## 2. 涉及范围

1. `Chips-EcoSettingsPanel`
2. `Chips-CardViewer`
3. `Chips-BoxLayoutHost`
4. 相关项目级文档

## 3. 当前基线

1. 设置面板未显示插件图标；
2. 查看器当前未发现明显 emoji 债务，但要完成正式巡检；
3. `Chips-BoxLayoutHost` 当前未发现明确图标入口，需要形成巡检结果。

## 4. 核心任务

1. 设置面板治理页接入正式图标展示；
2. 复核查看器运行时 UI、启动图标资产与文档；
3. 复核布局宿主是否存在图标入口，若有则统一接入；
4. 补齐应用层测试与文档。

## 5. 交付物

1. 设置面板图标展示闭环；
2. 查看器与布局宿主巡检结论；
3. 必要的代码与文档更新。

## 6. 验证

- `cd Chips-EcoSettingsPanel && npm run verify`
- `cd Chips-CardViewer && npm run build && npm test && npm run lint && npm run validate`

## 7. 完成判定

只有当治理应用和其他正式应用不再遗漏图标消费链路，本阶段才算完成。
