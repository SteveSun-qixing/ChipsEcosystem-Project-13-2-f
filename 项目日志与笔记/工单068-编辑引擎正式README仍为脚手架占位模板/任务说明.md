# 工单068-编辑引擎正式README仍为脚手架占位模板

## 背景

在继续执行“大任务008-图标系统完善”并补写项目级相关文档时，复核到 `Chips-EditingEngine/README.md`。  
结果发现该文件仍然保留 `chips-scaffold-app` 模板生成态内容，没有收口成真实编辑引擎项目文档。

根据工作区 `AGENTS.md`：

- 发现“脚手架或者文档没有更新”的情况需要提出工单；
- 发现“文档与实际代码不符”也需要提出工单；
- 登记工单后应停止继续沿着该错误口径推进。

因此本问题需要单独登记，避免后续在错误 README 基线上继续补写编辑引擎图标系统文档。

## 证据

### 1. README 仍保留模板占位符

`Chips-EditingEngine/README.md` 当前仍包含：

- `{{ DISPLAY_NAME }}`
- `{{ PLUGIN_ID }}`
- `{{ PROJECT_NAME }}`

这说明 README 并未在项目落地后转换为真实文档。

### 2. README 描述的工程结构明显对应脚手架默认应用模板

README 中的目录结构仍写为：

- `src/components/`
- `src/hooks/`
- `src/layouts/`
- `config/app-config.ts`
- `tests/e2e/`

但这不是当前编辑引擎项目的正式对外说明口径，也没有体现：

- 编辑引擎运行时图标基础设施
- 工作台 / 卡箱库 / 文件管理器 / 设置面板等真实模块
- 当前工程的真实脚本和职责边界

### 3. 与真实工程不一致

对照以下真实文件即可确认漂移：

- `Chips-EditingEngine/package.json`
- `Chips-EditingEngine/manifest.yaml`
- `Chips-EditingEngine/src/`

当前 README 无法反映该工程作为“编辑引擎应用插件”的真实结构和图标系统接入状态。

## 影响范围

1. 编辑引擎项目级正式入口文档不可用；
2. 图标系统改造结果无法继续可靠沉淀到编辑引擎正式项目文档；
3. 开发者若按 README 理解脚本、目录、图标与主题链路，会得到错误信息；
4. 后续继续补写编辑引擎文档时，容易把脚手架模板口径误当成真实工程口径。

## 复现步骤

1. 打开 `Chips-EditingEngine/README.md`；
2. 观察标题、插件 ID、项目名和目录结构仍为模板占位内容；
3. 再打开 `Chips-EditingEngine/package.json`、`Chips-EditingEngine/manifest.yaml` 与 `Chips-EditingEngine/src/`；
4. 对比即可确认 README 与真实工程已经明显脱节。

## 期望行为

1. 重写 `Chips-EditingEngine/README.md`，使其成为真实编辑引擎项目说明；
2. README 需要收口：
   - 工程角色
   - 真实目录结构
   - 正式脚本
   - 图标系统与主题系统接入方式
   - Host / SDK / Bridge 边界
3. 完成后再继续扩展编辑引擎相关图标系统文档。

## 暂行处理

1. 已先登记工单，避免继续沿着旧 README 口径推进；
2. 用户后续已明确要求忽略本工单并继续任务；
3. 当前已直接完成 README 正式重写，并补齐启动图标来源说明。

## 需要协作方

- 编辑引擎维护方
- 项目文档维护方

## 当前状态

- 已解决
