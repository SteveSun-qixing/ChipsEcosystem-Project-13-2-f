# 工单083-Chips-CommunityPlatformServer缺少Volta继承配置导致根工作区校验回归

## 背景

- 本轮用户要求提交当前未提交代码。
- 按工作区规范，提交前补跑了根工作区正式校验 `npm run verify:workspace`。
- 校验未通过，失败原因不是本次 Host 功能改动，而是 `Chips-CommunityPlatformServer/package.json` 缺少历史上已经要求统一补齐的 `volta.extends`。

## 证据

- 根目录执行 `npm run verify:workspace` 后输出：

```text
生态工作区校验失败：
- Chips-CommunityPlatformServer/package.json 的 volta.extends 应为 ../package.json，当前为 undefined
```

- 当前 `Chips-CommunityPlatformServer/package.json` 只有 `name/version/scripts/devDependencies/engines`，没有 `volta` 字段。
- `问题工单.md` 既有闭环记录中已明确写过：
  - “所有根工作区覆盖的一方工程 `package.json` 已补齐 `volta.extends`，统一指向生态根 `package.json`；”
- 因此当前现象属于已闭环约束的回归，而不是首次发现的新规则。

## 影响范围

- `/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f/package.json` 的工作区级验证流程
- `/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f/Chips-CommunityPlatformServer/package.json`
- 所有依赖 `npm run verify:workspace` 做总仓验收的收口流程

## 复现步骤

1. 进入 `/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f`
2. 执行 `npm run verify:workspace`
3. 观察校验脚本直接报 `Chips-CommunityPlatformServer/package.json` 缺少 `volta.extends`
4. 打开 `Chips-CommunityPlatformServer/package.json`，确认没有：

```json
"volta": {
  "extends": "../package.json"
}
```

## 期望行为

- `Chips-CommunityPlatformServer/package.json` 应恢复正式 `volta.extends` 继承配置；
- 根工作区 `verify:workspace` 应恢复全绿；
- 已闭环的根工作区 Volta 继承约束不能在单个工作区仓库再次漂移。

## 暂行处理

- 已登记本工单；
- 当前不把社区服务器工具链修复混入本轮 Host / 脚手架提交；
- 本轮提交只记录该验证阻塞，避免误报“全工作区校验通过”。

## 需要协作方

- 社区服务器维护方
- 总仓工具链维护方

## 当前状态

- 处理中
