# 工单084-应用脚手架生成工程残留模板身份

## 背景

- 本轮用户要求在 `/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f/Chips-VideoPlayer` 中创建并完成视频播放器应用插件。
- 按生态工作区规则，项目必须先通过官方应用插件脚手架初始化，再在正式基线上继续开发。
- 实际执行 `chipsdev create app Chips-VideoPlayer` 后，生成工程可以落盘，但仍带出模板身份残留，需要人工清理后才能继续当作正式项目使用。

## 证据

- 已执行命令：

```bash
cd /Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f
./node_modules/.bin/chipsdev create app Chips-VideoPlayer
```

- 生成结果中存在以下残留：
  - `Chips-VideoPlayer/template.json`
  - `Chips-VideoPlayer/src/App.tsx` 中的 `app-standard` 标识
  - `Chips-VideoPlayer/src/components/ExamplePanel.tsx` 中的脚手架示例文案
  - `Chips-VideoPlayer/i18n/zh-CN.json` 与 `Chips-VideoPlayer/i18n/en-US.json` 中的 `app-standard` 顶层键
  - `Chips-VideoPlayer/README.md` 中以模板说明为主体的内容

## 影响范围

- `chipsdev create app` 生成的所有应用插件工程
- `Chips-Scaffold/chips-scaffold-app` 模板产物质量
- 新建应用仓库的 README、i18n、组件标识与模板元数据收口

## 复现步骤

1. 进入 `/Users/sevenstars/Documents/ChipsCard/Develop/Project-13-2-f`
2. 执行 `./node_modules/.bin/chipsdev create app Chips-VideoPlayer`
3. 打开 `Chips-VideoPlayer/template.json`
4. 检查 `Chips-VideoPlayer/src/App.tsx` 与 `src/components/ExamplePanel.tsx`
5. 检查 `Chips-VideoPlayer/i18n/*.json`
6. 检查 `Chips-VideoPlayer/README.md`

## 期望行为

- 生成工程不再泄露模板 catalog 文件；
- 示例组件与词典命名应收口为可直接继续开发的正式项目基线；
- README 不再以模板说明为正式项目主体内容；
- 应用脚手架生成产物默认满足“无模板身份泄露”的正式规则。

## 暂行处理

- 本轮在 `Chips-VideoPlayer` 内手工替换和归档模板残留，继续推进视频播放器开发；
- 在本工单闭环前，后续所有新建应用工程都必须补做生成态复核。

## 需要协作方

- 应用脚手架维护方
- SDK CLI 维护方
- 生态文档维护方

## 当前状态

- 待处理
