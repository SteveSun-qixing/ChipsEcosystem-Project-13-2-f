# 工单103：chipsdev 组件矩阵误读归档 contract 阻断 SDK 全量测试

## 背景

在 `任务026.04-模块插件HostSDKCLI契约复核补强` 的提交前验证中，`Chips-SDK npm test` 运行到最后一个 tooling 命令测试时失败。前置 SDK 单元测试、CLI smoke、Host 管理命令、脚手架 E2E、模块 invoke 和打包兼容测试均已通过。

## 证据

- 失败位置：`Chips-SDK/tests/run-cli-tooling-tests.cjs:164`。
- 失败断言：`componentGallery.summary.status` 实际为 `failed`，期望为 `passed`。
- `chipsdev component gallery --json` 输出显示缺失 token 来源为：
  - `Chips-ComponentLibrary/packages/theme-contracts/contracts/components/归档/task016.03-form-compound/form-field.contract.json`
  - `Chips-ComponentLibrary/packages/theme-contracts/contracts/components/归档/task016.03-form-compound/form-group.contract.json`
- 上述文件属于历史归档 contract，不应进入当前正式组件矩阵。

## 影响范围

- `chipsdev component gallery`
- `chipsdev diagnostics`
- `chipsdev quality gate`
- SDK 全量测试
- 组件库正式 theme contract 覆盖统计

## 复现步骤

1. 进入 `Chips-SDK`。
2. 执行 `npm test`。
3. 观察 `run-cli-tooling-tests.cjs` 在 component gallery 断言处失败。
4. 单独执行 `chipsdev component gallery --json`。
5. 查看 `components[].contractPath`，确认归档 contract 被纳入统计。

## 期望行为

- SDK tooling 只读取当前正式组件 contract。
- `归档/` 与 `archive/` 目录下的历史 contract 不进入当前正式组件矩阵。
- 历史资料继续保留，但不影响质量门禁。

## 暂行处理

按用户要求，本工单登记后直接修复，不暂停当前任务。

## 当前状态

已解决。已让组件 contract 扫描跳过 `归档` 与 `archive` 目录，并补充 tooling 测试断言。
