# 工单107：评分组件 Contract 缺失 Token 阻断 SDK 工具链全量测试

## 背景

任务047 ColorPicker 模块插件迁移完成后，为确认 SDK 新增图像颜色提取类型出口不会影响工具链，执行 `Chips-SDK npm test`。全量测试进入 `run-cli-tooling-tests.cjs` 后，在 `chipsdev component gallery --json` 阶段失败。

## 证据

- `Chips-SDK npm test` 中：
  - 7 个 Vitest 文件、78 个测试通过；
  - CLI smoke 通过；
  - host-managed 通过；
  - create app/card/layout/module/theme 通过；
  - module invoke 通过；
  - package compatibility 通过；
  - tooling command test 在 component gallery 断言处失败。
- 单独执行 `chipsdev component gallery --json` 得到：
  - `summary.status = "failed"`；
  - `summary.missingRequiredTokenCount = 14`；
  - 失败组件为 `rating`；
  - contract 路径为 `Chips-ComponentLibrary/packages/theme-contracts/contracts/components/rating.contract.json`。

缺失 token：

- `chips.comp.rating.root.gap`
- `chips.comp.rating.item.size`
- `chips.comp.rating.item.radius`
- `chips.comp.rating.item.surface.idle`
- `chips.comp.rating.item.surface.hover`
- `chips.comp.rating.item.surface.active`
- `chips.comp.rating.item.surface.disabled`
- `chips.comp.rating.icon.color.idle`
- `chips.comp.rating.icon.color.active`
- `chips.comp.rating.icon.color.heart-active`
- `chips.comp.rating.icon.color.disabled`
- `chips.comp.rating.label.color`
- `chips.comp.rating.status.color.error`
- `chips.comp.rating.focus.outline`

## 影响范围

- `Chips-ComponentLibrary/packages/theme-contracts/contracts/components/rating.contract.json`
- `Chips-ComponentLibrary/packages/tokens/dist/json/tokens.json`
- `Chips-SDK/tests/run-cli-tooling-tests.cjs`
- `chipsdev component gallery`
- `chipsdev quality gate`
- `Chips-SDK npm test` 全量验证

## 复现步骤

1. 进入 `Chips-SDK`；
2. 执行 `npm test`；
3. 观察 `run-cli-tooling-tests.cjs` 在 component gallery 阶段失败；
4. 单独执行 `CHIPS_ECOSYSTEM_ROOT=<worktree> node Chips-SDK/cli/index.js component gallery --json`；
5. 查看输出中的 `rating` 组件缺失 token 清单。

## 期望行为

- 评分组件如果作为正式组件 contract 暴露，组件库 token 构建产物必须包含其 required token；
- 如果评分组件 contract 尚未正式发布，应从正式 component gallery 入口移出或降级为草稿/归档口径；
- `chipsdev component gallery` 与 `quality gate` 应继续输出可解释的正式质量门禁结果；
- SDK 全量测试不应被未收口的组件契约漂移长期阻断。

## 暂行处理

任务047范围内不修改评分组件或组件库 token。本工单作为独立组件库/主题 token 缺口追踪；任务047验证报告中将 SDK 全量测试失败标记为非 ColorPicker 回归。

## 需要协作方

- 组件库维护方
- 主题 token 维护方
- SDK / chipsdev 工具链维护方

## 当前状态

已闭环。

## 解决方案文档

- [解决方案.md](./解决方案.md)

## 处理结果

- 已补齐评分组件正式 contract 对应的组件库 rating token 源文件，并同步默认主题包与深色默认主题包的 rating token，使 `chips.comp.rating.*` required token 不再缺失。
- 已补充 `Chips-ComponentLibrary/packages/theme-contracts/contracts/components/rating.contract.json`，明确 rating 的 root、item、icon、label 与 status/error/focus token 对外契约。
- 已补充组件库测试，覆盖 rating contract、token map 和主题 contract 构建产物，避免后续评分组件 contract/token 再次漂移。
- 已重新构建组件库 tokens dist，`rating` token 可在 dist JSON 中解析到实际值。

## 验证记录

- `cd Chips-ComponentLibrary && npm run verify`
  - `validate:tokens`：通过，校验 802 个 token key。
  - `build:tokens`：通过，构建 802 个 token key。
  - `validate:contracts`：通过，校验 71 个 contract。
  - `test:types`：通过。
  - `test`：通过，253 条单测全绿。
- `CHIPS_ECOSYSTEM_ROOT=<worktree> node Chips-SDK/cli/index.js component gallery --json`
  - `summary.status = "passed"`。
  - `summary.componentCount = 71`。
  - `summary.missingRequiredTokenCount = 0`。
  - `rating.status = "passed"`，`rating.missingRequiredTokens = []`。
- `CHIPS_ECOSYSTEM_ROOT=<worktree> node Chips-SDK/cli/index.js quality gate --json`
  - `summary.status = "warning"`。
  - `summary.failedCheckCount = 0`。
  - `summary.warningCheckCount = 2`。
  - Warning 来源为根工作区 `chips.config.mjs` 与 `manifest.yaml` 项目配置检查，不是评分组件 contract/token 缺口。
- `cd Chips-SDK && npm test`
  - Vitest：7 个测试文件、78 条测试通过。
  - CLI smoke、host-managed、create app/card/layout/module/theme、module invoke、package compatibility、tooling command tests 全部通过。
  - 原始失败点 `run-cli-tooling-tests.cjs` 的 `component gallery` 断言已恢复通过。
