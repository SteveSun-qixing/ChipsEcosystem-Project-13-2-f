# 大任务008：图标系统实施任务清单

> 说明：本清单按“公共契约 -> 共享基础设施 -> 各仓接入 -> 验证收口”的顺序编排。  
> 要求：所有任务完成后才允许宣告“图标系统已正式收口”。

## A. 公共契约与文档基线

- [x] `ICON-001`：收口 `生态共用技术文档/主题系统/03-图标与字体系统设计规范.md` 与真实实现的最终目标映射，补齐实施约束与验收条目。
- [x] `ICON-002`：重写 `生态共用技术文档/主题系统/01-主题包开发指南.md` 中旧 `icons.css / svg / icon-sm/icon-md/icon-lg` 口径。
- [x] `ICON-003`：更新 `生态共用技术文档/组件库/02-组件契约标准.md`，明确统一图标组件、icon slot 与图标描述符口径。
- [x] `ICON-004`：更新 `生态共用技术文档/组件库/08-编辑引擎基础卡片装配与编辑运行时标准.md`，将 `icon?: string` 升级为正式图标描述模型。
- [x] `ICON-005`：更新 `生态共用技术文档/插件开发/03-卡片插件开发.md` 中 `BasecardDefinition.icon` 的正式类型与示例。
- [x] `ICON-006`：更新 `生态共用技术文档/插件开发/04-布局插件开发.md` 中布局显示身份的图标口径。
- [x] `ICON-007`：更新 `生态共用技术文档/插件开发/02-应用插件开发.md` 与 `06-Manifest配置规范.md`，明确“运行时 UI 图标”和“系统入口图标文件路径”的分层边界。
- [x] `ICON-008`：更新 `生态共用技术文档/协议与接口标准/07-运行时设置治理接口标准.md`，明确治理页图标读取与展示要求。
- [x] `ICON-009`：核对 `生态设计原稿/07-插件系统架构.md`、`14-编辑引擎设计.md`、`17-生态设置面板设计.md` 是否需要同步收口图标实施方向；若与正式口径冲突，先补工单再改。
- [x] `ICON-010`：清除所有正式共享文档中的 emoji 状态标记，改成纯文本表达。

## B. 共享图标资产与生成链路

- [x] `ICON-011`：盘点 `design-assets/material-symbols/` 的正式运行时输入文件、codepoints 和来源追溯，形成单一资产清单。
- [x] `ICON-012`：明确 `outlined / rounded / sharp` 三套风格在生态中的正式文件落点与命名约定。
- [x] `ICON-013`：明确 Material Symbols 运行时资源与 `design-assets/Appicon` 各应用启动图标源文件之间的职责边界。
- [x] `ICON-014`：建立图标资源生成或复制规范，避免各仓各自手抄字体文件或维护漂移版本。
- [x] `ICON-015`：统一 App 启动图标源与输出物（`.ico/.icns/.png/.svg`）的正式来源与更新流程。
- [x] `ICON-016`：补齐设计资产层的说明文档，写明哪些资源可直接进入运行时，哪些只用于来源追溯。
- [x] `ICON-017`：建立“图标资产变更必须同步 `SOURCE.md` / 来源说明”的正式检查规则。

## C. 组件库统一图标基础设施

- [x] `ICON-018`：在 `Chips-ComponentLibrary` 中正式实现 `ChipsIcon` 组件。
- [x] `ICON-019`：定义 `ChipsIconDescriptor`（或等价正式类型），作为全生态运行时图标输入模型。
- [x] `ICON-020`：移除组件库中 `createGlyphIcon(...)` / `getDefaultIconContent(...)` 的硬编码 SVG 主链路。
- [x] `ICON-021`：将 Select / DateTime / PanelHeader / DockPanel / ToolWindow / EmptyState 等组件的默认图标统一切换到 `ChipsIcon`。
- [x] `ICON-022`：保留图标槽位能力，但槽位默认值统一改为正式图标组件，不再直接输出内联 SVG。
- [x] `ICON-023`：建立组件级图标 token 与主题 token 的映射规范。
- [x] `ICON-024`：补齐 `icon.color / icon.weight / icon.fill / icon.grade / icon.opticalSize / icon.size` 的正式 token 结构。
- [x] `ICON-025`：补齐 `theme-contracts` 中相关组件的 icon token 契约覆盖。
- [x] `ICON-026`：补齐组件库类型声明、单测、a11y 测试和契约测试中的图标场景。
- [x] `ICON-027`：补齐“语义名到 glyph 可用性”与“功能图标标签”自动化测试。
- [x] `ICON-028`：更新 `Chips-ComponentLibrary` 内部技术文档和开发者接入指南，删除“默认语义 SVG”旧口径。

## D. ThemePack 与主题运行时

- [x] `ICON-029`：让 `ThemePack/Chips-default` 正式接入运行时图标资源，而不是只保留 `icons/README.md`。
- [x] `ICON-030`：让 `ThemePack/Chips-theme-default-dark` 同步接入运行时图标资源。
- [x] `ICON-031`：为默认主题与暗色主题补齐正式 icon token 默认值。
- [x] `ICON-032`：统一主题侧图标字体声明、变量轴默认值、尺寸规则和可访问性样式。
- [x] `ICON-033`：核对默认主题 `theme.css` / `base.css` 的字体基线是否与冻结规范一致，并一并收口。
- [x] `ICON-034`：将图标资源接入方式写回主题包正式文档与主题模板说明。
- [x] `ICON-035`：验证主题切换时图标风格、颜色、权重和尺寸能随主题正式变化。

## E. Host / SDK / Bridge / PAL 图标能力

- [x] `ICON-036`：复核 `Chips-Host` 对 `ui.launcher.icon` 的安装复制、快捷方式解析和多平台格式选择行为。
- [x] `ICON-037`：复核 `Chips-SDK` 对 `PluginShortcutRecord.iconPath` 的正式类型与调用文档。
- [x] `ICON-038`：明确 Bridge / SDK 暴露的“运行时 UI 图标语义数据”和“操作系统入口图标文件路径”边界，不允许混用。
- [x] `ICON-039`：补齐 Host 相关测试：安装、打包、内置插件、安装器、快捷方式状态读取。
- [x] `ICON-040`：核对 PAL 的通知、托盘等文件路径图标能力是否需要补充文档边界说明。
- [x] `ICON-041`：把图标链路的正式边界回写到 Host / SDK 技术文档。

## F. 基础卡片、布局插件与插件元数据重构

- [x] `ICON-042`：将 `Chips-BaseCardPlugin/richtext-BCP/src/index.ts` 中的 emoji 图标替换为正式图标描述符。
- [x] `ICON-043`：将 `Chips-BaseCardPlugin/image-BCP/src/index.ts` 中的 emoji 图标替换为正式图标描述符。
- [x] `ICON-044`：升级 `BasecardDefinition.icon` 与编辑引擎 `BasecardDescriptor.icon` 的正式类型。
- [x] `ICON-045`：升级 `Chips-EditingEngine/src/components/CardBoxLibrary/types.ts` 中 `icon: string` 旧模型。
- [x] `ICON-046`：重构卡箱库构建逻辑，使基础卡片和布局定义统一返回语义图标描述符而不是 emoji。
- [x] `ICON-047`：为 `Chips-BoxLayoutPlugin` 增加正式布局显示图标描述符或等价正式导出。
- [x] `ICON-048`：统一基础卡片、布局插件、应用治理清单中的图标展示数据模型。
- [x] `ICON-049`：核对 `Chips-ModulePlugin` 是否存在正式 UI 图标入口；若无，仅记录巡检结果，不制造无意义改动。

## G. 应用插件接入

### G1. 生态设置面板

- [x] `ICON-050`：在 `Chips-EcoSettingsPanel` 的应用插件治理页显示插件图标。
- [x] `ICON-051`：治理页图标展示统一使用正式图标组件或正式图标资源视图，不直接拼路径字符串。
- [x] `ICON-052`：为设置面板中主题、多语言、插件治理等页面补齐必要图标位，并统一接入正式图标体系。
- [x] `ICON-053`：更新设置面板需求文档、技术文档和验证口径。

### G2. 卡片查看器

- [x] `ICON-054`：复核 `Chips-CardViewer` 当前是否存在运行时图标缺口；若存在，统一接入正式图标组件。
- [x] `ICON-055`：统一查看器启动图标资产与正式来源，不保留旧版资源漂移。
- [x] `ICON-056`：更新查看器的 README / 项目文档中与图标相关的说明。

### G3. 编辑引擎

- [x] `ICON-057`：重构 `Dock` / `DockItem`，不再使用 emoji 作为程序坞图标。
- [x] `ICON-058`：重构 HeaderBar、LayoutSwitcher、WindowMenu、HistoryPanel、EditPanel、CardWindow 的所有旧字符图标。
- [x] `ICON-059`：重构 FileManager / FileItem / FileTree / ContextMenu / DragDrop 的全部 emoji 图标。
- [x] `ICON-060`：重构 CardBoxLibrary / CoverMaker / EngineSettings / CardSettings / ThemePanel / FileModeSettingsPanel 的全部 emoji 图标。
- [x] `ICON-061`：重构 `editor-runtime/EditorHost.tsx`、Workbench 布局、空态/错误态/加载态图标。
- [x] `ICON-062`：统一编辑引擎内部菜单、按钮、面板和状态占位的图标来源，不再允许字符图标散落在 JSX 中。
- [x] `ICON-063`：重构编辑引擎内部与图标相关的类型、配置和默认值，不保留 `icon: string`。
- [x] `ICON-064`：同步更新编辑引擎相关技术文档、需求文档、测试和样式。

### G4. 其他应用/宿主

- [x] `ICON-065`：复核 `Chips-BoxLayoutHost` 是否需要接入正式图标体系；若无，形成巡检结论文档化。

## H. 脚手架与模板

- [x] `ICON-066`：升级 `chips-scaffold-app` 的默认应用图标资产来源与模板说明。
- [x] `ICON-067`：升级 `chips-scaffold-theme`，使主题模板默认具备正式图标目录、说明和接线结构。
- [x] `ICON-068`：升级 `chips-scaffold-basecard`，使基础卡片模板默认输出正式图标描述口径和资源说明。
- [x] `ICON-069`：升级 `chips-scaffold-boxlayout`，使布局模板默认输出正式图标描述口径和布局图标资源。
- [x] `ICON-070`：清除脚手架技术文档、README 模板和生成样例中的旧图标说明与 emoji。
- [x] `ICON-071`：更新脚手架测试，确保生成工程带有正式图标资产和正确文档口径。
- [x] `ICON-072`：确保新脚手架生成项目不会再传播旧 `icon: string + emoji` 模型。

## I. App 启动图标资产替换

- [x] `ICON-073`：统一 `Chips-EcoSettingsPanel` 的 `app-icon.ico/.icns/.png/.svg` 与正式来源。
- [x] `ICON-074`：统一 `Chips-CardViewer` 的 `app-icon.*` 资产。
- [x] `ICON-075`：统一 `Chips-EditingEngine` 的 `app-icon.*` 资产。
- [x] `ICON-076`：统一 `Chips-BaseCardPlugin/*/assets/icons/basecard-icon.svg` 资产。
- [x] `ICON-077`：统一 `Chips-BoxLayoutPlugin/assets/icons/layout-icon.svg` 资产。
- [x] `ICON-078`：统一各脚手架模板内 `app-icon.* / basecard-icon.svg / layout-icon.svg` 的正式来源。
- [x] `ICON-079`：核对 `design-assets/Appicon/` 中源文件命名与当前各仓输出物是否一致，清理旧版残留命名漂移。

## J. emoji 清零专项

- [x] `ICON-080`：对 `Chips-EditingEngine` 执行代码级 emoji 清零，确保搜索结果归零。
- [x] `ICON-081`：对 `Chips-BaseCardPlugin` 执行代码级 emoji 清零，确保搜索结果归零。
- [x] `ICON-082`：对 `Chips-Scaffold` 的正式文档与模板执行 emoji 清零。
- [x] `ICON-083`：对 `生态共用技术文档` 中的正式文档执行 emoji 清零。
- [x] `ICON-084`：对本大任务相关活跃文档执行 emoji 清零。
- [x] `ICON-085`：建立最终复验命令，确保正式代码、正式模板、正式文档搜索结果为零。

## K. 回归验证与发布级收口

- [x] `ICON-086`：运行 `Chips-ComponentLibrary` 正式验证，确保组件、token、契约和测试全部通过。
- [x] `ICON-087`：运行 `Chips-Host`、`Chips-SDK` 的正式验证，确保打包、安装、快捷方式和 SDK 测试通过。
- [x] `ICON-088`：运行 `Chips-EcoSettingsPanel`、`Chips-CardViewer`、`Chips-EditingEngine`、`Chips-BoxLayoutPlugin`、`Chips-BaseCardPlugin/*` 的正式脚本验证。
- [x] `ICON-089`：运行各脚手架测试和生成样例回归，确保新工程默认图标系统可用。
- [x] `ICON-090`：运行根工作区或成品测试工作区的工作区级验证，确认全生态图标链路闭环。

## L. 完成判定

满足以下条件才可结束本大任务：

1. `ChipsIcon` 与统一图标描述符已经成为运行时正式主链路；
2. Material Symbols 资源、主题 token、ThemePack、组件库和应用层全部接通；
3. 应用启动图标与运行时 UI 图标的职责边界明确且已文档化；
4. 编辑引擎、基础卡片插件、设置面板和脚手架中的 emoji 与旧图标模型全部移除；
5. 正式共享文档与项目文档只保留一套真实生效的图标口径；
6. 所有相关正式脚本验证通过；
7. 工作区级最终复验通过。
