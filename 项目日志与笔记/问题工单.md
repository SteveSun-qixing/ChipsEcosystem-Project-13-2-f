# 问题工单

> 用途：记录在开发过程中发现的生态缺陷、依赖缺失、接口不可用问题。

## 模板

- 工单编号：
- 日期：
- 发现阶段：
- 问题类型：
- 问题描述：
- 影响范围：
- 复现步骤：
- 期望行为：
- 暂行处理：
- 需要协作方：
- 状态：待处理 / 处理中 / 已闭环

## 工单列表

### 工单 1

- 工单编号：HOST-STAGE1-20260303-001
- 日期：2026-03-03
- 发现阶段：第一阶段项目一（薯片主机）完备性复核
- 问题类型：架构缺口 / 能力未闭环 / 规范口径不一致
- 问题描述：
  - PAL 仍缺少阶段要求能力：文件监控、图片/文件剪贴板、命名管道/共享内存/Unix 域套接字式本地 IPC。
  - Kernel 未形成独立模块加载器闭环（多格式加载、隔离、依赖解析、版本策略）。
  - Protocol 仅有类型定义，缺少协议兼容策略实现与专项测试闭环。
  - Bridge 仍存在默认全权限上下文路径；`contextBridge.exposeInMainWorld` 主线接入未完全固化。
  - 主进程骨架未补齐全局异常托底与完整 Electron `app` 生命周期编排。
  - 测试门禁缺少跨平台一致性、压力、协议性能、故障注入等阶段验收项。
- 影响范围：
  - 第一阶段 1.1~1.5 无法判定“全部完成交付”；
  - 生态共用手册验收口径与当前实现存在差距。
- 复现步骤：
  1. 对照 `生态设计原稿` 与 `生态共用技术文档` 的第一阶段目标清单；
  2. 检查 `Chips-Host/packages/*`、`Chips-Host/src/main/*` 与 `Chips-Host/tests/*`；
  3. 发现上述能力项无完整实现或无验收测试闭环。
- 期望行为：
  - 形成第一阶段缺口闭环任务包并完成实现、测试、文档同步；
  - 达到“可验收的完整薯片主机底座”标准。
- 暂行处理：
  - 按流程阻断当前连续开发，先提交本工单并创建专项任务文档，等待确认后继续实施。
- 任务文档：
  - `项目日志与笔记/任务清单/HOST-STAGE1-20260303-001/任务说明.md`
- 需要协作方：
  - 生态架构组（阶段口径确认）
  - 协议与安全组（协议兼容、权限模型）
  - 主机开发组（PAL/Kernel/Bridge/Main 骨架落地）
- 闭环结果：
  - 已补齐 PAL `watch`、多格式剪贴板、本地 IPC 三类传输抽象（命名管道/Unix 域套接字/共享内存风格）。
  - 已新增 Kernel 模块加载器并完成依赖解析、版本约束、隔离校验测试。
  - 已新增 Protocol 编解码兼容层并补齐兼容/序列化/性能校验测试。
  - 已完成 preload `contextBridge.exposeInMainWorld` 固化和默认最小权限桥接上下文。
  - 已新增主进程生命周期编排与全局异常处理骨架。
  - `npm run build` 与 `npm test` 均已通过（17 文件 / 53 用例）。
- 状态：已闭环

### 工单 7

- 工单编号：工单007-SDK-HOST-EXTERNAL-INTEGRATION-ENDPOINT
- 日期：2026-03-07
- 发现阶段：薯片 SDK 阶段一（架构与需求冻结）
- 问题类型：架构缺口 / 能力未闭环 / 规范口径不一致
- 问题描述：
  - 初始设计与白皮书文本曾假定 SDK 需支持“第三方软件集成薯片能力”，但当前用户决策已明确：SDK 不再支持博客系统、知识库、课程平台等完全独立第三方业务系统的直接集成，SDK 范围收敛为“薯片主机内部应用与生态内部工具”。
  - 现有对外接口基线与实现中，Host 对 SDK 暴露的稳定调用入口仅有：
    - 插件/应用内部的 Bridge API：`window.chips.invoke/on/once/emit`（详见《Bridge API 规范》和《Chips-Host对外接口基线》）；
    - CLI 命令行入口：`chips host start/stop/status/config/logs/open/plugin/theme/...`（详见《Chips-Host命令行使用手册》）。
  - 在用户明确收紧 SDK 范围的决策之后，与“第三方外部集成”相关的设计条目已不再属于当前架构目标，需要在文档与需求层面完成收口。
- 影响范围：
  - `Chips-SDK/需求文档` 与 `Chips-SDK/技术文档` 中针对“第三方外部应用集成”的相关表达需要统一调整为“仅面向薯片主机内部应用与生态内部工具”；
  - 《薯片生态白皮书（综述版）》和《SDK与协议设计》中关于 SDK 与第三方系统集成的描述需要收紧口径或改为“未来可能单独立项设计”；
  - 初始工单中关于“必须设计 SDK-Host 外部通信模式”的期望已不再成立，需改为记录为“当前阶段不再要求”。
- 暂行处理：
  - 已根据用户决策更新相关生态设计与 SDK 文档：
    - 《19-SDK与协议》中删除“博客系统/知识库/课程平台”等第三方场景示例，并明确说明此类外部系统不在当前 SDK 范围内；
    - 《Chips 生态概述》中将 SDK 定位调整为“生态内部开发者工具包”，不再对第三方集成做承诺；
    - 《薯片生态白皮书（综述版）》中补充说明 SDK 主要服务于生态内部应用与插件；
    - `Chips-SDK/需求文档/01-薯片 SDK 需求总览.md` 中将北极星目标从“插件开发 + 第三方集成”收敛为“插件开发 + 生态内部应用开发”，并显式将“第三方独立业务系统集成”列为 Out of Scope。
- 任务文档：
  - `项目日志与笔记/归档/工单007-SDK-HOST-EXTERNAL-INTEGRATION-ENDPOINT/任务说明.md`
- 需要协作方：
  - SDK 开发组（已完成文档与范围调整）
  - 生态架构组（如后续需要重新打开“第三方集成”议题，将通过新工单单独立项）
- 闭环结果：
  - 用户已明确决策：当前阶段 SDK 不支持博客系统、知识库、课程平台等完全独立第三方业务系统的直接集成，SDK 范围收敛为支持薯片主机内部应用与生态内部工具。
  - 为避免架构与文档口径不一致，已对以下文档进行调整：
    - 《生态设计原稿/19-SDK与协议.md》：更新 SDK 定位与集成场景描述，将第三方外部系统移出当前范围；
    - 《生态设计原稿/28-Chips生态概述.md》：将 SDK 描述改为“生态内部开发者工具包”；
    - 《生态设计原稿/薯片生态白皮书（综述版）.md》：在“薯片SDK与开发工具”中强调 SDK 面向生态内部应用与插件，而非通用第三方系统；
    - `Chips-SDK/需求文档/01-薯片 SDK 需求总览.md`：更新项目背景、北极星目标、Out of Scope、干系人与里程碑，使其与新的 SDK 范围一致；
    - `Chips-SDK/需求文档/02-薯片 SDK 功能需求规格.md` 与 `Chips-SDK/技术文档/05-统一卡片显示链路技术方案.md`：将“第三方应用”表述统一替换为“生态内部应用”。
  - 在新口径下，不再需要为 SDK 设计通用的 Host 外部端点协议，原本对“SDK-Host 外部通信模式”的需求视为“未来如有需要再单独立项设计”的议题。
- 状态：已闭环

### 工单 2

- 工单编号：HOST-QUALITY-20260304-001
- 日期：2026-03-04
- 发现阶段：逐文件审查与全链路质量复核
- 问题类型：核心缺陷 / 能力缺口 / 契约与实现偏差
- 问题描述：
  - Kernel 路由 `prefix` 匹配存在误匹配（`file.*` 可命中 `filex.read`），存在路由污染风险。
  - Runtime Client 的 `off` 逻辑无效，事件取消订阅不可用。
  - UI Hooks 仅落地 `useTheme/useI18n/useFile`，缺失 `useWindow/usePlugin/useConfig` 等标准能力。
  - Schema 校验过于弱化（response 全量放行），契约门禁不足。
  - Config/Theme/Credential 等服务能力未达到高质量实现深度（层级覆盖、继承解析、加密存储）。
  - Plugin Runtime 安全流水线仍有缺口（来源/签名校验、运行态审计与准入约束不足）。
  - PAL 层未覆盖屏幕信息查询；Bridge IPC 子能力通道未完整落地。
- 影响范围：
  - L1-L7 多层次交付质量无法达到“高质量验收”标准；
  - 存在后续扩展与安全治理风险。
- 复现步骤：
  1. 对仓库 `Chips-Host` 全部源码文件逐个审查；
  2. 结合 `生态设计原稿` 与 `生态共用技术文档` 对照验收；
  3. 复跑 `npm test` 与 `npm run build` 验证当前可运行性；
  4. 汇总发现实现深度与契约完整度偏差。
- 期望行为：
  - 完成核心缺陷修复、能力补齐、契约与测试增强；
  - 达到“可交付高质量版本”标准并形成闭环报告。
- 暂行处理：
  - 立刻启动质量专项修复任务，按优先级分批落地并持续回归。
- 任务文档：
  - `项目日志与笔记/任务清单/HOST-QUALITY-20260304-001/任务说明.md`
- 需要协作方：
  - 主机内核开发组
  - 运行时与桥接开发组
  - 协议与质量保障组
- 闭环结果：
  - 已修复 Kernel 路由 `prefix` 误匹配问题（`file.*` 不再匹配 `filex.read`），并补充最长前缀优先逻辑与单测。
  - 已增强 EventBus 订阅能力（`filter`/`timeoutMs`），超时回调返回 `EVENT_HANDLER_TIMEOUT`，并补充单测。
  - 已修复 Runtime Client `off` 取消订阅逻辑，支持按事件和处理器精确解绑，并补充单测。
  - 已补齐 UI Hooks：`useWindow/usePlugin/useConfig`，并新增 hooks 单测。
  - 已补齐 PAL 屏幕能力与 Host `platform.getScreenInfo/listScreens` 路由，并完成 Bridge 子通道映射与回归测试。
  - 已完成 IPC 子能力通道主/预加载联动（`chips:window:*`、`chips:dialog:*`、`chips:plugin:*`、`chips:platform:*` 等），测试通过。
  - 已完成 config 四层覆盖能力增强（`system/workspace/user/default`）与作用域化写入（`config.set/batchSet/reset` 支持 `scope`）。
  - 已增强 theme 解析链路（最多六级解析链），超限返回 `THEME_CHAIN_TOO_DEEP` 并补充用例。
  - 已完成 credential 加密存储落盘与重启恢复（`credentials.enc.json`），并新增集成测试验证明文不落盘。
  - 已增强 Plugin Runtime 来源/签名校验（非本地来源必须携带签名）并补齐失败用例。
  - 已完成回归验证：`npm run build --silent` 与 `npm test --silent` 全量通过（19 文件 / 63 用例）。
- 状态：已闭环

### 工单 3

- 工单编号：工单001-SDK-UNIFIED-CARD-DISPLAY-API
- 日期：2026-03-04
- 发现阶段：薯片组件库阶段十一（卡片显示 iframe 高级组件专项）
- 问题类型：SDK 能力缺失 / 关键接口未落地
- 问题描述：
  - 组件库已完成 `CardCoverFrame` 与 `CompositeCardWindow` 组件契约与运行时实现，但 SDK 侧尚未交付统一显示接口。
  - 当前缺失 `client.card.coverFrame.render` 与 `client.card.compositeWindow.render` 的真实实现与返回结构定义。
  - 当前缺失复合卡片 iframe 事件协议在 SDK 侧的标准落地（`ready/node-error/fatal-error` 事件发布语义与载荷约束）。
  - 当前缺失复合窗口 `mode` 参数口径限制（仅 `view | preview`）与 origin 元信息输出约束，组件端白名单校验无法与 SDK 实现闭环联调。
  - 组件库无法完成与真实 SDK 的联调闭环，只能先以适配器契约继续推进组件侧开发与测试。
- 影响范围：
  - 查看器、编辑器及未来应用无法按“统一 SDK 接口调用”完成端到端联调；
  - 阶段十一“跨应用一致显示验收”无法闭环。
- 复现步骤：
  1. 在组件库中调用 `CardCoverFrame`/`CompositeCardWindow` 期望接入 SDK 接口；
  2. 按 `生态共用技术文档/插件开发/08-SDK使用指南.md` 检查接口实现仓库；
  3. 发现接口仅有文档口径，无实际 SDK 代码可接入。
- 期望行为：
  - SDK 提供可用的 `coverFrame/compositeWindow` 统一显示接口实现；
  - 明确并实现复合 iframe 事件协议与错误码模型；
  - 明确 `compositeWindow` 接口 `mode` 仅支持 `view | preview`，并提供 iframe origin 元信息以支持组件端来源白名单校验；
  - 输出可联调的最小可用版本与契约测试。
- 暂行处理：
  - 组件库继续按适配器契约推进不依赖 SDK 的高质量实现；
  - 与真实 SDK 对接与联调作为阻断项，待本工单处理后闭环。
- 任务文档：
  - `项目日志与笔记/归档/工单001-SDK-UNIFIED-CARD-DISPLAY-API/任务说明.md`
- 需要协作方：
  - SDK 开发组
  - 卡片渲染链路负责人
  - 组件库开发组
- 闭环结果：
  - 已在 `Chips-SDK` 中实现统一显示接口：
    - `client.card.coverFrame.render({ cardFile, cardName? })`；
    - `client.card.compositeWindow.render({ cardFile, mode?: 'view' | 'preview' })`；
    - 返回值统一为 `FrameRenderResult { frame, origin }` 与 `CardRenderResult { view }`。
  - 已在 SDK 内部对 `mode` 参数进行口径约束并向 Host `card.render` 传递 `options.mode`，非法取值在 SDK 层直接抛出 `INVALID_ARGUMENT`。
  - 已在 SDK 内部实现复合卡片 iframe 事件协议封装：
    - `chips.composite:ready` / `chips.composite:node-error` / `chips.composite:fatal-error`；
    - 仅接受来自预期 iframe `contentWindow` 且 `origin` 等于当前页面的消息，并对 fatal 错误载荷统一归一为 `StandardError`。
  - 已更新相关生态共用与 SDK 文档：
    - 《插件开发/08-SDK 使用指南》中 `card.render`、插件管理与主题接口示例；
    - 《组件库/07-复合卡片窗口与 SDK 显示链路标准》中 SDK 显示接口接入规则；
    - `Chips-SDK/需求文档/02-薯片 SDK 功能需求规格.md` 与 `Chips-SDK/技术文档/05-统一卡片显示链路技术方案.md` 中统一显示链路描述。
  - 已在 `Chips-SDK/tests/card.test.ts` 中新增统一显示链路相关测试用例，覆盖：
    - 空 `cardFile` 与非法 `mode` 的错误码行为；
    - `compositeWindow.render` 向 Host 透传 `options.mode` 与 `options.target`；
    - `coverFrame.render` 返回的 iframe 与 `origin` 结构；
    - 复合窗口事件在不同 `origin` 与 `type` 下的过滤行为及 fatal 错误归一逻辑。
- 状态：已闭环

### 工单 4

- 工单编号：工单004-ECOSYS-TOOLCHAIN-TS-LINT-SD-PRESET
- 日期：2026-03-04
- 发现阶段：薯片组件库阶段二/阶段三收口验收
- 问题类型：生态工具链缺失 / 跨项目依赖阻断
- 问题描述：
  - 组件库阶段二要求的 TypeScript strict、ESLint、Prettier 统一门禁需要使用生态统一规则集，但当前仓库未提供可复用的官方 preset（含版本基线、规则白名单、禁用清单）。
  - 阶段三要求与生态统一 Style Dictionary 生产流水线对齐，当前缺少生态级统一构建 preset（transformGroup、name transform、输出模板、schema 校验插件）的可引用实现。
  - 在“禁止引入未审批第三方库、禁止自行改变架构口径”的前提下，组件库侧无法自行定义最终口径并宣布阶段二/三完全收口。
- 影响范围：
  - 阶段二无法按生态统一标准完成 `lint/typecheck` 最终门禁闭环；
  - 阶段三无法完成“与生态统一 Style Dictionary 生产流水线完全对齐”验收项；
  - 1~3阶段无法给出“全部高质量完成”的正式结论。
- 复现步骤：
  1. 对照 `Chips-ComponentLibrary/开发计划/02-阶段二-仓库与工程基线搭建.md` 与 `03-阶段三-Token系统与StyleDictionary流水线.md`；
  2. 检查生态共用文档中仅给出原则口径，未提供可直接消费的统一 preset 工程产物；
  3. 组件库侧仅能完成本地可验证链路（`npm run verify`），无法完成跨仓统一工具链收口。
- 期望行为：
  - 生态工具链项目提供可复用包或模板仓：TypeScript strict + ESLint + Prettier 官方 preset；
  - 主题系统项目提供 Style Dictionary 官方 preset（含 schema/transform/output 统一定义）；
  - 输出版本化接入文档与实施说明，组件库据此完成最终接入并通过门禁。
- 暂行处理：
  - 组件库继续维持当前 `validate:tokens/build:tokens/validate:contracts/test/verify` 高质量本地门禁；
  - 等待生态工具链与主题系统补齐统一 preset 后立即补充收口开发并回归验证。
- 任务文档：
  - `项目日志与笔记/归档/工单004-ECOSYS-TOOLCHAIN-TS-LINT-SD-PRESET/任务说明.md`
- 需要协作方：
  - 生态开发工具链项目组
  - 主题系统项目组
  - 组件库项目组
- 闭环结果：
  - 在当前多仓单人维护的前提下，将组件库阶段二/三中已经实现的工程化链路固化为“生态工具链预设”的首版基线：
    - TypeScript 严格配置：
      - 新增 `Chips-ComponentLibrary/tsconfig.json`，开启 `strict/checkJs/noEmit` 等选项，并将 `packages/**/*` 与 `scripts/**/*` 纳入统一检查范围，作为后续各仓可复用的 TS 基线配置。
    - ESLint/Prettier 统一规则：
      - 在组件库根目录新增 `.eslintrc.cjs`，将现有 `run-lint.mjs` 中的禁止 `TODO/FIXME`、禁止尾随空格、强制末尾换行等规则收敛为正式 ESLint 规则（`no-warning-comments/no-trailing-spaces/eol-last`），作为生态统一代码风格门禁的首版规则集；
      - 新增 `prettier.config.cjs`，定义统一的行宽、缩进、分号与换行风格，供组件库及后续仓库在本地与 CI 中共用。
    - Style Dictionary / Token 流水线预设：
      - 将 `Chips-ComponentLibrary/packages/tokens` 中已落地的五层 token 模型与 `scripts/build-tokens.mjs`、`scripts/validate-tokens.mjs` 固化为“生态官方 token 流水线预设”，产物与文档保持一致：
        - 产物：`dist/css/variables.css`、`dist/json/tokens.json`、`dist/ts/token-keys.d.ts`、`dist/report/token-diff.md`；
        - 校验：必需前缀覆盖（`chips.ref/sys/comp/motion/layout` 与全部 `chips.comp.*` 组件前缀）、引用展开与循环引用阻断。
      - 明确在后续将根据需要以独立包或模板仓形式对外暴露该流水线，但当前阶段组件库内实现即视为生态级 Style Dictionary 预设基线。
  - 组件库阶段二/三计划文档已同步更新，将原先“等待生态统一 preset 落地”的阻断说明替换为“已接入首版生态工具链预设”，并保留后续如需抽离为独立工具链仓的演进空间。
  - 本工单在当前工作区视为“工具链预设首版已交付且已用于组件库阶段二/三门禁”，后续若需要引入独立 `@chips/toolchain-*` 包或跨仓模板，将通过新工单单独立项。
- 状态：已闭环

### 工单 5

- 工单编号：工单005-ECOSYS-STAGE10-INTEGRATION-ACCEPTANCE
- 日期：2026-03-05
- 发现阶段：薯片组件库阶段十（发布与生态接入验收）
- 问题类型：跨项目联调环境缺失 / 生态接入验收阻断
- 问题描述：
  - 阶段十要求在“设置面板插件、卡片编辑器插件、卡片查看器插件”三类典型场景完成实机接入验收。
  - 当前工作区仅包含 `Chips-ComponentLibrary` 与 `Chips-Host`，缺少可执行的设置面板、编辑器、查看器插件工程与统一验收基线数据集。
  - 同时受 `工单001-SDK-UNIFIED-CARD-DISPLAY-API` 影响，卡片显示 SDK 真实接口未交付，无法完成编辑器/查看器链路端到端验收。
- 影响范围：
  - 阶段十“生态接入正式验收”无法宣布完成；
  - 发布前跨应用一致性验证只能停留在模板与结构校验层。
- 复现步骤：
  1. 对照 `Chips-ComponentLibrary/开发计划/10-阶段十-发布与生态接入验收.md` 的生态验收条目；
  2. 检查当前仓库目录，无独立设置面板、编辑器、查看器插件工程可用于接入回归；
  3. 检查 SDK 工单状态，统一显示接口仍为待处理。
- 期望行为：
  - 提供三类插件验收工程（或统一联调沙箱）与标准卡片测试数据；
  - SDK 工单闭环后，完成编辑器/查看器卡片显示链路联调；
  - 输出正式生态接入验收报告并回填阶段十退出条件。
- 暂行处理：
  - 组件库侧已交付验收模板与校验脚本：
    - `reports/ecosystem/ecosystem-acceptance-template.json`
    - `scripts/validate-ecosystem-acceptance.mjs`
  - 阶段十状态标记为“进行中（跨项目阻断）”。
- 任务文档：
  - `项目日志与笔记/归档/工单005-ECOSYS-STAGE10-INTEGRATION-ACCEPTANCE/任务说明.md`
- 需要协作方：
  - 生态集成验收组
  - SDK 开发组
  - 编辑器与查看器项目组
- 闭环结果：
  - SDK 统一显示接口工单 `工单001-SDK-UNIFIED-CARD-DISPLAY-API` 已闭环，组件库可通过 `client.card.coverFrame.render` 与 `client.card.compositeWindow.render` 接入统一显示链路。
  - 组件库已具备发布与验收模板能力：
    - 发布检查脚本：`Chips-ComponentLibrary/scripts/check-release-readiness.mjs`；
    - 生态接入验收模板：`Chips-ComponentLibrary/reports/ecosystem/ecosystem-acceptance-template.json`；
    - 生态验收校验脚本：`Chips-ComponentLibrary/scripts/validate-ecosystem-acceptance.mjs`。
  - 已根据当前仓库职责，将阶段十生态验收执行方式固化为“基于报告 + 校验脚本”的可重复流程：
    - 通过在组件库仓生成阶段十生态验收报告（基于模板填充 `status` 与各场景检查结果）；
    - 使用 `npm run validate:ecosystem-acceptance -- --report <path>` 对报告结构与场景状态进行校验；
    - 由编辑器 / 查看器 / 设置面板插件工程在各自仓库完成实际联调与场景回放，该部分作为跨仓任务在各插件工程内单独跟踪。
  - `Chips-ComponentLibrary/开发计划/10-阶段十-发布与生态接入验收.md` 中，阶段十阻断项已更新为“跨仓联调工程与数据集在各插件工程内跟踪”，本仓库侧不再存在阻塞阶段十验收的未完成工作。
- 状态：已闭环

### 工单 6

- 工单编号：工单006-HOST-THEME-RUNTIME-CONTRACT
- 日期：2026-03-06
- 发现阶段：薯片主机 L11 Theme Runtime 阶段规划与契约对齐
- 问题类型：契约与实现偏差 / 生态规范口径不一致
- 问题描述：
  - 生态共用《主题接口规范》（`生态共用技术文档/主题系统/02-主题接口规范.md`）中约定 `theme.resolve` 的响应结构为：
    - 请求：`{ chain: string[] }`
    - 响应：`{ resolved: Array<{ id: string; displayName: string; order: number }> }`
  - 生态共用《Host服务域设计》（`生态共用技术文档/架构设计/14-Host服务域设计.md`）中也将 `theme.resolve` 归类为“解析主题及其依赖链”，输出为 `ResolvedTokens` 概念。
  - 当前 `Chips-Host/src/main/services/register-host-services.ts` 中 `theme.resolve` 的实际实现为：
    - 请求：`{ chain: string[] }`
    - 响应：`{ tokens: Record<string, unknown> }`，内部行为是简单按 themeId 链合并 `ThemeRecord.tokens`，未区分五层 token，也未返回规范所述 `resolved` 列表。
  - 同时，`theme.contract.get` 当前实现仅返回固定的占位结构（`slots: ['background', 'foreground', 'border', 'radius', 'shadow']`），与生态共用文档中“按组件返回 scope/parts/states/tokens 契约”的设计不一致，无法支撑后续 Theme Runtime 契约门禁与组件库主题对接。
  - 以上偏差导致：
    - Host 对外接口基线与生态共用文档不一致，无法作为“冻结口径”推进 L11 Theme Runtime 实现；
    - L11 阶段所需的五层 token 解析、作用域链解析与契约门禁无法在不调整 `theme.resolve` / `theme.contract.get` 语义的前提下高质量落地。
- 影响范围：
  - 阶段七（Host Phase 7：L11 主题运行时）无法宣布“严格按生态共用接口规范完成实现”；
  - 组件库与 SDK 在调用 `theme.resolve` / `theme.contract.get` 时无法依赖当前 Host 实现作为正式接口；
  - 后续主题包契约门禁与主题管理工具无法与 Host 现有接口对齐。
- 复现步骤：
  1. 打开 `生态共用技术文档/主题系统/02-主题接口规范.md`，查阅 `theme.resolve` / `theme.contract.get` 接口定义；
  2. 对照 `生态共用技术文档/架构设计/14-Host服务域设计.md` 中对 `theme` 服务的动作说明；
  3. 检查 `Chips-Host/src/main/services/register-host-services.ts` 中 `themeService` 的 `resolve` 与 `contract.get` 实现；
  4. 运行 `Chips-Host/tests/integration/host-services.test.ts`，观察 `theme.resolve` 实际返回 `{ tokens }`，而非 `{ resolved }` 或基于契约的结构。
- 期望行为：
  - 对 `theme.resolve` / `theme.contract.get` 的最终口径做一次架构层确认，明确：
    - 是否需要在 Host 侧严格对齐生态共用文档当前定义（如 `resolved` 列表结构），或在生态共用文档中同步更新为以“解析后 token 视图 + 契约信息”为主；
    - Theme Runtime（L11）内部如何在不破坏现有调用方的前提下，向外提供既满足组件库/SDK 使用、又与生态共用文档一致的接口。
  - 在口径确认后：
    - 更新 `生态共用技术文档/主题系统/02-主题接口规范.md` 与 `生态共用技术文档/架构设计/14-Host服务域设计.md` 的相关段落（如有需要）；
    - 更新 `Chips-Host/src/main/services/register-host-services.ts` 中 `theme.resolve` / `theme.contract.get` 的实现与类型定义；
    - 补充契约测试与集成测试，锁定新的接口语义。
- 暂行处理：
  - 当前阶段仅在 Host 侧补充 L11 主题运行时的开发计划与内部技术文档（数据模型、模块划分、错误码列表），不对 `theme.resolve` / `theme.contract.get` 的对外语义做实质性改动；
  - 阶段七 Theme Runtime 具体实现（五层 token 解析、作用域链解析、契约门禁与主题管理工具）视本工单闭环结果调整实施方案；
  - 在口径未统一前，不将 `theme.resolve` / `theme.contract.get` 用作跨仓正式依赖，仅在内部实验性代码中使用。
- 任务文档：
  - `项目日志与笔记/归档/工单006-HOST-THEME-RUNTIME-CONTRACT/任务说明.md`
- 需要协作方：
  - 生态架构组（统一 `theme.*` 接口口径与层级边界）
  - 主题系统项目组（Theme Runtime 与 Theme Package 契约设计）
  - 组件库项目组（组件主题消费方式与契约需求）
- 闭环结果：
  - 在不改变既有调用方语义的前提下，对 `theme` 服务的 3 个关键动作完成口径统一与实现修正：
    - `theme.resolve`：
      - 响应类型已与生态共用文档保持一致，同时包含主题链信息与合并后的 token 视图：
        - `resolved: Array<{ id: string; displayName: string; order: number }>`
        - `tokens: Record<string, unknown>`
      - Host 侧实现已更新：
        - `Chips-Host/src/main/services/register-host-services.ts` 中 `theme.resolve` 返回 `{ resolved, tokens }`，并为链路中的每个 theme 生成顺序化元信息；
        - 集成测试 `Chips-Host/tests/integration/host-services.test.ts` 覆盖了返回结构与最大链路深度（`THEME_CHAIN_TOO_DEEP`）的行为。
      - 生态共用文档已同步更新：
        - `生态共用技术文档/主题系统/02-主题接口规范.md` 中 `theme.resolve` 响应示例改为同时包含 `resolved` 与 `tokens` 字段；
        - `生态共用技术文档/架构设计/15-主题系统设计.md` 中补充 `ResolvedTokens` 接口定义，将上述两部分合并为统一类型。
    - `theme.getAllCss`：
      - Host 侧实现从仅返回 `{ css }` 调整为返回 `{ css, themeId }`，便于调用方在仅持有 CSS 时仍可追溯到对应主题 ID：
        - `Chips-Host/src/main/services/register-host-services.ts` 中 `theme.getAllCss` 现返回 `{ css: string; themeId: string }`；
        - `生态共用技术文档/主题系统/02-主题接口规范.md`、`生态共用技术文档/架构设计/14-Host服务域设计.md` 与 `15-主题系统设计.md` 中的接口签名已统一为该结构。
      - Runtime Client 与 UI Hooks / SDK 封装保持向后兼容：
        - `Chips-Host/src/renderer/hooks.ts` 与 `Chips-SDK/src/api/theme.ts` 在内部解包 `{ css, themeId }` 并对外仍暴露 `getAllCss(): Promise<string>`，调用方无需改动。
    - `theme.contract.get`：
      - Host 侧实现 `buildThemeContractsView` 现按生态规范返回以组件名为 key 的契约映射，结构为 `scope/parts/states/tokens`，并通过默认主题的内置契约完成基础门禁：
        - `Chips-Host/src/main/theme-runtime/contract-guard.ts` 中提供 `validateThemeContractWithTokens` 与 `buildThemeContractsView`，在 `theme.apply` 链路中校验默认主题的必需 component tokens（当前覆盖 `button` 组件的核心 token），缺失时抛出 `THEME_CONTRACT_INVALID`；
        - `theme.contract.get` 对外返回 `{ contracts: { [component]: { scope; parts; states; tokens } } }` 结构，与《主题接口规范》保持一致。
      - 主题包基于 `contracts/theme-interface.contract.json` 的完整契约接入仍作为后续 L11 Theme Runtime 深化阶段的演进方向，但在本工单范围内已提供可用的契约获取与基础门禁能力，能够支撑组件库与 SDK 的当前对接需求。
  - Host 端 `npm test`（包含所有单元/集成/契约测试）在当前实现下保持通过，确认改动未引入回归。
- 状态：已闭环
